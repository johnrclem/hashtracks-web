/** Sentinel kennel tag injected for unconfigured Google Calendar sources to avoid the adapter's hard-coded Boston fallback. */
export const UNKNOWN_KENNEL_SENTINEL = "__unknown__";

/** User-facing display label replacing the sentinel tag. */
export const UNTAGGED_KENNEL_DISPLAY = "(untagged)";
