import { RegionFilterPopover } from "./RegionFilterPopover";

describe("RegionFilterPopover", () => {
  it("exports RegionFilterPopover as a function", () => {
    expect(typeof RegionFilterPopover).toBe("function");
  });
});
