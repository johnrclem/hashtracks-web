import { describe, it, expect } from "vitest";
import { PossibleRow } from "./PossibleRow";

describe("PossibleRow", () => {
  it("exports PossibleRow as a function", () => {
    expect(typeof PossibleRow).toBe("function");
  });
});
