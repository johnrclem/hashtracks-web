import { describe, it } from "vitest";

// Placeholder for interaction coverage. Static empty-state copy + a
// link back to /travel; nothing dynamic to assert beyond render. Real
// coverage waits on a Travel-Mode RTL setup.
describe("SavedTripsEmpty", () => {
  it.todo("renders empty-state copy and CTA back to /travel");
});
