/**
 * Shared RSVP color tokens. Used by Going-badge + attendance-dot UI across
 * hareline and travel card variants so the visual language matches.
 */
export const RSVP_INTENDING_COLOR = "#3b82f6"; // blue-500 — "I'm planning to go"
export const RSVP_CONFIRMED_COLOR = "#16a34a"; // green-600 — "I was there"
