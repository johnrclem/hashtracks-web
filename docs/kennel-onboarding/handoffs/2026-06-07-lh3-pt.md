# Onboarding Handoff — Lisbon H3 (Lisbon, Portugal) — 2026-06-07

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/lh3-pt-20260607`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). This is HashTracks' **first
>    Portugal kennel** — add the Portugal COUNTRY + Lisbon METRO regions with **all 5 `region.ts`
>    edits** (see **Adapter notes → Region (NEW COUNTRY)**).
> 3. Configure the **config-only `HARRIER_CENTRAL`** source exactly as in **Ready-to-paste seed**
>    (the `HarrierCentralAdapter` already exists in `src/adapters/registry.ts` — no new adapter code).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: events non-empty +
>    upcoming, dates UTC-noon, `startTime` "HH:MM", `kennelTag` resolves to `lh3-pt` with no
>    unmatched. Resolve the one `⚠️ Claude Code must confirm` item in **Live source verification**
>    (the API is allowlist-blocked from the research sandbox — it was verified from a browser page
>    context instead; confirm it fetches from your local env, where the HC API is reachable). **DO
>    NOT run `npx prisma db seed` here.**
> 5. No historical backfill (none available — see **Historical backfill**).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, run after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge can silently drop a follow-up
>      commit): `git log -1 -- prisma/seed-data/sources.ts`, `… kennels.ts`, `… aliases.ts`,
>      `src/lib/region.ts`, `public/kennel-logos/lh3-pt.<ext>`. Open a small recovery PR if any is missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds new kennel/alias/source + Portugal/Lisbon regions)
>    - Trigger a scrape from `/admin/sources` (or the equivalent admin action) to publish events to prod
>    - Spot-check `https://www.hashtracks.xyz/kennels/lisbon-h3` for ~27 upcoming events + sample dates

## Summary
- Type: **full onboard** (🇵🇹 first Portugal kennel — first Iberian-Atlantic / first Portuguese-speaking-Europe kennel on HashTracks)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `registry.ts` & feed verified live)
- Effort estimate: **config-only** + Portugal/Lisbon region (5 `region.ts` edits) + self-host one logo. No new adapter code, no backfill script.
- One-line: Lisbon H3 (est. 1987) is one of Europe's long-running family hashes (run #1016+) and publishes a full ~1-year forward schedule on Harrier Central — 27 upcoming biweekly-Saturday runs, zero new code.

## Dedup result
- Kennel in seed: **no** (grep `lisbon|lisboa|portugal|lhhh` in `kennels.ts`/`sources.ts`/`aliases.ts` → only unrelated montreal/capital email hits)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP, **432 slugs**; no `lisbon*`/`lisboa*`/`portugal*`/`lh3`-as-slug match. (`lh3` is London H3's *kennelCode*; its slug is `london-h3`. No kennel generates slug `lisbon-h3`.)
- Decision: **full onboard**
- kennelCode: **`lh3-pt`** (collision check: bare `lh3` taken by London H3 `kennels.ts:958`; `lh3-fl`/`lh3-ks` also taken → suffixed `-pt`, matching HC's own `kennelUniqueShortName` "LH3-PT")
- slug: **`lisbon-h3`** (from shortName "Lisbon H3"; free — confirmed sitemap-absent; no accent so no `slug:` pin needed)
- Pre-onboarding admin-event check: kennel has no prod slug/page → no admin-seeded `Event` rows expected. Low risk; Claude Code may spot-check prod for stray `lh3-pt` events before first scrape but none anticipated.

## Live source verification  ⚠️ needs Claude Code to confirm from local env (API allowlist-blocked from research sandbox)
- Source: **HARRIER_CENTRAL** — `https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, filter `publicKennelId: 102bca91-1821-402d-a852-c64a51a1a2a1` (`kennelUniqueShortName: "LH3-PT"`). **Both filters verified to return the identical 27-event set, single kennel LH3-PT.**
  - ⚠️ The Azure HC API is **not in the research-sandbox network allowlist** (DNS `EAI_AGAIN`). It was verified by executing the time-token + `fetch` **inside a browser page context** (Chrome MCP) — `status 200`, real data. The HC API is reachable from a normal dev machine; **Claude Code: confirm `adapter.fetch(source)` returns the 27 events from your local env.** (Same plumbing as the live Tokyo/Singapore/Hamburg/Morgantown HC sources, so this is routine.)
- Events seen: **27**, date range **2026-06-13 (#1016) → 2027-05-29 (#1042)** — biweekly Saturdays, ~1 year forward.
- **Source-count parity:** HC by-kennel returns its full forward set (27). The kennel runs **biweekly**; 27 biweekly slots ≈ 54 weeks ≈ the full published schedule. Parity = **27/27 (100%)** — no pagination needed (single API response, no `nextPage`).
- Sample events (**verbatim from the API**, themed titles unaltered; trailing spaces are in the source):
  1. `#1016` — **2026-06-13** 13:00 — "Feast of St Anthony" — hares: *(none in feed)* — loc: "TBD"
  2. `#1017` — **2026-06-27** 05:45 — "Summer Solstice Sunrise Hash" — loc: "TBD"
  3. `#1018` — **2026-07-11** 13:00 — "Placeholder event for LH3" — loc: "No location provided"
  4. `#1021` — **2026-08-22** 13:00 — "AGPU" — loc: "ANNOUNCED LATER via Hares"
  5. `#1023` — **2026-09-19** 13:00 — "Red Dress Run " — loc: "TBD"
  6. `#1025` — **2026-10-09** 18:30 (Friday) — "Pan Iberian Hash Weekend" — loc: "Portugal"
  7. `#1030` — **2026-12-12** 13:00 — "Jingle Bell Hash" — loc: "TBD"
  - Real themed runs interleave with auto-generated **"Placeholder event for LH3"** rows (the kennel pre-creates its biweekly slots and renames them as hares sign up). Run numbers ARE present and sequential (#1016…#1042). Start time is consistently **13:00** for normal runs; specials vary (05:45 sunrise, 18:30 evening, 11:00 NYE recovery).
- History depth / pagination: **single API response, no pagination.** HC `getEvents` returns **future-only** (earliest is #1016, +6 days; no past events exposed). See **Historical backfill** — none available.
- Coord sanity: **⚠️ all 27 events share ONE identical pin `38.72272288, -9.144866305`** (HC kennel-default, central Lisbon). This is the default-pin pattern — but note it is **geographically correct (in Lisbon)**, not a wrong-continent corruption like the Squarespace-Manhattan trap. All per-event `locationOneLineDesc`/`resolvableLocation` are `TBD` / `No location provided` / `ANNOUNCED LATER via Hares` (no real venues; venues announced day-of). Adapter behaviour to expect:
  - The adapter's existing `hcGeocodeFailed(placeName, resolvable)` drops coords **only when `placeName === resolvable`** (e.g. #1016 `"TBD"`==`"TBD"` → coords dropped + `dropCachedCoords:true`).
  - It will **NOT** catch the placeholder rows (empty `locationOneLineDesc` + `resolvableLocation:"No location provided"` → `hcGeocodeFailed` returns false), so those would store the Lisbon-center pin.
  - **Recommendation (low priority — pick one):** (a) **accept** the Lisbon-centroid pin — it's correctly in Lisbon and these runs have no real venue anyway; or (b) optionally extend the geocode-fail sentinels to also treat `"No location provided"` / `"TBD"` / `"ANNOUNCED LATER via Hares"` as failures → drop coords + `dropCachedCoords:true` and let the merge pipeline fall back to the Lisbon region centroid. Either is acceptable; **this is NOT a blocker.** If touching the adapter for (b), keep it config-driven if possible to preserve "config-only."
- End times: **none** (HC `getEvents` payload has no end timestamp).
- Notes: Config-only HC, identical pattern to **Hamburg H7** (`sources.ts:3640`), which also uses `publicKennelId` + `defaultTitle` + `staleTitleAliases` for "Placeholder event for H7". Mirror it exactly.
- **Field-fill assertion table** (from the 27-event sample):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 27 / 27 | HC sets it; themed names pass through trimmed, "Placeholder event for LH3" → `defaultTitle`+`#N` ("Lisbon H3 #N") via `staleTitleAliases` |
  | `runNumber` | 27 / 27 | `eventNumber` (1016…1042), sequential |
  | `startTime` | 27 / 27 | `HH:MM` from `eventStartDatetime` (13:00 normal; specials vary) |
  | `endTime` | 0 / 27 | not in HC feed — accept absence |
  | `location` (venue name) | 0 / 27 | all TBD/placeholder — leave undefined (don't synth) |
  | `locationStreet` | 0 / 27 | not in feed |
  | `locationUrl` (Maps) | 0 / 27 | not in feed |
  | `hares` | 0 / 27 | empty in feed (announced day-of) |
  | `cost` | 0 / 27 | kennel default `€5` lives on `Kennel.hashCash`; no per-event override |
  | `description` | 0 / 27 | not in HC feed — leave undefined |
  | `coords` (lat/lng) | 27 / 27 *(all identical default pin)* | see **Coord sanity** — drop or accept Lisbon centroid |

## Kennel metadata (deep-dive complete)
- fullName: **Lisbon Hash House Harriers** / shortName: **Lisbon H3** / region: **Lisbon** / country: **Portugal**
- aliases: `["LH3-PT", "Lisbon Hash House Harriers", "Lisboa Hash House Harriers", "Lisboa H3"]`
  - 🔴 **Bare `LH3` OMITTED** — global alias collision: `LH3` already maps to London H3 (`aliases.ts:139`) **and** Lakeland H3 (`aliases.ts:195`). Adding it to a third kennel would make the alias 3-way ambiguous. The HC source assigns `kennelTag` via `defaultKennelTag: "lh3-pt"` (config-driven), so alias resolution isn't needed for ingestion. (Per the manual's cross-platform shortcode-collision rule — use the region-suffixed `LH3-PT` as the public short alias.)
  - All four proposed aliases verified absent from `aliases.ts` (no collisions).
- website: **none confirmed** — the old official site `angelfire.com/celeb/lh3/` renders "The LH3 Website has moved!!" but the new URL wasn't recoverable (Chrome auto-denied the new domain; web search surfaced no successor). The blog `lisbonhash.blogspot.com` is **dead** (4 posts, newest **2009-01-26**). ⚠️ Leave `website` blank / follow-up; use Facebook + Instagram below.
- facebookUrl: **`https://www.facebook.com/groups/lisbonh3/`** (source: genealogy.gotothehash.net "Lisbon H3 on the www" + web search; it's a FB **group**, not a Page → not usable for `FACEBOOK_HOSTED_EVENTS`)
- instagramHandle: **`lisbon_hash_house_harriers`** (source: instagram.com/lisbon_hash_house_harriers/)
- twitterHandle / discordUrl: none found
- schedule: **Saturday, 1:00 PM, Biweekly** (scheduleTime "1:00 PM" = HC's consistent 13:00 normal-run start). ⚠️ **Discrepancies noted** (canonical = live HC + kennel's own page):
  - Angelfire (kennel's own, stale page): *"once a fortnight on Saturdays — 2 pm"* → **2pm vs HC's 13:00**. HC live data is authoritative for the operational time (13:00); seed scheduleTime "1:00 PM" and mention 2pm history in notes.
  - genealogy.gotothehash.net (updated 2022): *"Sunday, Biweekly"* → contradicts Saturday. Angelfire + **all 27 live HC events are Saturdays** → Saturday is canonical; genealogy is stale.
- scheduleNotes: `"Biweekly Saturday runs (~1:00 PM). Meeting point traditionally the semicircular car park behind the Estoril Casino, Praça de Almeida Garrett, Estoril. Per-run venues announced day-of by the hares. (Older sources list 2pm / Sunday — superseded by the live Harrier Central schedule.)"`
- foundedYear: **1987** (source: genealogy.gotothehash.net — First Run **06 Jun 1987**; founder Kevin 'Uphill' O'Neill, assisted by Anne 'Draggedy Anne' O'Neill & Jeff van Etten; corroborated by portugalresident.com "founded by Jeff Van Etten, Anne & Kevin O'Neill on June 6, 1987")
- hashCash: **`€5 (adults) / €2 (kids) / €12 (family); dogs free`** (source: angelfire.com/celeb/lh3 + portugalresident.com — verbatim "5,00 Euros for adults, 2,00 Euros for kids, 12,00 Euros for families. Dogs run free.")
- dogFriendly: **true** (source: angelfire "Dogs run free")
- walkersWelcome: **true** (source: portugalresident.com — "family runs (for rambos) and walks (for wimps)" → a designated walkers' trail)
- description: `"Lisbon Hash House Harriers (LH3) is a family hash founded in 1987, running fortnightly on Saturdays year-round around Lisbon and the Estoril coast. Rambos run, wimps walk, dogs run free — followed by the usual beer. On On!"`
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/Lisbon%20H3.png`** (HC blob — non-tokenized but on a third-party CDN). ⚠️ **Self-host**: download to `public/kennel-logos/lh3-pt.<ext>` and reference `/kennel-logos/lh3-pt.<ext>`. **Confirm the real extension via magic bytes** (`file public/kennel-logos/lh3-pt` — could be PNG despite the `.png` URL; verify `\x89PNG`). (genealogy logo `logos/778.png` returned "Failed to retrieve" — use the HC blob.)
- lat/lng: **38.7223, -9.1393** (Lisbon city centre, kennel-level pin; the recurring meeting point is Estoril ~38.7057, -9.3967 but per-run venues vary — Lisbon-centre is the safe kennel pin and matches HC's own default).

## Historical backfill
- Available: **none.** HC `getEvents` exposes **future-only** events (earliest #1016, +6 days). Run #1016 implies ~1,000+ historic runs since 1987, but **no source exposes them**: the Blogspot archive has only **4 posts (newest 2009-01-26, runs #621–#623)**, Angelfire is a static "we moved" page, and there is no Google Sheet / WordPress / Squarespace archive. Not worth a backfill script.
- Plan: **none** (future-only adapter; the kennel page populates from the 27 live forward events on first scrape).

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` (kennelCode → string[]) — confirmed via `head prisma/seed-data/aliases.ts`. **Keyed by kennelCode (`lh3-pt`), NOT slug.**

```ts
// kennels.ts — Kennel[] (array of objects). Add near other European kennels.
{
  kennelCode: "lh3-pt", shortName: "Lisbon H3", fullName: "Lisbon Hash House Harriers",
  region: "Lisbon", country: "Portugal",
  foundedYear: 1987,
  scheduleDayOfWeek: "Saturday", scheduleTime: "1:00 PM", scheduleFrequency: "Biweekly",
  scheduleNotes: "Biweekly Saturday runs (~1:00 PM). Meeting point traditionally the semicircular car park behind the Estoril Casino, Praça de Almeida Garrett, Estoril. Per-run venues announced day-of by the hares. (Older sources list 2pm / Sunday — superseded by the live Harrier Central schedule.)",
  description: "Lisbon Hash House Harriers (LH3) is a family hash founded in 1987, running fortnightly on Saturdays year-round around Lisbon and the Estoril coast. Rambos run, wimps walk, dogs run free — followed by the usual beer. On On!",
  hashCash: "€5 (adults) / €2 (kids) / €12 (family); dogs free",
  dogFriendly: true,
  walkersWelcome: true,
  logoUrl: "/kennel-logos/lh3-pt.<ext>",   // ← self-host the HC blob; confirm ext via magic bytes
  facebookUrl: "https://www.facebook.com/groups/lisbonh3/",
  instagramHandle: "lisbon_hash_house_harriers",
  latitude: 38.7223, longitude: -9.1393,
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]). Bare "LH3" OMITTED (collision).
"lh3-pt": ["LH3-PT", "Lisbon Hash House Harriers", "Lisboa Hash House Harriers", "Lisboa H3"],

// sources.ts — Source[] (array of objects). Mirror the Hamburg H7 HC record (sources.ts:3640).
{
  name: "Lisbon H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter; kennelUniqueShortName "LH3-PT" returns the
    // identical 27-event set (both verified 2026-06-07).
    publicKennelId: "102bca91-1821-402d-a852-c64a51a1a2a1",
    defaultKennelTag: "lh3-pt",
    // LH3 pre-creates biweekly slots as "Placeholder event for LH3" and renames
    // them as hares sign up → synthesize "Lisbon H3 #N" (mirrors Hamburg H7 #1166).
    defaultTitle: "Lisbon H3",
    staleTitleAliases: ["Placeholder event for LH3"],
  },
  kennelCodes: ["lh3-pt"],
},
```

> **`upcomingOnly` intentionally OMITTED** — matches every existing HC source (Hamburg H7, Tokyo H3, Singapore Sunday H3, Morgantown H3 all omit it). The HC adapter emits future-only and the established HC kennels (incl. Hamburg H7's 101 backfilled past runs) survive reconciliation without it. Don't add it. (Lisbon has no archive to protect anyway.)

## Adapter notes / new-scraper plan

**Config-only — no new adapter code.** `HARRIER_CENTRAL` → `HarrierCentralAdapter` is registered at `src/adapters/registry.ts:140` (import line 127). The config above is the entire implementation. The adapter already handles: time-based token auth, the `[[...events]]` response shape, `staleTitleAliases`+`defaultTitle` fallback, TBA stripping, `hcGeocodeFailed` coord-dropping, run-number tri-state, and the `days`/`scrapeDays` window.

### Region (NEW COUNTRY — Portugal — all 5 `region.ts` edits required)
First Portugal kennel. Mirror the **Spain** COUNTRY+METRO pair (`region.ts:1745`) for shape/shade; use a **green** palette (distinct from Spain's rose next door, France's blue). COUNTRY = darker `-200`, METRO = lighter `-100`, different pins.

1. **`REGION_SEED_DATA`** (add after the Spain/France block, ~line 1792):
   ```ts
   // ── Portugal ──
   {
     name: "Portugal", country: "Portugal", level: "COUNTRY",
     timezone: "Europe/Lisbon", abbrev: "PT",
     colorClasses: "bg-green-200 text-green-800", pinColor: "#16a34a",
     centroidLat: 39.5, centroidLng: -8.0,
     aliases: ["PT", "Portuguesa"],
   },
   {
     name: "Lisbon", country: "Portugal",
     timezone: "Europe/Lisbon", abbrev: "LIS",
     colorClasses: "bg-green-100 text-green-700", pinColor: "#22c55e",
     centroidLat: 38.7223, centroidLng: -9.1393,
     aliases: ["Lisboa", "Lisbon, Portugal"],
   },
   ```
   (No trailing-zero literals; `-8.0`/`39.5` are fine as written — keep one decimal, e.g. write `centroidLng: -8` is also acceptable, but `-8.0` trips Sonar S6749 → use `-8`. Likewise prefer `39.5`, `38.7223`, `-9.1393` as-is.)
   🔴 **Sonar S6749:** write `centroidLng: -8` (NOT `-8.0`). Double-check no `X.0` literals slip in.
2. **`STATE_GROUP_MAP`** (~line 3535, after France): `"Lisbon": "Portugal",`
3. **`COUNTRY_GROUP_MAP`** (~line 3749, after France): `"Portugal": "Portugal",` (metro "Lisbon" resolves via STATE_GROUP_MAP first, mirroring the Spain/France precedent — only the country-group key is reachable here).
4. **`COUNTRY_CODE_TO_NAME`** (~line 3859, after `FR`): `PT: "Portugal",`
5. **`COUNTRY_INFERENCE_RULES`** (~line 3329, near the France rule): `[/\b(portugal|lisbon|lisboa|estoril|cascais)\b/, "Portugal"],`
   🔴 **Without this, `inferCountry()` falls through to `"USA"` for Lisbon kennels** — the gap that bit ONH3/Kenya in CI. Do not omit.

**⚠️ Claude Code: verify before writing real code.** Any snippet here is illustrative; the live repo is authoritative. Before finalizing:
- `RawEventData` field names — `kennelTags` is `string[]` (the HC adapter already emits `kennelTags: [kennelTag]`). The walker-friendliness `Kennel` field is `walkersWelcome` (NOT `walkerFriendly`); the dog field is `dogFriendly`. Both are plumbed into the seeder (per VH3 retro).
- Confirm the `Region` seed object's exact field names/optionals against the current `REGION_SEED_DATA` type (timezone/abbrev/colorClasses/pinColor/centroidLat/centroidLng/aliases) — copy a sibling record verbatim and edit values.
- `friendlyKennelName("Lisbon H3", "Lisbon Hash House Harriers")` — shortName is **9 chars (>4)** so it short-circuits to the shortName; titles are also set by the HC adapter (`defaultTitle: "Lisbon H3"`), so synthesized titles render "Lisbon H3 #1018" cleanly. No verbose-name risk.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob — flagged self-host to `public/kennel-logos/lh3-pt.<ext>`, confirm ext via magic bytes)  [x] foundedYear (1987, genealogy + portugalresident)  [x] socials (FB group + Instagram; no Twitter/Discord)  [x] schedule (Saturday/biweekly/13:00; discrepancies noted)  [x] hashCash (€5/€2/€12, dogs free)
- [x] description  [x] source live-verified (27 events via HC API, browser-context fetch — Claude Code to reconfirm from local env)  [x] history depth assessed (future-only; no backfill source)
- [x] coord sanity checked (single default pin, correct city — accept or extend sentinels)  [x] end times noted (none)  [x] kennelCode collision-checked (`lh3-pt`; bare `lh3`/`LH3` taken)  [x] kennelCodes (source guard) set (`["lh3-pt"]`)

## Sibling-kennel sweep
- HC `getEvents cityNames:"Lisbon"` returned **only LH3-PT** (1 kennel). No co-located siblings on this source.
- genealogy lists **Porto H3** as Lisbon H3's descendant — that's a **different city (Porto)**, not a sibling on this source. Queue it as a **separate future Portugal target** (added to the queue's leads).
- Other Portugal kennels noted for future targeting: **Algarve HHH** (`algarvehashhouseharriers.com`), **PH3 / Portugal hub** (`ph3.com.pt`) — different regions; verify a live dynamic source before queueing.

## Source-pivot evidence (queue proposed "MEETUP/website"; pivoted to HARRIER_CENTRAL)
Per the manual's dual-evidence rule, both sides documented:
- **(a) Proposed sources are dead/nonexistent:**
  - Meetup `meetup.com/lisbon-hash-house-harriers/events/` → **"Group not found — Sorry, the group you're looking for doesn't exist"** (rendered DOM, Chrome MCP 2026-06-07); `__NEXT_DATA__` Apollo `Event` count = 0.
  - Blogspot `lisbonhash.blogspot.com` → Blogger feed `openSearch$totalResults: 4`, newest `entry[0].published: 2009-01-26` (runs #621–#623). Dead ~17 years.
  - Angelfire `angelfire.com/celeb/lh3/` → static "The LH3 Website has moved!!" (successor URL not recoverable).
- **(b) Working replacement:** Harrier Central `getEvents` (publicKennelId `102bca91-…` / LH3-PT) → **27 upcoming events #1016 (2026-06-13) → #1042 (2027-05-29)**, verbatim sample captured above. Live, config-only, identical to the Hamburg H7 pattern.

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **HC API is allowlist-blocked from the research sandbox but reachable from a normal dev machine.** Live-verify with a throwaway `npx tsx -e` calling `adapter.fetch(source)` — expect 27 future events. (The 27-event count + verbatim titles in this handoff were captured via a browser page-context `fetch` because the sandbox can't resolve the Azure host.)
- **Coord default-pin (see Coord sanity):** if you choose to drop the Lisbon-centroid pin on placeholder rows, remember rejecting upstream coords needs **`dropCachedCoords: true`** (the adapter already sets this on the `hcGeocodeFailed` path; a config-only onboard can simply accept the pin instead).
- **`upcomingOnly` not set** — matches all existing HC sources; do not add it.
- **Self-host tokenized/CDN logos** → `public/kennel-logos/lh3-pt.<ext>`. **NEVER pre-fill the extension** — confirm via `curl -sI` Content-Type AND magic bytes (`\x89PNG`=PNG, `RIFF…WEBP`=WebP, `\xff\xd8`=JPEG). The handoff uses a literal `<ext>` placeholder in `logoUrl` deliberately.
- **New country region edits:** the 5 `region.ts` edits above are all required; `COUNTRY_INFERENCE_RULES` is the one that silently returns `"USA"` if omitted (CI catches it, but add it now). No `seed.ts` `stateMetroLinks` entry is needed (that's only for STATE_PROVINCE-under-country like VH3/BC; Portugal is COUNTRY→METRO, like Spain).
- **Sonar S6749:** no trailing-zero numeric literals in the region seed (`-8` not `-8.0`).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief — no separate prompt needed._
