# Onboarding Handoff — Rio H3 (Rio de Janeiro, Brazil) — 2026-07-10

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/rioh3-20260710`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source) **and the 4 `region.ts`
>    edits** in **Region seeding** — Brazil COUNTRY is seeded but the **Rio de Janeiro** METRO is
>    not.
> 3. Configure the adapter — **pure config-only HARRIER_CENTRAL** (no new adapter code); mirror the
>    **Bandung H3** HC source row (`prisma/seed-data/sources.ts:7443`) — the closest backfill-carrying
>    HC sibling.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: dates UTC-noon-safe,
>    `startTime` "HH:MM", `kennelTag` resolves to `rioh3` with no unmatched. **⚠️ Expect 0 events** —
>    at research time HC's future-only `getEvents` returned **0 events** and the global-runs future
>    feed had none either. This is a **recently-active** monthly kennel (last run #446 on 2026-06-06,
>    ~34 days ago on a 28-day cadence), NOT a dead one — the next run appears on the first scrape
>    after the kennel posts it. **DO NOT run `npx prisma db seed` here.**
> 5. **Recommended: the Historical backfill one-shot (3 HC runs; see below).** Without it the kennel
>    page is empty until the next run posts (currently 0 future events). Keep `config.upcomingOnly: true`.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- <path>` for the seed rows, the 4
>      `region.ts` edits, the backfill script + frozen JSON, and the self-hosted logo).
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod, reverting unrelated prod config drift — barbados #2471/#2463).
>    - Run the backfill: `BACKFILL_APPLY=1 npx tsx scripts/backfill-rioh3-history.ts`.
>    - Trigger a scrape from `/admin/sources` to publish events to prod.
>    - Spot-check `/kennels/rio-h3` for the 3 backfilled runs + the new Rio de Janeiro region.

## Summary
- Type: **full onboard** (Brazil's 3rd metro — Brasília + São Paulo already seeded)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `src/adapters/registry.ts` & feed verified live)
- Effort estimate: **config-only source** + **4 `region.ts` edits** (Rio de Janeiro METRO under existing Brazil COUNTRY) + a **recommended 3-run backfill script**. No new adapter code.
- One-line: Adds Rio H3 — the Rio de Janeiro Hash House Harriers — via a live, recently-active Harrier Central kennel (monthly Saturday afternoon; last run #446 on 2026-06-06). First hashtracks kennel in Rio de Janeiro.

## Dedup result
- Kennel in seed: **no** (`grep -in 'rioh3|rfmh3|"rio|rio-h3'` in `prisma/seed-data/` → nothing; only "OntaRIO" false-positives in `kennels.ts`)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-10, **485 slugs**; **zero** matches for `rio|niteroi|rioh3|rfmh3|janeiro`. Only Brazilian slug present is `brasilia-h3`. (São Paulo METRO is region-seeded but has no live kennel slug yet either.)
- Decision: **full onboard**
- kennelCode: **`rioh3`** (collision check: **clear** — `grep -in 'rioh3|rfmh3|"RIOH3"|"RIO H3"' prisma/seed-data/` returns nothing; no `rio*` code/alias exists). slug → `rio-h3` (`toSlug("Rio H3")`).

## Live source verification  ✅ (recently-active — 0 upcoming, monthly cadence)
- Source: **HARRIER_CENTRAL** — `harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, `publicKennelId: 8d230b92-a6e6-4d8b-8c13-fe004a260d25` (uniqueShortName `RIOH3`). Verified live 2026-07-10 by (a) **reproducing the adapter's exact `getEvents` POST + time-based token in-browser** (HTTP 200, returned **0 events** — no future or just-passed slot) AND (b) sweeping `hashruns.org/api/global-runs?isFuture=0` in 4 windows over 2024-07 → 2026-07 (same underlying HC data the backfill reads).
- Events seen: **0 via `getEvents`** (no upcoming) + **3 total in the HC past-runs feed** (2026-04-11 → 2026-06-06). Each window returned `totalMatchingEvents === runs.length` (no truncation); windows for 2024-07…2025-12 returned **0** Rio rows → the kennel **joined HC ~April 2026**, so only these 3 runs are on the feed.
- **Source-count parity:** N/A (platform UI shows **0 upcoming** — HC `getEvents` future-only and empty at research time). Applied the Step 3 **recently-active** rule instead: last run **#446 "RioHHH 466 - A Farewell to Bars" 2026-06-06 (Sat)** is **~34 days ago** on a **28-day cadence** (#444 → #445 → #446 all exactly 28 days apart) = ~1.2× interval, and the kennel ran in **3 of the last 4 months** → recently-active, onboard normally with a recent-history backfill.
- **Recently-active evidence (VERBATIM cadence — proves live, not stale):**
  - **#446** "RioHHH 466 - A Farewell to Bars " — **2026-06-06 (Sat) 15:00** — Antero de Quental Metro Station, Leblon
  - **#445** "👣 Hash 445 - Tropeça mas não derrama _(he stumbles but doesn't spill)_ " — **2026-05-09 (Sat) 15:00** — São Conrado Metrô Station exit C — hares "Beach Bum Bum & Just Eddelen"
  - **#444** "Hash No. 444 - The Rites of Infertility Hash" — **2026-04-11 (Sat) 15:00** — Cardeal Arcoverde Metrô Station, Acesso A — hares "Oral Skills & Burnt Sox"
  - Cadence: monthly, Saturday ~15:00 (America/Sao_Paulo, GMT 18:00Z = UTC−3), 28-day spacing.
- Sample events (VERBATIM from the HC `global-runs?isFuture=0` feed — titles kept as-is, incl. emoji/markdown/typo):
  1. **#446** — "RioHHH 466 - A Farewell to Bars " — 2026-06-06 (Sat) 15:00 — location "Antero de Quental Metro Station in Leblon" — **no coords** — 🔴 **`Hares` field is location-bleed** (== the location string) → **drop hares** for this row — price 30/30 *(title says "466" but `EventNumber` = 446 — trust the number field)*
  2. **#445** — "👣 Hash 445 - Tropeça mas não derrama _(he stumbles but doesn't spill)_ " — 2026-05-09 (Sat) 15:00 — hares "Beach Bum Bum & Just Eddelen" — "São Conrado Metrô Station exit C" — coords −22.9922/−43.2542 — price 30/30
  3. **#444** — "Hash No. 444 - The Rites of Infertility Hash" — 2026-04-11 (Sat) 15:00 — hares "Oral Skills & Burnt Sox" — "Cardeal Arcoverde Metrô Station, Acesso A (A to B trail)" — coords −22.9647/−43.1804 — price 30/30
- History depth / pagination: **3 events total in the HC feed** (2026-04-11 → 2026-06-06), all captured. Deep run number (#446) but **HC coverage starts at the kennel's HC-join (~April 2026)** — runs #1–#443 are historical, **not** archived on this source (no separate blog/sheet archive found — see backfill note). Single feed, date-windowed via `minEventDate`/`maxEventDate`; use `scripts/lib/hc-global-runs.ts` (mirror `backfill-bandung-h3-history.ts`).
- Coord sanity: **clean** — #444/#445 pins inside the Rio bbox (lat −22.96…−22.99, lng −43.18…−43.25); **#446 has no coords** (will render at the Rio de Janeiro region centroid, correct). No repeated default pin. Apply a **Rio bbox scrub** in the backfill (drop lat outside −23.15…−22.70 / lng outside −43.90…−43.00) as belt-and-suspenders — nothing in this sample would be dropped.
- End times: none (HC exposes start only).
- Notes: **tz is clean** — HC `KennelIANATimezone` = `America/Sao_Paulo` (correct for Rio; **no** HC-quirk override needed, unlike Belgrade/KRASH). Config-only; no browserRender/auth. HC `global-runs` carries **no currency field** — prices are bare numbers `30/30`; infer **BRL (R$30)** from the country (flag "confirm at build").
- **Field-fill assertion table** (from the 3 swept rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 3 / 3 | Real event names (incl. emoji/markdown/`#N` typo) / HC adapter sets `title` from `EventName`; `defaultTitle:"Rio H3"` synthesizes "Rio H3 #N" only for empty/placeholder names. |
  | `startTime` | 3 / 3 | From `EventStartDatetime` — all `15:00`. |
  | `endTime` | 0 / 3 | Not in feed — accept absence. |
  | `location` (venue name) | 3 / 3 | `LocationOneLineDesc` (verbatim metro-station strings). |
  | `locationStreet` | 0 / 3 | Not populated — leave undefined. |
  | `locationUrl` (Maps) | 0 / 3 | Not in feed — coords carry the pin. |
  | `hares` | 2 / 3 | `Hares` field; **#446 is location-bleed → drop** (hares == location). #444/#445 real. |
  | `cost` | 3 / 3 (as number 30) | HC price `30` on all 3; kennel-level `hashCash` "R$30" (currency inferred BRL — flag). |
  | `description` | (present in HC) | Live HC adapter does **not** emit `description`; **omit in the backfill too** (Bandung/Sembach precedent) so a backfilled row fingerprints identically to a live scrape. |
  | `trailLengthText` | 0 / 3 | Not in feed. |
  | `coords` (lat/lng) | 2 / 3 | `Latitude`/`Longitude` clean (#446 blank → region centroid); Rio bbox-scrub in backfill (no outliers found). |

## Kennel metadata (deep-dive complete)
- fullName: **Rio Hash House Harriers** / shortName: **Rio H3** / region: **Rio de Janeiro** / country: **Brazil**
  - (HC `KennelName` = "Rio Hash House Harriers"; `KennelShortName` = "RIOH3"; `KennelSlug` = "RIOH3"; `KennelContinent` = "South America". Source: `hashruns.org/api/global-runs` 2026-07-10.)
- aliases: `["Rio H3", "RIOH3", "RioHHH", "Rio Hash", "Rio Hash House Harriers"]` — **all clear** (grep-checked; no global collision).
- website: **none found** — the only historical site (`http://riohhh.multiply.com/`, cited by a 2009 Rio Times article) is on the **defunct** multiply.com; `rioh3.com`/`riohhh.com` return DNS Status 0 but **do not resolve in-browser** (`ERR_NAME_NOT_RESOLVED`) → not usable. ⚠️ blank + flag. Primary live presence is the Facebook group below. (source: WebSearch 2026-07-10; gotothehash.net/southam.html.)
- facebook: **`https://www.facebook.com/groups/riohash/`** (source: `gotothehash.net/southam.html` — "Rio H3 … Website: facebook.com/groups/riohash/"; corroborated by WebSearch).
- instagram: **`rioh3_official`** (source: WebSearch — `instagram.com/p/DVw8Jd3jMBY/` "@rioh3_official"). ⚠️ confirm the handle renders a live profile at build.
- twitter / discord: **none found** → omit.
- schedule: **monthly, Saturday ~15:00** — evidenced by the 3 HC runs (all Saturday 15:00, 28-day spacing). ⚠️ A 2009 Rio Times article said the Rio hash "meets twice a month" — that likely conflates the **Rio Full Moon H3** sibling (see sibling sweep); the HC feed for *this* kennel is monthly. Flat fields (not `scheduleRules` — single monthly pattern):
  ```ts
  // scheduleDayOfWeek/scheduleTime are the fallback; per-run times flow from the HC feed.
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "3:00 PM", // 12-hr per kennels.ts convention (HC startTime stays 24-hr "15:00")
  scheduleFrequency: "Monthly",
  ```
  (source: 3 HC runs, 2026-04-11 → 2026-06-06.)
- foundedYear: **unverifiable** → blank + flag. Run numbers are deep (#446) but no primary source confirms a founding year, and the historical cadence is unknown (deep # ≠ a clean per-year rate). Do NOT assert a year. (Milestone-post scan N/A — no archive/blog exists to scan.)
- hashCash: **"R$30"** (HC `EventPriceForMembers`/`ForNonMembers` = `30` on all 3 runs; currency **inferred BRL** from country — HC carries no currency field). ⚠️ flag "confirm currency/amount at build" (per the Algarve HC-currency rule).
- dogFriendly / walkersWelcome: unknown → omit (unverified).
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/RIOH3_20260409223332.avif`** → ⚠️ **self-host** to `public/kennel-logos/rioh3.<ext>` (HC blob; confirm the extension via **magic bytes**, NOT the `.avif` name — HC blobs are often WebP/AVIF/PNG regardless of the URL suffix). Reference `/kennel-logos/rioh3.<ext>`.
- lat/lng: **−22.9068 / −43.1729** (Rio de Janeiro city centre — matches the new Rio de Janeiro METRO centroid below).

## Region seeding — 4 `region.ts` edits (new **Rio de Janeiro** METRO under existing Brazil COUNTRY)
> 🟢 **Brazil COUNTRY is already seeded** (`region.ts:3681`, `America/Sao_Paulo`, emerald palette) with **Brasília** + **São Paulo** metros. Rio de Janeiro is a **new metro under an existing country** → the reduced edit set (METRO seed + 2 group maps + inference extension). Mirror the São Paulo METRO record exactly for palette/timezone.

1. **`REGION_SEED_DATA`** — add the METRO record immediately after the São Paulo block (`region.ts:3713`):
   ```ts
   {
     name: "Rio de Janeiro",
     country: "Brazil",
     timezone: "America/Sao_Paulo",
     abbrev: "RIO",
     colorClasses: "bg-emerald-100 text-emerald-700", // metro = lighter -100/-700 (mirror São Paulo)
     pinColor: "#10b981",
     centroidLat: -22.9068,
     centroidLng: -43.1729,
     aliases: ["Rio", "Rio de Janeiro, Brazil"],
   },
   ```
2. **`STATE_GROUP_MAP`** — add after `"São Paulo": "Brazil"` (`region.ts:4372`):
   ```ts
   "Rio de Janeiro": "Brazil",
   ```
3. **`COUNTRY_GROUP_MAP`** — add after `"Brasília": "Brazil"` (`region.ts:4596`):
   ```ts
   "Rio de Janeiro": "Brazil",
   ```
4. **`COUNTRY_INFERENCE_RULES`** — extend the existing Brazil rule (`region.ts:4032`) to add the **unambiguous** `rio de janeiro` token (🔴 do NOT add bare `rio` — it collides with Rio, WI / Rio Grande / Rio Rancho NM and `inferCountry()` is first-match with a USA default):
   ```ts
   // before: [/\b(brazil|brasil|bras[ií]lia|são paulo|sao paulo)\b/, "Brazil"],
   [/\b(brazil|brasil|bras[ií]lia|são paulo|sao paulo|rio de janeiro)\b/, "Brazil"],
   ```
   (The seed kennel carries explicit `country: "Brazil"`, so this only protects the free-form research/inference path. Add/keep a disambiguation test if the Brazil suite has one — e.g. assert "Rio, WI" is NOT inferred as Brazil.)

> Note: `COUNTRY_CODE_TO_NAME` already has `BR: "Brazil"` (`region.ts:4673`) — no edit. Verify at build that São Paulo is present in `COUNTRY_GROUP_MAP`; if the existing pattern only lists country + Brasília, keep the Rio addition consistent with whatever makes `groupRegionsByCountry` place Rio under Brazil.

## Historical backfill
- Available: **3 past runs** in the HC feed (2026-04-11 → 2026-06-06). Fields: date/title/coords(2 of 3)/venue(3)/hares(2 of 3 — #446 bleed). No standalone `description` emitted; `cost` from HC price 30 (kennel-level `hashCash`). **No separate archive** for #1–#443 — no Rio H3 blog/WordPress/Sheet was found (the FB group is the only external presence, and FB group history isn't a scrapeable feed), so this backfill covers **only the HC-join window**.
- Plan: **recommended** one-shot `scripts/backfill-rioh3-history.ts` — **frozen-JSON + dumb loader** (Bandung/H7/Sembach convention: extract once, commit `scripts/data/rioh3-history.json`, no parser committed). Extraction recipe (mirror `scripts/backfill-bandung-h3-history.ts` + the shared `scripts/lib/hc-global-runs.ts`):
  - Sweep `global-runs?isFuture=0&minEventDate=2026-01-01&maxEventDate=<today>` via `sweepGlobalRuns` (windows + halves on 500), filter client-side to `PublicKennelId 8d230b92-a6e6-4d8b-8c13-fe004a260d25`.
  - Map with `mapRunToRawEvent` (title-fallback + `composeHcLocation` parity). **Trust `EventNumber` for `runNumber` (446/445/444), NOT the title** — #446's title reads "466" (typo).
  - 🔴 **Drop the #446 `hares`** (it equals the `location` string — HC location-bleed; per the HC "omit `Hares` when it looks like a venue" rule).
  - Apply the **Rio bbox scrub** (drop coords outside lat −23.15…−22.70 / lng −43.90…−43.00 — none in this sample; #446 already has no coords) + a sane **06:00–21:59 time-gate** (all sampled `15:00` pass); **omit `description`** for live-adapter fingerprint parity.
  - Freeze to `scripts/data/rioh3-history.json`; loader binds to the `"Rio H3 Harrier Central"` source, `kennelTimezone: "America/Sao_Paulo"`.
- 🔴 **`config.upcomingOnly: true` is REQUIRED** (Bandung contract): HC `getEvents` is future-only, so without it `reconcile.ts` false-CANCELs the 3 backfilled past rows as they age off the 365-day window. Keep it in the seed below regardless of whether the backfill ships.
- **Frozen-dataset validation checklist** (run before shipping): PII scrub (grep `@`/phone shapes — none expected); run numbers are monotonic (444→445→446, key by date anyway); gap-sanity (28-day spacing, no misparse); field-bleed check on `hares` (#446 already flagged), `location`.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "rioh3", // clear; no rio* code/alias exists
  shortName: "Rio H3",
  fullName: "Rio Hash House Harriers",
  region: "Rio de Janeiro", // NEW METRO — see Region seeding (4 region.ts edits)
  country: "Brazil",
  // No website: the only historical site (riohhh.multiply.com) is on defunct multiply.com;
  // rioh3.com/riohhh.com don't resolve. The live external presence is the Facebook group.
  facebookUrl: "https://www.facebook.com/groups/riohash/",
  instagramHandle: "rioh3_official", // ⚠️ confirm the profile renders at build
  logoUrl: "/kennel-logos/rioh3.<ext>", // self-hosted from HC blob; verify ext by magic bytes
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "3:00 PM", // 12-hr per kennels.ts convention (HC startTime stays 24-hr "15:00")
  scheduleFrequency: "Monthly",
  scheduleNotes: "Monthly Saturday-afternoon trail (~3:00 PM) through Rio de Janeiro, often starting from a metro station (Leblon / São Conrado / Copacabana). See the Rio Full Moon H3 sibling for the full-moon runs.",
  hashCash: "R$30", // HC price 30; currency inferred BRL — ⚠️ confirm at build
  // foundedYear intentionally omitted — unverifiable (HC-only; deep run # but no primary founding source).
  description: "The Rio de Janeiro Hash House Harriers — 'a drinking club with a running problem.' Monthly Saturday-afternoon trails around Rio, frequently starting from a metro station, with beer stops along the way.",
  latitude: -22.9068, longitude: -43.1729,
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug)
"rioh3": ["Rio H3", "RIOH3", "RioHHH", "Rio Hash", "Rio Hash House Harriers"],

// sources.ts — Source[] (array of objects). Mirror the Bandung H3 HC row (sources.ts:7443).
{
  name: "Rio H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live 2026-07-10 via the adapter's exact
    // getEvents POST [0 future runs posted] + hashruns.org/api/global-runs [3 past runs #444–#446]).
    publicKennelId: "8d230b92-a6e6-4d8b-8c13-fe004a260d25",
    defaultKennelTag: "rioh3",
    // Rio names events with real themes ("The Rites of Infertility Hash", "A Farewell to Bars")
    // kept verbatim; defaultTitle synthesizes "Rio H3 #N" only for empty/placeholder names.
    defaultTitle: "Rio H3",
    staleTitleAliases: ["Placeholder event for RIOH3"],
    // upcomingOnly:true — REQUIRED (HC getEvents is future-only + this source owns a 3-run
    // backfill; without it reconcile.ts false-CANCELs the past rows as they age off the
    // 365-day window — Bandung #2340 contract).
    upcomingOnly: true,
  },
  kennelCodes: ["rioh3"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only HARRIER_CENTRAL — zero new adapter code.** The `HARRIER_CENTRAL` adapter already
exists in `src/adapters/registry.ts` (`src/adapters/harrier-central/adapter.ts` + `token.ts`) and is
used by ~30 seeded kennels (Bandung, Sembach, Barbados, BEER H3, Taiwan H3, …). The source row above is
the whole live implementation; the backfill reuses `scripts/lib/hc-global-runs.ts`. The adapter derives
events from HC's `getEvents` API (future-only), resolves `rioh3` via `defaultKennelTag`, and applies
`defaultTitle`/`staleTitleAliases` for name synthesis.

**⚠️ Claude Code: verify before writing/relying on any field.** This is config-only, so the main checks are:
- Confirm `publicKennelId` returns Rio's run(s) via a throwaway `adapter.fetch(source)`. **Expect 0
  events** — no future run was posted at research time (last run #446 was ~34 days ago on a monthly
  cadence). If a new run (#447+) has since been posted it will appear. **Do not treat 0 future events
  as a failure** — the backfill supplies the page's content and the daily scrape auto-adds the next run
  once posted (recently-active kennel).
- Confirm `kennelTag` resolves to `rioh3` with **no unmatched** and `startTime` is "HH:MM".
- Confirm the HC feed's `America/Sao_Paulo` tz flows through unchanged (it's correct — **no** tz-quirk
  override here, unlike Belgrade/KRASH).
- `RawEventData` field names are `kennelTags: string[]` (not `kennelTag`); `walkersWelcome` (not
  `walkerFriendly`). Don't hand-set `title` — the HC adapter + `defaultTitle` handle it.
- **`friendlyKennelName("Rio H3", …)`** — shortName is >4 chars, so it short-circuits to "Rio H3"
  (synth title "Rio H3 Trail #N"). Quick-verify at build if you touch title synthesis.

## Sibling sweep
- **Rio Full Moon H3 (RFMH3)** — a **distinct** kennel at the same city (`gotothehash.net/southam.html`:
  "Rio H3 (Also see Rio Full Moon H3 at same site)"), full-moon monthly, separate HC `PublicKennelId
  98a6791b-0514-4d0b-969c-f43d24e83413`. **Intentionally excluded** from this source (different GUID) —
  it's **already queued** as target-queue rank 9 and will reuse this Rio de Janeiro METRO (0 region.ts
  edits once Rio H3 ships first). Do NOT fold it into this source config.
- No other Rio sibling surfaced on the HC feed or the directories.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host + magic-byte ext)  [ ] foundedYear (unverifiable — flagged)  [x] socials (FB group + IG `rioh3_official`)  [x] schedule (monthly Sat + scheduleNotes)  [x] hashCash (R$30 — currency inferred, flagged)
- [x] description  [x] source live-verified (recently-active; #446 ~34d ago, 3-run HC archive)  [x] history depth assessed (3 on HC; HC-join ~Apr 2026; no separate archive)
- [x] coord sanity checked (2 of 3 in Rio bbox; #446 blank → centroid)  [x] end times noted (none)  [x] kennelCode collision-checked (`rioh3` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **Recently-active, NOT stale** — 0 future events at research time is expected; the kennel ran #446
  ~34 days ago on a 28-day monthly cadence. Ship the backfill so the page isn't empty; the daily scrape
  adds #447 when posted.
- **`config.upcomingOnly: true` required** (Bandung contract) — HC is future-only; protects the 3
  backfilled past rows from `reconcile.ts` cancellation.
- **Backfill = frozen JSON + dumb loader** (mirror `backfill-bandung-h3-history.ts`), not a committed
  parser. Trust `EventNumber` for `runNumber` (#446's title typo says "466"); drop the #446 `hares`
  (location-bleed).
- **Omit `description` in the backfill** — the live HC adapter doesn't emit it; including it would
  re-fingerprint every row on the first live re-scrape.
- **Self-host the HC blob logo** to `public/kennel-logos/rioh3.<ext>`; confirm ext by magic bytes
  (the `.avif` URL name is not authoritative).
- **4 region.ts edits** — new Rio de Janeiro METRO under existing Brazil COUNTRY (METRO seed +
  STATE_GROUP_MAP + COUNTRY_GROUP_MAP + Brazil inference-rule extension with `rio de janeiro`, NOT bare
  `rio`). Mirror the São Paulo METRO record for palette/timezone.
- **HC currency-free prices** — `30/30` are bare numbers; `hashCash: "R$30"` infers BRL from Brazil.
  Confirm at build (Algarve rule).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
