# Onboarding Handoff — Taiwan H3 (Taipei, Taiwan) — 2026-06-10

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/twh3-tw-20260610`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). This is HashTracks' **first
>    Taiwan kennel** → add a **Taiwan COUNTRY + Taipei METRO** region with **all 5 `src/lib/region.ts`
>    edits** (see **Adapter notes → Region (NEW COUNTRY)**). Taiwan is a country→metro country (like
>    Singapore / New Zealand / Hong Kong) — **no `seed.ts stateMetroLinks` entry needed** (that's only
>    for STATE_PROVINCE countries like Canada/BC).
> 3. Configure the **config-only `HARRIER_CENTRAL`** source exactly as in **Ready-to-paste seed**
>    (the `HarrierCentralAdapter` already exists in `src/adapters/registry.ts` — **no new adapter code**).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: events non-empty + all
>    upcoming, dates UTC-noon, `startTime` "HH:MM" (expect `14:30`), `kennelTag` resolves to `twh3-tw`
>    with no unmatched / no `SOURCE_KENNEL_MISMATCH`. Resolve the one `⚠️ Claude Code must confirm`
>    item in **Live source verification** (the Azure HC API is allowlist-blocked from the research
>    sandbox — it was verified from a browser page context instead; confirm it fetches from your local
>    env, where the HC API is reachable). **DO NOT run `npx prisma db seed` here.**
> 5. No historical backfill in this PR (see **Historical backfill** — HC is future-only; the deep
>    archive lives on the kennel's WordPress blog as unstructured prose, deferred).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, run after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge can silently drop a follow-up
>      commit): `git log -1 -- prisma/seed-data/sources.ts`, `… kennels.ts`, `… aliases.ts`,
>      `src/lib/region.ts`, `public/kennel-logos/twh3-tw.<ext>`. Open a small recovery PR if any is missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds new kennel/alias/source + Taiwan COUNTRY + Taipei METRO)
>    - Trigger a scrape from `/admin/sources` (or the equivalent admin action) to publish events to prod
>    - Spot-check `https://www.hashtracks.xyz/kennels/taiwan-h3` for the 3 upcoming events + sample
>      dates (2026-06-14 #2662, 2026-06-21 #2663, 2026-06-28 #2664). The next weekly Sunday run appears
>      on the first scrape after the kennel posts it to Harrier Central.

## Summary
- Type: **full onboard** (🇹🇼 **first Taiwan kennel on HashTracks**)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `registry.ts` & feed verified live)
- Effort estimate: **config-only** + **Taiwan COUNTRY + Taipei METRO** region (all 5 `region.ts` edits — new country) + self-host one logo. No new adapter code, no backfill script.
- One-line: Taiwan H3 (台灣健龍捷兔, est. 1975, run #2664) is one of Asia's oldest hashes — a weekly Sunday Taipei kennel that publishes its schedule to Harrier Central → zero new code, mirrors the Lisbon H3 / Porto Invicta H3 / Hamburg H7 HC pattern exactly.

## Dedup result
- Kennel in seed: **no** (grep `taiwan|taipei|twh3-tw|formosa|健龍|捷兔` in `kennels.ts` → 0 hits; the only `twh3` hit is **Tidewater H3** `kennels.ts:1431`, a different kennel — see kennelCode note)
- Source in seed: **no** (grep `taiwan|taipei|TwH3` in `sources.ts` → 0 hits)
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP, **436 slugs** (2026-06-10); **no `taiwan*`/`taipei*`/`twh3*`/`formosa*` slug** (regex `/taiwan|taipei|twh3|tw-h3|jianlong|formosa/i` → 0 matches). No kennel generates slug `taiwan-h3`.
- Decision: **full onboard**
- kennelCode: **`twh3-tw`** 🔴 (collision check: bare **`twh3` is TAKEN — it's Tidewater H3** `kennels.ts:1431`, Hampton Roads VA, with aliases `["TH3","Tidewater Hash","Tidewater","Tidewater HHH","TH3 VA"]`). Disambiguated with the `-tw` region suffix per the manual's cross-platform collision rule. `grep '"twh3-tw"'` → clear. HC's own `kennelUniqueShortName` is "TwH3" but that bare code can't be ours.
- slug: **`taiwan-h3`** (from shortName "Taiwan H3"; `toSlug("Taiwan H3")` = `taiwan-h3`, all-ASCII so **no `slug:` pin needed**; confirmed sitemap-absent)
- Pre-onboarding admin-event check: kennel has no prod slug/page → no admin-seeded `Event` rows expected. Low risk; Claude Code may spot-check prod for stray `twh3-tw` events before first scrape, but none anticipated.

## Live source verification  ⚠️ needs Claude Code to confirm from local env (API allowlist-blocked from research sandbox)
- Source: **HARRIER_CENTRAL** — `https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, filter `publicKennelId: f1330d14-e3b4-427a-9bea-639f18218804` (`kennelUniqueShortName: "TwH3"`). **All three filters — `publicKennelIds` (GUID), `cityNames:"Taipei"`, and `kennelUniqueShortName:"TwH3"` — returned the IDENTICAL 3-event set (single kennel "Taiwan Hash House Harriers | 台灣健龍捷兔"). Verified 2026-06-10.**
  - ⚠️ The Azure HC API is **not in the research-sandbox network allowlist** (DNS `EAI_AGAIN`). It was verified by executing the time-token + `fetch` **inside a browser page context** (Chrome MCP, `crypto.subtle` SHA-256, same epoch `Date.UTC(1963,7,15,9,52,28)` / divisor `86469` as `src/adapters/harrier-central/token.ts`) from `hashruns.org` — `status 200`, real data, count 3. The HC API is reachable from a normal dev machine; **Claude Code: confirm `adapter.fetch(source)` returns the 3 events from your local env.** (Same plumbing as the live Lisbon / Porto / Tokyo / Singapore / Hamburg / Morgantown HC sources — routine.)
- Events seen: **3** upcoming, **2026-06-14 → 2026-06-28** (all Sundays, weekly cadence).
- **Source-count parity:** HC by-kennel returns its full forward set (3). Filters by GUID, city, and short-name all return the same 3. Parity = **3/3 (100%)** — single API response, no `nextPage`, no pagination needed.
- Sample events (**verbatim from the API**, names/run numbers unaltered):
  1. `#2662` — **2026-06-14** 14:30 — "TwH³ 2662 - Low Hanging Fruit低垂的禁果" — hares "Only Grans;Shibari GayBlow" — venue "永定聖興宮 石碇區" (Shiding District) — resolvable "靜安路一段, 新北市, 223001, 新北市, 台灣" — coords `25.0147, 121.6988` (real New Taipei pin) — cc "新北市, 台灣"
  2. `#2663` — **2026-06-21** 14:30 — "TwH³ 2663" (no theme) — hares (none listed) — resolvable **"No location provided"** → coords `25.03583333, 121.5683333` (Taipei region-default pin — **dropped by adapter**, see Coord sanity) — cc "Taipei, Taiwan"
  3. `#2664` — **2026-06-28** 14:30 — "TwH³ 2664" (no theme) — resolvable **"No location provided"** → Taipei default pin (**dropped**) — cc "Taipei, Taiwan"
  - 🔴 **Run numbers are present and HUGE (#2662–#2664)** — Taiwan H3 has run weekly since 1975 (~2,664 runs). They are **sequential/monotonic** here. Event **names** are the kennel's own `TwH³ <N>` form (sometimes with a theme suffix, e.g. #2662); these are real event names, not placeholders — the HC adapter passes them through as `title` (see Field-fill table). No fabricated samples: #2663/#2664 genuinely have no theme.
- History depth / pagination: **single API response, no pagination.** HC `getEvents` returns **future-only** (no past events). Run #2664 implies a ~2,600-run history, but **HC won't serve it** — the only archive is the kennel's WordPress blog as unstructured prose (see **Historical backfill** — deferred).
- Coord sanity: **2 of 3 events** carry HC's **Taipei region-default pin** `25.03583333, 121.5683333` paired with `resolvableLocation: "No location provided"` → the existing adapter's `GEOCODE_FAIL_SENTINELS` set contains **`"no location provided"`**, so `hcGeocodeFailed()` returns true → the adapter **drops those coords (+ `dropCachedCoords:true`)** and `composeHcLocation`/`stripPlaceholderLocation` drops the placeholder text → those rows render unlocated and the merge falls back to the **Taipei metro centroid**. Event #2662 has a **real venue + real New Taipei coords** (`25.0147, 121.6988`) → kept. **No wrong-continent / silent-corruption risk; handled by existing adapter code, no config needed.**
- End times: **none** (HC `getEvents` payload has no end timestamp).
- Notes: Config-only HC, identical pattern to **Lisbon H3** (`sources.ts`), **Porto Invicta H3** (PR #2073), and **Hamburg H7** — all use `publicKennelId` + `defaultTitle` + `staleTitleAliases`. Mirror them exactly.
- **Field-fill assertion table** (from the 3-event sample):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 3 / 3 | HC sets it; real `TwH³ <N>` names pass through trimmed. Empty/placeholder names → `defaultTitle`+`#N` ("Taiwan H3 #N") via `staleTitleAliases`. The `TwH³ <N>` form is the source's own naming — faithful, kept |
  | `runNumber` | 3 / 3 | `eventNumber` (2662–2664). Monotonic; store faithfully |
  | `startTime` | 3 / 3 | `HH:MM` from `eventStartDatetime` → `14:30` (hares off 14:30; pack off ~15:00) |
  | `endTime` | 0 / 3 | not in HC feed — accept absence |
  | `location` (venue name) | 1 / 3 | #2662 real venue ("永定聖興宮 石碇區"); #2663/#2664 placeholder "No location provided" → dropped by adapter (renders unlocated, Taipei centroid). Don't synthesize |
  | `locationStreet` | 1 / 3 | #2662 resolvable street ("靜安路一段, 新北市…"); others not in feed |
  | `locationUrl` (Maps) | 0 / 3 | not in feed |
  | `hares` | 1 / 3 | #2662 "Only Grans;Shibari GayBlow"; #2663/#2664 none listed yet (hares post day-of) |
  | `cost` | 0 / 3 | kennel default lives on `Kennel.hashCash`; no per-event override in feed |
  | `description` | 0 / 3 | not in HC feed — leave undefined |
  | `coords` (lat/lng) | 1 / 3 *(real)* | #2662 real New Taipei pin kept; #2663/#2664 default pin dropped via `hcGeocodeFailed` ("no location provided") → re-geocode to Taipei |

## Kennel metadata (deep-dive complete)
> Every field cites a source URL. The metadata source domain **`taiwanhash.com` is the kennel's own live site** (HTTP 200, WordPress). HC `harriercentral.blob.core.windows.net` is the platform CDN.
- fullName: **Taiwan Hash House Harriers** (Chinese: **台灣健龍捷兔**) / shortName: **Taiwan H3** / region: **Taipei** / country: **Taiwan**
- aliases: `["TwH3-TW", "Taiwan Hash House Harriers", "Taiwan H3", "Taiwan Hash", "台灣健龍捷兔"]`
  - 🔴 **Bare `TwH3` / `TWH3` OMITTED** — lowercases to `twh3`, which is **Tidewater H3's kennelCode**; kennel resolution does kennelCode-exact-match first, so a bare `TwH3` alias would mis-route to Tidewater. Use the region-suffixed **`TwH3-TW`** as the public short alias (manual cross-platform collision rule).
  - 🔴 **The former name "China Hash House Harriers" is DELIBERATELY OMITTED as an alias** — the kennel ran under that name until Dec 2020 (`taiwanhash.com/about`), but "China Hash" is globally ambiguous (mainland-China kennels + a separate "China Hash Taipei" FB group exist) → including it risks mis-routing other kennels' events. The history is captured in `description`/`scheduleNotes` instead. All proposed aliases verified absent from `aliases.ts` (`grep` → 0 hits).
- website: **`https://taiwanhash.com/`** (source: live site, HTTP 200, WordPress; `meta-msapplication-TileImage` references the Taiwan Hash logo)
- facebookUrl: **`https://www.facebook.com/groups/taiwanhash/`** (source: web search "Taiwan Hash House Harriers Facebook"; titled "Taiwan Hash House Harriers | 台灣健龍捷兔"). It's a FB **group**, not a Page → **not** usable for `FACEBOOK_HOSTED_EVENTS`.
- twitterHandle: **`hashtaiwan`** (source: `x.com/hashtaiwan` — "Taiwan Hash House Harriers (@HashTaiwan)")
- instagramHandle / discordUrl: **none found** (leave undefined)
- schedule: **Sunday, 2:30 PM, Weekly** (source: `taiwanhash.com` — *"Running every Sunday since 1975"* + multiple run posts *"Hares Off 14:30"*; HC events all `14:30` on consecutive Sundays). ⚠️ A secondary web-search blurb says "every Sunday afternoon at 3:00pm" — that's the **pack-off** time; **hares off (and the HC event start) is 14:30**. Seed `scheduleTime: "2:30 PM"` (matches the authoritative HC feed); note the ~3:00pm pack-off in `scheduleNotes`.
- foundedYear: **1975** (source: `taiwanhash.com/about` — *"We were founded in 1975"*; corroborated by the **"Taiwan Hash 50th years 2025"** event on hashrego.com). ⚠️ **Internal note surfaced & resolved:** the kennel's own logo says **1976**, but their About page + a run post explicitly state the first run was **1975** ("despite our logo proudly stating we were founded in 1976 we actually held our first run in the year of 19 and 75"). Canonical **1975** (the kennel's own correction).
- hashCash: **`Approx. NT$250 (men) / NT$200 (women) per run`** (source: `taiwanhash.com` run posts — regular runs show "Men $250 / Women $200"; joint/special runs vary, e.g. "$200/$150" for an NTH3-organized beer truck). ⚠️ Gender-tiered and **per-run variable**; figures are from 2021-era posts (the homepage WordPress shows cached older events). Per-event variations belong on `Event.cost` (the HC feed carries no cost). Seeded as a free-form string with the caveat; **lower confidence** — flag for confirmation.
- dogFriendly: **unknown** (not stated on the site) → leave **undefined**.
- walkersWelcome: **unknown** (not explicitly stated; hashes generally welcome walkers but the site doesn't say) → leave **undefined** to avoid asserting unverified data.
- description: `"Taiwan Hash House Harriers (台灣健龍捷兔, 'TwH³') is one of Asia's oldest hashes — a mixed, English-and-Chinese-speaking 'drinking club with a running problem' that has run every Sunday afternoon around the Taipei / New Taipei area since 1975 (run #2664 as of June 2026). Founded out of the 1973 Taipei Hash, it ran as the China Hash House Harriers until December 2020, when it was renamed to reflect its Taiwanese identity. Hares off at 14:30; trails are followed by down-downs and the usual circle. On On!"`
- logoUrl: **self-host.** Primary stable URL: **`https://taiwanhash.com/wp-content/uploads/2021/01/Taiwan-Hash-Logo-e1609585534508.png`** (non-tokenized WordPress upload — stable). Alternative: the HC blob **`https://harriercentral.blob.core.windows.net/harrier/Taiwan%20H3%20Logo.png`** (also non-tokenized). ⚠️ **Self-host** either to `public/kennel-logos/twh3-tw.<ext>` and reference `/kennel-logos/twh3-tw.<ext>`. **Confirm the real extension via magic bytes** (`file public/kennel-logos/twh3-tw` — `\x89PNG`=PNG, `RIFF…WEBP`=WebP, `\xff\xd8`=JPEG); both source URLs end `.png` but verify the bytes.
- lat/lng: **25.0375, 121.5637** (Taipei City centroid — the kennel is Taipei-based; matches HC's own Taipei default pin `25.0358, 121.5683`. Per-run venues range across Taipei + New Taipei and are announced day-of.)

## Historical backfill
- Available: **none in this PR.** HC `getEvents` exposes **future-only** events (the 3 upcoming). Run #2664 implies ~2,600 runs since 1975, but **no structured source serves them**: the kennel's WordPress blog (`taiwanhash.com`) carries the archive as **free-form prose recaps** — theme titles (not `Run #N`), narrative jokey bodies, and a buried `Hares Off 14:30` line, with **no consistent labeled `Date:`/`Hare:`/`Venue:` structure** and the run number not in the post title (sampled the homepage posts directly — they are narrative prose, e.g. #N is not surfaced; hare names and venues are embedded in story text). This does not meet the clean-structured-archive bar for a one-shot backfill.
- Plan: **none** (future-only adapter; the kennel page populates from the 3 live HC events on first scrape, and fills weekly as the kennel posts each Sunday run to HC). A WordPress-REST backfill of ~2,600 prose posts would be a large bespoke effort — **deferred** as a possible future follow-up, not part of this config-only onboard. (Evidence standard met: archive sampled, found unstructured — not dismissed unseen.)

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` (kennelCode → string[]) — confirmed via `head prisma/seed-data/aliases.ts`. **Keyed by kennelCode (`twh3-tw`), NOT slug.**

```ts
// kennels.ts — Kennel[] (array of objects). Place near other East-Asian kennels.
{
  kennelCode: "twh3-tw", shortName: "Taiwan H3", fullName: "Taiwan Hash House Harriers",
  region: "Taipei", country: "Taiwan",
  foundedYear: 1975,
  scheduleDayOfWeek: "Sunday", scheduleTime: "2:30 PM", scheduleFrequency: "Weekly",
  scheduleNotes: "Weekly Sunday around the Taipei / New Taipei area. Hares off at 14:30 (pack off ~3:00pm). Per-run venues announced day-of by the hares via Harrier Central / Facebook; HC per-event start times are authoritative. Run #2664 as of June 2026 (running since 1975). Hash cash is gender-tiered and varies per run (typically ~NT$250 men / NT$200 women; special/joint runs differ — see Event.cost).",
  description: "Taiwan Hash House Harriers (台灣健龍捷兔, 'TwH³') is one of Asia's oldest hashes — a mixed, English-and-Chinese-speaking 'drinking club with a running problem' that has run every Sunday afternoon around the Taipei / New Taipei area since 1975 (run #2664 as of June 2026). Founded out of the 1973 Taipei Hash, it ran as the China Hash House Harriers until December 2020, when it was renamed to reflect its Taiwanese identity. Hares off at 14:30; trails are followed by down-downs and the usual circle. On On!",
  hashCash: "Approx. NT$250 (men) / NT$200 (women) per run (gender-tiered; varies per run)",
  logoUrl: "/kennel-logos/twh3-tw.<ext>",   // ← self-host taiwanhash.com or HC-blob logo; confirm ext via magic bytes
  facebookUrl: "https://www.facebook.com/groups/taiwanhash/",
  twitterHandle: "hashtaiwan",
  website: "https://taiwanhash.com/",
  latitude: 25.0375, longitude: 121.5637,
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]).
"twh3-tw": ["TwH3-TW", "Taiwan Hash House Harriers", "Taiwan H3", "Taiwan Hash", "台灣健龍捷兔"],

// sources.ts — Source[] (array of objects). Mirror the Lisbon H3 / Porto Invicta H3 HC record.
{
  name: "Taiwan H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter; cityNames:"Taipei" and kennelUniqueShortName:"TwH3"
    // both returned the identical 3-event set (all three verified 2026-06-10).
    publicKennelId: "f1330d14-e3b4-427a-9bea-639f18218804",
    defaultKennelTag: "twh3-tw",
    // The kennel names events "TwH³ <N>" (real names, kept). defaultTitle synthesizes
    // "Taiwan H3 #N" only if an event name is empty or a known placeholder appears.
    defaultTitle: "Taiwan H3",
    staleTitleAliases: ["Placeholder event for TwH3"],
  },
  kennelCodes: ["twh3-tw"],
},
```

> **`upcomingOnly` intentionally OMITTED** — matches every existing HC source (Lisbon H3, Porto Invicta H3, Hamburg H7, Tokyo H3, Singapore Sunday H3, Morgantown H3 all omit it). The HC adapter emits future-only and survives reconciliation without it. Don't add it. (Taiwan H3 has no archive ingested here to protect anyway.)

## Adapter notes / new-scraper plan

**Config-only — no new adapter code.** `HARRIER_CENTRAL` → `HarrierCentralAdapter` is registered in `src/adapters/registry.ts`. The config above is the entire implementation. The adapter already handles: time-based token auth, the `[[...events]]` response shape, `staleTitleAliases`+`defaultTitle` fallback, TBA/placeholder stripping (`GEOCODE_FAIL_SENTINELS` already contains `"no location provided"` → fires for #2663/#2664), `hcGeocodeFailed` coord-dropping + `dropCachedCoords:true`, run-number passthrough, and the `days`/`scrapeDays` window.

### Region (NEW COUNTRY — Taiwan; all 5 `src/lib/region.ts` edits)
🔴 **Taiwan is a brand-new country** → do **all 5** edits (the ONH3-in-CI gap was a missing `COUNTRY_INFERENCE_RULES` entry — do NOT omit #5). Taiwan is country→metro (like Singapore / NZ / Hong Kong): use the country name as the `STATE_GROUP_MAP` group; **no `seed.ts stateMetroLinks` entry** (that's STATE_PROVINCE-only). Pick a **sky** palette — distinct from East-Asian neighbors (Japan/Singapore/Hong Kong = red, Thailand = orange, Malaysia = green). Mirror the **Japan COUNTRY+Tokyo METRO** shade pattern exactly (`region.ts:1677` / `:1689`): country = `-200`/`-800` text + `-600` pin; metro = `-100`/`-700` text + `-500` pin.

1. **`REGION_SEED_DATA`** — add the COUNTRY **and** METRO records (copy the Japan/Tokyo pair verbatim and edit values; **note the METRO record OMITS `level`** — only the COUNTRY record sets `level: "COUNTRY"`):
   ```ts
   {
     name: "Taiwan",
     country: "Taiwan",
     level: "COUNTRY",
     timezone: "Asia/Taipei",
     abbrev: "TW",
     colorClasses: "bg-sky-200 text-sky-800",
     pinColor: "#0284c7",
     centroidLat: 23.97,
     centroidLng: 120.97,
     aliases: ["TW", "台灣"],
   },
   {
     name: "Taipei",
     country: "Taiwan",
     timezone: "Asia/Taipei",
     abbrev: "TPE",
     colorClasses: "bg-sky-100 text-sky-700",
     pinColor: "#0ea5e9",
     centroidLat: 25.0375,
     centroidLng: 121.5637,
     aliases: ["Taipei, Taiwan", "台北", "New Taipei"],
   },
   ```
   🔴 **Sonar S6749:** no trailing-zero numeric literals — `23.97`/`120.97`/`25.0375`/`121.5637` are all fine (do NOT write `121.0` / `25.50`).
2. **`STATE_GROUP_MAP`** (`region.ts:3445`) — add `"Taipei": "Taiwan",` near the other Asian metros (e.g. after `"Tokyo": "Japan"` `:3592`).
3. **`COUNTRY_GROUP_MAP`** (`region.ts:3770`) — add `"Taiwan": "Taiwan",` (the country-group key; per the Lisbon/Singapore precedent the metro resolves via `STATE_GROUP_MAP` first, so only the country key is reachable — mirror `"Singapore": "Singapore"` `:3839`. Do NOT add a `"Taipei"` key here).
4. **`COUNTRY_CODE_TO_NAME`** (`region.ts:3934`) — add `TW: "Taiwan",` (near `TH: "Thailand"` `:3955`).
5. **`COUNTRY_INFERENCE_RULES`** (`region.ts:3382`) — add **before** the closing of the Asia cluster (e.g. after the Thailand rule `:3406`):
   ```ts
   [/\b(taiwan|taipei|new taipei|formosa|kaohsiung|taichung|tainan)\b/, "Taiwan"],
   ```
   🔴 **Without this, `inferCountry()` falls through to "USA"** for any Taiwan text (the ONH3-in-CI failure). The English event text ("Taipei, Taiwan") matches `taiwan`/`taipei`. ⚠️ The one Chinese-only event field (`新北市, 台灣` on #2662) won't match an ASCII `\b` regex, but the kennel record sets `country: "Taiwan"` + `region: "Taipei"` explicitly and #2662 also carries the real New-Taipei coords, so its location resolves regardless; the inference rule is the backstop for venue-derived English text. (Optional: a separate non-`\b` rule for `台灣`/`台北` if you want CJK coverage — not required.)

**⚠️ Claude Code: verify before writing real code.** Any snippet here is illustrative; the live repo is authoritative. Before finalizing:
- `RawEventData` field names — `kennelTags` is `string[]` (the HC adapter already emits `kennelTags: [kennelTag]`). On `Kennel`, the fields used here — `website`, `facebookUrl`, `twitterHandle`, `scheduleNotes`, `hashCash`, `foundedYear`, `logoUrl`, `latitude`, `longitude` — were all confirmed present in `prisma/seed-data/kennels.ts` (grep). `dogFriendly`/`walkersWelcome` are intentionally omitted (unverified).
- Confirm the `Region` seed object's exact field names/optionals by copying the **Japan COUNTRY + Tokyo METRO** records (`region.ts:1677`/`:1689`) verbatim and editing values. The METRO record must NOT set `level` (Tokyo doesn't).
- `friendlyKennelName("Taiwan H3", "Taiwan Hash House Harriers")` — shortName is **9 chars (>4)** so it short-circuits to the shortName; titles are also set by the HC adapter (real `TwH³ <N>` names, or `defaultTitle: "Taiwan H3"` + `#N` for placeholders), so any synthesized title renders "Taiwan H3 #N" cleanly. No verbose-name risk. (Optional spot-check: `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Taiwan H3","Taiwan Hash House Harriers"));'`.)

## Deep-dive checklist (nothing deferred)
- [x] logo (taiwanhash.com WP upload / HC blob — flagged self-host to `public/kennel-logos/twh3-tw.<ext>`, confirm ext via magic bytes)  [x] foundedYear (1975, kennel About; 1976-on-logo discrepancy noted + resolved)  [x] socials (FB group + Twitter/X; no Instagram/Discord)  [x] schedule (Sunday/weekly/2:30 PM hares-off; 3:00pm pack-off noted)  [x] hashCash (gender-tiered ~NT$250/NT$200, per-run variable — flagged lower confidence)
- [x] description  [x] source live-verified (3 upcoming events via HC API, browser-context fetch — Claude Code to reconfirm from local env)  [x] history depth assessed (future-only; deep WP-prose archive sampled & deferred — not a clean backfill)
- [x] coord sanity checked (2/3 default pins dropped via `hcGeocodeFailed` "no location provided"; #2662 real New-Taipei pin kept)  [x] end times noted (none)  [x] kennelCode collision-checked (`twh3` = Tidewater → `twh3-tw`)  [x] kennelCodes (source guard) set (`["twh3-tw"]`)

## Sibling-kennel sweep
- HC `getEvents cityNames:"Taipei"` returned **only TwH3** (1 kennel, 3 events) — the `publicKennelIds` GUID and `kennelUniqueShortName` filters returned the identical set. **No co-located siblings on this Harrier Central source.**
- Taiwan has a dense hashing scene (the About page says "dozens of hash groups"). Distinct kennels surfaced in research — **separate kennels, NOT fed by this source**, candidates for future targeting (not bundled here):
  - **Taipei Hash** (`taipeihash.com.tw`, est. 1973 — Taiwan's oldest)
  - **New Taipei Hash / 新北捷兔** (`newtaipeihash.com`)
  - **Taoyuan Metro Hash / 桃園捷運兔** (`tymh3.com`)
  - **Kaohsiung Hash** (`kaohsiunghash.com`)
  - **228 Hash** (joint-run partner referenced in TwH3 posts)
  Each needs its own live-source verification (some may be config-only HC like TwH3 — worth an HC city sweep on "Taipei"/"Kaohsiung"/"Taoyuan" when refilling). Documented for the leads list; not onboarded here.

## Source confirmation (queue proposed HARRIER_CENTRAL — confirmed, no pivot)
- Queue proposed config-only **HARRIER_CENTRAL** with `kennelUniqueShortName:"TwH3"` / `cityNames:"Taipei"`. **Confirmed live** 2026-06-10 (browser page-context fetch, status 200, 3 upcoming events). Captured the stable `publicKennelId` GUID (`f1330d14-…`) — the most stable filter (mirrors Lisbon/Porto). No source pivot needed.
- The kennel's own website (`taiwanhash.com`) is **deliberately NOT the scrape source** — it's a WordPress site whose per-run details are narrative prose (no structured calendar/feed for forward events; the dated runs flow through Harrier Central). HC is the correct primary (and only structured forward) source.

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- 🔴 **`twh3` is Tidewater H3** — our kennelCode is **`twh3-tw`** and the bare **`TwH3`/`TWH3` alias is omitted** (it lowercases to Tidewater's kennelCode and would mis-route in kennel resolution). Public short alias is `TwH3-TW`.
- **HC API is allowlist-blocked from the research sandbox but reachable from a normal dev machine.** Live-verify with a throwaway `npx tsx -e` calling `adapter.fetch(source)` — expect the 3 future events (#2662–#2664, and any newer Sunday runs the kennel has posted since 2026-06-10). The count + verbatim names here were captured via a browser page-context `fetch` because the sandbox can't resolve the Azure host.
- **Coord default-pin (see Coord sanity):** the adapter already drops the Taipei default pin for #2663/#2664 via `GEOCODE_FAIL_SENTINELS` (`"no location provided"`) + `dropCachedCoords:true`; the merge re-geocodes to the Taipei centroid. #2662's real New-Taipei pin is kept. No action needed; keep config-only.
- **Run numbers are large + monotonic** — store HC `eventNumber` faithfully (2662–2664). Don't renumber.
- **`upcomingOnly` not set** — matches all existing HC sources; do not add it.
- **Self-host tokenized/CDN logos** → `public/kennel-logos/twh3-tw.<ext>`. **NEVER pre-fill the extension** — confirm via `curl -sI` Content-Type AND magic bytes (`\x89PNG`=PNG, `RIFF…WEBP`=WebP, `\xff\xd8`=JPEG). Both candidate URLs end `.png`; verify the bytes anyway. The handoff uses a literal `<ext>` placeholder deliberately.
- **NEW COUNTRY — do all 5 `region.ts` edits** (esp. #5 `COUNTRY_INFERENCE_RULES`, the ONH3-in-CI gap). Taiwan is country→metro → **no `seed.ts stateMetroLinks`** (STATE_PROVINCE-only). Pick the **sky** palette (distinct from red/orange/green Asian neighbors); mirror Japan/Tokyo's exact shade+pin pattern.
- **Sonar S6749:** no trailing-zero numeric literals in the Taiwan/Taipei region seed (`23.97`/`120.97`/`25.0375`/`121.5637`/`25.0375`/`121.5637` are all fine).
- **CJK in seed strings is fine** — `台灣健龍捷兔` / `台灣` / `台北` are valid TS string literals; ensure the file stays UTF-8 (it already contains CJK aliases for Japan/HK).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief — no separate prompt needed._
