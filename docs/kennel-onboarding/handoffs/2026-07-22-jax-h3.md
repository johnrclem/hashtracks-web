# Onboarding Handoff — JaxH3 (Jacksonville, FL) — 2026-07-22

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/jax-h3-20260722`.
> 2. This is a **source-add** (the kennel `jax-h3` is already live at `/kennels/jaxh3`, 0 events).
>    Do **NOT** re-create the kennel or the region. Apply ONLY: (a) the new **Source** row in
>    the **Ready-to-paste seed** below, and (b) the small **kennel field enrichment** (hashCash,
>    scheduleTime, walkersWelcome, logoUrl, lat/lng) on the existing `jax-h3` row in `kennels.ts`.
> 3. Configure the source as a **config-only `ICAL_FEED`** exactly as in **Adapter notes** — no new
>    adapter code (the `ICalAdapter` in `src/adapters/ical/adapter.ts` already exists).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `new ICalAdapter().fetch(source)` via a throwaway `npx tsx -e '…'` snippet. Resolve every
>    `⚠️ Claude Code must confirm` item in **Live source verification** (esp. the **TZID quirk** —
>    confirm `startTime` comes out as the real Eastern wall-clock e.g. `13:00`, NOT shifted 3h).
>    Validate: events non-empty + upcoming present, dates UTC-noon, `startTime` "HH:MM",
>    `kennelTags` all resolve to `jax-h3` with no unmatched. **DO NOT run `npx prisma db seed`
>    here** — seeding + prod scrape is the separate post-merge step (step 8).
> 5. No historical backfill script needed (the iCal feed is a full archive — it returns all ~11
>    events incl. ~9 months of past on every scrape; see **Historical backfill**).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR whose body carries the metadata, live-verification results, and the
>    deep-dive checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, run after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- prisma/seed-data/sources.ts`,
>      `-- prisma/seed-data/kennels.ts`, `-- public/kennel-logos/jax-h3.<ext>`).
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; adds the Source + SourceKennel link, updates the `jax-h3` row)
>    - Trigger a scrape from `/admin/sources` for the new source to publish events to prod
>    - Spot-check `/kennels/jaxh3` for the expected event count + sample dates (esp. upcoming #1420 Jul 25)

## Summary
- Type: **source-add** (existing live kennel `jax-h3`, currently sourceless / 0 events)
- Adapter: **ICAL_FEED** (config-only — `ICalAdapter` exists & feed HEAD-verified `text/calendar`, auth-free)
- Effort estimate: **config-only** (one `sources.ts` row + ~5 enrichment fields on the existing kennel; no new code, no region edits, no backfill script)
- One-line: Wires the first Jacksonville, FL kennel to a live feed — its own site embeds a **Timely
  (events.timely.fun) calendar** whose public ICS export is a clean, auth-free iCal feed with real
  venues and run numbers (upcoming #1420 this Saturday).

## Dedup result
- Kennel in seed: **yes** @ `prisma/seed-data/kennels.ts:2187` (`kennelCode: "jax-h3"`, shortName `JaxH3`)
- Aliases in seed: **yes** @ `prisma/seed-data/aliases.ts:226` (`"jax-h3": ["Jax Hash", "Jacksonville Hash", "Jax H3"]`)
- Source in seed: **no** (grep of `sources.ts` for jax/jacksonville → no source row) → **sourceless kennel = valid source-add**
- Live check (Chrome MCP, 2026-07-22): `/kennels/jaxh3` **renders live** — "JaxH3 · Jacksonville Hash
  House Harriers", region badge **JAX / USA**, "Saturdays · Biweekly", **"No events yet."** (0 events →
  no pre-existing admin rows to purge/dedup).
- Live sitemap dedup: 491 slugs (Chrome MCP 2026-07-22); slug is `jaxh3` (= `toSlug("JaxH3")`), already
  present/live — this is an enrich, not a create.
- Decision: **source-add** — add the ICS Source, link `kennelCodes: ["jax-h3"]`, enrich missing kennel fields.
- kennelCode: `jax-h3` (**already assigned — do not change**; permanent). No collision work.

## Live source verification  ✅ (feed HEAD-verified from sandbox; one ⚠️ TZID item for Claude Code)
- Source: **ICAL_FEED** — `https://events.timely.fun/api/calendars/54747312/export?format=ics`
  - Discovery path: `jaxh3.com` homepage embeds a **Timely** iframe (`events.timely.fun/gg1wvzf4`,
    "All-in-One Event Calendar" plugin). The calendar's "Add to Calendar → Download ICS" control
    exposes this public export URL (numeric calendar id **54747312**).
  - **Feed HEAD-check (verified live, same-origin fetch on events.timely.fun, 2026-07-22):**
    HTTP **200**, `content-type: text/calendar`, **46,791 bytes**, valid `BEGIN:VCALENDAR`,
    **11 `VEVENT`s** — and **no `X-Api-Key` header required** (the JSON API `…/events` needs one; the
    ICS export does not). ✅ This is the config-only path.
- Events seen: **11** (9 past + 2 upcoming), date range **2025-11-01 (#1403) → 2026-08-21 (#1422)**.
- **Source-count parity:** the platform UI (agenda) shows exactly the same set the feed yields
  (2 upcoming + past). Feed M = UI N = 11 → parity 100%. No pagination needed (single ICS body).
- Sample events (VERBATIM from the ICS `SUMMARY`/`DTSTART` — run numbers and themes are real, not constructed):
  1. 2026-07-25 13:00 — "Jax H3 - #1420 Kats Birthday Blast and Scavenger Hunt" — **UPCOMING**
  2. 2026-08-21 15:00→2026-08-23 11:00 — "#1422 AARP H3 Tubing and Chill!" — **UPCOMING, multi-day campout**
  3. 2026-06-13 13:00 — "Jax H3 - #1416 This Trail is Just Hot and Wet"
  4. 2026-06-06 18:20 — "Jax H3 - #1415 Yukon Count On Me"
  5. 2026-05-16 12:00 — "Jax H3 - #1414 A Pipe Down Trail"
  6. 2026-05-02 14:00 — "Jax H3 - #1413 A Stretch Trail"
  7. 2025-11-29 13:30 — "Jax H3 - #1405 Dutton Island Wine and Cheese Trail"
  8. 2025-11-01 11:00 — "Jax H3 - #1403 A Breastacle Spectacle Fundraiser" (oldest)
  - Also present: one **"Jax Urban H3 - #1417 Yaking the …"** trail (sibling sub-brand, shared run
    sequence — see Multi-kennel note). Run numbers are **not consecutive** in the feed (the calendar
    holds a subset of trails; #1420 biweekly ≫ 11 means the true trail count is far higher — do NOT
    infer archive depth from the run number).
- History depth / pagination: **single ICS body, ~11 events back to 2025-11-01.** The feed **returns
  the full set (incl. ~9 months of past) on every fetch** → it's a full-archive feed, NOT an
  "ages-out" feed → **OMIT `config.upcomingOnly`** and **no backfill script**. Set `scrapeDays: 400`
  so the adapter's lookback window (`= scrapeDays`) reaches #1403 (~264 days ago).
  ⚠️ *Claude Code confirm at live-verify:* the feed still returns the oldest past rows; if a scrape
  ever shows it dropping past events, add `upcomingOnly: true`.
- Coord sanity: **no `GEO` in the feed** (0/11) → clean (no default/garbage pins). The adapter derives
  `locationUrl` from a Google-Maps *search* on the `LOCATION` text; the map pin falls back to the
  Jacksonville region centroid. No `dropCachedCoords` needed.
- End times: **not usable** — 10/11 carry a placeholder `DTEND` of `…T235959` (end-of-day), which the
  adapter already drops (cross-date / same-as-start-day guard). #1422 is a *timed* multi-day event
  (Aug 21 15:00 → Aug 23 11:00); the adapter only sets `endDate` for **all-day** multi-day VEVENTs, so
  #1422 ships as a single-day Aug 21 event (acceptable — do not special-case).
- 🔴 **TZID quirk (must confirm):** the ICS `VTIMEZONE`/`DTSTART` are tagged **`TZID=America/Vancouver`**
  (Pacific) but the **wall-clock times are the true Jacksonville Eastern times** (e.g. `20260725T130000`
  == the "1:00 pm ET" the JSON API and agenda show). The `ICalAdapter`'s `formatDate`/`formatTime`
  round-trip through the event's own TZID, so the mislabel is **self-correcting** — `startTime` comes
  out as the authored wall-clock `13:00` and `date` as `2026-07-25`. **⚠️ Claude Code: at live-verify,
  assert `startTime` for #1420 == `"13:00"` (NOT `16:00`/`10:00`) and the date == `2026-07-25`.** If a
  future node-ical version stops preserving wall-clock, that's the canary.
- Notes: Timely SPA is JS-rendered, but the **ICS export needs no browser** (plain `safeFetch` of the
  `?format=ics` URL). See `source-platform-notes.md` → "Timely (events.timely.fun / All-in-One Event
  Calendar)" appended this run.
- **Field-fill assertion table** (from all 11 sampled VEVENTs):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 11 / 11 | SUMMARY; strip `"Jax H3 - #N"` / `"Jax Urban H3 - #N"` prefix via `titleStripPrefixAliases` |
  | `runNumber` | 11 / 11 | `#NNNN` in SUMMARY → shared `extractHashRunNumber` (auto in `parseICalSummary`) |
  | `startTime` | 11 / 11 | DTSTART wall-clock (see TZID quirk — comes out correct) |
  | `endTime` | 0 / 11 | placeholder `T235959` → adapter drops it; accept absence |
  | `location` (venue) | **11 / 11** | ICS `LOCATION` field — real Jacksonville addresses (44–100 chars, all comma'd, FL-like) |
  | `locationStreet` | 0 / 11 | not separated in feed (venue text carries it) |
  | `locationUrl` (Maps) | 0 / 11 direct | adapter synthesizes a Maps search from `location` |
  | `hares` | 0 / 11 | not in feed — leave undefined (merge preserves) |
  | `cost` | 0 / 11 | feed cost display is "0"; use kennel default `hashCash: "$6"` |
  | `description` | ~11 / 11 (thin) | body is minimal ("Details posted soonish.") — low value, fine to keep |
  | `coords` (lat/lng) | 0 / 11 | no GEO → Maps-search locationUrl + region centroid fallback |

## Kennel metadata (source-add enrichment — the kennel already exists; add only the missing fields)
Existing `jax-h3` row already has: `shortName: "JaxH3"`, `fullName: "Jacksonville Hash House Harriers"`,
`region: "Jacksonville, FL"`, `website`, `facebookUrl`, `contactEmail`, `scheduleDayOfWeek: "Saturday"`,
`scheduleFrequency: "Biweekly"`, `description`. **Add these (all cited):**
- **hashCash: `"$6"`** (source: `jaxh3.com/hash-guides` — *"$6 Hash Cash is typical cost … unless something beyond regular (pub crawls)"*)
- **scheduleTime: `"6:30 PM"`** (source: `jaxh3.com/hash-guides` — *"6:30 PM is usual meet for trail time – can change"*). ⚠️ *Per-trail start times actually vary widely in the feed (11:00–18:20); this is the kennel's stated default. `Kennel.scheduleTime` is 12-hour `"6:30 PM"`; the adapter's per-event `startTime` stays 24-hour `"HH:MM"` and is authoritative per trail.*
- **walkersWelcome: `true`** (source: `jaxh3.com` homepage + hash-guides — *"pack may be made up of both walkers and runners"*, trails 2–4 mi)
- **dogFriendly:** *unverifiable — not stated on the site* → leave unset + flag.
- **foundedYear:** *unverifiable — no cited founding date on jaxh3.com, genealogy, half-mind, or web search* → leave blank + flag. (Do NOT infer from run #1420 — it predates the current biweekly cadence.)
- **instagramHandle / twitterHandle / discordUrl:** none found (site links only the two Facebook groups) → leave blank + flag.
- **logoUrl:** ⚠️ **self-host** — WordPress upload candidates seen on the site (confirm which is the real
  roundel logo, then download to `public/kennel-logos/jax-h3.<ext>` and reference that path):
  - `https://www.jaxh3.com/wp-content/uploads/2023/11/Jax-Beach-Hash-Logo-Back.jpg`
  - `https://www.jaxh3.com/wp-content/uploads/2023/12/Jax.jpg`
  🔴 **NEVER pre-fill the extension** — confirm real ext via `curl -sI` Content-Type + magic bytes
  (`\xff\xd8`=JPEG, `\x89PNG`=PNG, `RIFF`=WebP) before writing `logoUrl`.
- **lat/lng (optional):** Jacksonville, FL ≈ `30.3322, -81.6557` (region centroid already covers the map;
  kennel-level coords optional).
- description: existing *"Every other Saturday runs in Jacksonville."* is fine (optionally enrich to note
  2–4 mi trails, walkers welcome, $6).

## Historical backfill
- Available: **none needed.** The ICS feed IS the archive — it returns all ~11 events (back to
  2025-11-01) on every scrape, idempotently. No separate one-shot script.
- Plan: **none** — `scrapeDays: 400` in the source config captures the full reachable window; OMIT
  `upcomingOnly` (full-archive feed; reconcile won't false-cancel because the past rows re-appear every scrape).

## Ready-to-paste seed

> `sources.ts` is `Source[]` (array of objects). `kennels.ts` edit is an in-place field add on the
> existing `jax-h3` object (do NOT add a new kennel object). No `aliases.ts` change required (jax-h3
> aliases already exist; optionally add `"Jax Beach H3"`, `"Jax Urban H3"` to the existing array).

```ts
// prisma/seed-data/sources.ts — ADD this Source object (config-only ICAL_FEED)
{
  name: "Jacksonville H3 Timely iCal",
  url: "https://events.timely.fun/api/calendars/54747312/export?format=ics",
  type: "ICAL_FEED" as const,
  trustLevel: 6,                 // iCal band (playbook §; 6–7)
  scrapeFreq: "daily",
  scrapeDays: 400,               // reach the ~9-month past archive (#1403 ~264d ago); feed retains all
  config: {
    defaultKennelTag: "jax-h3",
    // Titles are "Jax H3 - #NNNN Theme" / "Jax Urban H3 - #NNNN Theme" (hyphen, not colon) →
    // parseICalSummary can't split them, so strip the kennel-label + run marker off the title.
    // stripTitleKennelRunPrefix removes alias → connector → "#NNNN" → connector, leaving the theme.
    titleStripPrefixAliases: ["Jax Urban H3", "Jax H3"],
    // NO upcomingOnly — full-archive feed (returns all past every scrape).
  },
  kennelCodes: ["jax-h3"],       // source-kennel guard; single kennel
},
```

```ts
// prisma/seed-data/kennels.ts — ENRICH the EXISTING jax-h3 object (kennels.ts:2186–2193), add fields:
//   hashCash: "$6",
//   scheduleTime: "6:30 PM",   // 12-hr; kennel default (per-trail times vary — adapter startTime is authoritative)
//   walkersWelcome: true,
//   latitude: 30.3322, longitude: -81.6557,   // optional
//   logoUrl: "/kennel-logos/jax-h3.<ext>",     // after self-hosting; confirm ext via magic bytes
//   foundedYear / dogFriendly / instagramHandle — LEAVE UNSET (unverifiable; flagged)
```

## Adapter notes / config explanation

**Config-only `ICAL_FEED` — no new code.** The `ICalAdapter` (`src/adapters/ical/adapter.ts`) already:
- fetches + validates the ICS (`fetchAndValidateIcsContent` → `text/calendar`, `BEGIN:VCALENDAR`),
- parses via `node-ical`,
- routes every event to `config.defaultKennelTag` (`"jax-h3"`) — no `kennelPatterns` (single kennel),
- extracts `runNumber` from the SUMMARY `#NNNN` (shared `extractHashRunNumber`),
- populates `location` from the ICS `LOCATION` field (11/11 real venues),
- honors `config.titleStripPrefixAliases` to strip the `"Jax H3 - #N"` / `"Jax Urban H3 - #N"` prefix
  from the title (verified trace: `"Jax H3 - #1420 Kats…"` → `"Kats Birthday Blast and Scavenger Hunt"`,
  `runNumber 1420`; `"#1422 AARP H3 Tubing and Chill!"` → title `"AARP H3 Tubing and Chill!"`, `runNumber 1422`).

**Reference source rows to mirror:** any existing `ICAL_FEED` with `defaultKennelTag` +
`titleStripPrefixAliases` (e.g. the Reading H3 / ICH3 rows that introduced `titleStripPrefixAliases`, #2148/#2160).

**⚠️ Claude Code: verify before finalizing (illustrative config above — confirm against live repo):**
- `ICalSourceConfig` field names: `defaultKennelTag`, `titleStripPrefixAliases`, `scrapeDays`, `kennelCodes`
  are all real (see `src/adapters/ical/adapter.ts` + `Source` model). `RawEventData.kennelTags` is `string[]`.
- **TZID quirk** (above): assert `startTime` == `"13:00"` for #1420 at live-verify (NOT tz-shifted).
- Confirm the feed still needs **no** `X-Api-Key` (the ICS export was auth-free in this run; the JSON
  `…/events` endpoint is NOT — do not point the source at the JSON endpoint).
- Do NOT set `upcomingOnly` unless the feed is observed dropping past rows.

## Multi-kennel note (sibling sweep)
The single Timely calendar carries three title shapes, ALL folded into `jax-h3` (one Jacksonville Area
H3 with a **shared run-number sequence** — #1416 Jax H3, #1417 Jax Urban H3, #1420 Jax H3):
- **"Jax H3 - #N …"** — the primary Jacksonville / Jax Beach kennel → `jax-h3`. ✅ included (default).
- **"Jax Urban H3 - #N …"** — sub-brand that "runs when they can" (its own FB group
  `facebook.com/groups/567880180327641`), but it **shares the jax-h3 run sequence** → folded into
  `jax-h3` (NOT a separate kennel — a separate kennel would collide on the shared run numbers). ✅ included.
- **"#1422 AARP H3 …"** — a themed away/campout trail on the same jax-h3 run sequence → `jax-h3`. ✅ included.
- **Future split option:** if Jax Urban H3 ever diverges into its own run sequence, split it out with a
  `kennelPatterns: [["Jax Urban H3", "jax-urban-h3"]]` config + a new kennel row. Not warranted now.

## Deep-dive checklist (nothing deferred)
- [x] logo (candidates found; ⚠️ self-host + confirm ext)  [ ] foundedYear (unverifiable — flagged)
  [x] socials (FB ×2 present; IG/X none — flagged)  [x] schedule (Sat · Biweekly · 6:30 PM usual)
  [x] hashCash ($6, cited)  [x] description (present)  [x] walkersWelcome (true, cited)
  [ ] dogFriendly (unverifiable — flagged)
- [x] source live-verified (ICS HEAD-checked: text/calendar, 11 VEVENTs, auth-free)
- [x] history depth assessed (11, full-archive feed → no backfill, no upcomingOnly)
- [x] coord sanity (no GEO — clean)  [x] end times noted (placeholder → dropped; #1422 multi-day)
- [x] kennelCode already assigned (`jax-h3`)  [x] kennelCodes source guard set (`["jax-h3"]`)
- [x] region confirmed live (JAX metro @ `region.ts:1377` + STATE_GROUP_MAP `:4124`) → **0 region.ts edits**
- [x] pre-existing events checked (`/kennels/jaxh3` shows 0 → no purge/dedup needed)

## Implementation gotchas (for Claude Code — repo knowledge)
- **TZID `America/Vancouver` mislabel is self-correcting** but MUST be asserted at live-verify
  (`startTime` == authored Eastern wall-clock). This is the single highest-risk item.
- **OMIT `upcomingOnly`** — this is a full-archive iCal feed (returns all past every scrape); adding it
  is unnecessary and would be harmless but wrong per the "same-old-events" test. Only add if the feed is
  observed aging out past rows.
- **`titleStripPrefixAliases` ordering** — list `"Jax Urban H3"` before `"Jax H3"` (the helper also
  sorts longest-first, so this is belt-and-suspenders) so "Jax Urban H3 -" isn't half-stripped to "Urban H3 -".
- **No `dropCachedCoords`** — the feed has no coords, and the kennel currently has none, so there's no
  stale pin to clear.
- **Self-host the logo** into `public/kennel-logos/jax-h3.<ext>`; confirm the real extension via
  `curl -sI` Content-Type + magic bytes (WordPress `.jpg` uploads are usually JPEG, but verify).
- **`friendlyKennelName("JaxH3", "Jacksonville Hash House Harriers")`** — shortName is 5 chars (>4) →
  returns `"JaxH3"` directly → synth title `"JaxH3 Trail #N"` only fires when a feed title is empty
  (rare here; most rows carry a real theme). No action needed, but the snippet to confirm:
  `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("JaxH3","Jacksonville Hash House Harriers"));'`

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
