# Onboarding Handoff — Steel City H3 (Sheffield, England) — 2026-07-13

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/sch3-gb-20260713`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **2 `region.ts` edits** —
>    add a **Sheffield** METRO under the already-seeded UK COUNTRY (METRO record + `STATE_GROUP_MAP`
>    entry). No `COUNTRY_INFERENCE_RULES` / `COUNTRY_GROUP_MAP` edit (see **Region seeding**).
> 3. Configure the adapter — **pure config-only HARRIER_CENTRAL** (no new adapter code); mirror the
>    **Steel City-style live-only HC rows**, e.g. **Taiwan H3** / **Lisbon H3** (`prisma/seed-data/sources.ts`).
>    🔴 **This is a LIVE-ONLY HC source (no backfill) → OMIT `upcomingOnly`** (HC `getEvents` is
>    future-only and established HC kennels survive reconciliation without it — Lisbon/Taiwan contract).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: **1 upcoming event**
>    (#22, 2026-07-18), date UTC-noon-safe, `startTime` "12:00", `kennelTag` resolves to `sch3-gb`
>    with no unmatched. **DO NOT run `npx prisma db seed` here.**
> 5. **No historical backfill** — HC's `global-runs?isFuture=0` returns **0 past rows** for this kennel
>    (it joined HC ~June 2026; runs #1–#21 predate HC and are not on any clean machine-readable feed).
>    See **Historical backfill** for the marginal Meetup/Google-Sites options (do **not** script them
>    unless you want a small manual backfill — recommended: skip).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- <path>` for the seed rows, the
>      `region.ts` edits, and the self-hosted logo).
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod — barbados #2471/#2463).
>    - Trigger a scrape from `/admin/sources` to publish the event to prod.
>    - Spot-check `/kennels/steel-city-h3` for the #22 run (2026-07-18, Matlock).

## Summary
- Type: **full onboard** (first Sheffield kennel on hashtracks)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `src/adapters/registry.ts` & feed verified live)
- Effort estimate: **config-only source** + **2 `region.ts` edits** (new Sheffield METRO under UK COUNTRY). No new adapter code, **no backfill script**.
- One-line: Adds Steel City H3 — Sheffield's new (est. 2025) weekend Hash House Harriers — via a live Harrier Central kennel with a genuine upcoming event (#22, 2026-07-18).

## Dedup result
- Kennel in seed: **no** (`grep -in 'steel\|sheffield\|sch3-gb' prisma/seed-data/` → nothing; only `sch3-atl` / `sch3-ca` for the bare `SCH3` code, distinct)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-13, **485 slugs**; **zero** `steel` / `sheffield` / `sch3` slug fragments.
- Decision: **full onboard**
- kennelCode: **`sch3-gb`** (collision check: **clear** — `grep -in '"sch3-gb"' prisma/seed-data/kennels.ts prisma/seed-data/aliases.ts` returns nothing. Bare `sch3` is taken by **`sch3-atl`** [Southern Comfort, Atlanta] and **`sch3-ca`** [Surf City, Santa Cruz] → suffix required; matches the HC `kennelUniqueShortName` "SCH3-GB"). slug → `steel-city-h3` (`toSlug("Steel City H3")`, all-ASCII, free — NOT "SCH3" which would slugify to the taken `sch3`).
  - 🔴 **Omit the bare `"SCH3"` alias** — it's already claimed by `sch3-atl` + `sch3-ca` (global alias namespace; aliases.ts:241/321). Publish `SCH3-GB` instead.

## Live source verification  ✅ (1 genuine upcoming event — verified 2026-07-13 via Chrome page-context)
- Source: **HARRIER_CENTRAL** — `harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, `publicKennelId: dd9bdb87-bbf7-4b16-b458-fd0cb3059d8e` (uniqueShortName `SCH3-GB`). Verified live 2026-07-13 by (a) reproducing the adapter's exact `getEvents` POST in-browser (SubtleCrypto token) → **1 event #22, `daysUntilEvent: 5`**, AND (b) sweeping `hashruns.org/api/global-runs?isFuture=0` over 2024-06 → 2026-07 (each window returned 1159–1295 rows total but **0 for this `PublicKennelId`** → the kennel joined HC ~June 2026 and has **no HC past-run archive**).
- Events seen: **1 upcoming** (from HC `getEvents`) + **0 in the HC past-runs feed**. Date: 2026-07-18.
- **Source-count parity:** HC `getEvents` (the platform's own future feed) shows **1 upcoming**; the adapter will yield **1**. Ratio 1/1 = 100%. ✅ (Deep run # [#22] with 0 HC archive is expected — HC coverage starts at the kennel's join date, not its lifetime; see "Run-number ≠ archive depth". Runs #1–#21 are historical, not archived on this source.)
- Sample event (VERBATIM from `getEvents`, fields kept as-is incl. trailing spaces):
  1. **#22** — "Steel City H3 Run#22" — **2026-07-18 (Sat) 12:00** — location "Matlock (venue tbc)" — coords **53.140166 / -1.546235** (Matlock, Peak District — a real away-venue pin) — hares "Horse's Arse and Crooked Squire" — price 0 — `isCountedRun: 1` — `daysUntilEvent: 5`.
- History depth / pagination: **0 events in the HC feed's past window** (`global-runs?isFuture=0`, `PublicKennelId`-filtered, returned nothing across 2024→2026). HC `getEvents` is future-only. The kennel's real history (#1–#21) lives only on **stale Meetup** (#4–#7 only) + the **Google Sites** inaugural page (#1–#2) — see **Historical backfill**. Single future event; no pagination needed.
- Coord sanity: **clean.** The one event's coords (53.140166 / -1.546235) are a **real Matlock pin** (the #22 away venue), not a region-default sentinel. `locationOneLineDesc` = "Matlock (venue tbc)" is a real place name (NOT a `GEOCODE_FAIL_SENTINEL` like "TBD"/"No location provided"), so the location text survives and the pin is kept. No default-pin trap.
- End times: none (HC exposes start only).
- Notes: **tz — HC `KennelIANATimezone` = `Europe/London` (correct, no quirk)**; the Sheffield METRO is `Europe/London`. Config-only; no browserRender/auth. HC `getEvents` carries no currency field — the sample's price is `0` (unpopulated), so use the kennel-level `hashCash: "£3"` (from the kennel's own site). The Azure host is allowlist-blocked from the research sandbox (`EAI_AGAIN`) — verified via Chrome page-context fetch; **Claude Code: reconfirm `adapter.fetch(source)` from local env** where the host resolves.
- **Field-fill assertion table** (from the 1 upcoming row):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 1 / 1 | Real event name "Steel City H3 Run#22" / HC adapter sets `title` from `eventName`; `defaultTitle:"Steel City H3"` synthesizes only for empty/placeholder names. |
  | `startTime` | 1 / 1 | From `eventStartDatetime` (12:00). |
  | `endTime` | 0 / 1 | Not in feed — accept absence. |
  | `location` (venue name) | 1 / 1 | `locationOneLineDesc` "Matlock (venue tbc)" (verbatim — real place, survives sentinel filter). |
  | `locationStreet` | 0 / 1 | Not populated — leave undefined. |
  | `locationUrl` (Maps) | 0 / 1 | Not in feed — coords carry the pin. |
  | `hares` | 1 / 1 | `hares` = "Horse's Arse and Crooked Squire" (real hash names). |
  | `cost` | 0 / 1 | HC price `0` (unpopulated) → per-event cost undefined; kennel-level `hashCash` "£3". |
  | `description` | (n/a) | Live HC adapter emits no `description`. |
  | `trailLengthText` | 0 / 1 | Not in feed. |
  | `coords` (lat/lng) | 1 / 1 | `syncLat`/`syncLong` populated & real (Matlock). No bbox scrub needed on a single live event. |

## Kennel metadata (deep-dive complete)
- fullName: **Steel City Hash House Harriers** / shortName: **Steel City H3** / region: **Sheffield** / country: **United Kingdom**
  - (HC `KennelName` = "Steel City H3", `KennelShortName` = "SCH3", `KennelUniqueShortName` = "SCH3-GB"; search text places it in "Sheffield South Yorkshire United Kingdom". Source: HC `getEvents` 2026-07-13.)
- aliases: `["Steel City H3", "Steel City Hash House Harriers", "SCH3-GB"]` — all clear (grep-checked 2026-07-13). 🔴 **Omit bare `"SCH3"`** — claimed by `sch3-atl` + `sch3-ca` (aliases.ts:241/321).
- website: **`https://sites.google.com/view/steelcityh3/home`** (Google Sites — **verified live** 2026-07-13; the kennel's official "Inaugural Hash Weekend" page). *(Google Sites is a known platform — no DNS check needed.)*
- facebook / instagram / twitter / discord: **none confirmed** → blank + flag. A press article (thestar.co.uk) references "their Facebook group," and the Meetup page exists (see below), but no direct FB **URL** was confirmable → do not invent; leave blank. ⚠️ Claude Code may search for a `facebook.com/groups/…` URL at build.
- schedule: **4th Saturday of the month, 12:00 noon** — meeting the Sheffield Tap / Sheffield Station concourse (source: thestar.co.uk press + observed Meetup cadence: #4 Sat 2025-05-24, #5 Sat 2025-06-28, #6 Sat 2025-07-26, #7 Sat 2025-08-30 — all last/4th Saturday, 12:00). Multi-pattern? No — single monthly pattern → `scheduleRules` with one MONTHLY rule + flat fallback:
  ```ts
  // scheduleDayOfWeek/scheduleTime kept as fallback; scheduleRules is authoritative.
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "12:00 PM", // 12-hr per kennels.ts convention (HC/scheduleRules startTime stays 24-hr "12:00")
  scheduleFrequency: "Monthly",
  scheduleRules: [
    { rrule: "FREQ=MONTHLY;BYDAY=4SA", startTime: "12:00", label: "Monthly 4th Saturday" },
  ],
  ```
  ⚠️ Observed HC run numbering (#1 Apr 2025 → #22 Jul 2026 ≈ 1.4 runs/month) runs slightly hotter than strict monthly (inaugural-weekend extras + occasional bonus runs); the **documented regular cadence** is 4th-Saturday-monthly. Note #22 itself (2026-07-18, 3rd Sat) is a Matlock **away trip**, off the normal pattern.
- foundedYear: **2025** (source: `sites.google.com/view/steelcityh3/home` — "Inaugural Hash Weekend, 11th–13th April 2025", "INAUGURAL HASH (Run #1) — 12th April 2025"; corroborated by thestar.co.uk "Sheffield joins worldwide Hash House Harriers movement for the first time this weekend"). High confidence.
- hashCash: **"£3"** (source: `sites.google.com/view/steelcityh3/home` — "There will be a sub of £3 per run (not Friday) to cover costs"; press confirms **free for first-timers**). Preserve the free-for-virgins nuance in the description.
- dogFriendly: unknown → omit (unverified).
- walkersWelcome: **true** (source: thestar.co.uk — "All are welcome — runners, walkers, red dress enthusiasts, and the curious").
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/SCH3-GB_20260618222250.avif`** → ⚠️ **self-host** to `public/kennel-logos/sch3-gb.<ext>` (HC blob; confirm the extension via **magic bytes**, NOT the `.avif` name — HC blobs are often WebP/PNG/AVIF regardless of URL suffix). Reference `/kennel-logos/sch3-gb.<ext>`.
- lat/lng: **53.3811 / -1.4701** (Sheffield city centre — matches the Sheffield METRO centroid; the #22 event's own Matlock pin comes from the feed).

## Region seeding — 2 `region.ts` edits (new Sheffield METRO under UK COUNTRY)
UK COUNTRY is already seeded (`src/lib/region.ts:58`). Add a **Sheffield** METRO, mirroring the recent
UK-metro additions Lancaster (`:1597`) / Cornwall (`:1619`) / Plymouth (`:1608`) — violet palette:

1. **`REGION_SEED_DATA`** — insert a METRO record (place near the other UK metros, e.g. after Cornwall `:1628`):
   ```ts
   {
     name: "Sheffield",
     country: "UK",
     timezone: "Europe/London",
     abbrev: "SHF", // confirmed free (grep 'abbrev: "SHF"' → none)
     colorClasses: "bg-violet-100 text-violet-700",
     pinColor: "#8b5cf6",
     centroidLat: 53.3811,
     centroidLng: -1.4701,
     aliases: ["Sheffield, England", "South Yorkshire"],
   },
   ```
2. **`STATE_GROUP_MAP`** — add (in the UK block near `:4198`):
   ```ts
   "Sheffield": "United Kingdom",
   ```
- 🔴 **NO `COUNTRY_INFERENCE_RULES` edit** — the UK rule already covers `england` (`region.ts:3937`: `/\b(uk|england|scotland|wales|london|surrey|sussex)\b/`), and the metro alias "Sheffield, England" resolves the country via that. **Do NOT add a bare `sheffield` token** — it collides with US Sheffields (MA/AL). (The seed kennel carries explicit `country: "United Kingdom"` anyway; inference only guards the free-text research path.)
- 🔴 **NO `COUNTRY_GROUP_MAP` edit** — UK metros resolve via `STATE_GROUP_MAP` first (confirmed: Lancaster/Cornwall are in `STATE_GROUP_MAP` only, absent from `COUNTRY_GROUP_MAP`).

## Historical backfill
- Available on HC: **none** — `global-runs?isFuture=0` returns 0 rows for this `PublicKennelId` (HC-join ~June 2026; the adapter path is future-only). So **no HC backfill script.**
- Real history exists but only on **stale / partial** surfaces (do **not** treat as live sources):
  - **Meetup** (`meetup.com/steel-city-hash-house-harriers`) — Apollo `__NEXT_DATA__` past feed holds only **#4–#7** (2025-05-24 → 2025-08-30, all Sat 12:00); **0 upcoming** (the group is stale — the kennel moved to Harrier Central). Titles: "Steel City Hash House Harriers Run #4", "Steel City H3 Hash #5 - Dore and Totley", "Steel City H3 Hash #6 - Baslow", "Steel City Hash House Harriers Run #7 - Woodhouse".
  - **Google Sites** inaugural page documents **#1** (12 Apr 2025, Sheffield Tap) and **#2** ("Hangover Hash", 13 Apr 2025, Sheffield City Hall).
- Plan: **skip the backfill (recommended).** Rationale: the kennel is brand-new (2025) and actively posting on HC now; a manual patchwork of ~6 early runs from two stale sources (with #3, #8–#21 missing entirely) is low value and error-prone. The daily HC scrape accumulates history going forward. If Claude Code *wants* a small backfill, the 6 Meetup+GoogleSites runs above could be frozen into `scripts/data/sch3-gb-history.json` — but then **set `config.upcomingOnly: true`** (backfill contract, so reconcile doesn't cancel the aged past rows). **Default: no backfill → live-only source → `upcomingOnly` OMITTED.**

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "sch3-gb", // bare sch3 taken by sch3-atl/sch3-ca → suffixed; matches HC uniqueShortName SCH3-GB
  shortName: "Steel City H3",
  fullName: "Steel City Hash House Harriers",
  region: "Sheffield", // NEW METRO — see Region seeding (2 region.ts edits)
  country: "United Kingdom",
  website: "https://sites.google.com/view/steelcityh3/home", // Google Sites; verified live 2026-07-13
  // No confirmed socials → omit facebookUrl/instagramHandle (⚠️ search for the FB group URL at build if desired)
  logoUrl: "/kennel-logos/sch3-gb.<ext>", // self-hosted from HC blob; verify ext by magic bytes
  scheduleDayOfWeek: "Saturday", // fallback; scheduleRules is authoritative
  scheduleTime: "12:00 PM", // 12-hr per kennels.ts convention
  scheduleFrequency: "Monthly",
  scheduleRules: [
    { rrule: "FREQ=MONTHLY;BYDAY=4SA", startTime: "12:00", label: "Monthly 4th Saturday" },
  ],
  foundedYear: 2025, // Google Sites inaugural weekend 11–13 Apr 2025 (Run #1 = 12 Apr 2025)
  hashCash: "£3", // Google Sites: "£3 per run sub"; free for first-timers
  walkersWelcome: true, // thestar.co.uk: runners + walkers welcome
  description: "Steel City H3 is Sheffield's Hash House Harriers, founded in April 2025 — 'the drinking club with a running problem.' A friendly weekend hash for all-comers (runners, walkers, and hash virgins especially welcome), typically on the 4th Saturday of the month at noon around Sheffield and the Peak District, with at least two pub-stops. £3 per run; free for first-timers.",
  latitude: 53.3811, longitude: -1.4701, // Sheffield city centre
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug)
"sch3-gb": ["Steel City H3", "Steel City Hash House Harriers", "SCH3-GB"],
// 🔴 bare "SCH3" OMITTED — claimed by sch3-atl + sch3-ca (aliases.ts:241/321)

// sources.ts — Source[] (array of objects). Mirror a LIVE-ONLY HC row (Taiwan H3 / Lisbon H3).
{
  name: "Steel City H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live 2026-07-13 via HC getEvents [1 upcoming #22]
    // + hashruns.org/api/global-runs [0 past rows — kennel joined HC ~June 2026]).
    // HC KennelIANATimezone = Europe/London (correct, no quirk); Sheffield METRO is Europe/London.
    publicKennelId: "dd9bdb87-bbf7-4b16-b458-fd0cb3059d8e",
    defaultKennelTag: "sch3-gb",
    // HC names events "Steel City H3 Run#N" (real name) → kept verbatim by the adapter;
    // defaultTitle synthesizes only for empty/placeholder names.
    defaultTitle: "Steel City H3",
    staleTitleAliases: ["Placeholder event for Steel City H3"],
    // 🔴 upcomingOnly OMITTED — LIVE-ONLY HC source (no backfill). HC getEvents is future-only and
    //    established HC kennels survive reconciliation without it (Lisbon/Taiwan contract). Only add
    //    upcomingOnly:true IF a past-run backfill is shipped (see Historical backfill).
  },
  kennelCodes: ["sch3-gb"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only HARRIER_CENTRAL — zero new adapter code.** The `HARRIER_CENTRAL` adapter already
exists in `src/adapters/registry.ts` (`src/adapters/harrier-central/adapter.ts` + `token.ts`) and is
used by ~35 seeded kennels (Taiwan H3, Lisbon H3, KRASH, Bandung, Divahhh, …). The source row above is
the whole live implementation. The adapter derives events from HC's `getEvents` API (future-only),
resolves `sch3-gb` via `defaultKennelTag`, and applies `defaultTitle`/`staleTitleAliases` for name
synthesis.

**⚠️ Claude Code: verify before writing/relying on any field.** This is config-only, so the main checks are:
- Confirm `publicKennelId` returns Steel City's run via a throwaway `adapter.fetch(source)`. **Expect 1
  event** (#22, 2026-07-18) at build (if it has since passed and #23 is posted, expect that instead).
- Confirm `kennelTag` resolves to `sch3-gb` with **no unmatched** and `startTime` is "12:00".
- Confirm the display tz is **`Europe/London`** (from the Sheffield METRO; HC feed agrees — no override).
- `RawEventData` field names are `kennelTags: string[]` (not `kennelTag`); `walkersWelcome` (not
  `walkerFriendly`). Don't hand-set `title` — the HC adapter + `defaultTitle` handle it.
- **`friendlyKennelName("Steel City H3", …)`** — shortName is 13 chars (>4), so it short-circuits to
  "Steel City H3" (synth title "Steel City H3 Trail #N"). No verbose-synthesis risk. (The HC adapter
  uses the real `eventName` anyway, so synthesis is unlikely to fire.)

## Sibling sweep
- Swept the HC `global-runs` future + 2024→2026 past feed for other Sheffield / "Steel City" kennels:
  **none** share this `publicKennelId`; no additional Steel City-branded HC kennel surfaced.
- **Sheffield HHH** (`sheffieldhhh.blogspot.com`, and a Strava "Sheffield Hash House Harriers" club) is a
  **separate, older/likely-dormant** entity — NOT Steel City H3. Do not conflate or fold it in. (The 2025
  press framed Steel City as Sheffield's *new* hash.) Not queued here — a future lead only if a live
  dynamic source is confirmed (the blogspot's recency is unverified).
- No other kennel is fed by this GUID; `kennelCodes: ["sch3-gb"]` is complete.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host + magic-byte ext)  [x] foundedYear (2025 — Google Sites inaugural weekend, high confidence)  [ ] socials (none confirmed — flagged; FB group referenced but URL unverified)  [x] schedule (4th-Sat monthly noon + scheduleRules)  [x] hashCash (£3 — own site)
- [x] description  [x] source live-verified (1 genuine upcoming #22, 2026-07-18)  [x] history depth assessed (0 on HC; #4–#7 on stale Meetup, #1–#2 on Google Sites — backfill skipped)
- [x] coord sanity checked (single real Matlock pin; no default-pin trap)  [x] end times noted (none)  [x] kennelCode collision-checked (`sch3-gb` clear; bare SCH3 omitted)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- 🔴 **LIVE-ONLY HC → OMIT `upcomingOnly`.** There is no backfill, so this is a live-only source; per the
  HC platform contract (Lisbon/Taiwan) `upcomingOnly` is omitted and the established HC kennel survives
  reconciliation. Only add `upcomingOnly:true` if you decide to ship the marginal Meetup/GoogleSites
  backfill (then reconcile must not cancel the aged past rows).
- **Single live event, no backfill** — the kennel page shows just #22 at first; that's expected and
  accumulates via the daily scrape. Not a bug.
- **`sch3-gb` is a kennelCode-suffix collision fix** — bare `sch3` = `sch3-atl` (Southern Comfort) /
  `sch3-ca` (Surf City). Suffix the code AND omit the bare `SCH3` alias (kennelCode-exact-match runs
  before alias-match, so a bare `SCH3` alias would misroute).
- **Self-host the HC blob logo** to `public/kennel-logos/sch3-gb.<ext>`; confirm ext by magic bytes (the
  `.avif` URL name is not authoritative — HC blobs are often WebP/PNG).
- **2 region.ts edits only** — Sheffield METRO record + `STATE_GROUP_MAP` entry. No inference / no
  COUNTRY_GROUP_MAP edit (UK covers `england`; UK metros resolve via STATE_GROUP_MAP). Confirm `abbrev "SHF"`
  is still free at build (it was 2026-07-13).
- **No admin-seeded events to purge** — the kennel isn't live (no `steel-city-h3`/`sch3` slug in the
  sitemap), so there are no pre-existing prod `Event` rows to dedup against.
- **HC feed carries no currency** — the event price is `0` (unpopulated); use the kennel-level
  `hashCash: "£3"` from the kennel's own site (don't infer a per-event cost from `0`).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
