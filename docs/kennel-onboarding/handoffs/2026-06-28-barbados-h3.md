# Onboarding Handoff — Barbados H3 (Barbados / Caribbean) — 2026-06-28

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/barbados-h3-20260628`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Add the **Barbados COUNTRY
>    + Bridgetown METRO** region records + the 4 supporting map edits in `src/lib/region.ts`
>    (see **Adapter notes → region.ts edits** — 🇧🇧 first Caribbean country, so the 5-edit checklist
>    is mandatory; the `COUNTRY_INFERENCE_RULES` omission is the silent USA-routing trap).
> 3. Configure the **HARRIER_CENTRAL** source exactly as in **Ready-to-paste seed** — this is the
>    **config-only** path (the `HarrierCentralAdapter` already exists in `src/adapters/registry.ts`;
>    mirror the Lisbon / Porto / Taiwan / Shanghai / Bandung HC source rows). No new adapter code.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` snippet. Resolve every item
>    flagged `⚠️ Claude Code must confirm` in **Live source verification**. Validate: events
>    non-empty + upcoming, dates UTC-noon, `startTime` "16:00", `kennelTag` resolves to `barbados-h3`
>    with no unmatched. **DO NOT run `npx prisma db seed` here** — seeding + prod scrape is the
>    post-merge step (8 below).
> 5. Optional: the **Historical backfill** (only if the HC past-window pull is confirmed working —
>    see **Historical backfill**; if you ship it, set `config.upcomingOnly: true`).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR whose body carries the metadata, live-verification results, and the
>    deep-dive checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge sometimes drops a follow-up
>      commit): `git log -1 -- src/lib/region.ts`, `git log -1 -- prisma/seed-data/sources.ts`, etc.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds the new kennel/alias/source + region)
>    - Trigger a scrape from `/admin/sources` (or the equivalent admin action)
>    - Spot-check `hashtracks.xyz/kennels/barbados-h3` for the 4 upcoming runs + sample dates
> Everything you need is in the sections that follow.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (config-only — `HarrierCentralAdapter` exists & feed verified live)
- Effort estimate: **config-only** (zero new adapter code — one seed source row + ~5 `region.ts` edits)
- One-line: 🇧🇧 **First Barbados / first Caribbean kennel** on HashTracks — a very active 40-year-old
  island-wide hash (run #2342) flowing live through Harrier Central, pure config-only.

## Dedup result
- Kennel in seed: **no** (grep `prisma/seed-data/kennels.ts` — no `barbados`/`caribbean`)
- Source in seed: **no** (grep `prisma/seed-data/sources.ts` — no Barbados HC row)
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP
  (**461 slugs**, 2026-06-28). No `barbados`/`caribbean`/`bridgetown`/`barbados-h3` slug. The bare
  `bh3` slug was opened (`/kennels/bh3`) and renders **"Boulder Hash House Harriers" (BLD, USA)** —
  NOT Barbados (the proposed Barbados slug is `barbados-h3`, distinct). No `moon-over*` slug either
  (the Moon Over Barbados sibling is also absent → future target).
- Pre-onboarding admin-event check: kennel is not live (no slug) → no admin-seeded `Event` rows
  expected. (Optional: Claude Code may confirm via a prod query under `barbados-h3` at build; low risk.)
- Decision: **full onboard**
- kennelCode: `barbados-h3` (collision check: **clear** — bare `bh3` is taken by Boulder/Buffalo, so
  use the descriptive code; bare `"BH3"` alias **OMITTED**)

## Live source verification  ✅
- Source: **HARRIER_CENTRAL** — `https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/`,
  `publicKennelId: 78da30cc-66f5-4c9f-bd7b-ec3b0f54f8d0` (KennelSlug `BH3`, KennelShortName `BH3`,
  KennelName "Barbados Hash House Harriers", continent "North America"). Verified live **2026-06-28**
  via `hashruns.org/api/global-runs?isFuture=1` (Chrome MCP, same-origin fetch).
- Events seen: **4 upcoming**, date range **2026-07-04 → 2026-07-25** (weekly Saturday, 16:00 local).
- **Source-count parity:** N/A — Harrier Central `getEvents` is **future-only** (the public feed
  returns only upcoming runs), so there is no "total upcoming" UI count to parity-check against.
  4 confirmed upcoming runs → the recently-active bar is trivially satisfied (no `0-upcoming` concern).
- Sample events (**VERBATIM from the HC feed — do not embellish**):
  1. #2339 — Sat 2026-07-04 16:00 — name "Barbados H3 Run#2339" — hares **Flour Duck** — loc **TBD** — BDS$4
  2. #2340 — Sat 2026-07-11 16:00 — name "Barbados H3 Run#2340" — hares **ABC, Cherry-Ann & Sabrina** — loc **TBD** — BDS$4
  3. #2341 — Sat 2026-07-18 16:00 — name "Barbados H3 Run#2341" — hares **Gemma & Luther** — loc **Oldbury Park, Oldbury, St Philip** — BDS$4
  4. #2342 — Sat 2026-07-25 16:00 — name **"Barbados H3 Run#"** (🔴 source quirk — EventName missing the number; `EventNumber` field = 2342 is correct) — hares **Joanne "Fill Her Up" Rock & Family** — loc **TBD** — BDS$4
- History depth / pagination: HC `getEvents` (adapter path) is **future-only** → the adapter ingests
  only the 4 forward runs. Deep history (~130 runs, per the queue's 2026-06-18 recon) is reachable
  only via the `hashruns.org/api/global-runs` **past** window — see **Historical backfill** (optional).
- Coord sanity: **clean (no coord trap)** — all four upcoming runs have `LocationOneLineDesc: "TBD"`
  (or a free-text venue for #2341) and carry **no lat/lng** in the feed → no default-pin corruption
  risk. ⚠️ Claude Code: confirm whether the HC `getEvents` payload carries per-event coords at build
  (the Bandung HC run did); if a repeated default pin appears, reject + `dropCachedCoords: true`.
- End times: **none** — the feed exposes only `EventStartDatetime` (16:00 local) / `EventStartDatetimeGmt`.
- Notes: **🔴 TZ quirk — HC reports `KennelIANATimezone: "America/La_Paz"`** (Bolivia, UTC−4). This is
  **harmless** for UTC-start computation — La Paz and Barbados are **both UTC−4 with no DST**, so the
  composed UTC start is identical (same pattern as Bandung's `Asia/Bangkok`≡`Asia/Jakarta`). Set the
  **Bridgetown METRO** region zone to the correct `America/Barbados`. The kennel also has its own live
  Squarespace site `barbadoshash.com` (metadata source below) — but **HC is the config-only dynamic
  source**; the website is a possible future secondary (Squarespace, would need its own adapter).
- **Field-fill assertion table** (from the 4 sampled upcoming runs; this is config-only HARRIER_CENTRAL —
  the existing adapter already maps every field, the table just documents what the feed exposes):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 4 / 4 | EventName present, but all are generic "Barbados H3 Run#NNNN" and #2342 is the malformed "Barbados H3 Run#". See **Adapter notes → title handling** — add `staleTitleAliases` so merge synthesizes the clean form. |
  | `startTime` | 4 / 4 | "16:00" from `EventStartDatetime` (HC adapter composes via TZ). |
  | `endTime` | 0 / 4 | not in feed — accept absence. |
  | `location` (venue name) | 1 / 4 | `LocationOneLineDesc` ("TBD" → undefined; #2341 = "Oldbury Park, Oldbury, St Philip"). Don't synth TBD. |
  | `locationStreet` | 0 / 4 | not in feed. |
  | `locationUrl` (Maps) | 0 / 4 | not in feed. |
  | `hares` | 4 / 4 | `Hares` field — excellent fill (Flour Duck; ABC, Cherry-Ann & Sabrina; …). |
  | `cost` | 4 / 4 | `EventPriceForMembers`/`NonMembers` = 4 → "BDS $4" kennel default. |
  | `description` | 0 / 4 | `EventDescription` empty in sample — leave undefined. |
  | `trailLengthText` | 0 / 4 | not per-event (kennel-level 7–10K run / 5–6K walk only). |
  | `coords` (lat/lng) | 0 / 4 | TBD venues → none; merge falls back to Bridgetown centroid. ⚠️ confirm HC payload at build. |

## Kennel metadata (deep-dive complete)
- fullName: **Barbados Hash House Harriers** / shortName: **Barbados H3** / region: **Bridgetown** / country: **Barbados**
- aliases: `["Barbados H3", "Barbados Hash House Harriers", "Barbados Hash"]` — 🔴 **OMIT bare `"BH3"`**
  (collides globally with Boulder H3 + Buffalo H3 `"bh3"`).
- website: `https://www.barbadoshash.com` (source: live, Squarespace; DNS OK — fetched 2026-06-28)
- facebook: `https://www.facebook.com/BarbadosHash/` (source: barbadoshash.com homepage)
- instagram: `barbadosh3` (source: barbadoshash.com homepage → `instagram.com/barbadosh3/`)
- twitter: none found / discord: none (the kennel uses a WhatsApp group, not a public URL)
- schedule: **Saturday, 4:00 PM, Weekly** (source: barbadoshash.com homepage). 🟡 **Seasonal start-time
  variation** — "Saturdays at **3:30 pm in winter** and **4:00 pm in summer**, and **10:00 am on public
  holidays**." Config-only HC ingests the *actual* per-event start (16:00 for the July sample), so the
  flat profile fields + `scheduleRules` below are kennel-profile metadata for Travel Mode:
  ```ts
  // scheduleDayOfWeek/scheduleTime kept as fallback; scheduleRules is authoritative.
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "4:00 PM", // 12-hr profile field (current summer start). scheduleRules.startTime stays 24-hr.
  scheduleFrequency: "Weekly",
  scheduleRules: [
    // ⚠️ Claude Code: BYMONTH season boundaries are approximate (Caribbean "winter" ≈ Nov–Apr,
    // "summer" ≈ May–Oct) — confirm/adjust against the kennel's usage; HC supplies real times anyway.
    { rrule: "FREQ=WEEKLY;BYDAY=SA;BYMONTH=5,6,7,8,9,10", startTime: "16:00", label: "Summer" },
    { rrule: "FREQ=WEEKLY;BYDAY=SA;BYMONTH=11,12,1,2,3,4", startTime: "15:30", label: "Winter" },
  ],
  ```
- foundedYear: **1985** (source: barbadoshash.com — "BH3 40th Anniversary Celebrations" 15 Nov 2025
  ⇒ founded Nov 1985; corroborated by web search; run #2342 ≈ weekly-ish since 1985. High confidence.)
- hashCash: **"BDS $4"** (source: barbadoshash.com homepage — "Pay $4 BDS for registration" via the
  Harrier Central app; matches HC `EventPriceForMembers/NonMembers = 4`). 🟡 The site's **2017 FAQ**
  says "$1 BDS per person" — stale; the fee increased to BDS $4. (Bar drink tickets — "3 for $10 BB$,
  7 for $20 BB$, 18 for $50 BB$" — are separate, not hash cash.)
- dogFriendly: unset (not stated) / walkersWelcome: **true** (source: homepage — "open to all –
  runners, hikers, walkers, young, and old"; explicit walkers' route)
- logoUrl: ⚠️ **self-host** to `public/kennel-logos/barbados-h3.<ext>`. Two stable-ish sources, both
  tokenized/CDN → self-host either: HC blob `https://harriercentral.blob.core.windows.net/harrier/Barbados%20H3.png`
  (cleanest), or the site's `bh3_logo.png` on `images.squarespace-cdn.com` (tokenized — rotates).
  🔴 **Confirm the real extension by magic bytes**, not the `.png` URL / Content-Type (per the
  AH3-NZ / Riyadh lesson — CDNs mislabel; `RIFF`=WebP, `\x89PNG`=PNG, `\xff\xd8`=JPEG).
- description: "The Barbados Hash House Harriers — 'a drinking club with a running (or hiking)
  problem.' Open to all (no membership): runners, hikers and walkers follow flour trails all over
  the island (7–10K running / 5–6K walking), then gather for down-downs and a proper Bajan lime.
  Weekly Saturday runs island-wide, plus a yearly charity Red Dress run. Founded 1985." (source:
  barbadoshash.com homepage)
- lat/lng: Bridgetown ~**13.0975, -59.6167** (capital anchor; runs are island-wide). Barbados island
  centroid ~13.1939, -59.5432 if you prefer a central pin.

## Historical backfill
- Available: **~130 runs** reachable via `hashruns.org/api/global-runs` past windows (per the queue's
  2026-06-18 recon: "130 runs/24mo"). Fields per HC past row: date, run#, hares, fees, sometimes
  lat/lng + venue (same shape as the upcoming feed).
- Plan: **OPTIONAL one-shot** `scripts/backfill-barbados-h3-history.ts` (frozen-dataset + dumb loader
  per the H7/Bandung pattern). 🔴 **Caveat — confirm the past-window param at build:** at research
  time (2026-06-28) a bare `GET /api/global-runs?isFuture=0` returned **0 rows across all kennels**,
  and date-range query params got content-filtered in the research sandbox, so the exact past-pull
  signature could **not** be re-confirmed here. The queue's 2026-06-24 source-mechanics note says HC
  past pulls work via `global-runs` "in ≤6-month windows (≤2 MB cap)" and that this endpoint served
  Bandung (56), Moonshine (17) and **Barbados (130)** before — so the capability exists; Claude Code
  must rediscover the working window param (try `isFuture=0` with a date window, or the per-kennel HC
  PortalApi past endpoint the `HarrierCentralAdapter` already knows).
  - **If you ship the backfill → set `config.upcomingOnly: true`** on the source (per the Bandung
    retro: a source that owns past rows needs `upcomingOnly` or `reconcile.ts` false-CANCELs every
    backfilled row in the 365-day window).
  - **If you do NOT ship the backfill → leave `upcomingOnly` OMITTED** (HC convention; `getEvents` is
    future-only, matching Lisbon/Porto/Taiwan/Shanghai). This is the **default/lower-risk** path for a
    first-Caribbean config-only kennel.
  - Run the frozen-dataset validation (PII scrub for `@`/phones, run-number monotonicity, weekly-gap
    sanity) before shipping. 🔴 Honor HC source quirks faithfully (Bandung had dup/mis-dated run#s and
    `Hares`-carrying-location bleed) — don't renumber; let the merge collapse same-`(kennel,date)` rows.

## Ready-to-paste seed

> 🔴 `aliases.ts` is `Record<string, string[]>` keyed by **kennelCode** (here `barbados-h3` == slug,
> so identical). Confirmed shape: `"bh3": ["BH3", "Buffalo Hash", ...]`. NOT an object array.

```ts
// kennels.ts — Kennel[] (array of objects). Append one entry.
{
  kennelCode: "barbados-h3",
  shortName: "Barbados H3",
  fullName: "Barbados Hash House Harriers",
  region: "Bridgetown",
  country: "Barbados",
  website: "https://www.barbadoshash.com",
  facebookUrl: "https://www.facebook.com/BarbadosHash/",
  instagramHandle: "barbadosh3",
  foundedYear: 1985,
  hashCash: "BDS $4",
  walkersWelcome: true,
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "4:00 PM", // 12-hr fallback; scheduleRules authoritative
  scheduleFrequency: "Weekly",
  latitude: 13.0975,
  longitude: -59.6167,
  logoUrl: "/kennel-logos/barbados-h3.png", // ⚠️ self-host; confirm ext by magic bytes
  description:
    "The Barbados Hash House Harriers — 'a drinking club with a running (or hiking) problem.' " +
    "Open to all: runners, hikers and walkers follow flour trails all over the island, then gather " +
    "for down-downs and a proper Bajan lime. Weekly Saturday runs island-wide. Founded 1985.",
  // scheduleRules: [ ...the two seasonal Saturday rules from Kennel metadata above... ]
}

// aliases.ts — Record<string, string[]>. Append one entry (key = kennelCode).
"barbados-h3": ["Barbados H3", "Barbados Hash House Harriers", "Barbados Hash"],
// ❌ do NOT add bare "BH3" (Boulder H3 / Buffalo H3 own it globally).

// sources.ts — Source[] (array of objects). Append one entry. Mirror the Lisbon/Porto/Bandung HC rows.
{
  name: "Barbados H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (KennelSlug/ShortName "BH3" also resolves the same set).
    publicKennelId: "78da30cc-66f5-4c9f-bd7b-ec3b0f54f8d0",
    defaultKennelTag: "barbados-h3",
    // Events are named "Barbados H3 Run#NNNN" (generic) and one is the malformed "Barbados H3 Run#"
    // → synthesize "Barbados H3 #N" via defaultTitle + staleTitleAliases (mirrors Lisbon/Hamburg #1166).
    defaultTitle: "Barbados H3",
    staleTitleAliases: ["Placeholder event for BH3", "Barbados H3 Run#"],
    // upcomingOnly OMITTED — HC convention (getEvents is future-only). 🔴 If you ship the optional
    // historical backfill, ADD `upcomingOnly: true` (Bandung retro: source owns past rows → reconcile
    // would false-CANCEL them otherwise).
  },
  kennelCodes: ["barbados-h3"],
}
```

## Adapter notes / new-scraper plan

**This is config-only.** `HarrierCentralAdapter` already exists (`src/adapters/registry.ts` →
`HARRIER_CENTRAL`; `src/adapters/harrier-central/adapter.ts` + `token.ts`). No new code — just the
seed source row above. Live-verify with `adapter.fetch(source)` (it composes the time-based SHA-256
token, calls the PortalApi `getEvents`, filters by `publicKennelId`, and emits `kennelTags:
["barbados-h3"]`).

**Title handling.** All upcoming EventNames are the generic "Barbados H3 Run#NNNN", and #2342 is the
malformed "Barbados H3 Run#" (no number). The `defaultTitle` + `staleTitleAliases` above let merge
synthesize the clean "Barbados H3 #N" form for the placeholder/malformed cases. ⚠️ **Claude Code:
confirm how `HarrierCentralAdapter` treats a non-placeholder generic name** — if it passes
"Barbados H3 Run#2339" straight through as the title, decide whether to broaden `staleTitleAliases`
(or accept it; it's effectively the synthesized form). `shortName: "Barbados H3"` is >4 chars, so
`friendlyKennelName` short-circuits cleanly → "Barbados H3 Trail #N" / "Barbados H3 #N". Verify:
```bash
npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Barbados H3","Barbados Hash House Harriers"));'
```

### region.ts edits — 🇧🇧 first Caribbean country (ALL 5 mandatory)
Mirror the **Nepal / Cambodia / Vietnam / Sri Lanka / India** 2-level COUNTRY→METRO precedent exactly
(no state-province intermediate, no `seed.ts` `stateMetroLinks`). Palette: **fuchsia** — the only other
fuchsia owner is the **Philippines** (Pacific), nowhere near the Caribbean; every nearer/Atlantic
palette is taken (US-east=rose/emerald, UK=rose). ⚠️ grep-confirm fuchsia shades are neighbour-distinct
(Caribbean is currently isolated, so this is low-risk).

1. **`REGION_SEED_DATA`** — add the COUNTRY + METRO records:
   ```ts
   // ── Barbados (first 🇧🇧 / first Caribbean country: Barbados H3, est. 1985) — 2-level
   //    COUNTRY → METRO, mirrors the Nepal/Cambodia precedent (no seed.ts stateMetroLinks).
   //    Fuchsia palette — only other owner is the Philippines (Pacific), nowhere near the Caribbean. ──
   {
     name: "Barbados",
     country: "Barbados",
     level: "COUNTRY",
     timezone: "America/Barbados",
     abbrev: "BB",
     colorClasses: "bg-fuchsia-200 text-fuchsia-800",
     pinColor: "#c026d3",
     centroidLat: 13.1939,
     centroidLng: -59.5432,
     aliases: ["Barbados, Caribbean", "Bim"],
   },
   {
     name: "Bridgetown",
     country: "Barbados",
     timezone: "America/Barbados",
     abbrev: "BGI",
     colorClasses: "bg-fuchsia-100 text-fuchsia-700",
     pinColor: "#d946ef",
     centroidLat: 13.0975,
     centroidLng: -59.6167,
     aliases: ["Bridgetown, Barbados"],
   },
   ```
2. **`STATE_GROUP_MAP`** — `"Bridgetown": "Barbados"` (metro → country group; mirrors `"Kathmandu": "Nepal"`).
3. **`COUNTRY_GROUP_MAP`** — `"Barbados": "Barbados"` (country-group key only; the metro resolves via
   `STATE_GROUP_MAP` first, mirroring the Nepal precedent comment).
4. **`COUNTRY_CODE_TO_NAME`** — `BB: "Barbados"`.
5. **`COUNTRY_INFERENCE_RULES`** — `[/\b(barbados|bridgetown)\b/, "Barbados"]`. 🔴 Without this,
   `inferCountry()` falls through to `"USA"` for any Barbados text (the bug that bit ONH3 in CI). Both
   tokens are unambiguous (no common US place-name collision). Add a disambiguation test mirroring the
   existing pattern.

**⚠️ Claude Code: verify before writing real code.** Any snippet above is illustrative; the live repo
is the authority. Before finalizing:
- `Kennel` field names — `walkersWelcome` (NOT `walkerFriendly`); confirm `scheduleRules` shape in
  `prisma/schema.prisma` / `kennels.ts` (the `lbh3` rows are the reference) before pasting.
- Region map names + comment conventions — mirror the Nepal block verbatim (it's the cleanest recent
  precedent).
- `HarrierCentralAdapter` config knobs (`publicKennelId`, `defaultKennelTag`, `defaultTitle`,
  `staleTitleAliases`, `upcomingOnly`) against the actual adapter/types — don't assume.

## Deep-dive checklist (nothing deferred)
- [x] logo (tokenized → flag self-host, magic-byte ext)  [x] foundedYear (1985, 40th-anniv source)
  [x] socials (FB + IG `barbadosh3`; WhatsApp noted)  [x] schedule (+ seasonal scheduleRules)  [x] hashCash (BDS $4, 2017→$1 noted)
- [x] description  [x] source live-verified (HC global-runs, 4 upcoming)  [x] history depth assessed (~130, optional backfill)
- [x] coord sanity (TBD venues, no trap)  [x] end times (none)  [x] kennelCode collision-checked
  [x] kennelCodes (source guard) set  [x] sibling sweep (below)

### Sibling sweep
The Barbados scene has sibling kennels surfaced on barbadoshash.com's "Night & Bike Hash" page
(**Barbados Night Hash** + **Barbados Bike Hash**) and the queue's noted **Moon Over Barbados**
(full-moon, last run 2026-01-23). **None appear in the HC `global-runs` feed under a distinct GUID in
this pass** — the `78da30cc…` GUID returns only the main "Barbados H3" Saturday runs. So:
- **Included** in this source: `barbados-h3` only (single-kennel HC source).
- **Future targets** (separate sources, do NOT add to this `kennelCodes`): Moon Over Barbados,
  Barbados Night Hash, Barbados Bike Hash — queue them once a confirmed dynamic source (their own HC
  GUID, or the barbadoshash.com Squarespace "Night & Bike" / Events surface) is verified live.

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **HC TZ quirk is harmless here** — `America/La_Paz` ≡ `America/Barbados` (both UTC−4, no DST); set
  the Bridgetown METRO zone to `America/Barbados` and the composed UTC starts are identical.
- **`config.upcomingOnly` decision is conditional on the backfill** (see Historical backfill): omit if
  no backfill; set `true` if you ship one.
- **First Caribbean country → the `COUNTRY_INFERENCE_RULES` edit is the silent-USA-routing trap** — do
  not omit it; add the disambiguation test.
- **Self-host the logo + magic-byte the extension** — never trust the `.png` URL / Content-Type.
- **Backfill (if shipped): freeze a curated `scripts/data/barbados-h3-history.json` + dumb loader**
  (don't commit the parser); run the PII scrub + run-number-continuity validation first.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the
brief — no separate prompt needed._
