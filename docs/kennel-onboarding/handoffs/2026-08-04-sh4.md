# Onboarding Handoff — SH4 / South Hams H3-H4 (South Hams, Devon, England) — 2026-08-04

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/sh4-20260804`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Add the **South Hams METRO**
>    to `src/lib/region.ts` as noted in **Adapter notes / region** (2 edits; UK COUNTRY already seeded).
> 3. Implement the **new bespoke HTML adapter** exactly as in **Adapter notes** (this is NOT config-only —
>    see the earned-claim analysis there). Register it in `htmlScrapersByUrl`.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md`
>    — call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` snippet. Resolve every item flagged
>    `⚠️ Claude Code must confirm`. In particular: **capture the real DOM of `/auto-draft/`** (the research
>    sandbox could only read the readability-stripped markdown — see **Live source verification → Notes**)
>    and build the test fixture from that verbatim markup. Validate: events non-empty + the upcoming
>    #1594 present, dates UTC-noon, `startTime` "19:30", `kennelTag` resolves with no unmatched.
>    **DO NOT run `npx prisma db seed` here.**
> 5. The **Historical backfill** one-shot (worth it — ~640 runs). Freeze `scripts/data/sh4-history.json`
>    + dumb loader `scripts/backfill-sh4-history.ts` (H7 pattern — commit the frozen dataset, not the parser).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist below.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- <path>` for the adapter, the backfill
>      script, `scripts/data/sh4-history.json`, region.ts, the three seed files). Squash-merge sometimes drops
>      a follow-up commit — open a small recovery PR if anything's missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive)
>    - Run the backfill one-shot: `npx tsx scripts/backfill-sh4-history.ts`
>    - Trigger a scrape from `/admin/sources` to publish events to prod
>    - Spot-check `hashtracks.xyz/kennels/sh4` for the expected event count + sample dates
> Everything you need is in the sections that follow.

## Summary
- Type: **full onboard**
- Adapter: **HTML_SCRAPER — NEW bespoke adapter needed** (`SouthHamsH4Adapter`, ~150–220 LoC). NOT config-only.
- Effort estimate: **new ~150–220 LoC bespoke HTML adapter + tests** + a one-shot backfill script (~640 rows,
  frozen JSON + dumb loader). Mirror `dublin-hash.ts` / `burlington-hash.ts` / `mijas-hash.ts` `parseRunLabel`.
- One-line: **First South Hams (Devon) kennel** — a large, very active **weekly-Wednesday** hash (run **#1594**,
  founded **6 May 1998**, "Making a Hash of it in the South Hams") on its own self-hosted WordPress site whose
  **Events Manager "List of Hashes" page is fully SSR** (run#/date/venue/address/postcode + rich per-event
  detail pages), plus a complete SSR archive back to 2015 for backfill.

## Dedup result
- Kennel in seed: **no** (`grep -in 'sh4\|south ?ham' prisma/seed-data/kennels.ts` → only unrelated hits:
  a HK Ladies H4 wixsite URL `hklh4`, and a Chiang Mai `scheduleNotes` mention of a twinned "Scarborough Happy
  Hash (SH4)" — neither is this kennel)
- Source in seed: **no** (`grep -in 'sh4\|south ?ham' prisma/seed-data/sources.ts` → none)
- Alias in seed: **no** (`grep -in 'sh4\|south ?ham' prisma/seed-data/aliases.ts` → none)
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP (**491 slugs**,
  2026-08-04). No `sh4` / `south-ham` / `southhams` / `kingsbridge` / `totnes` / `salcombe` / `modbury` /
  `ivybridge` slug. (`7h4` = Seven Hills H4 Lynchburg, distinct.)
- Pre-existing admin-seeded events: **none expected** (kennel absent from prod). Claude Code: confirm no
  `Event` rows exist under `sh4` before first scrape (standard check).
- Decision: **full onboard**
- kennelCode: `sh4` (collision check: **clear** — not a kennelCode or alias anywhere in seed; not in the sitemap)

## Live source verification  ✅ (source fully verified live; ⚠️ raw DOM capture deferred to Claude Code)
- Source: **HTML_SCRAPER** — `https://www.sh4.org.uk/auto-draft/` (the "Hash Diary → List of Hashes" page;
  fully SSR, `web_fetch`-able, `text/html`). DNS `sh4.org.uk` → **217.160.0.9, Status 0** (resolved, non-platform
  domain checked per rule). Platform = **Events Manager** WordPress plugin (`PRODID:-//wp-events-plugin.com//7.4.1//EN`).
- Events seen: **1 upcoming + deep past** on the List page (Future Events section = #1594; Past Events back to
  ~#1544 on the same page). **Not a 0-upcoming case.**
- **Source-count parity:** N/A for a run-list page (no platform "total upcoming" counter). The kennel runs
  **weekly**, so 1 future posting is normal; the daily scrape auto-adds each next Wednesday as the committee posts it.
- Sample events (**VERBATIM** from `/auto-draft/`; the `#NNNN`/venue/postcode are the source's own strings —
  the run numbers are real, not constructed):
  1. **05/08/26** — **Hash 1594** — Ridge Road Junction — "Junction at merafield Road to Ridge Rd" — **PL7 1UF**
     — *(detail page `/events/8084/`: 7:30 pm – 11:00 pm; Hares **Silly Shunt & Fluffy Dice**; What3Words
     `menu.prices.order`; On Down = Elburton Arms; per-event ICS `GEO:50.379732;-4.068756`)*
  2. **29/07/26** — Hash 1593 — Bay Horse Inn (Ashburton) — 64 North St — TQ13 7QG
  3. **22/07/26** — Hash 1592 — Miner's Arms — Hemerdon Lane — PL7 5BU
  4. **15/07/26** — Hash 1591 *"OPTIONAL EARLY START 6:30PM … 7:00 AND 8:00PM START TIMES TOO."* — Village Inn (Thurlestone) — TQ7 3NN
  5. **08/07/26** — Hash 1590 — Leonards Road Car Park — PL21 0SL
  - Cadence: **weekly Wednesday, 19:30** (05/08/26 = Wednesday; every sample 7 days apart). Occasional extra
    holiday/social hashes (Boxing Day, Xmas pre-lube weekends) sit off the weekly cadence with their own run #.
- History depth / pagination: **~640 runs reachable** via the separate **`/historical-hashes/` SSR page** — a
  single page that embeds **every year 2015→2026** (year tabs are cosmetic; all lists are in the HTML) as
  `Hash NNNN – Venue – DD/MM/YY`, from **Hash 947 (2015)** through **#1594**, plus a **1998** tab (Hash 0001).
  The collection iCal (below) additionally carries per-event **GEO coords** for ~79 events (1998 + 2015→Jan-2017).
- Coord sanity: **List page carries NO coords** (only venue + postcode). Per-event ICS `GEO` is clean and
  **varies per event** (not a default pin) — e.g. #1594 `50.379732,-4.068756`. So there is **no coord-corruption
  trap**; coords are simply absent from the list. Plan: geocode the postcode in-merge (or optionally enrich the
  few future events via per-event `/events/<slug>/ical/` `GEO`). **No `dropCachedCoords` needed.**
- End times: **available on the ICS** (same-day `DTEND`, e.g. 23:00). Not on the list page. Optional `endTime`.
- Notes:
  - 🔴 **The collection iCal feed is NOT a usable config-only source.** `https://www.sh4.org.uk/events/?ical=1`
    returns HTTP 200 `text/calendar` but a **stale, capped 79-event window: Hash 0001 (1998) + Hash 947→1062
    (2015→Jan-2017), ordered by internal event-ID**. It does **not** contain #1594 or any recent/upcoming run,
    and appending `&scope=future` / `&scope=all` is **ignored** (byte-identical response). So Events Manager's
    collection ICS here is useless for the live feed — **do not seed it as an ICAL_FEED source.** (It IS handy as
    one input to the backfill for the 2015–2017 GEO coords.)
  - 🔴 **The homepage "Upcoming Hashes" widget shows "No events"** (misconfigured) even though `/auto-draft/`
    correctly lists #1594. **Scrape `/auto-draft/`, never the homepage widget.**
  - ⚠️ **Raw DOM not capturable from the research sandbox** — `web_fetch` returns readability-stripped markdown,
    Chrome MCP auto-denies the brand-new `sh4.org.uk` domain (no user present), and a cross-origin fetch from the
    hashtracks tab is CORS-blocked. The **source is fully verified live** (real samples above, real ICS, DNS OK);
    only the exact `<li>`/`<p>`/`<a>` tag structure for the test fixture must be captured at build. In the
    rendered markdown the List page structure is: an `<h?>` **"Today"** (→ "No Events"), **"Future Events"**, and
    **"Past Events"** heading, each followed by a list where every event is one line:
    `DD/MM/YY - [Hash NNNN <optional theme>](/events/<slug>/) - [Venue](/locations/<slug>/) - <address> - <POSTCODE>`.
- **Field-fill assertion table** (columns reflect what the **List page** exposes; the adapter's default source.
  `startTime` is a constant from the schedule, not the page):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | ~1 / 5 | Theme only when present in the `Hash NNNN <theme>` text (e.g. "BBQ hash", "Bring your own beer"). Otherwise **leave undefined** → `merge.ts` synthesizes "SH4 Trail #N". Never let the venue/address become the title. |
  | `startTime` | 5 / 5 | Constant **"19:30"** (homepage: "meet at 7:30pm" every Wednesday; every ICS `DTSTART` = `193000`). Rare special-start weeks note it in the title text (#1591) — accept the 19:30 default, don't parse it. |
  | `endTime` | 0 / 5 (list) | ICS has it (`23:00`); optional. Fine to omit — accept absence. |
  | `location` (venue) | 5 / 5 | Venue link text (e.g. "Ridge Road Junction"). |
  | `locationStreet` | 5 / 5 | The address + postcode after the venue (e.g. "Junction at merafield Road to Ridge Rd, PL7 1UF"). |
  | `locationUrl` (Maps) | 0 / 5 | The venue link is an internal `/locations/<slug>/` page, not a Maps URL → leave undefined. |
  | `hares` | 0 / 5 (list) | Only on the per-event **detail HTML** (`Hares: …` line) — NOT in the list or ICS. Optional enrichment: fetch `/events/<slug>/` for each **future** event (1–3 events). Otherwise leave undefined. |
  | `cost` | 0 / 5 (per-event) | Kennel default **£2** lives on `Kennel.hashCash`; only rare specials override (e.g. #1511 "£10 for run only, pasty and beer"). Don't synth per-event. |
  | `description` | 0 / 5 (list) | "On Down is at …" is on the detail page / ICS `DESCRIPTION`; optional. Leave undefined from the list. |
  | `trailLengthText` | 0 / 5 | Not in feed. |
  | `coords` (lat/lng) | 0 / 5 (list) | Not on the list. Geocode the postcode in-merge, or optionally enrich future events via per-event ICS `GEO`. Fallback = South Hams metro centroid. |

## Kennel metadata (deep-dive complete)
- fullName: **South Hams Hash House Harriers**  /  shortName: **SH4**  /  region: **South Hams** (Devon) / country: **England**
  - 🔴 **H3 vs H4 naming:** the kennel self-brands as **SH4** (domain `sh4.org.uk`, "SH4 On Strava", "SH4 branded
    Hash Clothing", ICS `SUMMARY:South Hams H4 Hash NNNN`, homepage "(**SH4**)") — but its **Facebook group is
    `southhamsh3`** and its **Strava club is `south-hams-h3-149357`**. Both forms are in active use. Use **SH4** as
    the shortName/kennelCode (dominant self-brand) and carry the H3 forms as **aliases**. (source: sh4.org.uk homepage + footer)
- aliases: `["South Hams Hash House Harriers", "South Hams H4", "South Hams H3", "South Hams Hash", "SH4 Hash"]`
  — **omit bare "SH3"** (global collision: Seattle/Stuttgart/etc.). "SH4" itself IS the shortName so it resolves without an alias.
- website: `https://www.sh4.org.uk/` (source: self) / facebook: `https://www.facebook.com/groups/southhamsh3/` (source: site footer)
  / instagram / twitter / discord: **none found → blank** / Strava club: `https://www.strava.com/clubs/south-hams-h3-149357`
  (source: How-To-Participate page) / contactEmail (scribe): `thewords@sh4.org.uk`
- schedule: **Wednesday, 7:30 PM, Weekly** (source: homepage — "run every Wednesday … every week of the year. We
  meet at 7:30pm"). Single clean pattern → flat fields sufficient; a one-rule `scheduleRules`
  (`FREQ=WEEKLY;BYDAY=WE`, `startTime:"18:30"`? **no — 19:30**) is optional (see gotcha on 12h vs 24h below).
- foundedYear: **1998** (source: `sh4.org.uk/sh4s-hashtory/` — "The first SH4 Hash was held on May the 6th 1998 …
  outside Kingsbridge's Tourist Information Centre"; corroborated by the iCal `Hash 0001` `DTSTART:19980506`). High confidence.
- hashCash: **"£2 (first run free; £40/calendar year)"** (source: homepage — "Your first run is free. After that
  it's £2 a run or £40 a calendar year")
- dogFriendly: **not explicitly stated → leave unset/flag** (there is a recurring "Hairy Dog Run", suggestive but not a statement)
- walkersWelcome: **true** (source: homepage — "from fast walkers to dedicated runners … at least one 'long/short
  split' where less athletic Hashers can take the 'short' option")
- logoUrl: `http://www.sh4.org.uk/wp-content/uploads/2015/12/SouthHamsPig300x300.png` — a stable, non-tokenized
  WordPress-uploads URL. **⚠️ self-host** to `public/kennel-logos/sh4.<ext>` per convention (confirm the real
  extension via `curl -sI` Content-Type **and** magic bytes — filename says `.png`, verify).
- description: `"The South Hams Hash House Harriers (SH4) run every Wednesday, rain or shine, daylight or pitch
  black, all year — meeting at 7:30pm, normally at a South Hams pub in Devon. A very social, all-abilities running
  group ('a club for drinkers with a running problem') founded in 1998."`
- lat/lng: **~50.283, -3.777** (Kingsbridge — the founding town / South Hams centre; refine at build if desired)

## Historical backfill
- Available: **~640 runs** — fields: **run # + venue + date** for all (2015→2026 via `/historical-hashes/`), plus
  **GEO coords** for the ~79 events the collection iCal covers (1998 + 2015→Jan-2017), plus Hash 0001 (1998-05-06).
  No hares in the archive surfaces. Deep pre-2015 words exist only as un-scanned paper (per Hashtory) → not available.
- Plan: **one-shot `scripts/backfill-sh4-history.ts` + frozen `scripts/data/sh4-history.json`** (H7 pattern —
  commit the frozen dataset + dumb loader, NOT the parser). Extraction inputs:
  - Primary: `https://www.sh4.org.uk/historical-hashes/` — one SSR page, all years embedded, lines
    `[Hash NNNN <theme?>](url) – <Venue> – DD/MM/YY`. **Strict date partition: backfill takes `date < today` only**
    (the page's 2026 tab also lists the future #1594 — exclude it; the adapter owns `>= today`).
  - Optional coord enrichment: merge in `GEO` from the collection iCal (`/events/?ical=1`) matched by run # for the
    ~79 covered events.
- Frozen-dataset validation (run before shipping): (1) PII scrub (no emails/phones — venue names only, should be
  clean); (2) run-number monotonicity over time; (3) gap-sanity — weekly ⇒ ~7-day steps, flag any run whose
  neighbours imply a mis-parsed `DD/MM/YY`; (4) require a numeric run # — **skip the rare no-run# specials**
  ("AGPU 2015", "Slapton Pre Christmas 2016") or map them by date only; (5) UTC-noon storage.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects). Add under a new "England — South West / Devon" grouping
// (near the other Devon kennels once they land, or wherever the UK block lives).
{
  kennelCode: "sh4",
  shortName: "SH4",
  fullName: "South Hams Hash House Harriers",
  region: "South Hams",
  country: "England",
  website: "https://www.sh4.org.uk/",
  facebookUrl: "https://www.facebook.com/groups/southhamsh3/",
  contactEmail: "thewords@sh4.org.uk",
  foundedYear: 1998,
  logoUrl: "/kennel-logos/sh4.png", // ⚠️ self-host; confirm ext via magic bytes at build
  // scheduleDayOfWeek/scheduleTime kept as fallback; scheduleRules is authoritative (if you add it).
  scheduleDayOfWeek: "Wednesday",
  scheduleTime: "7:30 PM", // 🔴 12-hr on Kennel; scheduleRules.startTime + RawEventData.startTime stay 24-hr "19:30".
  scheduleFrequency: "Weekly",
  hashCash: "£2 (first run free; £40/calendar year)",
  walkersWelcome: true,
  description:
    "The South Hams Hash House Harriers (SH4) run every Wednesday, rain or shine, all year — meeting at 7:30pm, normally at a South Hams pub in Devon. A very social, all-abilities running group ('a club for drinkers with a running problem') founded in 1998.",
  latitude: 50.283,
  longitude: -3.777,
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug). Real shape confirmed via head of file.
"sh4": ["South Hams Hash House Harriers", "South Hams H4", "South Hams H3", "South Hams Hash", "SH4 Hash"],

// sources.ts — Source[] (array of objects). Bespoke HTML adapter registered by URL (no `config` block needed
// beyond upcomingOnly). Mirror the RIH3 HTML row shape.
{
  name: "South Hams H3 (SH4) Website Hash Diary",
  url: "https://www.sh4.org.uk/auto-draft/",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: { upcomingOnly: true }, // 🔴 REQUIRED — rolling list; reconcile would false-cancel aged-off runs
  kennelCodes: ["sh4"],
},
```

## Adapter notes / new-scraper plan

**Why NOT config-only (claim earned):** (a) The only structured feed (Events Manager `?ical=1`) is a stale capped
79-event 1998→2017 window that ignores `scope=` and omits all recent/upcoming runs — it does **not** carry the live
events. (b) No existing adapter reads this Events-Manager "List of Hashes" one-line-per-run format. Both config-only
conditions fail → **new bespoke HTML adapter** (~150–220 LoC).

**`SouthHamsH4Adapter` (HTML_SCRAPER):**
- **Fetch:** `fetchHTMLPage(url)` from `@/adapters/utils` (static SSR; confirmed `web_fetch`-able). Honor
  `options.days` via `buildDateWindow`.
- **Parse (`/auto-draft/`):** locate the **"Future Events"** section (and, within the scrape window, recent
  "Past Events" — the merge pipeline dedups against the backfill). Each event is one line:
  `DD/MM/YY - <Hash NNNN [theme]> (link /events/<slug>/) - <Venue> (link /locations/<slug>/) - <address> - <POSTCODE>`.
  Extract in helpers to keep S3776 ≤ 15:
  - `parseListLine(line)` → `{ dateStr, runNumber, themeText, venue, street, postcode, detailUrl }`.
  - `runNumber`: `extractHashRunNumber` from `@/adapters/utils` (handles `Hash 1594` / lowercase `hash 1571`).
  - `date`: **UK `DD/MM/YY`** → `Date.UTC(2000+yy, mm-1, dd, 12)`. Validate via `Date.UTC` round-trip; reject overflow.
  - `title`: keep the theme **only** when the text after the run number is a real theme (e.g. "BBQ hash",
    "Bring your own beer", "New Years Day Hash"). Otherwise **leave `title` undefined** (merge synthesizes
    "SH4 Trail #N" — `friendlyKennelName("SH4", …)` short-circuits since `"SH4".length === 3`… **⚠️ ≤4 chars →
    run the `friendlyKennelName` check below at build**).
  - `startTime`: constant `"19:30"`.
  - `location` = venue; `locationStreet` = `<address>, <POSTCODE>`; `kennelTags: ["sh4"]`.
  - Optional future-event enrichment (1–3 events): fetch `/events/<slug>/` for `Hares: …` + `endTime` +
    "On Down …" `description`, and/or `/events/<slug>/ical/` for `GEO` coords.
- 🔴 **Fail-loud zero guard** (single-page adapter): if the Future+in-window rows parse to `[]`, push an
  `errors[]` entry so `scrape.ts` suppresses reconciliation — don't let a markup drift "succeed" with `events: []`
  (a brand-new source's zero-event baseline can't fire the health alert). Reference: `ah3-nz` retro Gap D.
- **Registry:** add `"https://www.sh4.org.uk/auto-draft/": new SouthHamsH4Adapter()` to `htmlScrapersByUrl` in
  `src/adapters/registry.ts` (mirror `rih3` / `dublin-hash` registration).
- **Reference adapters:** `dublin-hash.ts` (table/line plumbing + `fetchHTMLPage`), `burlington-hash.ts`
  (label/field cleaning, terminator-safe regexes), `mijas-hash.ts` `parseRunLabel` (run # + sub-label handling).

### region.ts — add South Hams METRO (2 edits; UK COUNTRY already seeded)
1. **`REGION_SEED_DATA`** — METRO record under UK: name `"South Hams"`, level METRO, violet palette **lighter
   shade (`-100`)** matching sibling England metros, pin color distinct from the country, timezone `Europe/London`,
   abbrev **`SHM`** (🔴 **confirm free** — `grep -n "SHM" src/lib/region.ts`; if taken try `SHH`/`SHS`), centroid
   **`52`?—no: `50.283, -3.777`** (Kingsbridge), aliases `["South Hams", "South Hams, Devon"]` (1–2 only; no
   trailing-zero numeric literals — Sonar S6749).
2. **`STATE_GROUP_MAP`** — `"South Hams": "England"`.
- **NO `COUNTRY_INFERENCE_RULES` / `COUNTRY_GROUP_MAP` edit** — the UK rule already covers `england`, and metros
  resolve via `STATE_GROUP_MAP`. (The seed kennel carries explicit `country: "England"` regardless.)
- **Alternative (0 region.ts edits):** reuse the existing **Plymouth METRO** (Tamar-Valley precedent — many SH4
  venues are in/near Plymouth: Plympton, Wembury, Elburton, Cann Woods). South Hams is the cleaner geographic label
  and the kennel's identity, so **South Hams METRO is the recommendation**; Plymouth-reuse is an acceptable fallback
  if you'd rather avoid the region churn.

**⚠️ Claude Code: verify before writing real code.** Any code snippet above is illustrative; the live repo is authority:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT `walkerFriendly`).
  Check `prisma/schema.prisma` for canonical names.
- Imports — `fetchHTMLPage` + `extractHashRunNumber` + date helpers from `@/adapters/utils`; use `safeFetch`
  (NOT raw `fetch`) for any per-event sub-fetch.
- `kennelPagesStopReason` — set ONLY on genuine truncation; a non-empty string suppresses stale-event reconciliation.
- `title` — leave `undefined` when there's no clean theme; never let venue/address/hare become the title.
- 🔴 **`friendlyKennelName` ≤4-char check** (shortName "SH4" is 3 chars): before shipping run
  `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("SH4","South Hams Hash House Harriers"));'`
  — if it synthesizes something verbose/garbled instead of "SH4 Trail #N", either lengthen the shortName or fix the fn.

## Deep-dive checklist (nothing deferred)
- [x] logo (stable WP-uploads URL; flag self-host + magic-byte ext)  [x] foundedYear (1998, cited ×2)  [x] socials (FB + Strava; IG/X/Discord none → blank)  [x] schedule (weekly Wed 19:30)  [x] hashCash (£2; first run free; £40/yr)
- [x] description  [x] source live-verified (DNS 217.160.0.9; real #1594 sample; ICS inspected)  [x] history depth assessed (~640 runs)
- [x] coord sanity (no default-pin trap; list has no coords → geocode postcode)  [x] end times noted (ICS `DTEND`)  [x] kennelCode collision-checked (`sh4` clear)  [x] kennelCodes source guard set (`["sh4"]`)

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — the `/auto-draft/` list is a rolling window; without it
  `reconcile.ts` false-`CANCEL`s aged-off past runs. (Full backfill lives in the one-shot, not the adapter.)
- **Do NOT seed the collection iCal as an ICAL_FEED source** — it's the stale capped 1998–2017 window (see
  Live-source Notes). It's a backfill-coord input only.
- **`Kennel.scheduleTime` is 12-hour `"7:30 PM"`; any `scheduleRules.startTime` + `RawEventData.startTime` are
  24-hour `"19:30"`.** Don't cross them.
- **Single-page adapter → explicit `rows.length === 0` fail-loud guard** (see above).
- **http-only literals → Sonar S5332:** the site's canonical logo URL is `http://…`. Use `https://` in mocked
  tests; mark the 2–3 real production literals SAFE via the SonarCloud REST API if flagged (AH3-NZ process note).
- **Self-host the logo** to `public/kennel-logos/sh4.<ext>`; confirm ext via Content-Type + magic bytes (don't pre-fill `.png`).
- **Sonar S5852/S5843** — keep the `DD/MM/YY` and run-number regexes simple (single `\s`, no stacked optional
  groups); let a `Date.UTC` round-trip validate rather than enumerating months.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
