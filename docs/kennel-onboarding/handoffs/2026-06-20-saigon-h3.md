# Onboarding Handoff — Saigon H3 (Ho Chi Minh City, Vietnam) — 2026-06-20

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/saigon-h3-20260620`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Add the **Vietnam COUNTRY +
>    Ho Chi Minh City METRO** region records — this is the **first 🇻🇳 Vietnam kennel**, so do all **5
>    `region.ts` edits** in the New-country checklist below (REGION_SEED_DATA ×2, STATE_GROUP_MAP,
>    COUNTRY_GROUP_MAP, COUNTRY_CODE_TO_NAME, COUNTRY_INFERENCE_RULES). No `seed.ts stateMetroLinks`
>    needed (2-level COUNTRY→METRO, mirror the Poland/Warsaw + Nepal/Kathmandu precedent).
>    🔴 **If the Hanoi H3 handoff (queue #2) merged first and already added Vietnam COUNTRY**, then add
>    ONLY the Ho Chi Minh City METRO record + its STATE_GROUP_MAP/COUNTRY_GROUP_MAP entries, and extend
>    the existing `COUNTRY_INFERENCE_RULES` Vietnam regex with `saigon|ho\s*chi\s*minh` if missing.
> 3. Implement the **NEW `SaigonH3Adapter`** exactly as in **Adapter notes** (static Cheerio; NOT
>    config-only — see effort note).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` snippet. Validate: events non-empty
>    + upcoming, dates UTC-noon, `startTime` "HH:MM" (or undefined here), `kennelTag` resolves with no
>    unmatched. Resolve every `⚠️ Claude Code must confirm` item below. **DO NOT run `npx prisma db
>    seed` here.**
> 5. Optional (low priority): a `/runs` or `/reports` historical backfill — **only if** you can find a
>    machine-readable archive endpoint at build (see **Historical backfill** — the stats/reports pages
>    did NOT SSR their content to the research sandbox).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- <path>` for the adapter, region.ts,
>      seed files, and any backfill script — squash-merge sometimes drops a follow-up commit).
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive)
>    - Trigger a scrape from `/admin/sources` to publish events to prod
>    - Spot-check `hashtracks.xyz/kennels/saigon-h3` for the expected event count + sample dates.

## Summary
- Type: **full onboard**
- Adapter: **HTML_SCRAPER (NEW `SaigonH3Adapter`)** — bespoke server-rendered hash-club site;
  parses the SSR'd markdown `<table>` on the **Receding Hairline** (`/hareline`) page. NOT config-only
  (run-type-vs-theme title discrimination + `Hares Needed!` placeholder stripping; `GenericHtmlAdapter`
  maps one selector → one field and can't do that logic).
- Effort estimate: **NEW small Cheerio adapter ~120–180 LoC + tests.** Model on `dublin-hash.ts`
  (table iteration) + `phnom-penh-h3.ts` / Kaohsiung patterns (per-run fail-loud, placeholder strip).
  Simpler than Phnom Penh — a single SSR table surface and **ISO `YYYY-MM-DD` dates (NO year inference)**.
- One-line: First 🇻🇳 **Vietnam** kennel — Ho Chi Minh City's weekly-Sunday country-bus hash (run #1834,
  est. 1990), 24-run forward hareline fully SSR'd; opens the Vietnam region (shared with Hanoi H3, queue #2).

## Dedup result
- Kennel in seed: **no** (grep `saigon-h3|"Saigon"|Ho Chi Minh|HCMC` in kennels/aliases/sources → 0 matches).
- Source in seed: **no** (grep `saigon|saigonhashers` → 0 matches).
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP
  2026-06-20, **447 kennel slugs**; zero matches for `saigon`/`vietnam`/`hcmc`/bare `sh3` (the only
  `monday` hit was the unrelated `bangkok-monday-h3`).
- DNS check (non-platform domain `saigonhashers.com`): **Status 0 (OK)**, A records `145.223.124.21` /
  `147.79.72.145` (Hostinger; via `dns.google/resolve`). Domain exists and resolves.
- Pre-onboarding admin-event check: kennel not live (no slug) → no pre-seeded admin `Event` rows to
  dedup/purge expected. (If Claude Code finds any under `saigon-h3` at build, list + purge before scrape.)
- Decision: **full onboard** (kennel + aliases + source + Vietnam region).
- kennelCode: `saigon-h3` (collision check: **clear** — `saigon-h3` free; descriptive city slug chosen
  over the **globally-taken bare `sh3`** [summit / sh3-wa / salemh3 own "SH3" — see `aliases.ts:81,258`],
  so the bare "SH3" alias is **omitted**).

## Live source verification ✅
- Source: HTML_SCRAPER — `https://saigonhashers.com/hareline` ("Receding Hairline", `text/html`, SSR).
  Verified live via `web_fetch` 2026-06-20 (sandbox has no raw network; `web_fetch` renders full SSR HTML).
- Platform: **bespoke server-rendered hash-club site** (markdown authored → real `<table>` HTML; clean
  nav paths `/home`, `/hareline`, `/runs`, `/reports`, `/hashers`, `/trails`; assets under
  `/assets/images/`). The home page AND `/hareline` are **fully SSR'd** (plain fetch returns the complete
  run tables — no JS, no browserRender). New platform shape → note appended to `source-platform-notes.md`.
- Events seen: **24 upcoming** on `/hareline` (run **#1834 → #1857**), date range **2026-06-21 →
  2026-11-29** (weekly Sunday). The home page repeats a 6-row subset (#1834 in "This Week's Run" +
  #1835–#1839 in "Upcoming Runs").
- **Source-count parity:** `/hareline` is the authoritative forward feed and lists all 24 forward runs in
  one SSR table (no pagination). Capture all 24 → parity N/A (no page-size lift needed). Strongly live
  (24 real future-dated runs, next one in 1 day) — recently-active rule not even needed.
- **Sample events (VERBATIM from `/hareline` — run numbers, dates, themes & hares are real):**

  | numbers | Date | Name/Occasion | Hares |
  |---|---|---|---|
  | 1834 | 2026-06-21 | Bus Trip/City Run | Hares Needed! |
  | 1837 | 2026-07-12 | William of Orange Run | Paddy Fag & Co |
  | 1840 | 2026-08-02 | Bus Trip/City Run | Rocky & Co |
  | 1844 | 2026-08-30 | **Saigon H3 36th Birthday Run** | One at a Time & Co |
  | 1856 | 2026-11-22 | Cherry Ho & Sore Arse B-Day Run | Cherry Ho & Sore Arse & Co |

  (Home page **"This Week's Run"** confirms #1834 — 2026-06-21 — "Bus Trip/City Run", A-Site & On-On
  columns empty.) The columns are: `numbers | Date | Name/Occasion | Hares | A-Site | On-On`.
  🔴 The **A-Site and On-On columns are EMPTY on every current row** (no per-event venue/Maps link in
  the table). The only Maps links on the site are prose-level: the weekly meeting point **Caravelle
  Hotel, Lam Son Square, District 1** (`https://maps.app.goo.gl/5yCXbbKa4yVwH5Th7`) and a District-2
  pickup near Estella (`https://maps.app.goo.gl/ZG78RPAAbxKF3tYU8`) — these are the *bus departure*
  point, NOT the (varying, unpublished) trail venue.
- History depth / pagination: **⚠️ on-site archive NOT sandbox-reachable.** The history page claims
  "over 1,500 runs since the first one" and run #1834 implies ~1,834 runs of history, but **run number ≠
  reachable archive depth.** The `/runs` (Run Stats) page **timed out** and `/reports` (Run Reports)
  returned **nav + logo only** (no SSR'd content) to a plain `web_fetch` → those surfaces are JS-rendered
  (likely DataTables fed by an AJAX/JSON endpoint) and could not be read here. ⚠️ **Claude Code: probe at
  build** for an underlying JSON/AJAX endpoint behind `/runs` or `/reports` (Network tab) before deciding
  whether a backfill is worth scripting. See **Historical backfill**.
- Coord sanity: **no decimal coords anywhere** — the table's A-Site/On-On columns are empty; the only
  Maps links are `maps.app.goo.gl` shortlinks (no extractable lat/lng) at the kennel level. **No
  default-pin corruption trap** (nothing to reject). Leave `latitude`/`longitude` undefined → merge
  geocodes the HCMC metro centroid.
- End times: none per-event. (FAQ: "the bus returns usually around 7pm" — a kennel-level return estimate,
  not an event end timestamp; do NOT synthesize `endTime`/`endDate`.)
- Notes:
  - **🔴 Dates are ISO `YYYY-MM-DD` and year-bearing → NO inference needed.** Parse straight to
    `Date.UTC(y, m-1, d, 12)`.
  - **Title discrimination:** `Name/Occasion` is usually the run-TYPE `"Bus Trip/City Run"` (logistics,
    NOT a theme) → leave `title` undefined so `merge.ts` synthesizes `"Saigon H3 Trail #N"`. Keep it as
    `title` ONLY when it's a real occasion/theme (`"William of Orange Run"`, `"Saigon H3 36th Birthday
    Run"`, `"Cherry Ho & Sore Arse B-Day Run"`). Reference blocklist: a case-insensitive exact match on
    `bus trip/city run` (and trivial variants) → drop.
  - **Hares placeholder:** `"Hares Needed!"` is a placeholder → `undefined`. Keep real hares verbatim
    (incl. the trailing `"& Co"`: `"Paddy Fag & Co"`, `"Rocky & Co"`, `"One at a Time & Co"`,
    `"Cherry Ho & Sore Arse & Co"`).
  - **A-Site / On-On → `locationUrl` IF present** (`maps.app.goo.gl` shortlink) — currently empty, but
    code for it (when populated it'll be a Maps anchor in the cell). No decimal coords either way.

- **Field-fill assertion table** (over the 24 `/hareline` forward runs):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | ~3 / 24 (real themes) | Keep real occasion themes; drop `"Bus Trip/City Run"` run-type → `title` undefined → `merge.ts` synthesizes `"Saigon H3 Trail #N"`. |
  | `startTime` | 0 / 24 | Not per-event in the table → leave undefined. Kennel `scheduleTime` (1:30 PM bus departure) carries the time on the page. (Optional: set every run's `startTime` to `"13:30"` from the fixed weekly departure — but City Runs may differ, so undefined is safer.) |
  | `endTime` | 0 / 24 | None per-event → undefined (7pm bus return is kennel-level). |
  | `location` (venue name) | 0 / 24 | Trail venue not published (A-Site empty) → undefined; merge geocodes HCMC centroid. The Caravelle Hotel is the bus-departure meeting point, not the trail. |
  | `locationStreet` | 0 / 24 | Not in feed → undefined. |
  | `locationUrl` (Maps) | 0 / 24 now | A-Site/On-On columns empty currently; parse the cell `<a href>` (`maps.app.goo.gl`) → `locationUrl` when present. |
  | `hares` | ~4 / 24 real | Strip `"Hares Needed!"` → undefined; keep real "& Co" strings verbatim. |
  | `cost` | 0 / 24 (per-event) | Kennel default lives on `Kennel.hashCash`; the table can't reliably tell bus (VND300k) from city (VND150k) per row → don't set `Event.cost`. |
  | `description` | 0 / 24 | None per-event → undefined. |
  | `trailLengthText` | 0 / 24 | Kennel-level only (FAQ: runs 6–10 km / walks 4–5 km) → not per-event. |
  | `coords` (lat/lng) | 0 / 24 | None → undefined; merge geocodes HCMC centroid. No default-pin trap. |

## Kennel metadata (deep-dive complete)
- fullName: **Saigon Hash House Harriers** (source: `saigonhashers.com` title + `/saigonhistory`)
- shortName: **Saigon H3**  ·  kennelCode: **`saigon-h3`**  ·  slug: **`saigon-h3`**
  - 🔴 shortName is **"Saigon H3"** (not bare "SH3" — globally taken) and is >4 chars, so
    `friendlyKennelName` short-circuits cleanly to it (synthesized title = "Saigon H3 Trail #N").
- region: **Ho Chi Minh City** (METRO)  ·  country: **Vietnam** (new COUNTRY)
- aliases: **["Saigon", "Saigon H3", "Saigon Hash House Harriers", "Ho Chi Minh City H3", "Ho Chi Minh
  Hash House Harriers"]**
  - 🔴 **OMIT bare "SH3"** — global collision (`summit`, `sh3-wa`, `salemh3` all carry "SH3" in
    `aliases.ts`). The "Ho Chi Minh …" forms are the kennel's original name (founded as the Ho Chi Minh
    H3 in 1990) — useful aliases (grep-confirmed free).
- website: **https://saigonhashers.com/** (source: live)
- facebook: **https://www.facebook.com/groups/saigonhash/** (source: web search; the public FB group.
  There is also a thinner FB *page* `facebook.com/people/Saigon-Hash-House-Harriers/61556661491262/` —
  use the group as the canonical social.)
- instagram / twitter / discord: none found.
  - Other contact (source `/contact`): email **saigonmmc@gmail.com**; phones for Mai Thi / Ian Brown
    (PII — do NOT seed phone numbers).
- schedule: **Sunday, 1:30 PM, Weekly** (source: `saigonhashers.com` home banner "New Departure Time:
  Bus now departs from the Caravelle Hotel at 1:30 pm!"; `/faq` "The bus leaves promptly at 1:30 pm…
  arrive at around 1:00–1:15"; home "We meet most Sundays at 1:15pm… The bus departs at 1:30 pm sharp").
  🔴 The mailchimp mirror / livinginvietnam.com list a **stale** "1:45pm / bus 2pm" — superseded by the
  live primary site's current banner. Single weekly pattern → no `scheduleRules` needed.
- foundedYear: **1990** (source: `/saigonhistory` — *"the hash was born on 26 August 1990"*, founded as
  the Ho Chi Minh Hash House Harriers). **Corroborated** by `/hareline` run **#1844 = "Saigon H3 36th
  Birthday Run" on 2026-08-30** (1990 + 36 = 2026, late-August birthday matches the 26-Aug founding).
  High confidence; no internal contradiction.
- hashCash: **"VND 300,000 (bus trip) / VND 150,000 (city run); Vietnamese VND 200,000 — includes
  transport, beverages & a snack"** (canonical source: home "Extra Information" *"Run fees are VND
  300.000 for Bus trips and VND 150.000 for City Runs"*; `/faq` adds the nationality split *"VND300,000
  for expats and VND200,000 if you are Vietnamese… includes transportation, all beverages, and a light
  snack"*). 🟡 The two sources frame fees on different axes (trip-type vs nationality) — both captured
  verbatim in the value; no contradiction, no double-symbol typo.
- dogFriendly: **not stated** → leave blank.
- walkersWelcome: **true** (source: `/faq` *"a wide range of abilities, from runners to walkers and
  everything in between. It's a family Hash and everyone is welcome"*; two trails set, runners + walkers).
- description: *"The Saigon Hash House Harriers (est. 1990) are a 'drinking club with a running problem'
  welcoming Vietnamese, expats and tourists of all abilities. Most Sundays a Hash bus departs the
  Caravelle Hotel in District 1 at 1:30 pm sharp for running (6–10 km) and walking (4–5 km) trails in
  the countryside around Ho Chi Minh City, followed by a circle and the On-On. Family-friendly;
  non-drinkers and walkers welcome."* (source: `/saigonhistory` + `/faq` + home)
- logoUrl: **https://saigonhashers.com/assets/images/logo_transparent.png** — a **stable asset path**
  (NOT a tokenized CDN URL), so durable; still **recommend self-host** to
  `public/kennel-logos/saigon-h3.<ext>` per convention. 🔴 **Confirm the real extension via magic bytes**
  (`curl -sI` + first bytes — declared `.png`, verify `\x89PNG`).
- lat/lng: not per-event. Ho Chi Minh City metro centroid ≈ **10.7769, 106.7009** (use for the METRO
  region record / kennel fallback). Weekly bus start = Caravelle Hotel, Lam Son Square, District 1.

## Historical backfill
- Available: **⚠️ unverified from the sandbox.** Run #1834 implies a deep history, and the site exposes
  `/runs` (Run Stats), `/reports` (Run Reports = recaps), `/hashers`, and `/trails` (Map of Previous
  Trails) — but `/runs` **timed out** and `/reports` returned **nav-only** to a plain fetch (JS-rendered;
  no SSR content). So the archive is real but not reachable from here.
- Plan: **defer — assess at build, LOW priority.** ⚠️ Claude Code: open `/runs` and `/reports` in the
  Network tab and look for an underlying JSON/AJAX endpoint (DataTables source). If one exists and is
  cleanly structured (run # / date / hares / venue), a one-shot frozen-dataset backfill
  (`scripts/data/saigon-h3-history.json` + dumb `scripts/backfill-saigon-h3-history.ts`, H7/Brasília
  pattern) is worth it (deep history, weekly cadence). If the archive is only JS-rendered HTML with no
  clean endpoint, **skip** — the live adapter (`upcomingOnly: true`) handles all 24 forward runs and the
  kennel page is healthy on day one without history.

## Ready-to-paste seed

> 🔴 `aliases.ts` is `Record<string, string[]>` keyed by **kennelCode** (NOT slug, NOT an object array).
> Confirmed shape: `"<kennelCode>": ["Alias One", "Alias Two"]`. (kennelCode == slug here, so the key is
> `"saigon-h3"` either way.)

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "saigon-h3",
  shortName: "Saigon H3",
  fullName: "Saigon Hash House Harriers",
  region: "Ho Chi Minh City",          // METRO region name (see region.ts edits)
  country: "Vietnam",
  website: "https://saigonhashers.com/",
  facebookUrl: "https://www.facebook.com/groups/saigonhash/",
  // scheduleDayOfWeek/scheduleTime kept as fallback; scheduleRules is authoritative (none here).
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "1:30 PM",             // 🔴 12-hr "H:MM AM/PM" in the Kennel profile field (NOT 24-hr).
  scheduleFrequency: "Weekly",
  foundedYear: 1990,
  hashCash: "VND 300,000 (bus trip) / VND 150,000 (city run); Vietnamese VND 200,000 - includes transport, beverages & snack",
  walkersWelcome: true,
  // dogFriendly: omitted - not stated.
  description: "The Saigon Hash House Harriers (est. 1990) are a 'drinking club with a running problem' welcoming Vietnamese, expats and tourists of all abilities. Most Sundays a Hash bus departs the Caravelle Hotel in District 1 at 1:30 pm sharp for running (6-10 km) and walking (4-5 km) trails in the countryside around Ho Chi Minh City, then a circle and the On-On. Family-friendly; non-drinkers and walkers welcome.",
  logoUrl: "/kennel-logos/saigon-h3.<ext>", // self-host from saigonhashers.com/assets/images/logo_transparent.png; confirm ext by magic bytes
  latitude: 10.7769,
  longitude: 106.7009,
}

// aliases.ts — Record<string, string[]> (keyed by kennelCode)
"saigon-h3": ["Saigon", "Saigon H3", "Saigon Hash House Harriers", "Ho Chi Minh City H3", "Ho Chi Minh Hash House Harriers"],
// 🔴 do NOT add bare "SH3" — global collision (summit / sh3-wa / salemh3).

// sources.ts — Source[] (array of objects). Include scrapeDays!
{
  name: "Saigon H3 Website",
  url: "https://saigonhashers.com/hareline",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,
  scrapeFreq: "daily",
  scrapeDays: 365,                     // /hareline reaches ~5 months ahead (24 runs to 2026-11-29)
  config: { upcomingOnly: true },      // receding hareline ages out → suppress reconcile false-cancel
  kennelCodes: ["saigon-h3"],
}
```

### New-country region edits (`src/lib/region.ts`) — all 5, mirror Poland/Warsaw + Nepal/Kathmandu

> 🔴 Vietnam is the **first 🇻🇳 kennel** → `inferCountry()` defaults to `"USA"` without rule #5.
> 🔴 **Coordinate with the Hanoi H3 handoff (queue #2):** both are "first Vietnam." Whichever merges
> first adds the Vietnam COUNTRY; the second adds only its METRO. If Vietnam COUNTRY already exists when
> you build this, skip the COUNTRY record + the COUNTRY-level map entries and only add the HCMC METRO
> (and extend the inference regex with `saigon|ho\s*chi\s*minh` if absent).

1. **`REGION_SEED_DATA`** — add COUNTRY + METRO (illustrative; mirror the Poland block at `region.ts:1888`
   / Nepal at `region.ts:1912`. Pick a free Asia palette distinct from SE-Asia neighbors — scan existing
   Bangkok/Singapore/Tokyo/Nepal/Poland entries; avoid trailing-zero literals per Sonar S6749):
   ```ts
   // ── Vietnam (first 🇻🇳 kennel: Saigon H3, Ho Chi Minh City) — 2-level COUNTRY→METRO,
   //    mirror Poland/Nepal (no seed.ts stateMetroLinks needed). ──
   {
     name: "Vietnam", country: "Vietnam", level: "COUNTRY",
     timezone: "Asia/Ho_Chi_Minh", abbrev: "VN",
     colorClasses: "bg-orange-200 text-orange-800", pinColor: "#ea580c",  // ⚠️ confirm free vs SE-Asia neighbors
     centroidLat: 16.0, centroidLng: 107.8,
     aliases: ["VN", "Việt Nam"],
   },
   {
     name: "Ho Chi Minh City", country: "Vietnam",
     timezone: "Asia/Ho_Chi_Minh", abbrev: "SGN",
     colorClasses: "bg-orange-100 text-orange-700", pinColor: "#f97316",  // lighter shade + diff pin
     centroidLat: 10.7769, centroidLng: 106.7009,
     aliases: ["Ho Chi Minh City, Vietnam", "Saigon"],
   },
   ```
   (🔴 Country = darker `-200`, Metro = lighter `-100`, different pin hexes — confirm the chosen palette
   isn't already used by an adjacent Asia region; orange is common, so verify or swap.)
2. **`STATE_GROUP_MAP`** — `"Ho Chi Minh City": "Vietnam"` (canonical display-name key).
3. **`COUNTRY_GROUP_MAP`** — `"Vietnam": "Vietnam"` (metro resolves via STATE_GROUP_MAP first, so only the
   country-group key is reachable here — mirror the Poland/Nepal comment at `region.ts:4026/4029`).
4. **`COUNTRY_CODE_TO_NAME`** — `VN: "Vietnam"`.
5. **`COUNTRY_INFERENCE_RULES`** — `[/\b(vietnam|viet\s*nam|saigon|ho\s*chi\s*minh|hanoi)\b/, "Vietnam"]`.
   Unambiguous tokens only (country + native + the two metro names; none collide with common US place
   names). Including `hanoi` also serves the Hanoi H3 sibling. Add a disambiguation test mirroring the
   Warsaw-IN / Victoria-BC ones.

## Adapter notes / new-scraper plan

**NEW `SaigonH3Adapter`** (static Cheerio; register in `htmlScrapersByUrl` keyed on `saigonhashers.com`).
Model on `dublin-hash.ts` (table iteration) + `phnom-penh-h3.ts` / Kaohsiung (per-run fail-loud,
placeholder strip, `maps.app.goo.gl` → `locationUrl`). **Single-surface design** (simpler than Phnom Penh):

1. **Fetch the Receding Hairline** (`fetchHTMLPage("https://saigonhashers.com/hareline")`). The site
   authors markdown that Grav-style renders a pipe table into a real `<table>`. Iterate `$("table tr")`;
   require a **numeric first cell** (run #) AND `cells.length >= 4` to reject the header row and any
   decorative rows. Columns in order: `numbers | Date | Name/Occasion | Hares | A-Site | On-On`.
   - `numbers` → `runNumber` (`Number.parseInt(cell, 10)`).
   - `Date` → ISO `YYYY-MM-DD` → `Date.UTC(y, m-1, d, 12)`. **No inference** (year is in the cell).
   - `Name/Occasion` → `title` ONLY if it's a real theme; if it equals the run-type `"Bus Trip/City Run"`
     (case-insensitive, allowing minor whitespace), leave `title` undefined.
   - `Hares` → strip `"Hares Needed!"` (and `TBC`/`TBA`/`N/A` if they appear) → undefined; else keep
     verbatim (incl. trailing `"& Co"`).
   - `A-Site` / `On-On` → read each cell's `<a href>`; a `maps.app.goo.gl` link → `locationUrl`
     (currently both columns are empty, but code for it).
2. **(Optional)** cross-check the home page "This Week's Run" for the current run — but `/hareline`
   already contains #1834 and is the superset, so a single-surface parse of `/hareline` is sufficient.

**startTime:** not per-event in the table → leave undefined (the kennel `scheduleTime` 1:30 PM displays
on the page). Do NOT invent per-event times.

**Coords:** none per-event (A-Site/On-On empty; prose Caravelle link is the bus departure, not the trail)
→ `latitude`/`longitude` undefined → merge geocodes the venue text / HCMC centroid. No default-pin trap.

**Fixture:** build the test fixture from the REAL DOM captured above (the `/hareline` `<table>` with its
`numbers | Date | Name/Occasion | Hares | A-Site | On-On` columns, a mix of `"Bus Trip/City Run"` +
`"Hares Needed!"` placeholder rows and real-theme/real-hare rows like #1837 / #1844 / #1856). Standard
`<table>`/`<thead>`/`<tbody>` markup — no exotic nesting.

**⚠️ Claude Code: verify before writing real code** (snippets above are illustrative; the live repo is
authority):
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma` for canonical `Kennel` field names (`hashCash`,
  `scheduleDayOfWeek`, `scheduleTime`, `logoUrl`, `foundedYear`, `latitude`/`longitude`) before using.
- Imports — `fetchHTMLPage` + date/extract helpers from `@/adapters/utils`; `safeFetch` from
  `@/adapters/safe-fetch` (NOT raw `fetch`) if you fetch a second surface.
- `kennelPagesStopReason` — set ONLY on genuine truncation (a fetch error / a full surface left
  unfetched); a non-empty string suppresses stale-event reconciliation. A clean single-page parse is NOT
  truncation.
- `title` — leave `undefined` for the `"Bus Trip/City Run"` run-type rows; `merge.ts` synthesizes
  `"Saigon H3 Trail #N"`. shortName "Saigon H3" is >4 chars so `friendlyKennelName` short-circuits cleanly
  (verify with `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Saigon H3","Saigon Hash House Harriers"))'`).
- **Single forward feed → mandatory fail-loud zero-row guard, PER-RUN:** if the table parses to 0 rows
  (or a numbered row fails to parse), push an `errors[]` entry so `scrape.ts` suppresses reconcile —
  don't let the scrape "succeed" with `events: []` (a brand-new source has a 0 baseline, so the
  zero-event health alert won't fire). Per the Kaohsiung/Manila/AH3-NZ lessons.

## Deep-dive checklist (nothing deferred)
- [x] logo (stable asset path; recommend self-host + magic-byte ext)  [x] foundedYear (1990, history page
  + 36th-birthday corroboration)  [x] socials (FB group; email noted, phones=PII not seeded)
  [x] schedule (Sunday 1:30 PM weekly)  [x] hashCash (VND300k bus / 150k city / 200k Vietnamese)
- [x] description  [x] source live-verified (SSR HTML via web_fetch; DNS Status 0; sitemap dedup 447)
  [⚠️] history depth (archive exists but `/runs`+`/reports` JS-rendered, not sandbox-reachable → assess at build)
- [x] coord sanity (no decimal coords; no default-pin trap)  [x] end times noted (none per-event)
  [x] kennelCode collision-checked (`saigon-h3` clear; bare "SH3" alias omitted)  [x] kennelCodes source
  guard set (`["saigon-h3"]`)

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is set** — the "Receding Hairline" ages out (runs recede off after they
  pass), so without it `reconcile.ts` would false-CANCEL aged-off runs. Correct for this source.
- **Single-surface fail-loud guard** (above) — per-run + total-zero.
- **Dates are ISO → trivial parse;** no chrono, no month alternation, no S5843/S5852 risk on dates. Keep
  any title/hare regexes simple (single `\s`, no `\s*`-adjacent-`.+`). Use `Number.parseInt(s,10)`,
  `s.replaceAll(...)`, `RegExp.exec()` for captures, no negated ternaries.
- **Self-host the logo** to `public/kennel-logos/saigon-h3.<ext>`; literal `<ext>` placeholder — confirm
  by magic bytes (declared `.png` at `/assets/images/logo_transparent.png`).
- **Codacy `eslint-plugin-security`** (not in local lint): prefer `Map.get()` / `for…of` over `obj[var]`
  indexing in the new adapter (e.g. a month map isn't needed here since dates are ISO, but the title
  blocklist / column-index access should use safe patterns).
- **First Vietnam country** — do NOT forget `COUNTRY_INFERENCE_RULES` (rule #5); without it
  `inferCountry()` returns `"USA"` for Vietnamese kennels (the ONH3-in-CI failure mode).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the
brief — no separate prompt needed._
