# Onboarding Handoff — Budapest H3 (Budapest, Hungary) — 2026-06-09

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/budapest-h3-20260609`.
> 2. Apply the **Ready-to-paste seed** below (kennel + aliases + TWO STATIC_SCHEDULE source rows).
>    Add the **Hungary COUNTRY + Budapest METRO** region as noted in **Adapter notes** (5 `region.ts`
>    edits — this is HashTracks' **first Hungary kennel**).
> 3. No adapter code to write — this is **config-only STATIC_SCHEDULE** (adapter already exists &
>    registered). Just the seed + region edits.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `new StaticScheduleAdapter().fetch(source)` for BOTH source rows via a throwaway
>    `npx tsx -e '…'` snippet. STATIC_SCHEDULE does no network I/O, so "verify" = confirm each row
>    generates weekly Sunday occurrences at the right time (summer row → 11:30, winter row → 10:30),
>    dates are UTC-noon, `kennelTag` resolves to `budapest-h3` with no unmatched. **DO NOT run
>    `npx prisma db seed` here** — seeding + prod scrape is the separate post-merge step (step 8).
> 5. No historical backfill (none available — see **Historical backfill**).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. **Self-host the logo** — the kennel logo URL could not be extracted from the sandbox (web_fetch
>    returns article text only; Chrome MCP auto-denied the brand-new domain with no user present).
>    Grab the logo from `https://www.budapesthashhouseharriers.org/` and self-host to
>    `public/kennel-logos/budapest-h3.<ext>` (confirm the real extension via `curl -sI` Content-Type
>    AND magic bytes — do NOT assume `.png`). If no usable logo is found, ship without and leave
>    `logoUrl` unset.
> 8. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 9. **Post-merge runbook (separate step, run after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge sometimes drops a follow-up
>      commit): `git log -1 -- prisma/seed-data/kennels.ts`, `… aliases.ts`, `… sources.ts`,
>      `git log -1 -- src/lib/region.ts`, and the logo file. Open a small recovery PR if anything's missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds the new kennel/aliases/sources + Hungary/Budapest regions)
>    - Trigger a scrape of BOTH Budapest source rows from `/admin/sources` to publish events to prod
>    - Spot-check `hashtracks.xyz/kennels/budapest-h3` for ~13 weekly-Sunday events over the next 90
>      days at 11:30 (summer) and the kennel metadata (Est. 1982, HUF 1,000, Batthyány tér).

## Summary
- Type: **full onboard** (new kennel + aliases + 2 sources + new Hungary region)
- Adapter: **STATIC_SCHEDULE** (config-only — adapter exists & registered; no feed to verify, generated)
- Effort estimate: **config-only** (seed + 5 region.ts edits + self-host logo; zero new adapter code)
- One-line: HashTracks' **first Hungary kennel** — a 1982-founded weekly-Sunday hash (~1,700 runs)
  whose only *public* dynamic surface is its live informational website; per-run details are
  membership-gated (private FB group), so a published-fixed-schedule STATIC_SCHEDULE is the right
  (and config-only) path. Mirrors the NOSE Hash summer/winter split.

## Dedup result
- Kennel in seed: **no** (grep `budapest`/`tah3`/`bh3` → only `tah3`=Tornado Alley/Wichita, `bh3`=Buffalo, `bh3-co`=Boulder)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read via Chrome MCP 2026-06-09 (**436 slugs**); no
  `budapest*`/`hungary*`/`magyar*` slug. `bh3` slug = **Boulder Hash House Harriers** (opened
  `/kennels/bh3`, rendered "Boulder Hash House Harriers / BLD / USA"), NOT Budapest. No collision.
- Decision: **full onboard** (first Hungary kennel)
- kennelCode: `budapest-h3` (collision check: bare `bh3` taken by Buffalo + `bh3-co` Boulder →
  used descriptive `budapest-h3`; grep `budapest-h3`/`bph3` in kennels.ts/aliases.ts = clean)

## ⚠️ Source pivot — the queued Meetup/website were BOTH dead (evidence for both sides)

The queue row (Rank 19) proposed `meetup.com/budapest-hash-house-harriers` + `budapesthash.hu`.
**Both are non-existent**, and the live source is a different, currently-active site:

**(a) Dead — original queued sources (negative-result evidence):**
- `meetup.com/budapest-hash-house-harriers` → **"Meetup | Group not found"** ("Sorry, the group
  you're looking for doesn't exist") — rendered via Chrome MCP 2026-06-09.
- `budapesthash.hu` → **NXDOMAIN** (`dns.google/resolve?name=budapesthash.hu&type=A` → `Status: 3`).
- Harrier Central `getEvents` (cityNames:"Budapest", real token via the HC API from hashruns.org
  page context) → **`status 200, count 0`** (no Budapest events on HC).
- `bph3.blogspot.com` (Budapest H3's old blog) → **dead since 2014** (newest post `published`
  `2014-06-16`, confirmed from the Blogger JSON feed).

**(b) Live — the replacement source (working sample):**
- `https://www.budapesthashhouseharriers.org/` → **live, fully server-rendered** (web_fetch returned
  the complete home page). Verbatim excerpts: *"Each Sunday we organise runs in Budapest or in the
  hills and villages around"*; *"We meet every Sunday at 11.30am but in winter we might bring this
  forward to 10.30am"*; *"You will also need Hungarian Forints 1,000 for the run fee"*; *"We usually
  meet at Batthyany Ter on the Buda side"*; on-after at *"the Ganz Bar, at Ganz utca 6"*; trail
  *"usually about 12km to 15km"*.
- Private FB group `facebook.com/groups/1014178591932185` (975 members, rendered via Chrome MCP)
  public **About** confirms the same current schedule verbatim: *"Every Sunday 1130-1155 at
  Batthyány Tér on Bem rakpart"*. A 975-member active private group + a live maintained website =
  current-activity evidence for a 1982-founded weekly hash.

## Live source verification  ✅ (STATIC_SCHEDULE — generated, no feed to fetch)
- Source: **STATIC_SCHEDULE** ×2 (summer + winter rows) — no external URL fetched; `url` is the
  kennel website as a human reference. Feed HEAD-check: **N/A** (no network I/O).
- Events seen: **generated** — weekly Sundays. **Source-count parity: N/A** (STATIC_SCHEDULE; the
  recently-active evidence above stands in for a feed sample, per Step 3).
- Sample (generated) events — next Sundays from 2026-06-09 (all summer → 11:30):
  1. 2026-06-14 (Sun) — "Budapest H3 Weekly Run" — 11:30 — Batthyány tér, Budapest
  2. 2026-06-21 (Sun) — "Budapest H3 Weekly Run" — 11:30 — Batthyány tér, Budapest
  3. 2026-06-28 (Sun) — "Budapest H3 Weekly Run" — 11:30 — Batthyány tér, Budapest
  4. 2026-07-05 (Sun) — "Budapest H3 Weekly Run" — 11:30 — Batthyány tér, Budapest
  5. 2026-07-12 (Sun) — "Budapest H3 Weekly Run" — 11:30 — Batthyány tér, Budapest
  (Winter row generates the Nov–Mar Sundays at 10:30.)
- History depth / pagination: **single rule, no history** (STATIC_SCHEDULE generates forward only).
- Coord sanity: **N/A** (no per-event coords; map pin falls back to the Budapest region centroid).
- End times: **none** (not published).
- Notes: per-run hares/locations live ONLY on the membership-gated private FB group + Google group
  (`groups.google.com/g/budapesthhh`) — not publicly scrapeable. The website is informational; its
  `/runs` and `/hareline` paths 404, `/wp-json` and `/sitemap.xml` empty. Hence STATIC_SCHEDULE, not
  an HTML scraper. This is the same pattern as NOSE Hash / Tidewater H3 / Rumson (Facebook-funnel
  kennels with a published fixed schedule).
- **Field-fill assertion table** (STATIC_SCHEDULE generates a fixed shape):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 5 / 5 | `config.defaultTitle` = "Budapest H3 Weekly Run" |
  | `startTime` | 5 / 5 | `config.startTime` ("11:30" summer / "10:30" winter) |
  | `endTime` | 0 / 5 | not published — leave undefined |
  | `location` (venue name) | 5 / 5 | `config.defaultLocation` = "Batthyány tér, Budapest" |
  | `locationStreet` | 0 / 5 | not in a generated schedule |
  | `locationUrl` (Maps) | 0 / 5 | not in a generated schedule |
  | `hares` | 0 / 5 | membership-gated — not public |
  | `cost` | 0 / 5 | kennel-level default `hashCash` = "HUF 1,000" (not per-event) |
  | `description` | 5 / 5 | `config.defaultDescription` |
  | `trailLengthText` | 0 / 5 | kennel runs ~12–15 km (noted in description, not per-event) |
  | `coords` (lat/lng) | 0 / 5 | none — fallback to Budapest region centroid |

## Kennel metadata (deep-dive complete)
- fullName: **The Budapest Hash House Harriers** / shortName: **Budapest H3** / region: **Budapest** / country: **Hungary**
- aliases: `["Budapest H3", "Budapest Hash", "Budapest HHH", "BPH3", "Budapest, Hungary"]`
  (omitted bare **"BH3"** — globally taken by Boston/Buffalo/Bristol/Boulder; omitted **"BHHH"** —
  taken by `bh4` Big Hump. `BPH3` matches the old blog `bph3.blogspot.com`; grep-confirmed free.)
- website: `https://www.budapesthashhouseharriers.org/` (source: live home page, DNS OK)
- facebook: `https://www.facebook.com/groups/1014178591932185` (private group — source: web search + Chrome MCP render)
- google group: `https://groups.google.com/g/budapesthhh` (membership-gated)
- instagram / twitter / discord: **none found**
- schedule: **Sunday, 11:30 (summer) / 10:30 (winter), weekly** — `scheduleDayOfWeek: "Sunday"`,
  `scheduleTime: "11:30"`, `scheduleFrequency: "Weekly"` (source: home page + FB group About, both DNS/render-verified)
- foundedYear: **1982** *(source: web-search aggregation incl. xpatloop.com 2019 feature + community
  descriptions — "founded in 1982 … almost 1,700 runs"; NOT stated on the DNS-verified home page →
  **confidence medium, Claude Code please confirm** against the kennel's About/history if reachable.
  ~1,700 runs ÷ weekly-with-gaps since 1982 is consistent.)*
- hashCash: **"HUF 1,000"** (source: home page — *"Hungarian Forints 1,000 for the run fee"*)
- dogFriendly: **unknown** (not stated) / walkersWelcome: **true** (home page: *"not competitive or a
  test of fitness or endurance and everyone is welcome to join us in exploring"* — walkers welcome)
- logoUrl: **⚠️ self-host to `public/kennel-logos/budapest-h3.<ext>`** — URL not extractable from the
  sandbox (web_fetch = text only; Chrome auto-denied the new domain). Grab from the home page; confirm
  extension via Content-Type + magic bytes. If none found, ship without.
- description: *"The Budapest chapter of the Hash House Harriers (est. 1982), a non-competitive
  running/hiking and social club. Runs every Sunday — usually meeting at Batthyány tér on the Buda
  side — followed by drinks at the Ganz Bar. Run fee HUF 1,000; trails ~12–15 km. Per-run details are
  posted to the members' Facebook group."* (sourced from the home page)
- lat/lng: Budapest ≈ 47.4979, 19.0402 (city centroid; meeting point Batthyány tér ≈ 47.5072, 19.0386)

## Historical backfill
- Available: **none worth scripting**. The old blog `bph3.blogspot.com` is **dead since 2014** and the
  ~1,700-run history isn't exposed on any fetchable structured source (FB group + Google group are
  membership-gated; the website carries no archive). No backfill.
- Plan: **none.**

## Ready-to-paste seed

```ts
// prisma/seed-data/kennels.ts — Kennel[] (append one object)
{
  kennelCode: "budapest-h3",
  shortName: "Budapest H3",
  fullName: "The Budapest Hash House Harriers",
  region: "Budapest",
  country: "Hungary",
  website: "https://www.budapesthashhouseharriers.org",
  facebookUrl: "https://www.facebook.com/groups/1014178591932185",
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "11:30",
  scheduleFrequency: "Weekly",
  foundedYear: 1982,            // ⚠️ medium-confidence — confirm vs kennel history if reachable
  hashCash: "HUF 1,000",
  walkersWelcome: true,
  description:
    "The Budapest chapter of the Hash House Harriers (est. 1982), a non-competitive running/hiking and social club. Runs every Sunday — usually meeting at Batthyány tér on the Buda side — followed by drinks at the Ganz Bar. Run fee HUF 1,000; trails ~12–15 km. Per-run details are posted to the members' Facebook group.",
  // logoUrl: "/kennel-logos/budapest-h3.<ext>"  // ← set after self-hosting (see directive step 7)
},
```

```ts
// prisma/seed-data/aliases.ts — Record<string, string[]> (keyed by KENNELCODE, not slug)
"budapest-h3": ["Budapest H3", "Budapest Hash", "Budapest HHH", "BPH3", "Budapest, Hungary"],
```

```ts
// prisma/seed-data/sources.ts — Source[] (append TWO rows; mirror the NOSE Hash summer/winter split)
{
  name: "Budapest H3 Static Schedule (Summer Sundays)",
  url: "https://www.budapesthashhouseharriers.org",
  type: "STATIC_SCHEDULE" as const,
  trustLevel: 3,
  scrapeFreq: "weekly",
  scrapeDays: 90,
  config: {
    kennelTag: "budapest-h3",
    rrule: "FREQ=WEEKLY;BYDAY=SU;BYMONTH=4,5,6,7,8,9,10",
    startTime: "11:30",
    defaultTitle: "Budapest H3 Weekly Run",
    defaultLocation: "Batthyány tér, Budapest",
    defaultDescription:
      "Summer schedule: Sundays 11:30am, usually meeting at Batthyány tér (Bem rakpart, Buda side). HUF 1,000 run fee. Per-run start point and hares are posted to the members' Facebook group: https://www.facebook.com/groups/1014178591932185",
  },
  kennelCodes: ["budapest-h3"],
},
{
  name: "Budapest H3 Static Schedule (Winter Sundays)",
  url: "https://www.budapesthashhouseharriers.org/#winter",
  type: "STATIC_SCHEDULE" as const,
  trustLevel: 3,
  scrapeFreq: "weekly",
  scrapeDays: 90,
  config: {
    kennelTag: "budapest-h3",
    rrule: "FREQ=WEEKLY;BYDAY=SU;BYMONTH=11,12,1,2,3",
    startTime: "10:30",
    defaultTitle: "Budapest H3 Weekly Run",
    defaultLocation: "Batthyány tér, Budapest",
    defaultDescription:
      "Winter schedule: Sundays 10:30am, usually meeting at Batthyány tér (Bem rakpart, Buda side). HUF 1,000 run fee. Per-run start point and hares are posted to the members' Facebook group: https://www.facebook.com/groups/1014178591932185",
  },
  kennelCodes: ["budapest-h3"],
},
```

> Note: distinct `url` values (`…/#winter` anchor) on the two rows keep them as separate Source
> records (mirrors the NOSE Hash `…#winter-wed` pattern). The summer/winter month boundary
> (Apr–Oct vs Nov–Mar) is the kennel's stated "winter we might bring this forward" — the exact
> switch isn't published, so this is the conventional approximation (same as NOSE/Tidewater). Each
> row's `BYMONTH` is disjoint, so no Sunday is double-generated.

## Adapter notes / new-scraper plan
**No adapter code.** STATIC_SCHEDULE is config-only — `StaticScheduleAdapter` already exists
(`src/adapters/static-schedule/adapter.ts`) and is registered in `src/adapters/registry.ts`. The two
source rows above are the entire implementation besides the region edits and logo.

### Region edits — Hungary is a NEW COUNTRY (5 `region.ts` edits + 0 seed.ts)
Country→metro country (no state province), so **no `seed.ts stateMetroLinks` entry** is needed
(mirrors Portugal/France/Spain — none appear in `seed.ts`). Mirror the **Portugal + Lisbon** pair
exactly. All 5 edits in `src/lib/region.ts`:

1. **`REGION_SEED_DATA`** — add the COUNTRY + METRO pair (place near the other European entries, e.g.
   after the Portugal block ~L1838). COUNTRY = darker `-200`, METRO = lighter `-100`, different pins:
   ```ts
   // ── Hungary ──
   {
     name: "Hungary",
     country: "Hungary",
     level: "COUNTRY",
     timezone: "Europe/Budapest",
     abbrev: "HU",
     colorClasses: "bg-orange-200 text-orange-800",
     pinColor: "#ea580c",
     centroidLat: 47.16,
     centroidLng: 19.5,
     aliases: ["HU", "Magyarország"],
   },
   {
     name: "Budapest",
     country: "Hungary",
     timezone: "Europe/Budapest",
     abbrev: "BUD",
     colorClasses: "bg-orange-100 text-orange-700",
     pinColor: "#f97316",
     centroidLat: 47.4979,
     centroidLng: 19.0402,
     aliases: ["Budapest, Hungary"],
   },
   ```
   (Orange picked to stay distinct from neighbors — Austria=teal, Portugal=green, Switzerland=red.
   If it clashes with an adjacent region on the map, swap to another unused-nearby hue.)
2. **`COUNTRY_INFERENCE_RULES`** (~L3400, near the Portugal rule) — add:
   ```ts
   [/\b(hungary|budapest|magyar)\b/, "Hungary"],
   ```
   (Without this, `inferCountry()` falls through to "USA" for any Budapest/Hungary text.)
3. **`STATE_GROUP_MAP`** (~L3614, after the Portugal entries) — add:
   ```ts
   // Hungary
   "Budapest": "Hungary",
   ```
4. **`COUNTRY_GROUP_MAP`** (~L3832, after `"Portugal": "Portugal"`) — add (metro "Budapest" resolves
   via STATE_GROUP_MAP first, so only the country-group key is reachable — mirror the Portugal comment):
   ```ts
   // Hungary — metro "Budapest" resolves via STATE_GROUP_MAP first, so only the
   // country-group key "Hungary" is reachable here (mirrors the Portugal precedent).
   "Hungary": "Hungary",
   ```
5. **`COUNTRY_CODE_TO_NAME`** (~L3945, after `PT: "Portugal"`) — add:
   ```ts
   HU: "Hungary",
   ```

**⚠️ Claude Code: verify before writing real code.** Snippets above are illustrative; the live repo
is authoritative. Before committing, confirm against current types/files:
- `Kennel` field names in `prisma/schema.prisma` — `walkersWelcome` (NOT `walkerFriendly`),
  `foundedYear`, `hashCash`, `scheduleDayOfWeek`/`scheduleTime`/`scheduleFrequency`, `facebookUrl`,
  `logoUrl`. Drop any field not on the model rather than guessing.
- `StaticScheduleConfig` (`src/adapters/static-schedule/adapter.ts`) — `kennelTag` (required),
  `rrule`, `startTime` ("HH:MM"), `defaultTitle`, `defaultLocation`, `defaultDescription`. The XOR
  validator requires exactly one of `rrule`|`lunar`. `kennelTag` = the kennelCode (`budapest-h3`),
  matching the NOSE/Rumson convention.
- `aliases.ts` is `Record<string, string[]>` keyed by **kennelCode** (`"budapest-h3": [...]`), NOT slug
  and NOT a `[{kennelCode, alias}]` array. `head -3 prisma/seed-data/aliases.ts` to confirm the shape.
- `region.ts` exact insertion points may have shifted — grep for `"Portugal": "Portugal"`,
  `PT: "Portugal"`, the Portugal `COUNTRY_INFERENCE_RULES` line, and the Portugal `REGION_SEED_DATA`
  block and place the Hungary edits alongside.

## Deep-dive checklist (nothing deferred)
- [x] logo (⚠️ self-host — URL not sandbox-extractable; grab + magic-byte the extension)
- [x] foundedYear (1982, medium-confidence — flagged to confirm)
- [x] socials (FB group + Google group; no public IG/Twitter)
- [x] schedule (weekly Sunday 11:30 summer / 10:30 winter — two STATIC_SCHEDULE rows)
- [x] hashCash (HUF 1,000)  [x] description  [x] source live-verified (website + FB group rendered; queued Meetup/`.hu` proven dead)
- [x] history depth/pagination assessed (none — blog dead 2014)  [x] coord sanity (N/A, generated)
- [x] end times noted (none)  [x] kennelCode collision-checked (`budapest-h3`, clean)
- [x] kennelCodes (source guard) set on both rows (`["budapest-h3"]`)
- [x] new-country region checklist (5 `region.ts` edits, 0 seed.ts — country→metro)

## Implementation gotchas (for Claude Code — repo knowledge)
- **STATIC_SCHEDULE does no reconciliation risk in the usual sense** but DOES generate forward
  events with no `upcomingOnly` knob — that's fine here (the rule itself only emits within the
  window; there's nothing to "age out" / false-cancel because there's no external feed).
- **`friendlyKennelName` is safe** — `shortName` "Budapest H3" is >4 chars so it short-circuits to
  itself; and STATIC_SCHEDULE sets an explicit `defaultTitle` anyway, so no synthesized-title risk.
- **New country → `COUNTRY_INFERENCE_RULES` is the edit that bites if omitted** (the ONH3/Kenya CI
  failure). Don't skip edit #2 — without it `inferCountry("Budapest")` returns "USA".
- **No trailing-zero numeric literals** in region seed (Sonar S6749): `centroidLng: 19.5` not `19.50`.
- **Region aliases 1–2 entries, name + native spelling only** — `Magyarország` for the country;
  `Budapest, Hungary` for the metro. No near-dup `"Budapest, HU"` forms (reviewer-flagged dup class).
- **Self-host the logo** into `public/kennel-logos/budapest-h3.<ext>`; confirm the extension by
  **magic bytes**, not Content-Type (CDNs mislabel — Rocketspark/Gamma served WebP as `image/png`).
- **Confirm `foundedYear` 1982** at build time if the kennel's history/About page is reachable from
  the local env (the sandbox couldn't load it past the home page). If unconfirmable, the value is
  medium-confidence secondary-source; keep it but the PR body should say so.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
