# Onboarding Handoff — Surrey H3 (Surrey, England) — 2026-08-06

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/surrey-h3-20260806`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). The **"Surrey" region
>    already exists** in `src/lib/region.ts` (used by `och3` Old Coulsdon) and is wired in
>    `STATE_GROUP_MAP` → **0 region.ts edits**.
> 3. Implement the adapter exactly as in **Adapter notes** (NEW bespoke HTML scraper — NOT
>    config-only; nothing in `registry.ts` handles this site).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md`
>    — call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: events non-empty +
>    upcoming, dates UTC-noon, `startTime` "11:00", `kennelTags` resolves to `surrey-h3` with no
>    unmatched, and a `rows.length === 0` fail-loud guard fires if the receding-hareline parse
>    yields nothing. **DO NOT run `npx prisma db seed` here.**
> 5. Apply the **Historical backfill** one-shot (worth it — ~224 structured rows back to Jan 2022).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge sometimes drops a follow-up
>      commit): `git log -1 -- src/adapters/html-scraper/surrey-hash.ts`,
>      `git log -1 -- scripts/backfill-surrey-h3-history.ts`,
>      `git log -1 -- scripts/data/surrey-h3-history.json`. Open a recovery PR if any is missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds the new kennel/alias/source)
>    - Run the backfill one-shot, then trigger a scrape from `/admin/sources`
>    - Spot-check `/kennels/surrey-h3` for the expected event count + sample dates
> Everything you need is in the sections that follow.

## Summary
- Type: **full onboard**
- Adapter: **HTML_SCRAPER** (NEW bespoke ~180–260 LoC adapter — no existing adapter fits this
  Elementor/Astra WordPress layout)
- Effort estimate: NEW ~180–260 LoC static-Cheerio adapter + tests, **plus** a frozen ~224-row
  backfill (script + committed JSON, H7 pattern). **0 region.ts edits** (Surrey region exists).
- One-line: First **Surrey (county-wide) Hash** on HashTracks — a large, ~50-year-old, weekly-Sunday
  English kennel (run #2637) with a clean self-hosted forward hareline + a 224-run structured archive.

## Dedup result
- Kennel in seed: **no** (`grep -in "surrey" prisma/seed-data/kennels.ts` → only `och3` *region* = "Surrey"; no Surrey Hash kennel)
- Source in seed: **no** (`grep -in "surreyhashhouseharriers" prisma/seed-data/sources.ts` → none)
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP,
  **491 slugs** (2026-08-06); no `surrey`/`surreyhash`/`sh3`(as-slug) kennel slug. (`och3` = Old
  Coulsdon Hash, a *different* Surrey-district kennel; distinct.)
- Pre-onboarding admin-event check: kennel not live, no `surrey-h3` events can exist yet (kennelCode brand-new).
- Decision: **full onboard**
- kennelCode: `surrey-h3` (collision check: **clear** — grep-clean in kennels.ts/aliases.ts.
  🔴 bare `sh3`/"SH3" is globally taken — kennelCodes `sh3-de`/`sh3-wa`/`sh3-au`/`sh3-kr`/`sh3-sg`
  + aliases on `summit`/`sh3-wa`/`salemh3` → **bare "SH3" alias OMITTED**; use city-based `surrey-h3`).

## Live source verification  ✅
- Source: HTML_SCRAPER — `https://surreyhashhouseharriers.com/` (self-hosted WordPress, Astra theme
  + Elementor 3.31.5 + All-in-One SEO). **DNS check: `surreyhashhouseharriers.com` → Status 0**
  (147.79.79.131 / 147.79.72.21, via `dns.google/resolve` from Chrome). Non-platform domain, resolved.
- **Two SSR surfaces, both `web_fetch`-able (no JS render needed — confirmed via `web_fetch` AND Chrome DOM):**
  - **Forward feed** = `/receding-hareline/` — Elementor 3-column `text-editor` grid (NO `<table>`;
    cells read in document order: `run# + date` → `hare(s)` → `location`). Dates are **year-bearing**
    (`"02 August 2026"`). Rows with a `¦` cell = hare/location TBD.
  - **Per-run detail** = `/trail-<runNo>-<dd>-<month>/` — Elementor `heading.default` `<h2>` labels
    with the value in the following widget: **`Trail no:`** (`"2637, 9 August 2026"` = run# + full
    date), **`Hare(s):`**, **`From:`** (venue + postcode), **`On on:`** + `Map link to Start`
    (`maps.app.goo.gl`), `What3Words` (`w3w.co`), `Map link to On On`, and prose Directions.
- Events seen: **12 forthcoming** on the receding hareline (#2636 02 Aug 2026 → #2647 18 Oct 2026),
  plus **224 past** on the archive table (see History). Next upcoming: **#2637 Sun 9 Aug 2026 11:00**.
  Current run #2637 → very active. **NOT a 0-upcoming case.**
- **Source-count parity**: platform publishes a rolling forward window (~12 forthcoming) + a 224-row
  archive; the adapter reads the full receding-hareline grid (all forthcoming) → parity N/A for
  "upcoming" (all forthcoming rows are reachable in one fetch, no pagination). Backfill reaches the
  full 224-row archive in one fetch.
- Sample events (VERBATIM from `/receding-hareline/` + `/trail-2637-…/`):
  1. **2637** — 9 August 2026 — hares **FRB & Atalanta** — from **Bookham Commons, Cobham, KT11 3JT** — On on: 90th-birthday picnic, Downside Common KT11 2NN — 11:00 (no theme title)
  2. **2636** — 2 August 2026 — hares **Tiller & Petal** — **Runnymede NT car park (NOT the Pleasure Ground)** — 11:00
  3. **2638** — 16 August 2026 — hare **Hash Flash** — **The Plough, Leigh, RH2 8NJ** — 11:00
  4. **2639** — 23 August 2026 — hares **Chastity Belt & 'Iskneesrbuggered** — **The Green, Chiddingfold** — 11:00
  5. **2640** — 30 August 2026 — hare **No Nookie** — **Jolly Farmer, Worplesdon** — 11:00
  🔴 **No theme/run titles anywhere** (leave `title` undefined → `merge.ts` synthesizes
  `"Surrey H3 Trail #N"`). 🔴 **No run numbers are "constructed"** — every run# above is verbatim
  from the source. Later receding-hareline rows (#2643, #2645, #2646) have `¦` for hare/location =
  genuinely TBD; leave those fields undefined, do NOT synthesize.
- History depth / pagination: **224 structured rows** on `/archive-runday-shags/` (a real single
  `<table>`, header `Issue no. | Date | Hare(s) | Location | On-on | Directions`), **rs2398 (2 Jan
  '22) → rs2616 (15 Mar '26)**, dates `DD Mon 'YY`. Pre-2022 issues are **PDFs in a Google Drive
  folder (off-site)** → out of scope. Single page, no pagination.
- Coord sanity: **no lat/lng on the forward grid**; detail pages carry a `maps.app.goo.gl` "Map link
  to Start" + a `w3w.co` What3Words + a venue **postcode** (e.g. `KT11 3JT`). No default/duplicate-pin
  trap (every venue is real). → geocode the postcode (preferred) or parse the Maps URL; fall back to
  the Surrey region centroid.
- End times: **none published** (Sunday 11:00 start only).
- Notes: fully SSR (no browserRender). Astra + Elementor; forward grid is `text-editor` cells (parse
  by document order), detail pages are `heading.default` label/value pairs, archive is a real
  `<table>`. See **source-platform-notes.md → "Elementor/Astra WordPress hash-club site …"** (appended).
- **Field-fill assertion table** (from the samples above):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 0 / 5 | No themes — leave undefined; `merge.ts` synthesizes `"Surrey H3 Trail #N"` |
  | `startTime` | 5 / 5 | Constant `"11:00"` (kennel default; hareline omits per-run time) |
  | `endTime` | 0 / 5 | Not published — accept absence |
  | `location` (venue name) | 5 / 5 | receding-hareline `location` cell; richer from detail `From:` |
  | `locationStreet` | 5 / 5 (detail) | detail `From:` carries venue + postcode |
  | `locationUrl` (Maps) | 5 / 5 (detail) | `Map link to Start` goo.gl on detail pages |
  | `hares` | 4 / 5 | receding-hareline `hare(s)` cell / detail `Hare(s):`; `¦` rows → undefined |
  | `cost` | 0 / 5 | kennel default `£2` (visitors); no per-event override seen |
  | `description` | 5 / 5 (detail) | detail `On on:` + Directions prose |
  | `trailLengthText` | 0 / 5 | not in feed (site says "4–5 miles" generic) |
  | `coords` (lat/lng) | 0 / 5 raw | geocode postcode from `From:` / parse Maps URL; else Surrey centroid |

## Kennel metadata (deep-dive complete)
- fullName / shortName / region / country: **Surrey Hash House Harriers** / **Surrey H3** / **Surrey** / **UK**
- aliases: `["Surrey Hash", "Surrey Hash House Harriers", "Surrey HHH"]`
  🔴 **bare "SH3" OMITTED** (global collision — see kennelCode note).
- website: `https://surreyhashhouseharriers.com/` (source: DNS-verified, Status 0)
- facebook / instagram / twitter / discord: **none found on the site → blank + flag.** Contact is
  email + a WhatsApp "Trails Line" only (source: `/contact/` — `OnSec@`, `harerazor@`, `GM@`,
  `hashcash@surreyhashhouseharriers.com`; WhatsApp 07484 134 245). Do NOT invent a FB/IG handle.
- schedule: **Sunday, 11:00, weekly** (source: every page footer "Hashing in Surrey every Sunday at
  11:00" + `/sh3-history/`). Single pattern → flat `scheduleDayOfWeek`/`scheduleTime` sufficient
  (no `scheduleRules` needed).
  ```ts
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "11:00 AM", // 🔴 12-hr in Kennel.scheduleTime; adapter RawEventData.startTime stays 24-hr "11:00"
  scheduleFrequency: "Weekly",
  ```
- foundedYear: **1975** (source: `/sh3-history/` — inaugural run "6pm on Monday 14 April 1975" by
  founder **Jim Raper**, ex-Singapore Hash; runs moved to Sunday 11am from run #2; page written
  "as we approach nearly 49 years" ≈ 1975).
- hashCash: **"£2 (visitors; first visit free)"** (source: `/` home — "visitor's fee of £2 … your
  first visit is free"). Membership is £12/quarter, £20 joint (source: `/contact/`) — that's subs,
  not per-run hash cash; keep `£2` as the kennel-level standard.
- dogFriendly: **unstated → leave undefined + flag** (dogs appear in run reports, e.g. "Spud the
  dog", but no stated policy).
- walkersWelcome: **true** (source: `/` — "non-competitive running, walking … people of all
  abilities … short cuts and loops").
- logoUrl: `https://surreyhashhouseharriers.com/wp-content/uploads/2023/06/cropped-SH3-Green-tree-logo-2023.jpg`
  (green-tree SH3 roundel; stable `wp-content` URL — not tokenized). ⚠️ **Self-host** to
  `public/kennel-logos/surrey-h3.<ext>` per convention; **confirm the real extension via `curl -sI`
  Content-Type + magic bytes** (looks like JPEG but verify — do NOT hardcode `.jpg`).
- description: `"Surrey Hash House Harriers — a non-competitive running and walking club founded in
  1975 by Jim Raper (ex-Singapore Hash). Every Sunday at 11:00 the pack follows a flour trail of
  4–5 miles around the Surrey countryside, with shortcuts and loops so all abilities finish together,
  then heads 'On On' to a local pub. Visitors welcome — £2, first visit free."`
- lat/lng: not set at kennel level (Surrey region centroid covers it); per-event geocode from postcode.

## Historical backfill
- Available: **~224 rows** — fields: run#, **date (year-bearing `DD Mon 'YY`)**, hares, start
  location, On-on pub (`/archive-runday-shags/` single `<table>`, rs2398 **2 Jan 2022** → rs2616
  **15 Mar 2026**). A handful of non-run rows exist (`rs n/a`, sub-letter `rs2568a`/`2513a`/`2504a`
  /`2435a`/`2416a` away/extra runs) — skip or keep as `eventLabel` A/B; drop the `rs n/a` "No trail" row.
- **Gap rs2617–rs2635** (Mar–Jul 2026): NOT on the archive table yet (they exist as individual
  `/rs<NNNN>/` report pages, labeled `Issue N / Date / Hare / Venue / On On`). The adapter's forward
  window only reaches #2636+, so to make history contiguous, **also capture rs2617–rs2635** by
  fetching those ~19 report pages (same label parse as detail pages) OR accept a ~19-run gap.
  Recommended: capture them → complete 2022→present (~243 rows total).
- Plan: **frozen `scripts/data/surrey-h3-history.json` + dumb loader `scripts/backfill-surrey-h3-history.ts`**
  (H7 pattern — commit the curated dataset, NOT the parser). Set source `config.upcomingOnly: true`
  so `reconcile.ts` never CANCELs the aged-out archive rows.

> **Frozen-dataset validation checklist (run before shipping):**
> 1. **PII scrub** — the run reports contain member hash-names (fine) but grep the archive rows for
>    `@` / phone patterns and redact any that leak (the report *bodies* carry a phone on the contact
>    page — do NOT include report bodies, only the archive `<table>` fields).
> 2. **Run-number sequence continuity** — weekly (~7-day) steps; flag any `(gap-days / step) < 4d`
>    or `> 20d` as a probable date misparse (year-pivot `'22`→2022 … `'26`→2026).
> 3. **Gap sanity** — event date within −1…+7d of a weekly Sunday cadence.
> 4. **Monotonicity** — run numbers must not decrease over time.
> 5. **Field bleed** — a `hares`/`location` value containing a stray `|` or a pub name = missed cell
>    boundary; the archive table is clean but the sub-letter rows share a trail link.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects). Region "Surrey" already exists; country "UK".
{
  kennelCode: "surrey-h3",
  shortName: "Surrey H3",
  fullName: "Surrey Hash House Harriers",
  region: "Surrey",
  country: "UK",
  website: "https://surreyhashhouseharriers.com",
  foundedYear: 1975,
  hashCash: "£2 (visitors; first visit free)",
  walkersWelcome: true,
  // dogFriendly: leave unset (unstated on site)
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "11:00 AM", // 12-hr for Kennel.scheduleTime; adapter startTime = "11:00"
  scheduleFrequency: "Weekly",
  logoUrl: "/kennel-logos/surrey-h3.<ext>", // ⚠️ self-host from wp-content JPG; confirm ext via magic bytes
  description:
    "Surrey Hash House Harriers — a non-competitive running and walking club founded in 1975 by Jim Raper (ex-Singapore Hash). Every Sunday at 11:00 the pack follows a flour trail of 4–5 miles around the Surrey countryside, with shortcuts and loops so all abilities finish together, then heads 'On On' to a local pub. Visitors welcome — £2, first visit free.",
},

// aliases.ts — Record<string, string[]> keyed by KENNELCODE (NOT slug). Bare "SH3" OMITTED.
"surrey-h3": ["Surrey Hash", "Surrey Hash House Harriers", "Surrey HHH"],

// sources.ts — Source[] (array of objects)
{
  name: "Surrey H3 Website Hareline",
  url: "https://surreyhashhouseharriers.com/receding-hareline/",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,
  scrapeFreq: "daily",
  scrapeDays: 120,               // narrow forward feed (~12 forthcoming); backfill handles history
  config: {
    upcomingOnly: true,          // 🔴 REQUIRED — rolling forward hareline ages out; reconcile would false-CANCEL
  },
  kennelCodes: ["surrey-h3"],    // source-kennel guard
},
```

## Adapter notes / new-scraper plan

**NEW bespoke static-Cheerio adapter `SurreyHashAdapter` (`src/adapters/html-scraper/surrey-hash.ts`).**
Not config-only — nothing in `registry.ts` handles this Elementor/Astra layout. Register in
`htmlScrapersByUrl` keyed on `surreyhashhouseharriers.com`. Model on: **`dublin-hash.ts`** (Cheerio
plumbing), **`burlington-hash.ts`** (label-field parse + atomic-bundle `null`/`undefined` semantics),
and the **Creek H3 / DFW detail-page-enrichment pattern** for the per-trail fetch.

**Parse plan:**
1. `fetchHTMLPage("https://surreyhashhouseharriers.com/receding-hareline/")` (from `@/adapters/utils`).
   The forward run list is an Elementor **`text-editor` 3-column grid** (NOT a `<table>`) — cells in
   document order are `run# + date`, `hare(s)`, `location`, repeating. 🔴 **Capture the exact cell
   selector from the live DOM at build** (verified live: cells are `.elementor-widget-text-editor`
   inside a column row; header cells read "Run no. / Date", "Hare(s)", "Location …"). Robust parse:
   walk the content region's text cells in order, detect a run row by a cell matching
   `/^\s*(\d{4})\s+(\d{1,2}\s+[A-Za-z]+\s+20\d\d)\s*$/` (run# + year-bearing date), then take the
   next two cells as hare + location. Treat a `¦`/empty cell as undefined (TBD), never synthesized.
2. For each forthcoming run that also has a `/trail-<runNo>-…/` detail page (the receding-hareline
   content **and** the header nav submenu both link them), fetch the detail page and enrich from the
   `heading.default` `<h2>` labels — value = the text of the widget following each label heading:
   - `Trail no:` → `"2637, 9 August 2026"` (run# + full date; cross-check vs the grid)
   - `Hare(s):` → hares
   - `From:` → `location` + `locationStreet` (venue + postcode)
   - `On on:` → part of `description`
   - `Map link to Start` (`maps.app.goo.gl`) → `locationUrl`; `What3Words` (`w3w.co`) supplementary
   Runs without a detail page (later `¦`-heavy rows) emit from the grid row only.
3. **Dates:** parse the year-bearing `"9 August 2026"` via `chronoParseDate` (from `@/adapters/utils`)
   → store **UTC noon**. `startTime` constant `"11:00"` (kennel default; hareline omits per-run time).
4. **Coords:** geocode the postcode from `From:` (preferred) or parse the `maps.app.goo.gl` URL; no
   default-pin trap. If neither resolves, fall back to the Surrey region centroid (do NOT emit a
   fabricated pin — leave coords undefined and let the region centroid render).
5. **Fail-loud zero guard (REQUIRED — single-page forward feed):** if the receding-hareline parse
   yields `rows.length === 0`, `errors.push("Surrey H3: 0 rows parsed from receding-hareline")` so
   `reconcile` is suppressed and the zero-event failure surfaces (this is a brand-new source whose
   health baseline is 0 — a silent `events: []` would never alarm).

**Backfill script** `scripts/backfill-surrey-h3-history.ts` (dumb loader over frozen
`scripts/data/surrey-h3-history.json`). Extract the ~224 rows from the `/archive-runday-shags/`
`<table>` (`Issue no. | Date | Hare(s) | Location | On-on | Directions`; date `DD Mon 'YY`, 2-digit
year pivot `'22`→2022 … `'26`→2026) + optionally the rs2617–rs2635 report pages, run the validation
checklist above, then commit the curated JSON (NOT the throwaway parser). Strict `date < today`;
partition against the adapter's forward window so re-runs are idempotent.

**⚠️ Claude Code: verify before writing real code.** Any snippet above is illustrative; the live
repo is the authority. Before writing the adapter, confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma` for canonical names.
- Imports — `fetchHTMLPage` + `chronoParseDate` from `@/adapters/utils`; `safeFetch` (NOT raw
  `fetch`) if any direct fetch is needed; geocode via `geocodeAddress` from `@/lib/geo`.
- `title` — leave `undefined` (no themes); `merge.ts` synthesizes `"Surrey H3 Trail #N"`.
  `friendlyKennelName("Surrey H3", "Surrey Hash House Harriers")` → shortName is >4 chars so it
  short-circuits to `"Surrey H3"` (clean) — no verbose-title risk, but run the check to confirm.
- `kennelPagesStopReason` — set ONLY on genuine truncation (a detail-page fetch erroring), never on
  a clean end; a non-empty string suppresses stale-event reconciliation.
- Capture the **exact live DOM** of `/receding-hareline/` (Elementor `text-editor` cells) and a
  `/trail-…/` page (`heading.default` `<h2>` label + following-widget value) for the test fixtures —
  do NOT hand-write a flat fixture; mirror the real Elementor structure (see source-platform-notes).

## Deep-dive checklist (nothing deferred)
- [x] logo (stable wp-content JPG; flag self-host + magic-byte ext)  [x] foundedYear (1975, cited)
  [x] socials (none on site → blank+flag)  [x] schedule (Sunday 11:00 weekly)  [x] hashCash (£2 visitors)
- [x] description  [x] source live-verified (web_fetch SSR + Chrome DOM; DNS Status 0)
  [x] history depth/pagination assessed (224-row archive table + gap noted)
- [x] coord sanity (no default-pin trap; geocode postcode)  [x] end times noted (none)
  [x] kennelCode collision-checked (surrey-h3 clear; SH3 omitted)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — the receding-hareline is a rolling forward window
  (old runs drop off); without it `reconcile.ts` false-`CANCEL`s aged-off events. The backfill rows
  are frozen history, so `upcomingOnly` also protects them.
- **Self-host the logo** into `public/kennel-logos/surrey-h3.<ext>`; **NEVER pre-fill the extension**
  — confirm via `curl -sI` Content-Type AND magic bytes (`\xff\xd8` JPEG / `\x89PNG` / `RIFF` WebP).
- **`friendlyKennelName` check** — shortName "Surrey H3" (9 chars) short-circuits to itself; synth
  title is `"Surrey H3 Trail #N"`. Confirm at build:
  `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Surrey H3","Surrey Hash House Harriers"));'`
- **Single-page forward feed → explicit `rows.length === 0` fail-loud guard** (see Adapter notes step 5).
- **Sonar S5852 / S5843** — parse the date with a loose `\d{1,2}\s+\w+\s+20\d\d` match + `chronoParseDate`
  validation rather than enumerating month names; keep the run-row regex simple (single `\s+`, no
  stacked optional groups). For the 2-digit archive year, a plain `Number.parseInt` + century pivot
  (not a regex alternation).
- **Rejecting upstream coords needs `dropCachedCoords: true`** if you ever emit then clear a pin;
  here you geocode fresh, so only set coords when the postcode/Maps URL resolves.
- **Tests:** `beforeEach(vi.restoreAllMocks)` for accurate per-`it()` fetch-call counts.
- **`Number.parseInt(s, 10)`** (not bare `parseInt`); `s.replaceAll(...)` (not `.replace(/g/)`) where
  no regex is needed; no negated ternaries (S3358).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is
the brief — no separate prompt needed._
