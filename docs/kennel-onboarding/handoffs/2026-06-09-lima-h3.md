# Onboarding Handoff — Lima H3 (Lima, Peru) — 2026-06-09

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/lima-h3-20260609`.
> 2. **🔴 LIVE-VERIFY THE SOURCE FIRST — this is mandatory and blocking for this handoff.** The
>    daily-research sandbox **could not reach `limahashash.blogspot.com` by ANY method** (Blogger
>    JSON feed `…/feeds/posts/default?alt=json` → empty body; HTML page → empty; `?m=1` mobile →
>    empty; `site:limahashash.blogspot.com` Google search → **0 indexed pages**; Chrome MCP domain
>    access denied; JSONP CSP-blocked). **The user (John) explicitly confirms the blog is live.**
>    Your local env can reach Blogspot, so before writing any adapter code:
>    `npx tsx -e 'import {fetchBloggerPosts} from "./src/adapters/blogger-api"; fetchBloggerPosts("https://limahashash.blogspot.com/", 12).then(p=>console.log(JSON.stringify(p.slice(0,5),null,2)))'`
>    Inspect the real posts and CONFIRM: (a) recency — newest post date (the kennel runs **every
>    other Saturday**, so "recently active" = newest run within ~4 weeks; if the newest post is months
>    old, STOP and report back rather than shipping a dormant source), (b) the title format + run-number
>    scheme, (c) the body field labels + date format. **The field structure in §Adapter notes is
>    INFERRED from a sibling site and MUST be validated against the real posts.**
> 3. Apply the **Ready-to-paste seed** (kennel + aliases + source). Add the **Peru COUNTRY + Lima
>    METRO** region (5 `region.ts` edits — HashTracks' **first Peru kennel / 3rd South America country**).
> 4. Implement the **NEW `LimaH3Adapter`** per **Adapter notes** (Blogger API body-parse; mirror
>    `brasilia-h3.ts` / `ofh3.ts`). Register in `htmlScrapersByUrl`.
> 5. **Live-verify the adapter** per `.claude/rules/live-verification.md` — `adapter.fetch(source)`
>    against the live blog; events non-empty + dates UTC-noon + `startTime` "HH:MM" + `kennelTag`
>    resolves to `lima-h3`. **DO NOT run `npx prisma db seed` here.**
> 6. **Historical backfill** — if the blog archive is deep (assess via `fetchBloggerPosts(url, 200)`),
>    freeze a curated `scripts/data/lima-h3-history.json` + dumb loader (H7/Brasília pattern). If the
>    archive is shallow/dormant, skip.
> 7. **Self-host the logo** (see §Kennel metadata) into `public/kennel-logos/lima-h3.<ext>` (magic-byte
>    the extension). If none found, ship without.
> 8. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 9. Commit + open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 10. **Post-merge runbook** (after merge): `git checkout main && git pull`; verify each expected file
>     landed (squash-merge can drop a follow-up commit — `git log -1 -- <file>` per file incl. the
>     backfill script + logo); `npx prisma db seed`; trigger a scrape from `/admin/sources`; spot-check
>     `hashtracks.xyz/kennels/lima-h3`.

## Summary
- Type: **full onboard** (new kennel + aliases + source + new Peru region)
- Adapter: **HTML_SCRAPER (NEW `LimaH3Adapter`, Blogger API body-parse)** — **NOT config-only** (no
  shared Blogger config adapter exists; each Blogspot kennel is bespoke ~180–240 LoC)
- Effort estimate: **new ~180–240 LoC adapter + tests** (mirror `brasilia-h3.ts`), + optional one-shot
  backfill, + 5 `region.ts` edits (new country)
- One-line: HashTracks' **first Peru kennel** (the only hash in Peru) — a real **live dynamic source**
  with per-run hares / themes / venues / hash cash, replacing today's earlier static-schedule pick.

> **🟢 Why this replaces the Budapest static-schedule handoff as today's primary target:** John's
> directive was to onboard **live dynamic sources** (real per-run data), not RRULE static schedules.
> Lima Hash publishes per-run announcements (date, hares, theme, venue, hash cash) to its blog — a
> genuine dynamic source. The Budapest H3 handoff (`2026-06-09-budapest-h3.md`) remains a valid
> config-only STATIC_SCHEDULE option but is **deprioritized** per this preference.

## Dedup result
- Kennel in seed: **no** (grep `lima`/`lh3-pe`/`lima-h3` in seed-data → none)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read via Chrome MCP 2026-06-09 (**436 slugs**); no
  `lima*`/`peru*`/`lhh` slug. (`lh3`-family hits — `clh3,colh3,dlh3,glh3,palh3,slh3,stlh3` — are all
  unrelated US/UK kennels.)
- Decision: **full onboard** (first Peru kennel)
- kennelCode: `lima-h3` (collision check: bare `lh3` taken by **London H3**, `lh3-fl`/`lh3-ks` taken
  → used descriptive `lima-h3`; grep in seed-data = clean)

## ⚠️ Live source verification  🔴 UNVERIFIABLE FROM SANDBOX — Claude Code MUST confirm at build
- Source: **HTML_SCRAPER (Blogger API)** — `https://limahashash.blogspot.com/`
- **Sandbox reach: FAILED by every method** (see directive step 2 for the full list). **User asserts
  the blog is live** — trust that, but Claude Code's local `fetchBloggerPosts` run is the real
  verification gate. Do NOT ship if the live archive turns out dormant (newest run months old).
- Events seen: **0 from sandbox (unreachable)** — no sample could be captured here.
- **Source-count parity / recency: N/A from sandbox** — Claude Code captures the real newest-run date
  + last 3–5 runs as the live-verify sample, and applies the *recently-active* rule (every-other-Sat →
  newest within ~4 weeks).
- History depth / pagination: **unknown** — assess via `fetchBloggerPosts(url, 200)`. The public
  `/feeds/` endpoint caps ~150; use the **keyed Blogger API** (`fetchBloggerPosts`, `GOOGLE_CALENDAR_API_KEY`)
  for the full archive (per the Brasília lesson).
- Coord sanity / end times: unknown — confirm at build (Lima posts may carry a venue name only, no
  lat/lng → merge geocodes the venue text).
- Notes: a **sibling** Lima hash exists — **Lima Extra Miércoles** (`limahash.com`, est. Jan 2022, last
  Wednesday of the month). Its self-hosted WordPress REST API (`limahash.com/wp-json/wp/v2/posts`) IS
  reachable + working from the sandbox, **but currently holds only ONE real hash post** (the Feb 2024
  "Earl Grey & Inca Gold" run) + a "Hello world!" stub → dormant. It is a **separate kennel**, not this
  target — **queue it separately** as a future target (see §Sibling sweep). Do NOT bundle.

## Adapter notes / new-scraper plan  🔴 STRUCTURE INFERRED — VALIDATE AGAINST REAL POSTS FIRST

**No config-only path** — onboard via a **new `LimaH3Adapter`** mirroring `src/adapters/html-scraper/brasilia-h3.ts`
(Blogspot body-parse) and `ofh3.ts` (single-kennel trail posts). Use `fetchBloggerPosts(url, maxResults)`
from `src/adapters/blogger-api.ts` (Blogger API v3, keyed — bypasses cloud-IP 403s + the ~150-entry
public-feed cap). Register the adapter by URL pattern in `htmlScrapersByUrl`.

**🔴 The field structure below is INFERRED from the sibling `limahash.com` Feb-2024 post** (same Lima
hashing community, likely the same scribe/format). The real `limahashash.blogspot.com` posts MUST be
inspected first (directive step 2) and the parser built from what they actually contain. The sibling
post's labeled English structure was:

```
🍻 ... <Theme> Hash! 🍻
When: On the morrow (Saturday), precisely at the hour of 16:00 ... Pack away at 16:30.
Where: ... Houlihan's, Miraflores ...
Trail: A to B ...
Theme: Pisco & Tea Party – A Clash of Cultures!
Hares: Callboy and Half Twin
Hash Cash: 10 soles.
```

So expect **English labeled fields**: `When:` (weekday + `HH:MM`), `Where:` (venue/area), `Trail:`
(A-to-B), `Theme:`, `Hares:` (CSV), `Hash Cash:` (e.g. "10 soles"). Likely **no per-run lat/lng** (venue
name only → merge geocodes). Suggested parse shape (mirror `brasilia-h3.ts` — confirm names against live types):
- Flatten body with `stripHtmlTags(html, "\n")`; detect a run post by the presence of the `When:`/`Hares:`
  label block (skip socials / non-runs); pair with a `events.length===0 && posts.length>0` **fail-loud guard**.
- **Run number:** UNKNOWN scheme — Lima posts may title as a theme with no number (like the sibling post)
  or carry `#N`. If there's a clean `#\d+`, use `extractHashRunNumber` (`src/adapters/utils.ts`); else leave
  `runNumber` undefined and `title` undefined so `merge.ts` synthesizes `"Lima H3 Trail #N"` — **but** if
  the post has a real theme, set `title` to the theme (like Bali Hash 2). Decide from the real posts.
- **Date:** parse the `When:` weekday + the post's `published` date as the anchor; Lima posts may be year-less
  ("Saturday, 16:00") → infer the year as the candidate ∈ {pubYear−1,pubYear,pubYear+1} **closest to
  `post.published`** (the Brasília rule — handles recaps published after the run). Store UTC noon.
- **startTime** "HH:MM" from `When:` (e.g. "16:00"); **hares** CSV from `Hares:`; **location** from `Where:`;
  **cost** from `Hash Cash:` (e.g. "10 soles") — but `cost` is per-event; the kennel default goes on
  `Kennel.hashCash`. **theme** → `title` or `description`.
- **Spanish/English drift:** the community is bilingual (the FB groups are Spanish) — the blog may mix
  ES/EN labels (`Cuándo:`/`Liebres:`) or Spanish month names. Sample across the archive (not just the
  latest) and build an EN+ES label/month map if needed (the Asunción WP.com lesson).

**⚠️ Claude Code: verify before writing real code.** Snippets are illustrative; the live repo + real
posts are authoritative. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags: string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma` for canonical names.
- Imports — `fetchBloggerPosts` from `@/adapters/blogger-api`; `safeFetch` (NOT raw `fetch`);
  date/extract helpers from `@/adapters/utils` (`extractHashRunNumber`, `chronoParseDate`); `stripHtmlTags`.
- `title` — leave undefined when no clean theme; `merge.ts` synthesizes `"Lima H3 Trail #N"`. Never let a
  hare name or a labeled-field fragment become the title.
- `kennelPagesStopReason` — set ONLY on genuine truncation; a 400/empty past the last Blogger page is a
  clean end (leave null).
- `friendlyKennelName("Lima H3", "Lima Hash House Harriers")` — `shortName` "Lima H3" is >4 chars → short-
  circuits cleanly; no garbled-title risk.

### Region edits — Peru is a NEW COUNTRY (5 `region.ts` edits, 0 seed.ts)
Country→metro (no state province), so **no `seed.ts stateMetroLinks` entry** (mirrors Paraguay/Brazil —
neither is in `seed.ts`). Mirror the **Paraguay + Asunción** / **Brazil + Brasília** South-America pairs.
All 5 edits in `src/lib/region.ts`:

1. **`REGION_SEED_DATA`** — add after the Brazil block (~L3160). COUNTRY darker `-200`, METRO lighter `-100`,
   different pins. Rose palette keeps Peru distinct from Paraguay (purple) + Brazil (emerald):
   ```ts
   // ── South America — Peru (country → metro, no state-province intermediate,
   // mirroring Paraguay/Brazil). Rose palette keeps it distinct within South America. ──
   {
     name: "Peru",
     country: "Peru",
     level: "COUNTRY",
     timezone: "America/Lima",
     abbrev: "PE",
     colorClasses: "bg-rose-200 text-rose-800",
     pinColor: "#e11d48",
     centroidLat: -9.19,
     centroidLng: -75,
     aliases: ["PE", "Perú"],
   },
   {
     name: "Lima",
     country: "Peru",
     timezone: "America/Lima",
     abbrev: "LIM",
     colorClasses: "bg-rose-100 text-rose-700",
     pinColor: "#f43f5e",
     centroidLat: -12.046,
     centroidLng: -77.043,
     aliases: ["Lima, Peru"],
   },
   ```
2. **`COUNTRY_INFERENCE_RULES`** (~L3417, after the Brazil rule) — add (without it `inferCountry` falls to "USA"):
   ```ts
   [/\b(peru|lima|miraflores)\b|perú/, "Peru"],  // bare `perú` (no \b — non-ASCII; \b is ASCII-only)
   ```
3. **`STATE_GROUP_MAP`** (~L3714, after `"Brasília": "Brazil"`) — add:
   ```ts
   // Peru — country → metro (no state-province intermediate)
   "Lima": "Peru",
   ```
4. **`COUNTRY_GROUP_MAP`** (~L3900, after the Brazil entries) — add BOTH:
   ```ts
   // Peru — both the country-level region and its metro map to "Peru"
   "Peru": "Peru",
   "Lima": "Peru",
   ```
5. **`COUNTRY_CODE_TO_NAME`** (~L3963, after `BR: "Brazil"`) — add:
   ```ts
   PE: "Peru",
   ```
- **No trailing-zero numeric literals** (Sonar S6749): `centroidLng: -75` not `-75.0`.

## Kennel metadata (deep-dive — some fields need build-time confirmation)
- fullName: **Lima Hash House Harriers** / shortName: **Lima H3** / region: **Lima** / country: **Peru**
- aliases: `["Lima Hash", "Lima HHH", "Lima H3", "LH3 Peru", "Lima, Peru"]` (omit bare **"LH3"** — taken by
  London H3 `lh3` + `lh3-fl`/`lh3-ks`; grep each proposed alias against `aliases.ts` before committing)
- website / source: `https://limahashash.blogspot.com/` (per John — live; ⚠️ sandbox-unreachable, build-verify)
- facebook: `https://www.facebook.com/groups/14448170326/` (Lima Hash main group — source: web search)
- schedule: **every other Saturday, ~16:00 (Saturday afternoons)** — `scheduleDayOfWeek: "Saturday"`,
  `scheduleTime: "16:00"`, `scheduleFrequency: "Biweekly"` (source: limahash.com + gotothehash; the
  every-other-Saturday cadence — confirm the exact start time from live posts' `When:` line)
- foundedYear: **unverified** — leave null + flag (the only hash in Peru; founding not on a fetchable source;
  check the blog archive's milestone posts / gotothehash South America contacts at build)
- hashCash: **"10 soles"** (source: sibling limahash.com Feb-2024 run post; confirm against live Lima Hash posts)
- dogFriendly / walkersWelcome: unknown — confirm from the blog (HHH are generally walkers-welcome)
- logoUrl: **⚠️ self-host** to `public/kennel-logos/lima-h3.<ext>` — grab from the blog header at build;
  magic-byte the extension. If none, ship without.
- description: *"Lima Hash House Harriers — the only Hash House Harriers kennel in Peru. A non-competitive
  social running group that meets every other Saturday afternoon in and around Lima, following a flour
  trail set by the hares, with beer stops and an On-In afterwards."* (sourced from limahash.com About +
  gotothehash; trim/confirm at build)
- lat/lng: Lima ≈ -12.046, -77.043 (city centroid; many runs start in Miraflores ≈ -12.121, -77.030)

> 🔴 **Sibling sweep — Lima Extra Miércoles is a SEPARATE kennel, queue it, don't bundle.** Lima has TWO
> hashes: (1) **Lima Hash** (this target — every other Saturday, blog `limahashash.blogspot.com`, FB group
> `14448170326`); (2) **Lima Extra Miércoles** (last Wednesday monthly, est. Jan 2022, site `limahash.com`,
> FB group `limahash`). They share members but are distinct kennels. Extra Miércoles' `limahash.com/wp-json`
> WP REST API works but is **dormant** (1 hash post, Feb 2024) and its linked Meetup
> (`lima-extra-miercoles-hash-house-harriers`) is **dead ("Group not found")** — so it's a *future*
> source-add target, gated on the WP blog resuming. Added to the queue's Leads, NOT onboarded here.

## Historical backfill
- Available: **UNKNOWN from sandbox** — assess at build via `fetchBloggerPosts("https://limahashash.blogspot.com/", 200)`.
- Plan: if the archive is deep (many dated run posts), **freeze a curated `scripts/data/lima-h3-history.json`
  + dumb loader** (`scripts/backfill-lima-h3-history.ts`) per the H7/Brasília/Asunción pattern (throwaway
  parser → frozen dataset → loader delegating to `runBackfillScript`; PII-scrub the JSON). If shallow, skip.
- Set `config.upcomingOnly: true` ONLY if the blog's display window ages out (recent-posts-only listing);
  a full Blogger archive (returns the same old posts every scrape) does NOT need it — decide from the real
  archive depth (Brasília used it; a full archive like most WP.com does not).

## Ready-to-paste seed

```ts
// prisma/seed-data/kennels.ts — Kennel[] (append one object)
{
  kennelCode: "lima-h3",
  shortName: "Lima H3",
  fullName: "Lima Hash House Harriers",
  region: "Lima",
  country: "Peru",
  website: "https://limahashash.blogspot.com/",
  facebookUrl: "https://www.facebook.com/groups/14448170326/",
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "16:00",            // confirm from live posts' When: line
  scheduleFrequency: "Biweekly",    // every other Saturday
  hashCash: "10 soles",             // confirm against live Lima Hash posts
  description:
    "Lima Hash House Harriers — the only Hash House Harriers kennel in Peru. A non-competitive social running group that meets every other Saturday afternoon in and around Lima, following a flour trail set by the hares, with beer stops and an On-In afterwards.",
  // foundedYear: leave unset until confirmed
  // logoUrl: "/kennel-logos/lima-h3.<ext>"  // set after self-hosting
},
```

```ts
// prisma/seed-data/aliases.ts — Record<string, string[]> (keyed by KENNELCODE, not slug)
"lima-h3": ["Lima Hash", "Lima HHH", "Lima H3", "LH3 Peru", "Lima, Peru"],
```

```ts
// prisma/seed-data/sources.ts — Source[] (append one row)
{
  name: "Lima Hash Blogspot",
  url: "https://limahashash.blogspot.com/",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,                 // Blogger HTML scrape
  scrapeFreq: "daily",
  scrapeDays: 365,               // archive-bearing blog
  config: {
    // upcomingOnly: true,       // ← add ONLY if the blog is recent-posts-only (decide from real archive)
  },
  kennelCodes: ["lima-h3"],
},
```

## Deep-dive checklist (nothing deferred that's verifiable from the sandbox)
- [ ] **source live-verified** — 🔴 **deferred to Claude Code (sandbox could not reach the blog; user-confirmed live)** — THE gating item
- [x] dedup (sitemap 436, no lima/peru slug)  [x] kennelCode collision (`lima-h3`, clean)  [x] kennelCodes guard set
- [x] new-country region checklist (5 `region.ts` edits, rose, 0 seed.ts)
- [x] sibling sweep (Lima Extra Miércoles = separate kennel, queued not bundled)
- [~] schedule (biweekly Sat ~16:00 — confirm start time from live posts)  [~] hashCash ("10 soles" — confirm)
- [ ] foundedYear (unverified — check blog milestones at build)  [ ] logo (self-host at build)
- [~] history depth / backfill (assess via fetchBloggerPosts at build)  [~] coord/end-time (confirm at build)

## Implementation gotchas (for Claude Code)
- **🔴 The whole adapter hinges on the live posts — inspect them FIRST (directive step 2).** Everything in
  §Adapter notes is inferred from a sibling site. If the real blog differs (different labels, Spanish fields,
  numbered titles, recap-only posts), build to what's actually there.
- **Blogger public `/feeds/` caps ~150 entries → use `fetchBloggerPosts` (keyed Blogger API v3)** for the full
  archive (Brasília lesson). The sandbox got EMPTY from the public feed for this blog specifically — your local
  keyed call should succeed; if it ALSO returns empty/`[]`, the blog may be private/deleted → report back, do
  not ship a no-event source (the fail-loud guard + a `posts.length===0` check must surface this).
- **Year-less dates → infer the year CLOSEST to `post.published`** (not naive +1) — recaps publish after the
  run (Brasília `brasilia-h3.ts`). Validate each candidate via `Date.UTC` round-trip; store UTC noon.
- **Bilingual content** — the Lima community is Spanish-speaking; build an EN+ES month/label map if the posts
  mix languages (Asunción WP.com recipe in `source-platform-notes.md`).
- **New country → `COUNTRY_INFERENCE_RULES` is the edit that bites if omitted** (the ONH3/Kenya CI failure) —
  don't skip region edit #2.
- **Faithfully store source errors** (dup run numbers, typos) — never renumber to satisfy a linter (Brasília).
- **Self-host the logo** (magic-byte the extension, not Content-Type).
- Sonar: `Number.parseInt(s,10)`, `s.replaceAll(...)`, no negated ternaries, `RegExp.exec` for capture-heavy
  use; keep date/label regexes simple (S5852/S5843) — match loose + validate with `chronoParseDate`.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
