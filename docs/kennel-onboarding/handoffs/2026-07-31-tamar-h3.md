# Onboarding Handoff — Tamar Valley H3 (Tavistock / Plymouth, Devon, England) — 2026-07-31

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/tamar-h3-20260731`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **Full onboard.**
>    Region needs **0 `region.ts` edits** — reuse the existing **Plymouth** METRO (`region.ts:1608`).
> 3. Implement the **new `TamarValleyH3Adapter`** exactly as in **Adapter notes** (bespoke HTML
>    scraper of the homepage; NOT a config-only path — see effort estimate).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md`
>    — call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` snippet or a small script
>    under `scripts/`. Resolve every item flagged `⚠️ Claude Code must confirm` below. **Capture
>    the REAL homepage DOM** with `curl -s https://www.tvh3.co.uk/ | head -300` and build the test
>    fixture from that verbatim markup (the sandbox `web_fetch` returned rendered text, not raw
>    HTML — the fixture MUST mirror the real DOM). Validate: events non-empty + upcoming, dates
>    UTC-noon, `startTime` "19:00", `kennelTag` resolves to `tamar-h3` with no unmatched.
>    **DO NOT run `npx prisma db seed` here.**
> 5. Backfill: **none** (see Historical backfill — the archive is weekly PDF hash mags, prose).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (adapter, adapter test, registry edit, seed
>      edits): `git log -1 -- <path>` for each. Recover any dropped file with a small follow-up PR.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds new kennel/alias/source)
>    - Trigger a scrape from `/admin/sources` to publish events to prod
>    - Spot-check `hashtracks.xyz/kennels/tamar-valley-h3` for the next run (Hash #~2367+, Monday).

## Summary
- Type: **full onboard**
- Adapter: **HTML_SCRAPER** — **NEW bespoke adapter needed** (`TamarValleyH3Adapter`). Not
  config-only: no existing adapter matches `tvh3.co.uk`, and the homepage is a bespoke
  MS-Word/FrontPage-exported static page with a single label/value "next run" block, not a
  repeating event list that `GenericHtmlAdapter` can drive.
- Effort estimate: **new ~150–220 LoC adapter + tests** (single-block label/value parse; simpler
  than the recent Blogger adapters — no pagination, no JSON feed).
- One-line: First **Tamar Valley / Tavistock** kennel — a large, very active weekly-Monday Dartmoor
  hash (founded 1981, run **#2367**) with a live, recently-updated self-hosted site whose homepage
  publishes the next run in full detail. Continues the productive non-HC Devon pass (surfaced while
  chasing City-of-Exeter's sibling leads Ashburton/Otter Valley).

## Dedup result
- Kennel in seed: **no** (grep `prisma/seed-data/*.ts` for `tamar` / `tvh3.co.uk` — only match is
  `titsh3`'s description text mentioning the "Plymouth/Tamar-valley border", a different kennel).
- Source in seed: **no** (`tvh3.co.uk` absent from `sources.ts`).
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP,
  **491 slugs**, no `tamar`/`tavistock`/`tvh3`/`teign`/`devon`/`dartmoor`/`plymouth`/`cornwall`
  fragment (also confirms the pending Devon handoffs — Teign Valley, Devon Lunatics, City of Exeter,
  Devonport — are not yet shipped).
- Decision: **full onboard.**
- kennelCode: `tamar-h3` (collision check: **bare `tvh3` intentionally avoided** — it is reserved
  by the pending **Teign Valley H3** handoff `handoffs/2026-07-25-tvh3.md` (`kennelCode: tvh3`).
  Two distinct Devon kennels both abbreviate "TVH3"; `tamar-h3` is grep-clean in `kennels.ts` +
  `aliases.ts`. See collision note in Step 5.)

## Live source verification  ✅ (HTML listing verified in-sandbox; feed = the homepage itself)
- Source: **HTML_SCRAPER** — `https://www.tvh3.co.uk/` (homepage `index.html`). DNS A-record
  **Status 0** (`dns.google/resolve?name=tvh3.co.uk` → `83.223.106.15`), non-platform domain check
  passed. Content-Type `text/html`, SSR (fetchable in-sandbox).
- Events seen: **1 upcoming** (the homepage "next run" block) + a full-year **forward schedule**
  (Hash Planner page) confirming weekly-Monday cadence with hares assigned through Dec 2026.
- **Source-count parity:** **N/A** — the homepage is a rolling *single-next-run* announcement, not
  a paginated feed. The site is unambiguously live and active: homepage banner reads *"Website
  updated on Tuesday 28th July 2026"* (3 days before research) and the next run is a genuine
  **upcoming** event (Aug 3 > research date Jul 31), so the Step 3 recently-active/0-upcoming rule
  does not even apply. Weekly cadence independently corroborated by the Hash Planner + Hash Stats
  (member totals up to 1886 hashes; ~2367 runs since 1981 ≈ weekly).
- Sample events (VERBATIM from the live homepage, 2026-07-31):
  1. **Hash No 2367** — **Date: 3rd August 2026** (Monday) — Hares: **Gannet & Scrotey** — Start:
     **Four Winds Car Park** (what3words `///strain.indicate.obstruct`) — On Down: **Peter Tavy Inn,
     Peter Tavy** (`///magic.pulsing.detriment`) — 19:00 — £2.00.
  - Forward schedule (Hash Planner page, dates + hares only, no run#/venue): Aug 3 Gannet & Scrotey,
    Aug 10 Butt Loose & Dodo, Aug 17 Dodo (Red Dress Run), Aug 24 Scupper Sucker & Pist 'N' Broke,
    Aug 31 Slush & Sprout; Sep 7 Cod Piece & Soggy; … all **Mondays**.
  > 🔴 **Run number IS present and verbatim** ("Hash No: 2367") — the adapter parses it from the
  > homepage. There is **no theme/event title** (leave `title` undefined → merge synthesizes
  > "Tamar Valley H3 Trail #N"). Do not invent a theme title.
  > 🔴 **`NextRun.htm` is a STALE 2020 SNAPSHOT — do NOT scrape it.** `https://www.tvh3.co.uk/Pages/NextRun.htm`
  > still shows "Hash No 2060 / 27th January" with a Feb-2020 forward table. The LIVE next run lives
  > on the homepage (`index.html`). Target the homepage only.
- History depth / pagination: **single page** (homepage = one next run). Separate **Hash Mag
  Archive** = weekly PDF magazines **2012–2022** (~500 PDFs, prose recaps; archive stops mid-2022).
  Not a structured feed. See Historical backfill.
- Coord sanity: **no lat/lng on the source** — venues are named + given as **what3words**
  (`///word.word.word`), not coordinates. Adapter must NOT fabricate coords; geocode the venue name
  or fall back to the kennel/Plymouth centroid. (No default-pin corruption risk — there are no pins.)
- End times: **none** (start time only).
- Notes: Static HTML exported from MS Word / FrontPage / Excel (`meta-Generator: Microsoft Word 9`
  on the homepage; Excel on the Hash Planner/Stats pages). No JS render needed — plain
  `fetchHTMLPage`. **Time changed 7:30pm → 7:00pm**: the live homepage says **"runs start at
  7.00pm"** (the stale NextRun.htm's "7.30pm" is 2020). Use **19:00**. See the new
  `source-platform-notes.md` → "Legacy MS-Office static HTML" section appended by this run.
- **Field-fill assertion table** (from the single homepage sample #2367; mark every cell):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 0 / 1 | No theme on homepage → leave undefined; `merge.ts` synthesizes "Tamar Valley H3 Trail #N" (shortName >4 → clean) |
  | `startTime` | 1 / 1 | "19:00" — parse the "runs start at 7.00pm" banner, else default "19:00" (weekly-Monday standard) |
  | `endTime` | 0 / 1 | none — accept absence |
  | `location` (venue name) | 1 / 1 | "Four Winds Car Park" — text after `Start:` label |
  | `locationStreet` | 0 / 1 | not in feed (what3words only) — leave undefined |
  | `locationUrl` (Maps) | 0 / 1 | not in feed (a what3words link, not a Maps URL) — leave undefined; do NOT synth |
  | `hares` | 1 / 1 | "Gannet & Scrotey" — text after `Hares:` label |
  | `cost` | 1 / 1 | kennel default "£2" (homepage "The cost is £2.00"); set only if it differs per-run |
  | `description` | 1 / 1 | On Down venue ("On Down: Peter Tavy Inn, Peter Tavy") → optional description |
  | `trailLengthText` | 0 / 1 | not in feed |
  | `coords` (lat/lng) | 0 / 1 | what3words only → geocode venue name or fall back to Plymouth centroid; never fabricate |

## Kennel metadata (deep-dive complete)
- fullName: **Tamar Valley Hash House Harriers** / shortName: **Tamar Valley H3** /
  region: **Plymouth** (existing METRO; Tavistock ≈ 15 mi N of Plymouth, West Devon) / country: **UK**
- aliases: `["Tamar Valley H3", "Tamar Valley Hash House Harriers", "Tamar Valley HHH"]`
  — 🔴 **bare "TVH3" OMITTED** (reserved by the pending Teign Valley H3 handoff; a global-namespace
  alias collision — see Step 5).
- website: **https://www.tvh3.co.uk/** (DNS Status 0) — source: homepage
- facebook: **https://www.facebook.com/profile.php?id=100051557537843** (source: homepage nav "Facebook" link)
- instagram / twitter / discord: **none found** → leave blank
- schedule: **Monday, 7:00 PM, Weekly** — sources: homepage ("TVH3 runs start at 7.00pm"), Hash
  Planner (all run dates are Mondays), Hash History ("non-competitive **Monday** madness")
  - `scheduleDayOfWeek: "Monday"`, `scheduleTime: "7:00 PM"` (12-hr Kennel field), `scheduleFrequency: "Weekly"`
- foundedYear: **1981** — source: `tvh3.co.uk/Pages/Hash Info/HASH HISTORY.htm` ("TVH3's first Hash
  was in 1981 … founder members Chris Laurence-King 'Bloodnock', Sara Laurence-King 'Shortcut' and
  Simon Trehane 'Trehanrehan'")
- hashCash: **"£2"** — sources: homepage ("The cost is £2.00") + Hash History ("Hash Cash of £2.00
  is expected from everyone — hashers, walkers and virgins … First time visitors and children are
  not charged"). (Self-validated: single £, two-digit, no doubled prefix/typo.)
- dogFriendly: **true** — source: Hash History ("Keep your dogs on leads and away from other
  runners' legs" — dogs welcome on lead)
- walkersWelcome: **true** — source: Hash History ("£2.00 … from everyone — hashers, **walkers** and
  virgins")
- logoUrl: **⚠️ none confirmed — Claude Code to locate + self-host.** The homepage has a masthead
  image but no stable logo URL was confirmed in-sandbox. Do NOT invent one. At build, inspect the
  homepage `<img>` tags (`curl -s https://www.tvh3.co.uk/ | grep -oiE 'src="[^"]*\.(png|gif|jpe?g)"'`),
  download a suitable masthead into `public/kennel-logos/tamar-h3.<ext>`, and confirm the real
  extension via magic bytes (`file public/kennel-logos/tamar-h3.*`). If none is suitable, ship
  without a logo (leave `logoUrl` unset).
- description: "Tamar Valley Hash House Harriers (est. 1981) — a weekly Monday-evening (7pm) Hash
  House Harriers kennel based in Tavistock, running across Dartmoor and the Devon/Cornwall Tamar
  Valley. £2 hash cash; walkers and (leashed) dogs welcome. Annual Red Dress Run and Brown Gin Run."
- lat/lng: **~50.5486, -4.1425** (Tavistock town centre — the club's base per Hash History "Living
  in Tavistock"; approximate — runs roam Dartmoor/Tamar Valley). Optional; the Plymouth centroid is
  a fine fallback.

## Historical backfill
- Available: **none worth scripting.** The **Hash Mag Archive** (`tvh3.co.uk/Pages/Hash Mag Archive.htm`)
  is ~500 weekly PDF magazines **2012–2022** (prose recaps, per-week PDFs; archive stops mid-2022).
  No structured date/run#/hare fields without per-PDF extraction of free-form prose — high effort,
  low structured value. The **Hash Planner** page holds a forward-year table (dates + hares, no run
  numbers/venues) — not history.
- Plan: **not worth it — no backfill script.** `config.upcomingOnly: true` is REQUIRED (the homepage
  shows only the single next run; without it `reconcile.ts` would false-CANCEL last week's event
  every scrape). The daily scrape adds each new week's run as the homepage updates (weekly).

## Ready-to-paste seed

> 🔴 `aliases.ts` is `Record<string, string[]>` (slug/kennelCode key → string[]). Confirmed by
> `head prisma/seed-data/aliases.ts`. Key the entry by **kennelCode `tamar-h3`** (the alias map is
> looked up against kennelCode in `prisma/seed.ts`).

```ts
// kennels.ts — Kennel[] (array of objects). Add near the other UK/Devon rows (e.g. after titsh3 @ line ~1276).
{ kennelCode: "tamar-h3", shortName: "Tamar Valley H3", fullName: "Tamar Valley Hash House Harriers",
  region: "Plymouth", country: "UK",
  website: "https://www.tvh3.co.uk/",
  facebookUrl: "https://www.facebook.com/profile.php?id=100051557537843",
  scheduleDayOfWeek: "Monday", scheduleTime: "7:00 PM", scheduleFrequency: "Weekly", // 12-hr Kennel field
  scheduleNotes: "Weekly Monday 7pm trail from Tavistock across Dartmoor and the Devon/Cornwall Tamar Valley.",
  foundedYear: 1981, // tvh3.co.uk Hash History: "TVH3's first Hash was in 1981"
  hashCash: "£2",    // homepage + Hash History: "Hash Cash of £2.00 … from everyone"
  dogFriendly: true, walkersWelcome: true,
  description: "Tamar Valley Hash House Harriers (est. 1981) — a weekly Monday-evening (7pm) Hash House Harriers kennel based in Tavistock, running across Dartmoor and the Devon/Cornwall Tamar Valley. £2 hash cash; walkers and (leashed) dogs welcome. Annual Red Dress Run and Brown Gin Run.",
  // logoUrl: self-host to public/kennel-logos/tamar-h3.<ext> once located (see metadata) — else omit.
  latitude: 50.5486, longitude: -4.1425 }, // Tavistock (approx; club base)

// aliases.ts — Record<string, string[]>, keyed by kennelCode. Bare "TVH3" OMITTED (Teign Valley collision).
"tamar-h3": ["Tamar Valley H3", "Tamar Valley Hash House Harriers", "Tamar Valley HHH"],

// sources.ts — Source[] (array of objects).
{ name: "Tamar Valley H3 Website", url: "https://www.tvh3.co.uk/", type: "HTML_SCRAPER" as const,
  trustLevel: 5, scrapeFreq: "daily", scrapeDays: 90,
  config: { upcomingOnly: true },
  kennelCodes: ["tamar-h3"] },
```

## Adapter notes / new-scraper plan

**New adapter:** `src/adapters/html-scraper/tamar-valley-h3.ts` → `TamarValleyH3Adapter`.
**Registry entry** (add to `htmlScraperEntries` in `src/adapters/registry.ts`, ~line 171+, and
import the class):
```ts
{ pattern: /tvh3\.co\.uk/i, name: "TamarValleyH3Adapter", factory: () => new TamarValleyH3Adapter() },
```

**Shape (ILLUSTRATIVE — verify against current types):**
- Fetch the homepage via `fetchHTMLPage(source.url ?? "https://www.tvh3.co.uk/")`
  (`@/adapters/utils`, uses `safeFetch` under the hood — do NOT call raw `fetch`).
- `cheerio.load(html)`; read the page text (`$("body").text()` or a targeted container) and parse
  the single next-run block by its labels. The relevant lines (verbatim today):
  - `Hash No:` → **2367** (run number) → `extractHashRunNumber` (`@/adapters/utils`) or a
    `/Hash\s*No:?\s*(\d{3,5})/i` capture.
  - `Date:` → `3rd August 2026` → normalize ordinal then `chronoParseDate(text, "en-GB")`
    (`@/adapters/utils`, already ordinal-aware; returns `"YYYY-MM-DD"`) → store **UTC noon**.
  - `Start:` → venue name (`Four Winds Car Park`), strip the trailing ` - what3words - ///…` tail.
  - `On Down:` → down-down venue (`Peter Tavy Inn, Peter Tavy`) → `description`.
  - `Hares:` → `Gannet & Scrotey` → `hares`.
  - Cost/time banner: `TVH3 runs start at 7.00pm - The cost is £2.00` → `startTime` "19:00" (parse
    `(\d{1,2})(?:[.:](\d{2}))?\s*pm`), `cost` "£2" (kennel default; only emit per-event if differs).
- `kennelTags: ["tamar-h3"]` (🔴 field is `kennelTags: string[]`, NOT `kennelTag`).
- `title` left **undefined** (merge synthesizes; shortName >4 → clean — see gotcha).
- No coords on source → leave lat/lng undefined (geocoded downstream / Plymouth centroid). Never fabricate.
- **Honor `options.days`** via `buildDateWindow(options?.days ?? 90)`; keep only the current/next run
  (it's a single event, but filter defensively).
- **Fail-loud single-block guard:** if the "Hash No"/"Date" block isn't found (markup drift), push an
  error (`errors.push("Tamar Valley homepage: no next-run block found")`) so `scrape.ts` suppresses
  stale-event reconciliation instead of silently succeeding with `events: []`. (New-source baseline
  is 0, so the zero-event health alert won't fire on its own — see AH3-NZ retro Gap D.)

**Reference adapters to mirror:** `src/adapters/html-scraper/burlington-hash.ts` (labeled-field
extraction with terminator helpers + S5852/S5843-safe regex splitting) and
`src/adapters/html-scraper/dublin-hash.ts` (`fetchHTMLPage` + Cheerio). Model the label/value parse
on Burlington; model the fetch plumbing on Dublin.

**Test fixture:** 🔴 build it from **real `curl -s https://www.tvh3.co.uk/` output** (the sandbox
`web_fetch` returned rendered text, not raw HTML). The page is MS-Word-exported — the label/value
lines may be wrapped in `<p class="MsoNormal">` / `<b>`/`<span>` runs with `&nbsp;` and mixed
casing; the fixture MUST mirror that real DOM or tests pass then live-verify fails.

**⚠️ Claude Code: verify before writing real code.** Any snippet above is illustrative; the live
repo is authoritative. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma` for canonical `Kennel`/`RawEventData` names.
- Imports — `fetchHTMLPage`, `chronoParseDate`, `extractHashRunNumber`, `buildDateWindow` from
  `@/adapters/utils`; `safeFetch` is used inside `fetchHTMLPage` (don't call raw `fetch`).
- `kennelPagesStopReason` — leave null on a clean single-page parse; set only on a genuine
  fetch/HTTP error (a non-empty string suppresses stale-event reconciliation).
- `title` — leave undefined; never let a hare name or venue become the title.
- Date parse — `chronoParseDate("3rd August 2026", "en-GB")` returns `"2026-08-03"` (unit-tested in
  `utils.test.ts`); if you strip the weekday/ordinal first, keep en-GB day-first.

## Deep-dive checklist (nothing deferred)
- [x] logo (⚠️ locate + self-host at build; do not invent)  [x] foundedYear (1981)  [x] socials (FB; IG/X/Discord none)  [x] schedule (Mon 7pm weekly)  [x] hashCash (£2)
- [x] description  [x] source live-verified (homepage SSR, DNS Status 0)  [x] history depth assessed (PDF mags 2012–2022 → no backfill)
- [x] coord sanity (no coords on source — what3words; geocode/centroid)  [x] end times noted (none)  [x] kennelCode collision-checked (`tamar-h3`; TVH3 avoided)  [x] kennelCodes source guard set (`["tamar-h3"]`)
- [x] sibling sweep: the source feeds **one** kennel (Tamar Valley H3). Ashburton H3 (FB/Strava/dead
  site — swh3.info) and Otter Valley H3 (no confirmed dynamic source) remain **separate** leads, not
  fed by this source — carried forward, not queued.

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — the homepage shows only the single next run; it
  "ages out" weekly, so without this `reconcile.ts` false-CANCELs last week's event on the next scrape.
- **Single-block adapter needs an explicit fail-loud guard** (see Adapter notes) — a markup drift
  otherwise "succeeds" with `events: []` and the zero-event alert never fires (new-source baseline 0).
- **`friendlyKennelName("Tamar Valley H3", "Tamar Valley Hash House Harriers")`** — shortName is
  >4 chars, so merge short-circuits to it → synthesized title "Tamar Valley H3 Trail #2367" (clean).
  Quick confirm at build: `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Tamar Valley H3","Tamar Valley Hash House Harriers"));'`
- **http/https:** `tvh3.co.uk` serves https (DNS + web_fetch confirmed) — no S5332 http-only hotspots expected.
- **Sonar S5852/S5843:** keep the date/time/run-number regexes simple and split (one concern each);
  let `chronoParseDate` do month-name validation rather than enumerating months.
- **Self-host the logo** into `public/kennel-logos/tamar-h3.<ext>` (literal `<ext>` until confirmed
  via Content-Type + magic bytes) — do NOT hardcode `.png`.

---
_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
