# Onboarding Handoff — Poznan H3 (Poznań, Poland) — 2026-07-11

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/ph3-pl-20260711`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source) **and the 3 `region.ts`
>    edits** in **Region seeding** — Poland COUNTRY is seeded (Warsaw METRO exists) but the
>    **Poznan** METRO is not.
> 3. Configure the adapter — **pure config-only HARRIER_CENTRAL** (no new adapter code); mirror the
>    **Bandung H3** HC source row (`prisma/seed-data/sources.ts:7463`) — the closest backfill-carrying
>    HC sibling.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: dates UTC-noon-safe,
>    `startTime` "HH:MM", `kennelTag` resolves to `ph3-pl` with no unmatched. **⚠️ Expect 0 events** —
>    at research time HC's future-only `getEvents` returned **0 events** and the global-runs future
>    feed had none either. This is a **recently-active** monthly kennel (last run #13 on 2026-06-13,
>    ~28 days ago on a 28-day cadence), NOT a dead one — the next run appears on the first scrape after
>    the kennel posts it. **DO NOT run `npx prisma db seed` here.**
> 5. **Recommended: the Historical backfill one-shot (3 HC runs; see below).** Without it the kennel
>    page is empty until the next run posts (currently 0 future events). Keep `config.upcomingOnly: true`.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- <path>` for the seed rows, the 3
>      `region.ts` edits, the backfill script + frozen JSON, and the self-hosted logo).
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod, reverting unrelated prod config drift — barbados #2471/#2463).
>    - Run the backfill: `BACKFILL_APPLY=1 npx tsx scripts/backfill-ph3-pl-history.ts`.
>    - Trigger a scrape from `/admin/sources` to publish events to prod.
>    - Spot-check `/kennels/poznan-h3` for the 3 backfilled runs + the new Poznan region.

## Summary
- Type: **full onboard** (first hashtracks kennel in Poznań; Poland's 2nd metro after Warsaw)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `src/adapters/registry.ts` & feed verified live)
- Effort estimate: **config-only source** + **3 `region.ts` edits** (Poznan METRO under existing Poland COUNTRY) + a **recommended 3-run backfill script**. No new adapter code.
- One-line: Adds Poznan H3 — the Poznań Hash House Harriers — via a live, recently-active Harrier Central kennel (monthly Saturday afternoon; last run #13 on 2026-06-13). Poland's 2nd metro after Warsaw.

## Dedup result
- Kennel in seed: **no** (`grep -in 'ph3-pl|poznan' prisma/seed-data/` → nothing)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-11, **485 slugs**; **zero** `pozna`/`poland`/`polska` slug. The only Poland-region live slug is `warsaw-h3` (Warsaw METRO). ⚠️ The bare `ph3` slug DOES exist — I opened `/kennels/ph3` and it renders **"Petaling Hash House Harriers" (KL, Malaysia)**, a DISTINCT kennel (`ph3-my`); NOT Poznan. (Also `bmph3`/`gph3`/`stumph3`/`suph3` matched the loose filter — all unrelated.)
- Decision: **full onboard**
- kennelCode: **`ph3-pl`** (collision check: bare `ph3` is taken — `ph3-my` [Petaling] shortName "PH3" → slug `ph3`, plus `pattaya-h3` and `ph3-atl` [Pinelake] carry "PH3" aliases → **suffixed `-pl`**; `grep -in 'ph3-pl' prisma/seed-data/` returns nothing → clear). Matches HC `KennelSlug` "PH3-PL". slug → `poznan-h3` (`toSlug("Poznan H3")`; ASCII shortName → clean slug, no explicit `slug:` needed).

## Live source verification  ✅ (recently-active — 0 upcoming, monthly cadence)
- Source: **HARRIER_CENTRAL** — `harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, `publicKennelId: b193f2ad-a2b2-4f48-b8be-b7145423faaf` (HC `KennelShortName` "PH3", `KennelSlug` "PH3-PL", `KennelName` "Poznan H3", `KennelContinent` "Europe"). Verified live 2026-07-11 by (a) the adapter's `getEvents` filter equivalent (`global-runs?isFuture=1` → **0** Poznan rows) AND (b) sweeping `hashruns.org/api/global-runs?isFuture=0` in dated windows 2024→2026 (same underlying HC data the backfill reads).
- Events seen: **0 via the future feed** (no upcoming) + **3 total in the HC past-runs feed** (2026-04-18 → 2026-06-13). Every window returned `totalMatchingEvents === runs.length` (no truncation); windows before 2026-04 returned **0** Poznan rows → the kennel **joined HC ~April 2026**, so only these 3 runs (#11–#13) are on the feed.
- **Source-count parity:** N/A (future feed shows **0 upcoming**). Applied the Step 3 **recently-active** rule instead: last run **#13 on 2026-06-13 (Sat)** is **~28 days ago** on a **28-day cadence** (#11 → #12 → #13 all exactly 28 days apart) = ~1× interval, and the kennel ran in **Apr, May AND Jun 2026** (3 of the last 3 months) → recently-active, onboard normally with a recent-history backfill. (Today 2026-07-11 is 28 days after #13 — the next run is due about now and will appear on the first scrape once posted.)
- **Recently-active evidence (VERBATIM cadence — proves live, not stale):**
  - **#13** — name `"*un #13"` — **2026-06-13 (Sat) 14:00** — "Park Izabeli i Jarogniewa Drwęskich w Poznaniu"
  - **#12** — name `"PZNH3 "` — **2026-05-16 (Sat) 14:00** — "Piwna Stopa" (brewery just south of Poznań Garbary) — real hare (from description) "German DP"
  - **#11** — name `"POZH3 *UN #11"` — **2026-04-18 (Sat) 14:00** — "U GOSI Food and Pub" (Dworcowa 9, near Poznań train station) — hares "Go Speed Racist"
  - Cadence: monthly, Saturday 14:00 (Europe/Warsaw, = 12:00Z summer / UTC+2), exact 28-day spacing.
- Sample events (VERBATIM from the HC `global-runs?isFuture=0` feed — the `EventName` values are garbled placeholders, see title note):
  1. **#11** — `EventName` "POZH3 *UN #11" — 2026-04-18 (Sat) 14:00 — location "U GOSI Food and Pub" — hares **"Go Speed Racist"** (real) — coords 52.40658/16.91425 — HC price 5/7 *(EventDescription says "Hash cash: 20 zł")*
  2. **#12** — `EventName` "PZNH3 " — 2026-05-16 (Sat) 14:00 — location "Piwna Stopa" — 🔴 **`Hares` field is location-bleed** ("Start point: Piwna Stopa, Brewery just south of Poznan Garbary…") → **drop hares** (the real hare "German DP" only appears in `EventDescription`, which the HC adapter doesn't parse) — coords 52.41021/16.93637 — HC price 7/7
  3. **#13** — `EventName` "*un #13" — 2026-06-13 (Sat) 14:00 — location "Park Izabeli i Jarogniewa Drwęskich w Poznaniu" — 🔴 **`Hares` field == the location string** (bleed) → **drop hares** — **no coords** (→ region centroid) — HC price 7/7
- History depth / pagination: **3 events total in the HC feed** (2026-04-18 → 2026-06-13), all captured. Run numbers are shallow (#11–#13) and **HC coverage starts at the kennel's HC-join (~April 2026)** — runs #1–#10 (est. mid-2025 onward) are **not** archived on this source (no separate blog/Meetup/Sheet archive found — see backfill note). Single feed, date-windowed via `minEventDate`/`maxEventDate`; use `scripts/lib/hc-global-runs.ts` (mirror `backfill-bandung-h3-history.ts`).
- Coord sanity: **clean** — #11/#12 pins inside the Poznań bbox (lat 52.406…52.410, lng 16.914…16.936); **#13 has no coords** (renders at the Poznan region centroid, correct). No repeated default pin. Apply a **Poznań bbox scrub** in the backfill (drop lat outside 52.30…52.55 / lng outside 16.70…17.15) as belt-and-suspenders — nothing in this sample would be dropped.
- End times: none (HC exposes start only).
- Notes: **tz is clean** — HC `KennelIANATimezone` = `Europe/Warsaw` (correct for Poland; **no** HC-quirk override needed, unlike Belgrade/KRASH where `Europe/Warsaw` was wrong). Confirmed by `EventStartDatetimeGmt` (14:00 local = 12:00Z = UTC+2 summer). Config-only; no browserRender/auth. HC `global-runs` carries **no currency field** — the numeric price fields (5/7, 7/7) are unreliable and **contradict** the EventDescription, which explicitly states **"Hash cash: 20 zł"** on #11 and #12 → trust the description (kennel-level `hashCash: "20 zł"`).
- **Field-fill assertion table** (from the 3 swept rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 0 / 3 (garbled placeholders) | `EventName`s are "POZH3 *UN #11" / "PZNH3 " / "*un #13" — kennel-code + run-# noise, NOT themes → route all 3 through `staleTitleAliases` so `defaultTitle:"Poznan H3"` synthesizes "Poznan H3 #N". |
  | `startTime` | 3 / 3 | From `EventStartDatetime` — all `14:00`. |
  | `endTime` | 0 / 3 | Not in feed — accept absence. |
  | `location` (venue name) | 3 / 3 | `LocationOneLineDesc` (verbatim venue strings). |
  | `locationStreet` | 0 / 3 | Not populated — leave undefined (full addresses live in `EventDescription`, unparsed). |
  | `locationUrl` (Maps) | 0 / 3 | Not in feed (a `maps.app.goo.gl` link appears in #12's description only) — coords carry the pin. |
  | `hares` | 1 / 3 | `Hares` field; **#12 & #13 are location-bleed → drop** (undefined). #11 real ("Go Speed Racist"). |
  | `cost` | 0 / 3 usable | HC numeric price fields (5/7, 7/7) unreliable — leave per-event `cost` unset; kennel-level `hashCash` "20 zł" from `EventDescription`. |
  | `description` | (present in HC) | Live HC adapter does **not** emit `description`; **omit in the backfill too** (Bandung/Rio precedent) so a backfilled row fingerprints identically to a live scrape. |
  | `trailLengthText` | 0 / 3 | Not in feed. |
  | `coords` (lat/lng) | 2 / 3 | `Latitude`/`Longitude` clean (#13 blank → region centroid); Poznań bbox-scrub in backfill (no outliers found). |

## Kennel metadata (deep-dive complete)
- fullName: **Poznan Hash House Harriers** / shortName: **Poznan H3** / region: **Poznan** / country: **Poland**
  - (HC `KennelName` = "Poznan H3"; `KennelShortName` = "PH3"; `KennelSlug` = "PH3-PL". Source: `hashruns.org/api/global-runs` 2026-07-11.) Kept the ASCII shortName "Poznan H3" (matches HC) → clean slug `poznan-h3`; the native spelling "Poznań H3" is an alias.
- aliases: `["Poznan H3", "Poznań H3", "Poznan Hash", "Poznan Hash House Harriers", "PZNH3", "POZH3"]` — **all grep-clean** against `aliases.ts` + `kennels.ts` (no global collision). 🔴 **bare "PH3" OMITTED** (collides with `ph3-my` Petaling / `pattaya-h3` / `ph3-atl` Pinelake).
- website: **none found** — no dedicated Poznan H3 site surfaced (WebSearch 2026-07-11 returned only Warsaw H3 / Petaling / generic H3 chapters; `thehashhouse.org` Poland list 500'd). ⚠️ blank + flag. The HC feed is the only confirmed live presence.
- facebook / instagram / twitter / discord: **none found** → omit + flag. (Runs are posted via Harrier Central; no kennel-owned social surfaced.)
- schedule: **monthly, Saturday 14:00** — evidenced by the 3 HC runs (all Saturday 14:00, exact 28-day spacing; #12 description: "Beer Hour 13:00 / On-Out (ish) 14:00"). Flat fields (single monthly pattern — not `scheduleRules`):
  ```ts
  // scheduleDayOfWeek/scheduleTime are the fallback; per-run times flow from the HC feed.
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "2:00 PM", // 12-hr per kennels.ts convention (HC startTime stays 24-hr "14:00")
  scheduleFrequency: "Monthly",
  ```
  (source: 3 HC runs, 2026-04-18 → 2026-06-13.)
- foundedYear: **unverifiable** → blank + flag. Latest run is #13 (2026-06-13); a strict monthly cadence would place #1 ~mid-2025, but the early cadence is unknown and **no primary source confirms a founding year**. Do NOT assert one. (Milestone-post scan N/A — no archive/blog exists to scan.)
- hashCash: **"20 zł"** (PLN) — **verified from `EventDescription`** on #11 AND #12 ("Hash cash: 20 zł"). 🔴 The HC numeric price fields (5/7, 7/7) disagree and are unreliable (bare numbers, no currency) → trust the description. (source: HC `EventDescription`, runs #11/#12.)
- dogFriendly: **true** — #12 description: "Hound friendly" (source: HC `EventDescription` #12).
- walkersWelcome: unknown → omit (unverified).
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/PH3-PL_20260514230922.avif`** → ⚠️ **self-host** to `public/kennel-logos/ph3-pl.<ext>` (HC blob; confirm the extension via **magic bytes**, NOT the `.avif` name — HC blobs are often WebP/AVIF/PNG regardless of the URL suffix). Reference `/kennel-logos/ph3-pl.<ext>`.
- lat/lng: **52.4064 / 16.9252** (Poznań city centre — matches the new Poznan METRO centroid below).

## Region seeding — 3 `region.ts` edits (new **Poznan** METRO under existing Poland COUNTRY)
> 🟢 **Poland COUNTRY is already seeded** (`region.ts:2010`, `Europe/Warsaw`, indigo palette) with the **Warsaw** METRO. Poznan is a **new metro under an existing country** → the reduced edit set. Mirror the **Warsaw** METRO record exactly for palette/timezone. Per the Poland/Hungary precedent, the metro resolves via `STATE_GROUP_MAP` first, so **`COUNTRY_GROUP_MAP` needs NO edit** (see the comment at `region.ts:4491`).

1. **`REGION_SEED_DATA`** — add the METRO record immediately after the Warsaw block (`region.ts:2032`):
   ```ts
   {
     name: "Poznan",
     country: "Poland",
     timezone: "Europe/Warsaw",
     abbrev: "POZ",
     colorClasses: "bg-indigo-100 text-indigo-700", // metro = lighter -100/-700 (mirror Warsaw)
     pinColor: "#6366f1",
     centroidLat: 52.4064,
     centroidLng: 16.9252,
     aliases: ["Poznań", "Poznan, Poland"],
   },
   ```
2. **`STATE_GROUP_MAP`** — add after `"Warsaw": "Poland",` (`region.ts:4248`):
   ```ts
   "Poznan": "Poland",
   ```
3. **`COUNTRY_INFERENCE_RULES`** — extend the existing Poland rule (`region.ts:3964`) to add the **unambiguous** `pozna[nń]` token (Poznań is not a US/other-country place name, so it's safe; the seed kennel carries explicit `country: "Poland"` so this only protects the free-form research path):
   ```ts
   // before: [/\b(poland|warszawa|polska)\b/, "Poland"],
   [/\b(poland|warszawa|polska|pozna[nń])\b/, "Poland"],
   ```
   (Keep the existing "bare `warsaw` intentionally excluded" comment untouched — `poznan` doesn't have the US-collision problem `warsaw` does, so it's fine to include.)

> Note: `COUNTRY_CODE_TO_NAME` already has `PL: "Poland"` (`region.ts:4642`) — no edit. `COUNTRY_GROUP_MAP` "Poland" key already exists (`region.ts:4493`) and is what `groupRegionsByCountry` consumes for the metro — **do NOT add a "Poznan" key there** (dead no-op per the Warsaw/Hungary precedent).

## Historical backfill
- Available: **3 past runs** in the HC feed (2026-04-18 → 2026-06-13). Fields: date/venue(3)/coords(2 of 3)/hares(1 of 3 — #12/#13 bleed). No standalone `description` emitted; no usable per-event `cost`. **No separate archive** for #1–#10 — no Poznan H3 blog/WordPress/Sheet/Meetup was found (HC is the only confirmed presence), so this backfill covers **only the HC-join window**.
- Plan: **recommended** one-shot `scripts/backfill-ph3-pl-history.ts` — **frozen-JSON + dumb loader** (Bandung/Rio/Sembach convention: extract once, commit `scripts/data/ph3-pl-history.json`, no parser committed). Extraction recipe (mirror `scripts/backfill-bandung-h3-history.ts` + the shared `scripts/lib/hc-global-runs.ts`):
  - Sweep `global-runs?isFuture=0&minEventDate=2026-01-01&maxEventDate=<today>` via `sweepGlobalRuns`, filter client-side to `PublicKennelId b193f2ad-a2b2-4f48-b8be-b7145423faaf`.
  - Map with `mapRunToRawEvent` (title-fallback + `composeHcLocation` parity). **Trust `EventNumber` for `runNumber` (11/12/13)**, NOT the garbled `EventName`.
  - 🔴 **Drop the #12 and #13 `hares`** (both are location-bleed — #12 == "Start point: Piwna Stopa…", #13 == the venue string). Keep #11 "Go Speed Racist".
  - 🔴 **Route the 3 garbled `EventName`s through `staleTitleAliases`** (or leave title undefined in the frozen JSON) so every row synthesizes "Poznan H3 #N" — do NOT let "POZH3 *UN #11" / "PZNH3" / "*un #13" become titles.
  - Apply the **Poznań bbox scrub** (drop coords outside lat 52.30…52.55 / lng 16.70…17.15 — none in this sample; #13 already has no coords) + a sane **06:00–21:59 time-gate** (all sampled `14:00` pass); **omit `description`** for live-adapter fingerprint parity.
  - Freeze to `scripts/data/ph3-pl-history.json`; loader binds to the `"Poznan H3 Harrier Central"` source, `kennelTimezone: "Europe/Warsaw"`.
- 🔴 **`config.upcomingOnly: true` is REQUIRED** (Bandung contract): HC `getEvents` is future-only, so without it `reconcile.ts` false-CANCELs the 3 backfilled past rows as they age off the 365-day window. Keep it in the seed below regardless of whether the backfill ships.
- **Frozen-dataset validation checklist** (run before shipping): PII scrub (grep `@`/phone shapes — none expected); run numbers monotonic (11→12→13, key by date anyway); gap-sanity (exact 28-day spacing, no misparse); field-bleed check on `hares` (#12/#13 flagged), `location`.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "ph3-pl", // bare ph3 taken (Petaling ph3-my / Pattaya / Pinelake) → suffixed; matches HC KennelSlug PH3-PL
  shortName: "Poznan H3",
  fullName: "Poznan Hash House Harriers",
  region: "Poznan", // NEW METRO — see Region seeding (3 region.ts edits)
  country: "Poland",
  // No website/socials found — HC is the only confirmed live presence (⚠️ flag; revisit if a site surfaces).
  logoUrl: "/kennel-logos/ph3-pl.<ext>", // self-hosted from HC blob; verify ext by magic bytes (URL says .avif — don't trust it)
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "2:00 PM", // 12-hr per kennels.ts convention (HC startTime stays 24-hr "14:00")
  scheduleFrequency: "Monthly",
  scheduleNotes: "Monthly Saturday-afternoon trail (~2:00 PM, beer hour from 1:00 PM) around Poznań, often starting near the main train station (Poznań Główny). A-to-B trails with beer stops en route; hound-friendly.",
  hashCash: "20 zł", // from HC EventDescription (#11/#12: "Hash cash: 20 zł"); HC numeric price fields unreliable
  dogFriendly: true, // #12 description: "Hound friendly"
  // foundedYear intentionally omitted — unverifiable (HC-only; run #13 Jun 2026, ~2025 start inferred but unconfirmed).
  description: "The Poznań Hash House Harriers — 'a drinking club with a running problem.' Monthly Saturday-afternoon trails around Poznań, frequently starting near the train station, with beer stops along the way. Hound-friendly.",
  latitude: 52.4064, longitude: 16.9252,
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug). bare "PH3" OMITTED (collides).
"ph3-pl": ["Poznan H3", "Poznań H3", "Poznan Hash", "Poznan Hash House Harriers", "PZNH3", "POZH3"],

// sources.ts — Source[] (array of objects). Mirror the Bandung H3 HC row (sources.ts:7463).
{
  name: "Poznan H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live 2026-07-11: getEvents/global-runs future = 0,
    // global-runs?isFuture=0 = 3 past runs #11–#13, monthly Sat 14:00, Europe/Warsaw).
    publicKennelId: "b193f2ad-a2b2-4f48-b8be-b7145423faaf",
    defaultKennelTag: "ph3-pl",
    // Poznan's HC EventNames are garbled placeholders ("POZH3 *UN #11", "PZNH3 ", "*un #13") —
    // NOT themes. defaultTitle synthesizes "Poznan H3 #N"; staleTitleAliases catch the 3 seen forms.
    defaultTitle: "Poznan H3",
    staleTitleAliases: ["POZH3 *UN #11", "PZNH3", "*un #13", "Placeholder event for PH3-PL"],
    // upcomingOnly:true — REQUIRED (HC getEvents is future-only + this source owns a 3-run
    // backfill; without it reconcile.ts false-CANCELs the past rows as they age off the
    // 365-day window — Bandung #2340 contract).
    upcomingOnly: true,
  },
  kennelCodes: ["ph3-pl"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only HARRIER_CENTRAL — zero new adapter code.** The `HARRIER_CENTRAL` adapter already
exists in `src/adapters/registry.ts` (`src/adapters/harrier-central/adapter.ts` + `token.ts`) and is
used by ~30 seeded kennels (Bandung, Rio, Sembach, KRASH, BEER H3, Taiwan H3, …). The source row above
is the whole live implementation; the backfill reuses `scripts/lib/hc-global-runs.ts`. The adapter
derives events from HC's `getEvents` API (future-only), resolves `ph3-pl` via `defaultKennelTag`, and
applies `defaultTitle`/`staleTitleAliases` for name synthesis.

**⚠️ Claude Code: verify before writing/relying on any field.** This is config-only, so the main checks are:
- Confirm `publicKennelId` returns Poznan's run(s) via a throwaway `adapter.fetch(source)`. **Expect 0
  events** — no future run was posted at research time (last run #13 was ~28 days ago on a monthly
  cadence; today is 28 days later, so #14 is due about now). If #14+ has since been posted it will
  appear. **Do not treat 0 future events as a failure** — the backfill supplies the page's content and
  the daily scrape auto-adds the next run once posted (recently-active kennel).
- Confirm `kennelTag` resolves to `ph3-pl` with **no unmatched** and `startTime` is "HH:MM".
- Confirm the HC feed's `Europe/Warsaw` tz flows through unchanged — it's **correct** for Poland (no
  tz-quirk override here, unlike Belgrade/KRASH where `Europe/Warsaw` was wrong).
- `RawEventData` field names are `kennelTags: string[]` (not `kennelTag`); `walkersWelcome` (not
  `walkerFriendly`). Don't hand-set `title` — the HC adapter + `defaultTitle`/`staleTitleAliases` handle it.
- **`friendlyKennelName("Poznan H3", …)`** — shortName is >4 chars, so it short-circuits to "Poznan H3"
  (synth title "Poznan H3 Trail #N" or "Poznan H3 #N" via defaultTitle). Quick-verify at build.

## Sibling sweep
- No Poznan sibling surfaced on the HC feed (`global-runs` carried only this one Poznan kennel) or in
  WebSearch. Warsaw H3 (Poland's other metro) is already live (`warsaw-h3`) and is a **different city**,
  not a sibling of this source. Single-kennel source — `kennelCodes: ["ph3-pl"]` is complete.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host + magic-byte ext)  [ ] foundedYear (unverifiable — flagged)  [ ] socials (none found — flagged)  [x] schedule (monthly Sat + scheduleNotes)  [x] hashCash (20 zł — from EventDescription)
- [x] description  [x] source live-verified (recently-active; #13 ~28d ago, 3-run HC archive)  [x] history depth assessed (3 on HC; HC-join ~Apr 2026; no separate archive)
- [x] coord sanity checked (2 of 3 in Poznań bbox; #13 blank → centroid)  [x] end times noted (none)  [x] kennelCode collision-checked (`ph3-pl` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **Recently-active, NOT stale** — 0 future events at research time is expected; the kennel ran #13
  ~28 days ago on a 28-day monthly cadence (#14 due ~2026-07-11). Ship the backfill so the page isn't
  empty; the daily scrape adds the next run when posted.
- **`config.upcomingOnly: true` required** (Bandung contract) — HC is future-only; protects the 3
  backfilled past rows from `reconcile.ts` cancellation.
- **Garbled placeholder titles** — the 3 HC `EventName`s are kennel-code + run-# noise, not themes.
  Route them through `staleTitleAliases` (listed in the seed) + `defaultTitle:"Poznan H3"` so rows
  synthesize "Poznan H3 #N". Trust `EventNumber` for `runNumber`.
- **`Hares` location-bleed on #12 & #13** — both equal the venue/start-point string → drop (undefined).
  The real hares live only in `EventDescription`, which the HC adapter doesn't parse. Keep #11.
- **hashCash from EventDescription, not the price fields** — HC numeric prices (5/7, 7/7) are unreliable
  and contradict the description's explicit "Hash cash: 20 zł". Kennel-level `hashCash: "20 zł"`; leave
  per-event `cost` unset.
- **Backfill = frozen JSON + dumb loader** (mirror `backfill-bandung-h3-history.ts`), not a committed
  parser. **Omit `description`** so a backfilled row fingerprints identically to a live scrape.
- **Self-host the HC blob logo** to `public/kennel-logos/ph3-pl.<ext>`; confirm ext by magic bytes
  (the `.avif` URL name is not authoritative).
- **3 region.ts edits** — new Poznan METRO under existing Poland COUNTRY (METRO seed + STATE_GROUP_MAP +
  Poland inference-rule extension with `pozna[nń]`). **No `COUNTRY_GROUP_MAP` edit** (Warsaw/Hungary
  precedent — metro resolves via STATE_GROUP_MAP first). Mirror the Warsaw METRO record for palette/tz.
- **ASCII shortName → clean slug** — `toSlug("Poznan H3")` = `poznan-h3` (no non-ASCII char, so **no
  explicit `slug:` needed**). The native "Poznań H3" is an alias only.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
