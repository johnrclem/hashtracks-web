# Onboarding Handoff — Seoul H3 (Seoul, South Korea) — 2026-06-16

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/sh3-kr-20260616`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Korea is a **new country** —
>    add the **South Korea COUNTRY + Seoul METRO** region records + inference rule + group/code maps
>    (all 5 `region.ts` edits — see **Region work**, mirror the Philippines block).
> 3. Implement the **NEW `SeoulH3Adapter`** exactly as in **Adapter notes** (single current-run SSR
>    parse of `index.php`; NOT config-only). Register in `htmlScrapersByUrl`.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: ≥1 event, date is
>    UTC-noon, `startTime` "HH:MM", `kennelTags:["sh3-kr"]` resolves with no unmatched. Resolve every
>    `⚠️ Claude Code must confirm` item below (esp. the real `index.php` DOM structure + `archive.php`
>    backfill shape). **DO NOT run `npx prisma db seed` here** — that's the post-merge step.
> 5. Optional but recommended: the **Historical backfill** one-shot from `archive.php` (assess its
>    real structure first — see Historical backfill).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 8. **Post-merge runbook (after the PR merges):** `git checkout main && git pull` → verify each new
>    file landed (`git log -1 -- <path>`) → `eval "$(fnm env)" && fnm use 20` → `npx prisma db seed`
>    → trigger a scrape from `/admin/sources` → spot-check `/kennels/seoul-h3` for the run + region.

## Summary
- Type: **full onboard**
- Adapter: **HTML_SCRAPER** (**NEW `SeoulH3Adapter`** — single current-run SSR parse; NOT config-only)
- Effort estimate: **new ~150–230 LoC Cheerio adapter + tests**, + a new region (South Korea/Seoul,
  5 `region.ts` edits), + an **optional one-shot `archive.php` backfill** (assess structure first).
- One-line: 🇰🇷 **HashTracks' first South Korea kennel** — Seoul H3, "Korea's Mother Hash" (est. 1972),
  a live PHP site that server-renders the current weekly run; ~run #2897 with a deep on-site archive.

## Dedup result
- Kennel in seed: **no** (no `seoul`/`korea`/`sh3-kr` in `prisma/seed-data/kennels.ts`).
- Source in seed: **no**.
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP,
  **444 slugs** (2026-06-16); no `seoul`/`korea`/`shhh` slug. ⚠️ Note: `s2h3` IS in the sitemap but
  that is **Siam Sunday H3 (Bangkok)**, NOT Seoul Southside — Seoul's scene is entirely absent.
- Decision: **full onboard**.
- kennelCode: `sh3-kr` (collision check: bare `sh3` is unused as a kennelCode but the **alias "SH3" is
  taken** by `summit` + `sh3-wa` Seattle; siblings already suffix — `sh3-de`/`sh3-wa`/`sh3-sg`/`sh3-au`.
  Use `sh3-kr` and **omit the bare "SH3" alias**). Slug = `toSlug("Seoul H3")` = `seoul-h3` (ASCII, clean).

## Live source verification  ✅ (HTML listing verified live from sandbox; feed = the page itself)
- Source: HTML_SCRAPER — `https://seoulhash.com/index.php` (current run, SSR) +
  `https://seoulhash.com/archive.php` (full history, SSR). DNS OK; site serves `text/html` SSR.
- Events seen: **1 current run** on `index.php` (the page shows only the current/most-recent run).
  Date range: the single current run, **13 Jun 2026** (run #2897). `calendar.php` is **JS-rendered**
  (empty SSR) → not usable; the daily scrape captures each new week's run as it's posted on `index.php`.
- **Source-count parity:** N/A for the forward feed (single-current-run page, same pattern as Manila H3 /
  Boise H3). Deep history lives on `archive.php` → handle via one-shot backfill, not the adapter.
- Sample event (VERBATIM from `index.php`, 2026-06-16):
  1. **Run #2897** — "**Anti-Celibacy Day**" — Meeting Time `2026/06/13 16:00` — Location
     "Samgakji Station Line 4, exit 8" — Hares "GM Professor D'Erections" — Apres "Local Craft Beer place"
     — **Hash Cash W10,000** — Geo Coordinates: *(empty — `maps/place/` link with no coords)* —
     + a long prose description (Martin Luther / anti-celibacy theme; GM-changeover-on-20-June notice).
  - 🔴 **Run numbers and titles are VERBATIM** above — `#2897` and `Anti-Celibacy Day` are real, read
    off the live page. The page exposes exactly one run at a time; do not synthesize others.
- History depth / pagination: `archive.php` is a **single ~96 KB SSR page** (no pagination) covering the
  full run history back toward 1972 (run #2897 implies ~2,900 runs). ⚠️ **Claude Code must inspect its
  real table/row structure at build** (the sandbox could not slice the saved body) before scripting backfill.
- Coord sanity: **no per-run coords** — `Geo Coordinates` was empty (a bare `maps/place/` link); `Location`
  is a text venue/subway-exit. `latitude`/`longitude` stay undefined → merge geocodes the venue text
  (Korea/Seoul bias) or falls back to the Seoul metro centroid. **No default-pin trap.**
- End times: **none** (single start time only).
- Notes: **Men-only** kennel ("A Men's Drinking Club"; "Discerning Gentlemen Only"). `index.php` is plain
  SSR PHP → `fetchHTMLPage` + Cheerio (no browserRender). **The web_fetch view FLATTENED the labels**
  (`Title:`/`Meeting Time:`/`Location:`/`Geo Coordinates:`/`Hares:`/`Apres Trail:`/`Hash Cash:` ran
  together) — ⚠️ **capture the REAL DOM** (likely a labeled table/`<div>` block) and build the test
  fixture from that verbatim markup, not from the flattened text above.
- **Field-fill assertion table** (from the single sampled run; the adapter reads these labels):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 1 / 1 | from the run heading / `Title:` (e.g. "Anti-Celibacy Day"). If the value is a bare placeholder, leave `title` undefined → merge synthesizes "Seoul H3 Trail #N" |
  | run number | 1 / 1 | from the heading (`2897`) — `extractHashRunNumber`/local regex; sits adjacent to the time in the flattened text, so read the heading element directly |
  | `startTime` | 1 / 1 | `HH:MM` from `Meeting Time: YYYY/MM/DD HH:MM` (e.g. `16:00`) |
  | date | 1 / 1 | **`Meeting Time` carries a full `YYYY/MM/DD` → NO year inference needed** → UTC noon |
  | `endTime` | 0 / 1 | not published → undefined |
  | `location` (venue) | 1 / 1 | `Location:` text ("Samgakji Station Line 4, exit 8") |
  | `locationStreet` | 0 / 1 | not separated → leave undefined |
  | `locationUrl` (Maps) | 0 / 1 | `Geo Coordinates` link was bare `maps/place/` (no coords) — store only if it carries a real place id |
  | `hares` | 1 / 1 | `Hares:` text |
  | `cost` | per-event only | kennel default `W10,000` lives on `Kennel.hashCash`; set `Event.cost` only if a run differs |
  | `description` | 1 / 1 | the prose body (theme/notes) |
  | `coords` (lat/lng) | 0 / 1 | none on page → undefined → centroid fallback |

## Kennel metadata (deep-dive complete)
- fullName: **Seoul Hash House Harriers** / shortName: **Seoul H3** / region: **Seoul** / country: **South Korea**
- aliases: `["Seoul Hash", "Seoul H3", "Seoul Hash House Harriers", "Korea's Mother Hash"]`
  (⚠️ **omit bare "SH3"** — taken by `summit`/`salemh3`/`sh3-wa` — **AND bare "SHHH"** — taken by `sech3`/`sgharriets`; both removed in the shipped PR #2227. source: `prisma/seed-data/aliases.ts`)
- website: `https://seoulhash.com` (source: live site)
- facebook / instagram / twitter / discord: **none found on-site** — leave blank / follow up
- schedule: **Saturday, ~16:00, Weekly** (current live data: run #2897 = Sat 2026/06/13 16:00).
  `scheduleNotes`: *historically a Wednesday-evening hash (per the 1979 Arirang article on the About page);
  current runs are Saturday afternoon, and the start time shifts seasonally with daylight — confirm the
  current time from `index.php`/`calendar.php` at build.* (source: seoulhash.com/index.php + /about.php)
- foundedYear: **1972** (source: seoulhash.com/about.php — "alive and well since 1972", "Korea's Mother
  Hash", "still crazy after all these beers since 1972"; Arirang 1979 article: "Run Number One seven
  years ago" → 1972).
- hashCash: **"₩10,000"** (visitors US$10 / ₩10,000; source: seoulhash.com/index.php run #2897 +
  /info.php "visitors should be paid up (US$10 or W10,000)").
- dogFriendly: not stated → leave default. walkersWelcome: **not advertised** (men's running hash;
  A/D trail splits exist but no explicit walkers' trail) → leave default/false unless found at build.
- description: "Korea's Mother Hash — the original Seoul men's hash, *"Only Half A Mind"* on Freedom's
  Frontier, trailing the hills and rice paddies around Seoul since 1972. A drinking club with a running
  problem; weekly trail, beer, a bonfire, and the Hash Trash." (source: seoulhash.com/about.php)
- logoUrl: **`https://seoulhash.com/images/shhh_logo_120.png`** — a **stable, non-tokenized** path. Prefer
  self-hosting to `public/kennel-logos/sh3-kr.<ext>` per convention (confirm ext by magic bytes; it's a
  small `.png` by name — verify).
- lat/lng (kennel): Seoul city centroid ≈ **37.5665, 126.9780** (use the METRO centroid you seed in region.ts).

## Historical backfill
- Available: **yes — `https://seoulhash.com/archive.php`** (single SSR page, ~96 KB, full run history toward
  run #1 / 1972). Fields per row: ⚠️ **unknown until inspected** (run #, date, hares, venue, theme likely).
- Plan: **one-shot `scripts/backfill-sh3-kr-history.ts`** IF the archive rows are structured (per the
  ONH3/Brasília/Bangkok-Monday frozen-dataset pattern: throwaway parser → commit
  `scripts/data/sh3-kr-history.json` + a dumb loader). **First inspect the real DOM** —
  `curl -s https://seoulhash.com/archive.php | less` — and confirm a per-run row shape. If it's freeform
  prose, scope the backfill down or defer (don't dismiss without pulling the markup). Set source
  `config.upcomingOnly: true` so `reconcile.ts` never false-cancels aged-off archive runs.

## Ready-to-paste seed

```ts
// kennels.ts — KennelSeed[] (array of objects). Mirror the Manila H3 entry (kennels.ts:4687).
{
  kennelCode: "sh3-kr", shortName: "Seoul H3", fullName: "Seoul Hash House Harriers",
  slug: "seoul-h3",
  region: "Seoul", country: "South Korea",
  website: "https://seoulhash.com",
  scheduleDayOfWeek: "Saturday", scheduleTime: "4:00 PM", scheduleFrequency: "Weekly", // 12-hr seed format — NOT "16:00" (corrected in PR #2227; see retro Gap B)
  scheduleNotes:
    "Korea's Mother Hash, men-only. Historically a Wednesday-evening hash (1979 Arirang article); current runs are Saturday afternoon with a seasonally-shifting start time — verify current time at build. Run #2897 as of Jun 2026.",
  foundedYear: 1972,
  hashCash: "₩10,000",
  logoUrl: "/kennel-logos/sh3-kr.png",   // self-hosted from seoulhash.com/images/shhh_logo_120.png — confirm ext by magic bytes
  latitude: 37.5665, longitude: 126.978,
  description:
    "Korea's Mother Hash — the original Seoul men's hash, \"Only Half A Mind\" on Freedom's Frontier, trailing the hills and rice paddies around Seoul since 1972. A drinking club with a running problem.",
},

// aliases.ts — Record<string, string[]>, keyed by KENNELCODE (NOT slug). Mirror aliases.ts:562.
"sh3-kr": ["Seoul Hash", "Seoul H3", "Seoul Hash House Harriers", "Korea's Mother Hash"],
// ⚠️ Do NOT add bare "SH3" (taken by summit/salemh3/sh3-wa) OR bare "SHHH" (taken by sech3/sgharriets) — both omitted in PR #2227.

// sources.ts — Source[] (array of objects). Mirror the Manila H3 Website source (sources.ts ~5894).
{
  name: "Seoul H3 Website",
  url: "https://seoulhash.com/index.php",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,
  scrapeFreq: "daily",
  scrapeDays: 90,
  config: { upcomingOnly: true },
  kennelCodes: ["sh3-kr"],
},
```

## Region work — NEW country (South Korea) + metro (Seoul): all 5 `region.ts` edits
Mirror the **Philippines** block (added for Manila H3) verbatim in structure:
1. **`REGION_SEED_DATA`** (cf. `src/lib/region.ts:2691` Philippines COUNTRY + `:2705` Manila METRO):
   add a **South Korea** COUNTRY record (`country:"South Korea"`, `timezone:"Asia/Seoul"`, ISO `KR`,
   centroid ≈ `36.5,127.85`, `aliases:["Korea","Republic of Korea","KR","South Korea"]`, **darker `-200`
   color shade** + a distinct pin) **+** a **Seoul** METRO record under it (`timezone:"Asia/Seoul"`,
   centroid `37.5665,126.978`, `aliases:["Seoul, South Korea","Seoul, Korea","서울"]`, **lighter `-100`
   shade** + different pin). Pick an Asia palette distinct from Japan/Singapore/Taiwan (scan those entries).
2. **`COUNTRY_INFERENCE_RULES`** (cf. `:3486`): add `[/\b(south korea|korea|seoul)\b/, "South Korea"]`.
   🔴 **Without this `inferCountry()` returns "USA"** for Korean text. seoulhash.com is all-English, so the
   ASCII rule suffices; optionally append a Hangul branch (`서울|대한민국|한국`) defensively (mirror the
   Taiwan/Shanghai CJK precedent — `\b` is ASCII-only and won't anchor Hangul), but it's low-risk here.
   ⚠️ Place the rule so "korea" can't be shadowed; "North Korea" has no kennels so the bare `korea` token is safe.
3. **`STATE_GROUP_MAP`** (cf. `:3692`): `"Seoul": "South Korea"`.
4. **`COUNTRY_GROUP_MAP`** (cf. `:3946`): `"South Korea": "South Korea"` + `"Seoul": "South Korea"`.
5. **`COUNTRY_CODE_TO_NAME`** (cf. `:4056`): `KR: "South Korea"`.
   - No trailing-zero numeric literals in any seed (Sonar S6749: `126.978` not `126.9780`).

## Adapter notes / new-scraper plan  (`src/adapters/html-scraper/seoul-h3.ts`)
**Single current-run SSR parser** — the closest existing reference is **`manila-h3.ts`** (single current
run, `upcomingOnly`, fail-loud guard) and **`nswhhh.ts`** (label-keyed SSR block). Shape:

- `fetch(source)`: `fetchHTMLPage(source.url)` (static Cheerio — **no browserRender**), parse the **one**
  current run, emit a single `RawEventData` in `result.events`. Honor `options.days` via `buildDateWindow`.
- **Parse the current-run block by its labels** (capture the real DOM first — the flattened web_fetch text
  showed these labels in order): run-number + theme in the heading; then `Title:`, `Meeting Time:`
  (`YYYY/MM/DD HH:MM`), `Location:`, `Geo Coordinates:` (often empty), `Hares:`, `Apres Trail:`,
  `Hash Cash:`, then a prose description. Key on the visible label text (PHP class names may be unstable),
  e.g. a small `parseLabeledBlock` helper returning a `Map<label,value>` (use `Map.get`, not `obj[key]`,
  to dodge Codacy `detect-object-injection`).
- **Date**: `Meeting Time` carries a full `YYYY/MM/DD HH:MM` → parse directly to **UTC noon** for the date
  and `"HH:MM"` for `startTime`. **No year inference needed** (unlike the year-less siblings).
- **Run number**: from the heading (`2897`). It abuts the time in the flattened text (`16:002897`), so read
  the heading element directly (not a regex over joined text). Store the bare integer.
- **`title`**: use the run theme (`Anti-Celibacy Day`) when present; if it's a bare placeholder, leave
  `title` undefined → `merge.ts` synthesizes `"Seoul H3 Trail #N"`. (`shortName` "Seoul H3" is >4 chars →
  `friendlyKennelName` short-circuits cleanly — verify with the one-liner in the gotchas.)
- **Location**: `Location:` text → `location`. **No coords** (Geo Coordinates empty) → leave lat/lng
  undefined; don't fabricate. `hares`, `description`, `cost`(only if ≠ kennel default) opportunistically.
- **`kennelTags: ["sh3-kr"]`** (string[], NOT `kennelTag`).
- **`config.upcomingOnly: true`** (single rolling current run → protects `reconcile` as it ages off).
- **Single-block source → add an explicit `rows.length === 0` / no-current-run fail-loud guard**
  (`errors.push("Seoul H3: no current run parsed")`) so a markup drift surfaces instead of a silent
  `events:[]` + reconcile false-cancel (brand-new source has a 0 baseline the zero-event alert misses).
- **Registry**: add a `htmlScrapersByUrl` entry keyed on `seoulhash.com` → `SeoulH3Adapter`.
- **Test fixture**: build from the REAL `index.php` markup (`curl -s https://seoulhash.com/index.php`),
  not the flattened text in this handoff.

**⚠️ Claude Code: verify before writing real code.** Any snippet here is illustrative; the live repo is
authority. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma`.
- Imports — `fetchHTMLPage` + date/extract helpers from `@/adapters/utils`; `safeFetch` from
  `@/adapters/safe-fetch` if you fetch directly (NOT raw `fetch`).
- `kennelPagesStopReason` — leave null on a clean single-run parse; set ONLY on genuine truncation
  (fetch/HTTP error). A non-empty string suppresses stale-event reconciliation.
- `title` — leave `undefined` when no clean theme; never let a label fragment or hare name become the title.

## Implementation gotchas (repo knowledge, not source knowledge)
- **`config.upcomingOnly: true` is REQUIRED** here — `index.php` shows only the current run, which ages
  off weekly; without it `reconcile.ts` false-`CANCEL`s last week's run (Manila/Boise/Bali pattern).
- **No coords on page → do NOT set fake lat/lng**; leave undefined and let the merge geocode the venue
  text / fall back to the Seoul centroid. (No `dropCachedCoords` needed — nothing bad is ever set.)
- **`archive.php` backfill is a SEPARATE one-shot**, not the adapter; freeze a curated
  `scripts/data/sh3-kr-history.json` + dumb loader (don't commit the throwaway parser). PII-scrub
  (`@`/phones), run-number monotonicity + ~weekly-gap sanity, before shipping.
- **`friendlyKennelName("Seoul H3", …)`** — `shortName` >4 chars so it short-circuits to "Seoul H3";
  still verify: `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("Seoul H3","Seoul Hash House Harriers"));'`
- **New country → all 5 `region.ts` edits** (esp. `COUNTRY_INFERENCE_RULES`, the one that returns "USA"
  if omitted). CI caught this for Kenya/ONH3.
- **Self-host the logo** to `public/kennel-logos/sh3-kr.<ext>`; confirm the real extension by magic bytes
  (`file` / first 4 bytes — `\x89PNG`=PNG), NOT the `.png` in the URL.
- **http/https + Sonar**: site is https; tests should mock `fetchHTMLPage`/`safeFetch` with `https://` URLs.
- Sonar: `Number.parseInt(s,10)`, `s.replaceAll(...)`, no negated ternaries, keep regexes S5852/S5843-safe
  (simple label regexes — colon immediately follows the label word, so no `\s*` needed).

## Deep-dive checklist (nothing deferred)
- [x] logo (stable `shhh_logo_120.png` → self-host)  [x] foundedYear (1972, cited)  [ ] socials (none found — follow up)  [x] schedule (Sat ~16:00 weekly; Wed→Sat shift noted)  [x] hashCash (₩10,000, cited)
- [x] description  [x] source live-verified (index.php SSR, verbatim run #2897 sample)  [x] history depth assessed (archive.php — inspect DOM at build)
- [x] coord sanity (none on page → centroid fallback)  [x] end times (none)  [x] kennelCode collision-checked (`sh3-kr`, bare SH3 omitted)  [x] kennelCodes (source guard) set  [x] new-country region plan (5 edits)

---
_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
