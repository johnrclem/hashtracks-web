# Onboarding Handoff — EELS H3 (London, England) — 2026-07-17

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/eels-h3-20260717`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **0 `region.ts` edits** —
>    EELS is an East-London kennel and reuses the existing **"London"** METRO (confirmed seeded,
>    `region.ts:1456`).
> 3. Configure the adapter exactly as in **Adapter notes** — this is **pure config-only
>    HARRIER_CENTRAL** (the adapter already exists at `src/adapters/harrier-central/adapter.ts`,
>    registered in `registry.ts`). Mirror the Lisbon/Hamburg-H7 HC source rows in `sources.ts`.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md`
>    — call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Because EELS shows **0
>    upcoming** at research time (recently-active — see below), the live `getEvents` path may
>    return 0 events; that is EXPECTED and not a failure. Confirm the adapter resolves
>    `kennelTag: "eels-h3"` with no unmatched, and that the historical backfill script (step 5)
>    yields the 30 counted runs. **DO NOT run `npx prisma db seed` here.**
> 5. Apply the **Historical backfill** one-shot (`scripts/backfill-eels-h3-history.ts`) — 30
>    counted monthly runs #30→#59 via `global-runs?isFuture=0`. Worth it (deep, clean, 30-month
>    unbroken record).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- <path>` for the seed edits
>      AND `scripts/backfill-eels-h3-history.ts` / `scripts/data/eels-h3-history.json` — squash
>      merges have silently dropped follow-up commits before).
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds the new kennel/alias/source)
>    - Run the backfill one-shot, then trigger a scrape from `/admin/sources`.
>    - Spot-check `hashtracks.xyz/kennels/eels-h3` for ~30 historical events (monthly, #30→#59)
>      + the next run appearing on the first scrape after EELS posts it to HC.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists & feed verified via `hashruns.org/api/global-runs`)
- Effort estimate: **config-only** (mirror the Lisbon/Hamburg-H7 HC source row) + a ~30-run frozen-JSON backfill one-shot (mirror `backfill-sembach-h3-history.ts` / `backfill-krashh3` pattern)
- One-line: First **East London** kennel — a very consistent monthly (first-Thursday) London pub hash with a clean 30-month HC record; fills a real gap (London METRO currently has London H3 / West London / SW London / Barnes / Enfield / Old Coulsdon but no East-End kennel).

## Dedup result
- Kennel in seed: **no** (`grep -i eels prisma/seed-data/*` → 0 matches)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-17 (**485 slugs**); **no `eels` / `east-end` / `eel` slug**. London-area slugs present = `london-h3`, `west-london-h3`, `slh3`, `barnes`(? via barnesh3), `och3`, `bruh3` — none is EELS. Per-slug check: `/kennels/eels-h3` renders a hard **404** (Chrome-rendered, not just curl).
- Decision: **full onboard**
- kennelCode: `eels-h3` (collision check: **clear** — grep-clean in `kennels.ts` + `aliases.ts`; bare `EELS`/`EELS H3` unused globally)
- Pre-onboarding admin-event check: no partial/live page exists (404) → no admin-seeded `Event` rows to dedup/purge. (Claude Code: re-confirm at build if desired.)

## Live source verification  ✅
- Source: **HARRIER_CENTRAL** — `publicKennelId: 15940f8a-517e-4a13-b289-6709aaa259e9` (shortName `EELS H3`, HC slug `EELSH3`). Enumerated via `hashruns.org/api/global-runs` from the Chrome extension (same-origin fetch on `hashruns.org`), 2026-07-17.
- Events seen: **32 HC rows** (30 counted) — **#30 2024-02-01 → #59 2026-07-02**; **0 upcoming** on the future feed at research time.
- **Source-count parity:** HC platform UI shows **0 upcoming** → parity N/A; applied the **Step 3 recently-active rule** instead (see below). The **past** feed is a COMPLETE monthly record (not a sparse subset like Douliu/Prague) — 30 counted runs, one per calendar month, unbroken Feb 2024 → Jul 2026.
- **Recently-active rationale (0 upcoming, NOT blocked):** monthly cadence; last run **#59 2026-07-02 (15 days ago) ≤ 2× the ~monthly interval**; ran in **every one of the last 6+ months** (Feb/Mar/Apr/May/Jun/Jul 2026) — far exceeds the "≥3 of last 4 months" bar. Independently corroborated by the kennel's Facebook group: *"Hashing on the first Thursday of every month from somewhere with an E in the postcode"* (source: facebook.com/groups/381336404144042). This is a robust recurring cadence, not a thin single-datapoint (contrast Rio Full Moon hold). **Onboard with `upcomingOnly:true` + a recent-history backfill; the next run appears on the first scrape after EELS posts it to HC.**
- Sample events (VERBATIM from `global-runs`, most-recent first):
  1. 2026-07-02 — **"Run #59 - Aldgate"** — hares **Cardinal Sin** — **The Hoop & Grapes** (51.51426, −0.07418) — 18:30 (desc: "Meet at 6:45 for a 7pm")
  2. 2026-06-04 — **"Run #58 - Chingford"** — hares **Fishy Fingers** — **Queen Elizabeth** (51.63018, 0.01565) — 18:45 (desc references a birthday drink stop)
  3. 2026-05-07 — **"Run #57 - Canary Wharf"** — hares **Perv and Ride** — **CRATE Bar & Pizzeria** (51.50281, −0.01346) — 18:45 (desc: "Gather 18:45 for 19:15 On-Out, Nearest Station: Canary Wharf")
  4. 2026-04-02 — **"Run #56 - Wood Street"** — hares **Wrecking Balls** — **Walthamstow Cricket Tennis & Squash Club** (51.5853, 0.0002) — 18:45
  5. 2024-02-01 — **"EELS #30 - Bethnal Green"** — hares **Broke Vac Mounting** — (oldest HC row)
- History depth / pagination: **32 HC rows total, #30→#59** (30 counted + 2 uncounted away-weekend dups). Reachable in full via 6-month `global-runs?isFuture=0&minEventDate/maxEventDate` windows filtered on `PublicKennelId`. Runs **#1–#29 predate the kennel's HC join (before 2024-02) and are NOT on the feed** — no deeper HC backfill possible. (FB group has older posts but is a static/private-ish group — not a structured backfill source; skip.)
- Coord sanity: **clean** — all 32 pins fall inside the Greater London bbox (lat 51.50–51.63, lng −0.07…+0.05); **0 default/garbage pins**, 0 missing coords. No `dropCachedCoords` needed, but keep the standard fail-safe bbox scrub (UK: lat 50.5–52.2, lng −1.5…+1.2) as a precaution.
- End times: none exposed (HC carries only start). Descriptions sometimes state an "On-Out" time (e.g. 19:15) but there's no structured `endTime` → accept absence.
- Notes: tz field on HC = **`Europe/London`** (correct — **no HC quirk**, unlike Belgrade/KRASH `Europe/Warsaw` or Divahhh `Europe/Berlin`). Prices: **all rows £0** (members & non-members) → the kennel is free / pay-your-own pub hash, OR the price simply isn't entered in HC → **hashCash unverified, flag** (do not assert a value). Two `IsCountedRun=0` rows share run **#49** with the counted one — see the away-weekend quirk under Adapter notes.
- **Field-fill assertion table** (from the 5 sample rows above):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 5 / 5 | HC `EventName` verbatim ("Run #59 - Aldgate" etc.) — all real, keep; merge synthesizes "EELS H3 Trail #N" only if ever blank |
  | `startTime` | 5 / 5 | `HH:MM` from `EventStartDatetime` (note: HC time is the *meet* time 18:30/18:45; on-out is ~19:00–19:15) |
  | `endTime` | 0 / 5 | accept absence |
  | `location` (venue name) | 5 / 5 | `LocationOneLineDesc` ("The Hoop & Grapes", "CRATE Bar & Pizzeria") |
  | `locationStreet` | 0 / 5 | not in feed — leave undefined |
  | `locationUrl` (Maps) | 0 / 5 | not in feed — leave undefined |
  | `hares` | 4 / 5 | `Hares` field (#39 was blank) — CSV; leave `undefined` on blank (not `null` — blank means "not provided", not an explicit clear) |
  | `cost` | 0 / 5 | HC price £0 → leave undefined (kennel default unverified; do NOT synth a value) |
  | `description` | 5 / 5 | `EventDescription` (meet-time notes, station info) — 🔴 scan for PII before backfill (see gotchas) |
  | `trailLengthText` | 0 / 5 | not in feed |
  | `coords` (lat/lng) | 5 / 5 | `Latitude`/`Longitude` (clean London pins) |

## Kennel metadata (deep-dive complete)
- **fullName:** East End of London Something Hash House Harriers  (source: facebook.com/groups/381336404144042 — the "Something" is the joke word behind the "S" in EELS)
- **shortName:** EELS H3  (source: HC `KennelShortName` + FB group name)  — length 7 (>4) → `friendlyKennelName` returns it directly, synth title "EELS H3 Trail #N" is clean (no need to run the ≤4 check).
- **region:** London  (reuse existing METRO — **0 region.ts edits**; East London has no dedicated sub-region and "London" METRO centroid 51.51/−0.13 covers EELS's East/NE-London venues)
- **country:** UK
- **aliases:** ["EELS", "EELS H3", "East End of London Something H3", "East End of London Something Hash House Harriers", "EELS Hash"]  (all grep-clean globally; bare `EELS`/`EELS H3` unused)
- **website:** none found — FB group only → blank + flag
- **facebook:** https://www.facebook.com/groups/381336404144042/  (source of the schedule confirmation; DNS n/a — facebook.com is a known platform)
- **instagram / twitter / discord:** none found → blank
- **schedule:** **1st Thursday of every month**, ~19:00 on-out (meet 18:45)  (source: FB group tagline *"first Thursday of every month from somewhere with an E in the postcode"* + confirmed by all 30 counted HC dates, each a first-Thursday). Single pattern → flat `scheduleDayOfWeek`/`scheduleTime` sufficient (no `scheduleRules` needed):
  - `scheduleDayOfWeek: "Thursday"`
  - `scheduleTime: "7:00 PM"`  (🔴 12-hour in the Kennel profile field; the adapter's `RawEventData.startTime` stays 24-hour "19:00" / verbatim HC meet time)
  - `scheduleFrequency: "Monthly"`
- **foundedYear:** **unverified** → leave blank + flag. (Back-extrapolating monthly from HC #30 @ 2024-02 puts #1 ≈ mid-2021, so ~2020–2021 — but that's inference, not a cited fact; per the manual, do not assert. If a "Run #1" / anniversary post surfaces on the FB group at build, record it with the citation.)
- **hashCash:** **unverified** → blank + flag (HC price fields = £0 for all rows; likely free / pay-your-own pub hash, but not confirmed by a citable source)
- **dogFriendly / walkersWelcome:** unknown → blank
- **description:** "East London branch of the Hash House Harriers — a monthly first-Thursday pub hash meeting 'somewhere with an E in the postcode'."  (paraphrased from the FB group tagline; keep short)
- **logoUrl:** `https://harriercentral.blob.core.windows.net/harrier/Eels%20H3.png` → **⚠️ self-host** to `public/kennel-logos/eels-h3.<ext>` — HC blob URLs are stable-ish but self-host per convention. 🔴 **NEVER pre-fill the extension** — confirm via `curl -sI` Content-Type AND magic bytes (`\x89PNG`=PNG, `RIFF`=WebP, `\xff\xd8`=JPEG). The URL ends `.png` but HC blobs have shipped as AVIF/WebP before — verify.
- **lat/lng:** East London — use ~51.55, −0.02 for the kennel centroid, or simply inherit the London METRO centroid (either is fine; venues span Bethnal Green → Chingford).

## Historical backfill
- Available: **30 counted runs** (#30 2024-02-01 → #59 2026-07-02), monthly first-Thursday — fields: date, title (`EventName`), hares, venue (`LocationOneLineDesc`), coords, description (meet notes). All 30 CONFIRMED, clean coords, times 10:00–18:45 (all within the 06:00–21:59 gate; one 10:00 = a Saturday away run, legit).
- Plan: **one-shot `scripts/backfill-eels-h3-history.ts`** — worth it (deep, clean, 30-month unbroken record; the kennel page should show real history immediately). Set source `config.upcomingOnly: true` so `reconcile.ts` doesn't false-cancel the aged-out backfill rows.
- **Preferred pattern (freeze a curated dataset + dumb loader):** pull the 30 rows via `global-runs?isFuture=0`, curate to a frozen `scripts/data/eels-h3-history.json`, commit the JSON + a dumb loader `scripts/backfill-eels-h3-history.ts` (mirror `backfill-sembach-h3-history.ts` / `backfill-krashh3-history.ts`). Do NOT commit the parser.
- **Frozen-dataset validation (run before shipping):**
  1. **PII scrub** — grep the `description` bodies for `@` (emails) and phone-like patterns; redact. (EELS descriptions are venue/meet-time notes — low PII risk, but scrub anyway.)
  2. **Run-number continuity** — #30→#59 should be one-per-month; flag any month gap or a step whose day-gap is <8.5d or >40d (none expected — the record is clean).
  3. **Gap sanity** — each event date within −1…+60d of its slot; catches misparse (none expected — HC serves ISO datetimes).
  4. **Monotonicity** — run numbers non-decreasing over time (the only exception is the #49 away-weekend dup — handle per below).
  5. **Field bleed** — `hares`/`location` strings containing `:` may indicate a missed boundary (none seen).
- 🔴 **Away-weekend `#49` quirk (must handle):** run **#49** appears **3×** — 2025-08-16 (`IsCountedRun=0`, "EELS - Rochester Away Hangover Trail"), 2025-08-17 (`IsCountedRun=0`, Rochester away), and 2025-09-04 (`IsCountedRun=1`, "EELS #49 - Buckhurst Hill", the real monthly). **Recommended:** **drop the two `IsCountedRun=0` rows** (Bandung/TITs contract — uncounted away-weekend duplicates) → a clean monotonic 30-run set #30–#59, one per month. (Alternative: keep them as distinct-date events — dates differ so fingerprints won't collide — but that double-lists #49; dropping is cleaner.)

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` keyed by **kennelCode** — confirmed via `head prisma/seed-data/aliases.ts`. NOT an object array.

```ts
// kennels.ts — Kennel[] (array of objects). Append one entry:
{
  kennelCode: "eels-h3",
  shortName: "EELS H3",
  fullName: "East End of London Something Hash House Harriers",
  region: "London",              // reuse existing METRO — 0 region.ts edits
  country: "UK",
  // website / facebookUrl / instagramHandle / etc.:
  facebookUrl: "https://www.facebook.com/groups/381336404144042/",
  scheduleDayOfWeek: "Thursday",
  scheduleTime: "7:00 PM",       // 🔴 12-hour in the profile field (on-out ~19:00; HC meet time 18:45)
  scheduleFrequency: "Monthly",  // 1st Thursday
  description:
    "East London branch of the Hash House Harriers — a monthly first-Thursday pub hash meeting 'somewhere with an E in the postcode'.",
  logoUrl: "https://harriercentral.blob.core.windows.net/harrier/Eels%20H3.png",
  // foundedYear / hashCash / dogFriendly / walkersWelcome: unverified → omit (leave null)
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]). Add one key:
"eels-h3": ["EELS", "EELS H3", "East End of London Something H3", "East End of London Something Hash House Harriers", "EELS Hash"],

// sources.ts — Source[] (array of objects). Append one row (mirror Lisbon/Hamburg-H7 HC rows):
{
  name: "EELS H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified via hashruns.org/api/global-runs 2026-07-17).
    publicKennelId: "15940f8a-517e-4a13-b289-6709aaa259e9",
    defaultKennelTag: "eels-h3",
    defaultTitle: "EELS H3",     // merge fallback only — HC EventNames are real, kept verbatim
    upcomingOnly: true,          // REQUIRED — backfill present + HC getEvents is future-only (reconcile would false-CANCEL past rows)
    // No staleTitleAliases needed — all 32 HC EventNames are real ("Run #N - <area>" / "EELS #N - <area>"), no placeholders.
  },
  kennelCodes: ["eels-h3"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only** — the `HARRIER_CENTRAL` adapter already exists (`src/adapters/harrier-central/adapter.ts`, registered in `registry.ts`) and drives every row above. No new adapter code. The config keys off `publicKennelId` (the GUID) exactly like Lisbon/Hamburg-H7/Bandung. The daily `getEvents` path is future-only (0 upcoming today is expected); the backfill script supplies history.

**⚠️ Claude Code: verify before writing real code.** The only code you author is the backfill one-shot (`scripts/backfill-eels-h3-history.ts`) + its frozen `scripts/data/eels-h3-history.json`. Mirror `backfill-sembach-h3-history.ts` / `backfill-krashh3-history.ts`. Confirm against current types:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); dates **UTC noon**; `startTime` a `"HH:MM"` string.
- The backfill inserts canonical `Event` rows directly (frozen JSON → dumb loader), same as the Sembach/KRASH one-shots — not through the adapter.
- Coords are clean; keep the UK bbox scrub (lat 50.5–52.2, lng −1.5…+1.2) + 06:00–21:59 time gate as fail-safes (no rows should be dropped by them for EELS).
- Drop the two `IsCountedRun=0` #49 away-weekend rows (see backfill quirk).
- Self-host the logo — confirm the real extension via magic bytes; never pre-fill `.png`.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host, confirm ext via magic bytes)  [x] foundedYear (unverifiable → blank+flag)  [x] socials (FB group only)  [x] schedule (1st-Thu monthly, single pattern → flat fields)  [x] hashCash (HC £0 → unverified, blank+flag)
- [x] description  [x] source live-verified (HC `global-runs`, 32 rows)  [x] history depth/pagination assessed (30 counted #30→#59, complete)
- [x] coord sanity checked (all clean London)  [x] end times noted (none)  [x] kennelCode collision-checked (`eels-h3` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — backfill present + HC `getEvents` is future-only, so without it `reconcile.ts` false-CANCELs the 30 backfilled past rows on the next scrape (Bandung contract).
- **Rejecting upstream coords needs `dropCachedCoords: true`** — n/a here (coords clean), but keep in mind if a future scrape returns a bad pin.
- **Self-host tokenized/CDN logos** into `public/kennel-logos/eels-h3.<ext>`; confirm the extension via `curl -sI` + magic bytes (HC blobs have shipped as AVIF/WebP despite `.png` names).
- **`friendlyKennelName` check** — shortName "EELS H3" is 7 chars (>4) → returns shortName directly; synth "EELS H3 Trail #N" is clean. No fix needed. (The ≤4-char trap that bit H7 does not apply.)
- **PII scrub the backfill `description` bodies** — grep `@`/phone patterns, redact before committing the frozen JSON.
- **Sonar** — the backfill is a dumb JSON loader; no regex/complexity concerns. Keep any UK URL literals `https://` in tests.
- **Away-weekend `#49` dup** — drop the two `IsCountedRun=0` rows (2025-08-16/17 Rochester); keep the counted 2025-09-04 #49.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
