# Onboarding Handoff — Divahhh (Brussels, Belgium) — 2026-07-12

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/divahhh-20260712`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **0 `region.ts` edits** —
>    Belgium COUNTRY + **Brussels** METRO are already seeded (`src/lib/region.ts:1871`/`:1883`,
>    `Europe/Brussels`, amber palette). Nothing to add.
> 3. Configure the adapter — **pure config-only HARRIER_CENTRAL** (no new adapter code); mirror the
>    **KRASH H3** / **Bandung H3** HC source rows (`prisma/seed-data/sources.ts:8160` / `:7443`).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: dates UTC-noon-safe,
>    `startTime` "HH:MM", `kennelTag` resolves to `divahhh` with no unmatched. **⚠️ Expect 0 events** —
>    at research time HC's future-only `getEvents` returned **0 events** and the global-runs future
>    feed had none either. This is a **recently-active** ~monthly women's hash (last run #44 on
>    2026-07-09, **3 days before this handoff**), NOT a dead one — the next run appears on the first
>    scrape after the kennel posts it. **DO NOT run `npx prisma db seed` here.**
> 5. **Recommended: the Historical backfill one-shot (9 HC runs #36–#44; see below).** Without it the
>    kennel page is empty until the next run posts (currently 0 future events). Keep
>    `config.upcomingOnly: true`.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- <path>` for the seed rows, the
>      backfill script + frozen JSON, and the self-hosted logo).
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod, reverting unrelated prod config drift — barbados #2471/#2463).
>    - Run the backfill: `BACKFILL_APPLY=1 npx tsx scripts/backfill-divahhh-history.ts`.
>    - Trigger a scrape from `/admin/sources` to publish events to prod.
>    - Spot-check `/kennels/divahhh` for the 9 backfilled runs.

## Summary
- Type: **full onboard** (first Brussels women's hash on hashtracks; 4th live Brussels kennel after BMPH3, BruH3, Brussels Blue Moon)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `src/adapters/registry.ts` & feed verified live)
- Effort estimate: **config-only source** + **0 `region.ts` edits** (Brussels METRO under Belgium COUNTRY already seeded) + a **recommended 9-run backfill script**. No new adapter code.
- One-line: Adds Divahhh — a Brussels women's ("finer things in life") Hash House Harriers — via a live, recently-active Harrier Central kennel (~monthly, latest run #44 on 2026-07-09). Bubbly-weekend traveler with away runs across Europe.

## Dedup result
- Kennel in seed: **no** (`grep -in 'divahhh' prisma/seed-data/` → nothing; only `divah3-eh3` = the **distinct** Edmonton "Diva HHH")
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-12, **485 slugs**; **zero** `divahhh` slug. The only `diva` slug is **`divah3`** = **Diva H3 Edmonton** (kennelCode `divah3-eh3`, Canada — confirmed distinct 2026-07-09). Brussels metro already live via `bmph3` + `bruh3`.
- Decision: **full onboard**
- kennelCode: **`divahhh`** (collision check: **clear** — `grep -in 'divahhh' prisma/seed-data/kennels.ts prisma/seed-data/aliases.ts` returns nothing; Edmonton's code is `divah3-eh3`, its aliases are `["Diva HHH", "DivaH3", "Edmonton Diva Hash"]` — none overlap). slug → `divahhh` (`toSlug("Divahhh")`, all-ASCII, free).
  - 🔴 Use kennelCode **`divahhh`** (NOT bare `divah3` — that slug belongs to Edmonton's `divah3-eh3`). This matches the HC `KennelShortName`/`KennelSlug` = "Divahhh".

## Live source verification  ✅ (recently-active — 0 upcoming, ~monthly, last run 3 days ago)
- Source: **HARRIER_CENTRAL** — `harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, `publicKennelId: 0ed7a8e6-3371-41cf-a539-753f3c07e944` (uniqueShortName `Divahhh`). Verified live 2026-07-12 by (a) **HC `getEvents` future feed** (`hashruns.org/api/global-runs?isFuture=1` → **0** Divahhh rows) AND (b) sweeping `hashruns.org/api/global-runs?isFuture=0` over 2024→2026-07 (returned the 9 rows below; 2024 & pre-2026 windows returned 0 → the kennel **joined HC ~Jan 2026**).
- Events seen: **0 via future feed** (no upcoming) + **9 total in the HC past-runs feed** (2026-01-01 → 2026-07-09). Each window returned `totalMatchingEvents === runs.length` (no truncation).
- **Source-count parity:** N/A (platform UI shows **0 upcoming** — HC `getEvents` future-only and empty at research time). Applied the Step 3 **recently-active** rule instead: last run **#44 "Pick Up Hash" 2026-07-09 (Thu)** is **3 days ago**, and the kennel ran in **every month Jan→Jul 2026** (#36 Jan, #37 Feb, #38 Mar, #39–#41 Apr, #42 May, #43 Jun, #44 Jul) → strongly recently-active, onboard normally with the 9-run backfill.
- **Recently-active evidence (VERBATIM from `global-runs?isFuture=0` — proves live, not stale):**
  - **#44** "Pick Up Hash" — **2026-07-09 (Thu) 19:30** — Leopold II Laan 55, 1080 Brussel — 50.85999/4.34282 — price 10/20 — hares "Will be pulled from a hat!" *(placeholder → drop)*
  - **#43** "Wine-a-thon! " — **2026-06-27 (Sat) 08:00** — Avenue General Dossin de Saint-Georges — 50.8095/4.3876 — 10/20 — hares "Multiple Hares " *(placeholder → drop)*
  - **#42** "Bold Divas!" — **2026-05-23 (Sat) 15:00** — Forêt De Soignes — 50.77575/4.39037 — 10/20 — hares "Rack of Lamb"
  - Cadence: ~monthly, day/time varies (mostly Saturday afternoon; Europe/Brussels).
- Sample events (VERBATIM from the HC feed — titles/hares kept as-is incl. trailing spaces):
  1. **#44** — "Pick Up Hash" — 2026-07-09 (Thu) 19:30 — "Leopold II Laan 55, 1080 Brussel" (Brussels) — 50.85999/4.34282 — 10/20 — 🔴 hares "Will be pulled from a hat!" = **placeholder → drop hares**
  2. **#43** — "Wine-a-thon! " — 2026-06-27 (Sat) 08:00 — "Avenue General Dossin de Saint-Georges" (Brussels) — 50.8095/4.3876 — 10/20 — 🔴 hares "Multiple Hares" = **placeholder → drop hares**
  3. **#42** — "Bold Divas!" — 2026-05-23 (Sat) 15:00 — "Forêt De Soignes" (Brussels) — 50.77575/4.39037 — 10/20 — hares "Rack of Lamb"
  4. **#41** — "DIVA H3 Bubbly Hangover Trail" — 2026-04-19 (Sun) 10:00 — "Tribe Carcassonne" (**Carcassonne, FR — away weekend**) — 43.21034/2.35771 — 10/20 — hares "Cheesy Lollipop, I Like your Boobs"
  5. **#40** — "DIVA H3 Bubbly Weekend - Runners" — 2026-04-18 (Sat) 09:30 — "Tribe Carcassonne" (FR) — 43.21034/2.35771 — **0/0 (free)** — hares "Horny Tail, John Cleese"
  6. **#39** — "DIVA H3 Bubbly Weekend - Walkers" — 2026-04-18 (Sat) 09:30 — "Tribe Carcassonne" (FR) — 43.21034/2.35771 — **0/0 (free)** — hares "Horny Tail, John Cleese"
  7. **#38** — "DIVA Invades SNONADS" — 2026-03-03 (Tue) 16:00 — "Das Alphenhaus Kaprun" (**Kaprun, Austria — ski/away**) — 47.2728/12.7605 — **0/0 (free)** — hares "Joystick, Flaming Banana"
  8. **#37** — "Febrewery Valentines Run " — 2026-02-14 (Sat) 12:30 — "Buizingen Station" (Halle, BE) — 50.7506/4.2581 — 10/20 — hares "Cockatool"
  9. **#36** — "New Years Run! " — 2026-01-01 (Thu) 12:03 — "Leiden" (**Leiden, NL — away**) — 52.16109/4.49015 — 10/20 — 🔴 hares "Leiden" = **location-bleed → drop hares**
- History depth / pagination: **9 events total in the HC feed** (2026-01-01 → 2026-07-09), all captured. **HC coverage starts at the kennel's HC-join (~Jan 2026)** — the kennel's *bubbly-weekend* history goes back to 2009 (see metadata), but those are on the Webnode site, **not** archived on this HC source. Single feed, date-windowed via `minEventDate`/`maxEventDate`; use `scripts/lib/hc-global-runs.ts` (mirror `backfill-krashh3-history.ts`).
- Coord sanity: **clean, but geographically wide by design.** Brussels-area home runs (#42/#43/#44) + Belgium suburb (#37 Halle) sit in the Brussels bbox; **#36 (Leiden, NL), #38 (Kaprun, AT), #39–#41 (Carcassonne, FR) are legit AWAY runs** (bubbly/ski weekends — corroborated by the kennel's Events archive). **🔴 Do NOT apply a Belgium bbox scrub** — it would wrongly drop the real away-run pins. No repeated default pin; every coord is distinct and real. (Contrast Rio/KRASH which *do* bbox-scrub — Divahhh is a traveler, so pins legitimately span 4 countries.)
- End times: none (HC exposes start only).
- Notes: **tz — HC `KennelIANATimezone` = `Europe/Berlin` (a quirk); the Brussels METRO is correctly `Europe/Brussels`.** No source-config override needed — display tz comes from the METRO record (same pattern as KRASH's Warsaw→Berlin note), and both zones share the identical UTC+1/+2 offset so event times are unaffected. Config-only; no browserRender/auth. HC `global-runs` carries **no currency field** — prices are bare numbers `10/20`; infer **EUR (€10/€20)** from the country (flag "confirm at build").
- **Field-fill assertion table** (from the 9 swept rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 9 / 9 | Real event names (themes) / HC adapter sets `title` from `EventName`; `defaultTitle:"Divahhh"` synthesizes "Divahhh #N" only for empty/placeholder names. |
  | `startTime` | 9 / 9 | From `EventStartDatetime` (varies: 19:30/08:00/15:00/10:00/09:30/16:00/12:30/12:03). |
  | `endTime` | 0 / 9 | Not in feed — accept absence. |
  | `location` (venue name) | 9 / 9 | `LocationOneLineDesc` (verbatim). |
  | `locationStreet` | 0 / 9 | Not populated — leave undefined. |
  | `locationUrl` (Maps) | 0 / 9 | Not in feed — coords carry the pin. |
  | `hares` | 6 / 9 | `Hares` field; **drop #44 "Will be pulled from a hat!" + #43 "Multiple Hares" (placeholders) + #36 "Leiden" (location-bleed)**; #37/#38/#39/#40/#41/#42 are real hash names. |
  | `cost` | 9 / 9 (as number) | HC price `10/20` on home runs; **#38/#39/#40 = `0/0` (free away/ski events)** → per-event `cost`; kennel-level `hashCash` "€10 members / €20 non-members" (currency inferred EUR — flag). |
  | `description` | (present in HC) | Live HC adapter does **not** emit `description`; **omit in the backfill too** (Bandung/KRASH precedent) so a backfilled row fingerprints identically to a live scrape. |
  | `trailLengthText` | 0 / 9 | Not in feed. |
  | `coords` (lat/lng) | 9 / 9 | `Latitude`/`Longitude` all populated & distinct (incl. legit away-run pins); **no bbox scrub** — the away runs are real. |

## Kennel metadata (deep-dive complete)
- fullName: **Divahhh — Brussels Women's Hash House Harriers** / shortName: **Divahhh** / region: **Brussels** / country: **Belgium**
  - (HC `KennelName`/`KennelShortName`/`KennelSlug` all = "Divahhh"; `KennelContinent` = "Europe". Source: `hashruns.org/api/global-runs` 2026-07-12. ⚠️ No *formal* full name is published — "Divahhh" already encodes "Diva Hash House Harriers"; the descriptive fullName above disambiguates from Edmonton's "Diva Hash House Harriers".)
- aliases: `["Divahhh", "Diva H3 Brussels", "Brussels Diva H3", "Diva Hash Brussels"]` — **all clear** (grep-checked 2026-07-12; 0 global collisions). 🔴 **Omit bare `"DivaH3"` and `"Diva HHH"`** — both belong to Edmonton's `divah3-eh3` (global alias namespace).
- website: **`https://divahhh.webnode.page/`** (Webnode; **verified live** 2026-07-12 via fetch — SSR HTML renders; meta-description "Hash House Harriers who prefer the finer things in life").
- facebook / instagram / twitter / discord: **none found** → blank + flag. (The Webnode About page carries no social links; a `/contact/` + `/our-people/` page exist but weren't exhaustively scraped — ⚠️ Claude Code may check `/contact/` at build.)
- schedule: **~monthly, day/time varies (mostly Saturday afternoon)** — evidenced by the 9 HC runs. Flat fields (not `scheduleRules` — irregular single-pattern), Saturday chosen as the modal day (6 of 9 runs):
  ```ts
  // scheduleDayOfWeek/scheduleTime are the fallback; per-run day+time flow from the HC feed.
  scheduleDayOfWeek: "Saturday", // modal day (6/9 runs); day varies month to month
  scheduleTime: "3:00 PM", // 12-hr per kennels.ts convention; per-run times vary (HC startTime stays 24-hr)
  scheduleFrequency: "Monthly",
  ```
  (source: 9 HC runs, 2026-01-01 → 2026-07-09.)
- foundedYear: **~2009** (source: `divahhh.webnode.page/events/` — earliest documented Divahhh weekend is "2009 – Champagne"; the archive then runs 2012 Prosecco → 2014 → 2016 → 2018 → 2020/2022 → 2024 → 2026). ⚠️ **Flag** — this is the earliest *documented event*, not a stated founding year; the group may predate it. Do not assert with false precision — set `foundedYear: 2009` with the caveat in the PR.
- hashCash: **"€10 members / €20 non-members"** (HC `EventPriceForMembers`/`ForNonMembers` = `10`/`20` on the Brussels-area home runs #37/#41/#42/#43/#44; the away campout/ski events #38/#39/#40 were `0/0` = free — those are per-event overrides, not the kennel default). Currency **inferred EUR** from Belgium (HC carries no currency field). ⚠️ flag "confirm currency/amount at build".
- dogFriendly: unknown → omit (unverified).
- walkersWelcome: **true** (evidence: run **#39 "DIVA H3 Bubbly Weekend - Walkers"** is an explicit walkers split alongside #40 "…- Runners"). Reasonable to set; flag if you prefer to omit.
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/divah3.avif`** → ⚠️ **self-host** to `public/kennel-logos/divahhh.<ext>` (HC blob; confirm the extension via **magic bytes**, NOT the `.avif` name — HC blobs are often WebP/AVIF/PNG regardless of the URL suffix). Reference `/kennel-logos/divahhh.<ext>`.
- lat/lng: **50.8503 / 4.3517** (Brussels city centre — matches the Brussels METRO centroid 50.85/4.35).

## Region seeding — 0 `region.ts` edits
> 🟢 **Belgium COUNTRY + Brussels METRO are both already seeded** (`src/lib/region.ts:1871` Belgium / `:1883` Brussels, both `Europe/Brussels`, amber palette). `COUNTRY_INFERENCE_RULES` already covers `belgium|brussels|bruxelles|…` (`:3946`); `STATE_GROUP_MAP "Brussels":"Belgium"` (`:4237`); `COUNTRY_GROUP_MAP "Belgium":"Belgium"` (`:4483`); `COUNTRY_CODE_TO_NAME BE:"Belgium"` (`:4638`). **Nothing to add** — `region: "Brussels"` on the kennel resolves cleanly. (Live siblings BMPH3 / BruH3 already use this metro.)

## Historical backfill
- Available: **9 past runs** in the HC feed (2026-01-01 → 2026-07-09). Fields: date/title/venue(9)/coords(9)/hares(6 of 9 — 3 placeholder/bleed)/price(9). No standalone `description` emitted; `cost` from HC price (kennel-level `hashCash`). **No HC archive before ~Jan 2026** (HC-join window); the kennel's 2009→ bubbly-weekend history lives only on the Webnode site (not a scrapeable events feed), so this backfill covers only the HC-join window.
- Plan: **recommended** one-shot `scripts/backfill-divahhh-history.ts` — **frozen-JSON + dumb loader** (Bandung/KRASH/H7 convention: extract once, commit `scripts/data/divahhh-history.json`, no parser committed). Extraction recipe (mirror `scripts/backfill-krashh3-history.ts` + the shared `scripts/lib/hc-global-runs.ts`):
  - Sweep `global-runs?isFuture=0&minEventDate=2026-01-01&maxEventDate=<today>` via `sweepGlobalRuns` (windows + halves on 500), filter client-side to `PublicKennelId 0ed7a8e6-3371-41cf-a539-753f3c07e944`.
  - Map with `mapRunToRawEvent` (title-fallback + `composeHcLocation` parity). **Trust `EventNumber` for `runNumber` (36–44), NOT any title digits.**
  - 🔴 **Drop the `hares` on #44 ("Will be pulled from a hat!"), #43 ("Multiple Hares") — placeholders — and #36 ("Leiden") — location-bleed** (== the `location` string). Keep #37/#38/#39/#40/#41/#42.
  - 🔴 **NO bbox scrub** — this is a traveling women's hash; the away-run coords (#36 Leiden NL, #38 Kaprun AT, #39–#41 Carcassonne FR) are **real**. A Belgium-only bbox would wrongly drop 5 of 9 runs. (This is the deliberate exception to the Rio/KRASH bbox-scrub rule.)
  - Apply a sane **06:00–23:59 time-gate** (all 9 sampled times pass: 08:00–19:30); **omit `description`** for live-adapter fingerprint parity.
  - **#39 & #40 share a date/time/venue (2026-04-18 09:30, Tribe Carcassonne) but have distinct `EventNumber`s (39 vs 40) and distinct titles (Walkers vs Runners)** → they fingerprint as two separate events. Keep both (they are two logged HC runs, not a duplicate).
  - Freeze to `scripts/data/divahhh-history.json`; loader binds to the `"Divahhh Harrier Central"` source, `kennelTimezone: "Europe/Brussels"`.
- 🔴 **`config.upcomingOnly: true` is REQUIRED** (Bandung/KRASH contract): HC `getEvents` is future-only, so without it `reconcile.ts` false-CANCELs the 9 backfilled past rows as they age off the 365-day window. Keep it in the seed below regardless of whether the backfill ships.
- **Frozen-dataset validation checklist** (run before shipping): PII scrub (grep `@`/phone shapes — none expected); run numbers monotonic (36→44); gap-sanity (~monthly spacing, no misparse); field-bleed check on `hares` (#36/#43/#44 already flagged) and `location`.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "divahhh", // clear; Edmonton's is divah3-eh3 (distinct)
  shortName: "Divahhh",
  fullName: "Divahhh — Brussels Women's Hash House Harriers",
  region: "Brussels", // Brussels METRO already seeded (0 region.ts edits)
  country: "Belgium",
  website: "https://divahhh.webnode.page/", // Webnode; verified live 2026-07-12
  // No socials found on the About page → omit facebookUrl/instagramHandle (⚠️ check /contact/ at build if desired)
  logoUrl: "/kennel-logos/divahhh.<ext>", // self-hosted from HC blob; verify ext by magic bytes
  scheduleDayOfWeek: "Saturday", // modal day (6/9 runs); day varies
  scheduleTime: "3:00 PM", // 12-hr per kennels.ts convention; per-run times vary
  scheduleFrequency: "Monthly",
  scheduleNotes: "Brussels women's hash — 'Hash House Harriers who prefer the finer things in life.' Roughly monthly trails (day and time vary), often around the Forêt de Soignes, plus an annual 'bubbly weekend' away trip (2026: Carcassonne; past: Bairrada, Penina, Sekt, Cava, Prosecco, Champagne).",
  hashCash: "€10 members / €20 non-members", // HC prices on home runs; away/ski events were free — ⚠️ confirm currency at build
  walkersWelcome: true, // evidenced by run #39 "…- Walkers" split
  // foundedYear: 2009 — earliest documented weekend (divahhh.webnode.page/events); ⚠️ may predate, flag in PR
  foundedYear: 2009,
  description: "Divahhh — a Brussels-based women's Hash House Harriers 'who prefer the finer things in life.' Roughly monthly trails around Brussels with a champagne-and-fizz theme, plus a long-running annual 'bubbly weekend' that travels across Europe (Carcassonne, Bairrada, Kaprun, and more).",
  latitude: 50.8503, longitude: 4.3517,
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug)
"divahhh": ["Divahhh", "Diva H3 Brussels", "Brussels Diva H3", "Diva Hash Brussels"],

// sources.ts — Source[] (array of objects). Mirror the KRASH H3 / Bandung H3 HC rows.
{
  name: "Divahhh Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live 2026-07-12 via the HC future feed [0 posted]
    // + hashruns.org/api/global-runs [9 past runs #36–#44]). HC's KennelIANATimezone is
    // Europe/Berlin for this kennel — a data quirk; the Brussels metro is (correctly)
    // Europe/Brussels (same offset, so times are unaffected — no source override needed).
    publicKennelId: "0ed7a8e6-3371-41cf-a539-753f3c07e944",
    defaultKennelTag: "divahhh",
    // Divahhh names events with real themes ("Bold Divas!", "Wine-a-thon!") kept verbatim;
    // defaultTitle synthesizes "Divahhh #N" only for empty/placeholder names.
    defaultTitle: "Divahhh",
    staleTitleAliases: ["Placeholder event for Divahhh"],
    // upcomingOnly:true — REQUIRED (HC getEvents is future-only + this source owns a 9-run
    // backfill; without it reconcile.ts false-CANCELs the past rows as they age off the
    // 365-day window — Bandung/KRASH contract).
    upcomingOnly: true,
  },
  kennelCodes: ["divahhh"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only HARRIER_CENTRAL — zero new adapter code.** The `HARRIER_CENTRAL` adapter already
exists in `src/adapters/registry.ts` (`src/adapters/harrier-central/adapter.ts` + `token.ts`) and is
used by ~35 seeded kennels (KRASH, Bandung, Sembach, BEER H3, Rio H3, Taiwan H3, …). The source row above
is the whole live implementation; the backfill reuses `scripts/lib/hc-global-runs.ts`. The adapter derives
events from HC's `getEvents` API (future-only), resolves `divahhh` via `defaultKennelTag`, and applies
`defaultTitle`/`staleTitleAliases` for name synthesis.

**⚠️ Claude Code: verify before writing/relying on any field.** This is config-only, so the main checks are:
- Confirm `publicKennelId` returns Divahhh's run(s) via a throwaway `adapter.fetch(source)`. **Expect 0
  events** — no future run was posted at research time (last run #44 was 3 days ago on a monthly cadence).
  If a new run (#45+) has since been posted it will appear. **Do not treat 0 future events as a failure** —
  the backfill supplies the page's content and the daily scrape auto-adds the next run once posted.
- Confirm `kennelTag` resolves to `divahhh` with **no unmatched** and `startTime` is "HH:MM".
- Confirm the display tz is **`Europe/Brussels`** (from the Brussels METRO) — HC's feed says `Europe/Berlin`
  (a quirk), but both share the identical UTC+1/+2 offset so run times are unaffected; **no** source-config
  tz override is needed (same as KRASH's Warsaw→Berlin note).
- `RawEventData` field names are `kennelTags: string[]` (not `kennelTag`); `walkersWelcome` (not
  `walkerFriendly`). Don't hand-set `title` — the HC adapter + `defaultTitle` handle it.
- **`friendlyKennelName("Divahhh", …)`** — shortName is 7 chars (>4), so it short-circuits to "Divahhh"
  (synth title "Divahhh Trail #N"). No verbose-synthesis risk.

## Sibling sweep
- Swept the HC `global-runs` feed (future + 2024→2026 past) for other Brussels / Diva kennels: **none** share
  Divahhh's `publicKennelId` and no additional Divahhh-branded kennel surfaced.
- Other Brussels kennels are **already live**: **BMPH3** (`bmph3`), **BruH3** (`bruh3`) — separate sources,
  not fed by this GUID. **Brussels Blue Moon H3** (`bbmh3-be`) is seeded (GOOGLE_CALENDAR). No overlap —
  Divahhh is a distinct women's kennel with its own GUID. Do NOT fold any of them into this source.
- **Edmonton "Diva HHH"** (`divah3-eh3`) is an unrelated women's hash in Canada — same "Diva" theme, distinct
  kennel; its slug `divah3` is why we use kennelCode/slug `divahhh` here.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host + magic-byte ext)  [x] foundedYear (2009 — earliest documented weekend, flagged)  [ ] socials (none found — flagged; check /contact/ at build)  [x] schedule (~monthly Sat + scheduleNotes)  [x] hashCash (€10/€20 — currency inferred, flagged)
- [x] description  [x] source live-verified (recently-active; #44 3 days ago, 9-run HC archive)  [x] history depth assessed (9 on HC; HC-join ~Jan 2026; 2009+ weekend history only on Webnode)
- [x] coord sanity checked (all 9 real & distinct; away runs legit → NO bbox scrub)  [x] end times noted (none)  [x] kennelCode collision-checked (`divahhh` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **Recently-active, NOT stale** — 0 future events at research time is expected; the kennel ran #44 three
  days ago and every month Jan→Jul 2026. Ship the backfill so the page isn't empty; the daily scrape adds
  #45 when posted.
- **`config.upcomingOnly: true` required** (Bandung/KRASH contract) — HC is future-only; protects the 9
  backfilled past rows from `reconcile.ts` cancellation.
- **NO bbox scrub in the backfill** — Divahhh travels; #36 (Leiden NL), #38 (Kaprun AT), #39–#41
  (Carcassonne FR) are real away-run pins. A Belgium-only bbox would drop 5 of 9 events. This is the
  deliberate exception to the Rio/KRASH bbox-scrub convention.
- **Drop 3 placeholder/bleed hares** — #44 "Will be pulled from a hat!", #43 "Multiple Hares", #36 "Leiden"
  (== location). Keep the 6 real ones. Trust `EventNumber` for `runNumber`.
- **#39/#40 are a Walkers/Runners split of the same slot** — distinct run numbers + titles → two events,
  not a duplicate. Keep both.
- **Away/free events (#38/#39/#40 = 0/0)** → per-event `cost` (free); kennel-level `hashCash` stays
  "€10 members / €20 non-members". HC prices are currency-free numbers — infer EUR (Belgium); confirm at
  build (Algarve rule).
- **Omit `description` in the backfill** — the live HC adapter doesn't emit it; including it would
  re-fingerprint every row on the first live re-scrape.
- **Self-host the HC blob logo** to `public/kennel-logos/divahhh.<ext>`; confirm ext by magic bytes (the
  `.avif` URL name is not authoritative — HC blobs are often WebP/PNG).
- **0 region.ts edits** — Brussels METRO under Belgium COUNTRY is fully seeded; `region: "Brussels"` resolves.
- **No admin-seeded events to purge** — the kennel isn't live (no `divahhh` slug in the sitemap), so there
  are no pre-existing prod `Event` rows under `divahhh` to dedup against.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
