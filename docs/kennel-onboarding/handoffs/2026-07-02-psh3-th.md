# Onboarding Handoff — Pranburi H3 (Hua Hin, Thailand) — 2026-07-02

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/psh3-th-20260702`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **No `region.ts` edits** —
>    the "Hua Hin" region already exists and is fully wired (verified below).
> 3. Configure the **config-only `HARRIER_CENTRAL`** source exactly as shown (mirror the Bandung H3
>    source row `prisma/seed-data/sources.ts:7157`). No new adapter code.
> 4. **Re-confirm dedup at build** (this research run could not drive Chrome — see Dedup result):
>    open `https://www.hashtracks.xyz/kennels/pranburi-h3` and confirm it 404s (not live). `psh3` is a
>    DIFFERENT live kennel (Puget Sound, Seattle) — do not confuse it.
> 5. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` — call
>    `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. The Azure HC host is reachable from your
>    local env (it is NOT from the research sandbox). ⚠️ **0 upcoming is EXPECTED** — this is a new,
>    ~monthly kennel between postings (recently-active: last run #5 on 2026-06-07; see sample). Validate:
>    events (if any) are UTC-noon, `startTime` "HH:MM", `kennelTag` resolves (`psh3-th`, no unmatched).
>    An empty upcoming set at build is fine; the daily scrape picks up the next run once posted.
> 6. **Historical backfill (recommended — the page is empty without it):** one-shot
>    `scripts/backfill-psh3-th-history.ts` from `hashruns.org/api/global-runs?isFuture=0` (see
>    **Historical backfill**). Only ~5 runs exist (#1→#5), so the archive is shallow — but with 0
>    upcoming, the backfill is what populates the kennel page on day one. Because a backfill is present,
>    `config.upcomingOnly: true` is **REQUIRED** (Bandung #2340 contract). If you decide ~5 rows aren't
>    worth a script, **remove `upcomingOnly` from the source config** (live-only HC omits it).
> 7. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 8. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 9. **Post-merge runbook (separate step, after PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge sometimes drops a follow-up commit):
>      `git log -1 -- scripts/backfill-psh3-th-history.ts` (+ the seed files). Recovery PR if missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's subset**
>      over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source whose
>      seed-data differs from prod, which can revert unrelated production config drift — barbados #2471/#2463 lesson).
>    - Trigger a scrape from `/admin/sources` to publish events to prod
>    - Spot-check `/kennels/pranburi-h3` for the backfilled runs (#4 on 2026-05-10, #5 on 2026-06-07) + schedule.

## Summary
- Type: **full onboard**
- Adapter: `HARRIER_CENTRAL` (**config-only — adapter exists (`src/adapters/harrier-central/adapter.ts`) & feed verified live**)
- Effort estimate: **config-only** (zero new adapter code) + **0 `region.ts` edits** (Hua Hin region exists) + a small one-shot backfill script (~5 rows)
- One-line: Adds **Pranburi H3**, a new monthly-Sunday trail in Pranburi (~25 km south of Hua Hin on Thailand's Prachuap Khiri Khan coast), via the same config-only Harrier Central pattern as Bandung/Barbados/Moonshine/Hua Hin Full Moon. Run by the same expat "Moonies" crew as the already-live Cha-Am/Hua Hin kennels (shared hares) — fills out the Hua Hin cluster.

## Dedup result
- Kennel in seed: **no** (`grep -in "psh3\|pranburi" prisma/seed-data/*.ts` → the only `psh3` hits are **Puget Sound H3 (Seattle)**: `kennels.ts:2553` `kennelCode:"psh3"`, `aliases.ts:267`, `sources.ts:2915/2944/2989/2991`; no "Pranburi" anywhere)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live as of 2026-07-01** — Chrome MCP read of `hashtracks.xyz/sitemap.xml`, **466 slugs**, no `pranburi` / `pranburi-h3` / `psh3-th` slug; the live `psh3` slug is **Puget Sound H3 (Seattle)**, a distinct kennel. ⚠️ **This research run (2026-07-02) could not re-read the sitemap** — the Chrome MCP requires interactive browser selection and no user is present in a scheduled run. The 1-day-old sitemap read + the seed-file confirmation that `psh3` = Puget Sound stand; **Claude Code must re-confirm at build** (step 4: `/kennels/pranburi-h3` 404).
- Decision: **full onboard**
- kennelCode: `psh3-th` (collision check: bare `psh3` is **Puget Sound H3 (Seattle)** → suffixed with `-th`; `psh3-th`/`pranburi`/`pranburi-h3` all grep-clean. Matches the HC `KennelSlug` "PSH3-TH".)
- Pre-onboarding admin-event check: no prior `Event` rows expected (kennel absent from sitemap). Claude Code: confirm no admin-seeded `psh3-th` events before first scrape; none anticipated.

## Live source verification  ✅ (recently-active — 0 upcoming, past runs captured VERBATIM)
- Source: `HARRIER_CENTRAL` — `publicKennelId: e717ca00-fdc4-4e3f-a90d-7faab04adbe9` (shortName `PSH3`, slug `PSH3-TH`).
  Discovery/verification feed: `https://hashruns.org/api/global-runs` (web_fetch, 2026-07-02), filtered
  client-side to this GUID. **Adapter path** (`harriercentralpublicapi.azurewebsites.net`) is
  Azure-allowlist-blocked from the research sandbox → ⚠️ **Claude Code must reconfirm `adapter.fetch(source)`
  from local env** (routine for HC; the host is reachable there).
- Events seen: **0 upcoming** (checked `?isFuture=1` on 2026-07-02 — Pranburi's GUID/name is ABSENT while
  dozens of other kennels' runs are present, i.e. the feed works and Pranburi simply has no posted next run).
  This is the **recently-active** case, not "dead."
- **Recently-active evidence (VERBATIM past-run excerpts — same evidence bar as a "dead" claim):**
  - **#5** — `EventNumber:5`, `EventName:"Pranburi hash #5"`, `EventStartDatetime:"2026-06-07T16:30:00"`
    (`…GmtOffset` GMT `2026-06-07T09:30:00Z`), `KennelIANATimezone:"Asia/Bangkok"` — **Sunday 2026-06-07**.
  - **#4** — `EventNumber:4`, `EventName:"Pranburi Hash #4"`, `EventStartDatetime:"2026-05-10T16:30:00"` —
    **Sunday 2026-05-10**.
  - **Cadence:** both runs fall on **Sundays, 16:30, exactly 28 days apart → monthly Sunday afternoon.**
    Latest run **2026-06-07 = 25 days before this research run (2026-07-02)** — well within 2× a monthly
    interval (~8 weeks) ⇒ **recently active**. Brand-new kennel (only ~5 runs total).
- **Source-count parity:** **N/A** — platform UI shows 0 upcoming for this kennel; applied the Step 3
  recently-active rule instead (past-run cadence sample above), per the manual.
- Sample events (VERBATIM from `global-runs` — real, not constructed):
  1. **#5** — "Pranburi hash #5" — **Sun 2026-06-07 16:30** (Asia/Bangkok) — hares field **"Tap Thai lake"**
     ⚠️ (this equals `LocationOneLineDesc` — HC `Hares` **location-bleed**, NOT a real hare name; treat as
     unreliable) — location "Tap Thai lake behind huay Wat mongkol, soi 112" (from `EventDescription`;
     "4.15 for 4.30 start", on-after "mango restaurant") — price **200 members / 100 non-members** —
     **no coords**.
  2. **#4** — "Pranburi Hash #4" — **Sun 2026-05-10 16:30** — hares **"Penny lame, knockout neptune"**
     (real — and identical to Hua Hin Full Moon H3 #45's hares → confirms shared "Moonies" organizers) —
     location "Pranburi central" — coords **12.359534671365676, 99.9666479856192** (real Pranburi pin) —
     price **200 / 100**.
- History depth / pagination: HC adapter is future-only. `hashruns.org/api/global-runs?isFuture=0` serves
  the HC archive. Only **~5 past runs** exist (#1→#5, monthly, kennel started ~early 2026). Very shallow.
  See **Historical backfill**. (Windowed pull: `isFuture=0` **requires** `minEventDate`+`maxEventDate`
  and **ignores `publicKennelId`** → filter client-side by `PublicKennelId`; response rows under `.runs`.
  The global feed caps ~200 rows/window, so a monthly kennel is easily crowded out of a wide window — use
  **tight (≤2-week) windows** per month to reliably catch each Pranburi run.)
- Coord sanity: **mixed** — #4 carries a real Pranburi pin (12.36, 99.97); #5 has none. For the **backfill**,
  apply the Thailand country bounding-box scrub (lat 5.5–20.5, lng 97–106) and re-geocode from venue text
  where absent (as with Barbados / Hua Hin Full Moon).
- End times: **none** (HC exposes start only) → `endTime` undefined.
- Timezone: HC `KennelIANATimezone` = **`Asia/Bangkok`** — **correct** (no Bandung-style quirk); the existing
  "Hua Hin" region is already `Asia/Bangkok`.
- Notes: Not JS-rendered / no auth. Config-only HC. 🔴 **HC `Hares` is unreliable for this kennel** — #5's
  `Hares` is the venue name (location-bleed); #4's is real. The live HC adapter emits **no `description`/`cost`**
  (verified PR #2484), so the only ingest path for the `EventDescription` blurbs is the backfill — omit
  `description` there to match live output (and it also avoids any PII; the shared "Moonies" crew's other
  kennel, h2fmh3, carried bank-account PII in a description, so **PII-strip any description you keep**).
- **Field-fill assertion table** (from the 2 sampled past runs):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 2 / 2 | HC `EventName` = "Pranburi hash #N" — kennel-name + run-number (inconsistent case "hash"/"Hash"), NOT a real theme. `defaultTitle:"Pranburi H3"` + `staleTitleAliases` fire only on empty/exact-placeholder names, so these pass through verbatim. ⚠️ Cosmetic only; if you'd rather normalize to synthesized "Pranburi H3 #N", confirm whether the HC adapter has a name+number strip — otherwise accept the source names. |
  | `startTime` | 2 / 2 | "16:30" from `EventStartDatetime` (local). |
  | `endTime` | 0 / 2 | HC has no end field — accept absence. |
  | `location` (venue) | 2 / 2 | `LocationOneLineDesc` ("Pranburi central" / "Tap Thai lake"). |
  | `locationStreet` | 0 / 2 | not in HC feed. |
  | `locationUrl` (Maps) | 0 / 2 | not a discrete field (Maps links sit inside descriptions). |
  | `hares` | 1 / 2 | #4 real ("Penny lame, knockout neptune"); #5 is location-bleed → **HC `Hares` unreliable here; verify per row, drop when it equals `LocationOneLineDesc`.** |
  | `cost` | n/a | Live HC adapter emits **no per-event `cost`** — the flat fee lives on `Kennel.hashCash`; backfill omits `cost` to match live output. |
  | `description` | n/a | Live HC adapter emits **no `description`** — backfill omits it (matching live output; also avoids any PII). |
  | `trailLengthText` | 0 / 2 | not in feed. |
  | `coords` (lat/lng) | 1 / 2 | #4 real (12.36, 99.97); #5 none. Backfill: bbox-scrub Thailand + re-geocode from venue. |

## Kennel metadata (deep-dive complete)
- fullName: **Pranburi Hash House Harriers** / shortName: **Pranburi H3** / region: **Hua Hin** / country: **Thailand**
  (source: HC `KennelName` = "Pranburi Hash H3", `KennelShortName` = "PSH3", `hashruns.org/api/global-runs` 2026-07-02)
  - 🔴 shortName is **"Pranburi H3"** (>4 chars → `friendlyKennelName` synthesizes clean "Pranburi H3 #N";
    do NOT use HC's "PSH3" as shortName). slug = `toSlug("Pranburi H3")` = **`pranburi-h3`** (no accents → no
    explicit `slug:` override needed).
- aliases: `["Pranburi H3", "PSH3-TH", "Pranburi Hash", "Pranburi Hash House Harriers"]`
  (`PSH3-TH` = HC `KennelSlug`). 🔴 **Do NOT add bare `"PSH3"`** — it belongs to **Puget Sound H3 (Seattle)**
  (`aliases.ts:267`). All four listed aliases are grep-clean.
- website / facebook / instagram / twitter / discord: **unverifiable from a working source — leave blank + flag.**
  HC is the only confirmed live source; no own dynamic site found. Claude Code may check `hashruns.org/PSH3-TH/about`
  for a FB/website link at build. (The Cha-Am/Hua Hin day kennel `cah3` is a *different* kennel — do not attribute here.)
- schedule: **Monthly**, **Sunday**, **16:30** (from HC #4 Sun 2026-05-10 16:30 + #5 Sun 2026-06-07 16:30, 28 days
  apart). Description note: "4.15 for 4.30 start" (gather 16:15, run 16:30). 🔴 `scheduleTime` is 12-hour in
  `kennels.ts` → **"4:30 PM"**; `scheduleFrequency: "Monthly"`. Day-of-month drifts (#4 = 2nd Sun, #5 = 1st Sun),
  so keep the weekday but add a `scheduleNotes` describing "monthly Sunday afternoon" rather than a fixed week.
- foundedYear: **2026** — inferred: run **#5** in June 2026 on a ~monthly cadence ⇒ #1 ≈ early 2026; a brand-new
  kennel. Confidence **medium** — flag "inferred from run number + cadence; confirm at build if a primary source surfaces."
- hashCash: **"฿200 members / ฿100 non-members"** (source: HC `EventPriceForMembers`=200 / `EventPriceForNonMembers`=100
  on both #4 and #5; currency THB inferred from country — HC has no currency field). ⚠️ **members > non-members is
  unusual** (fees usually run the other way) — likely a source data-entry swap; flag "verify at build; sibling `cah3`
  is ฿100 adults." Self-validated: no double-symbol / repetition typos.
- dogFriendly / walkersWelcome: unknown → leave blank.
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/PSH3-TH_20260421224824.avif`** (HC `KennelLogo`).
  🔴 **AVIF** on a platform-CDN blob → **self-host** to `public/kennel-logos/psh3-th.<ext>`; confirm the real
  extension by **magic bytes** (`avif` / `RIFF…WEBP` / `\x89PNG`), do NOT hard-code `.avif`/`.png`.
- description (short about-us): *"Pranburi Hash House Harriers runs monthly on Sunday afternoons in Pranburi,
  just south of Hua Hin on Thailand's Prachuap Khiri Khan coast — bamboo trails, pineapple fields and sea-view
  viewpoints, with an on-after at a local lakeside restaurant. A new kennel started in 2026 by the same
  full-moon 'Moonies' crew behind the area's other trails."* (synthesized from verified region + cadence +
  #4's description; no unverified claims)
- lat/lng: **12.36, 99.97** (Pranburi, from #4's real pin).

## Region — NO `region.ts` edits required (verified)
The **"Hua Hin"** region already exists and is fully wired (confirmed in `src/lib/region.ts`, 2026-07-02):
- `REGION_SEED_DATA` — "Hua Hin" METRO record present (**L2955**), `timezone: "Asia/Bangkok"`, centroid
  **12.57, 99.96**, aliases `["Hua Hin, Thailand", "Hua Hin, TH"]`.
- `STATE_GROUP_MAP` — `"Hua Hin": "Thailand"` (**L4050**).
- `COUNTRY_GROUP_MAP` — `"Hua Hin": "Thailand"` (**L4351**).
- `COUNTRY_INFERENCE_RULES` — Thailand rule already covers `hua hin` (**L3841**).
- Thailand COUNTRY + code-map already seeded. → **0 edits.**

**Metro decision:** Pranburi (~12.36, 99.97) is ~25 km south of the Hua Hin centroid, same province cluster
(Prachuap Khiri Khan / greater Hua Hin), and the runs describe themselves as "just south of Hua Hin." Group it
under the existing **"Hua Hin"** METRO (mirrors the Hua Hin Full Moon H3 decision, 2026-07-01) rather than
minting a Pranburi metro. The kennel seed carries `region: "Hua Hin", country: "Thailand"` explicitly, so
country inference isn't relied on. (Optional robustness: append `pranburi` to the Thailand
`COUNTRY_INFERENCE_RULES` regex at L3841 — NOT required, since the seed is explicit.)

## Historical backfill
- Available: **~5 past runs** (#1 → #5, monthly, ~early 2026 → 2026-06-07) via `hashruns.org/api/global-runs?isFuture=0`
  — fields per row: date, run #, EventName, Hares (unreliable — see Notes), LocationOneLineDesc, lat/lng (some),
  member/non-member price, description. Shallow archive (brand-new kennel).
- Plan: **one-shot `scripts/backfill-psh3-th-history.ts`** (per the Bandung/Barbados/Hua Hin Full Moon pattern).
  **Recommended** — with 0 upcoming at build, the backfill is what gives the kennel page its schedule + recent
  runs on day one; the daily scrape then auto-adds the next run once posted. Because it is present,
  **`config.upcomingOnly: true` is REQUIRED** (else `reconcile.ts` false-CANCELs the aged past rows the
  future-only adapter never re-confirms). 🔴 If you skip it (only ~5 rows), **remove `upcomingOnly`** (live-only HC omits it).
  - `isFuture=0` **requires** `minEventDate`+`maxEventDate` (HTTP 400 without) and **ignores `publicKennelId`**
    → filter client-side by `PublicKennelId === "e717ca00-fdc4-4e3f-a90d-7faab04adbe9"`. **Global feed caps ~200
    rows/window** → use **tight (≤2-week) windows per month** so this monthly kennel isn't crowded out; dedup by `PublicEventId`.
  - Response shape: rows under **`.runs`** (`const arr = raw.runs ?? (Array.isArray(raw) ? raw : [])`).
  - 🔴 **Coord bbox scrub (Thailand ≈ lat 5.5–20.5, lng 97–106):** drop out-of-box lat/lng → merge re-geocodes from venue text.
  - 🔴 **`Hares` field:** drop when it equals `LocationOneLineDesc` (location-bleed, e.g. #5 "Tap Thai lake").
  - 🔴 **Omit `description`** to match live-adapter output (also strips any bank/phone PII the "Moonies" crew embeds).
  - 🔴 **Time gate 06:00–20:00:** drop AM/PM-typo start times to `undefined` (don't fabricate a corrected time).
  - Freeze the curated output as `scripts/data/psh3-th-history.json` + a dumb loader (H7 pattern); don't commit the parser.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects). Insert near the other Thailand kennels (cah3, crh3, h2fmh3).
{
  kennelCode: "psh3-th",
  shortName: "Pranburi H3",
  fullName: "Pranburi Hash House Harriers",
  region: "Hua Hin",
  country: "Thailand",
  // website/facebook/etc. intentionally omitted — no verified dynamic own-site (HC-only). Add at build if found.
  logoUrl: "/kennel-logos/psh3-th.<ext>", // 🔴 self-host the HC AVIF blob; confirm <ext> by magic bytes
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "4:30 PM", // 12-hr in kennels.ts; RawEventData.startTime stays 24-hr "16:30"
  scheduleFrequency: "Monthly",
  scheduleNotes: "Monthly Sunday afternoon trail (~16:15 gather for 16:30 start) in Pranburi, just south of Hua Hin.",
  hashCash: "฿200 members / ฿100 non-members", // ⚠️ HC #4 & #5; members>non-members is unusual — verify at build
  foundedYear: 2026, // inferred from run #5 (Jun 2026) on a monthly cadence — new kennel; confirm at build
  description: "Pranburi Hash House Harriers runs monthly on Sunday afternoons in Pranburi, just south of Hua Hin on Thailand's Prachuap Khiri Khan coast — bamboo trails, pineapple fields and sea-view viewpoints, with an on-after at a local lakeside restaurant. A new kennel started in 2026 by the same full-moon 'Moonies' crew behind the area's other trails.",
  latitude: 12.36, longitude: 99.97,
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]). Key is the KENNELCODE, not the slug.
"psh3-th": ["Pranburi H3", "PSH3-TH", "Pranburi Hash", "Pranburi Hash House Harriers"],

// sources.ts — Source[] (array of objects). Mirror the Bandung H3 HC row (sources.ts:7157).
{
  name: "Pranburi H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    publicKennelId: "e717ca00-fdc4-4e3f-a90d-7faab04adbe9",
    defaultKennelTag: "psh3-th",
    // HC names events "Pranburi hash #N" (kennel-name + run-number, inconsistent case) — kept verbatim.
    // defaultTitle synthesizes "Pranburi H3 #N" only for empty/placeholder names (mirrors Bandung/Barbados).
    defaultTitle: "Pranburi H3",
    staleTitleAliases: ["Placeholder event for PSH3-TH"],
    // REQUIRED because this source owns a one-shot historical backfill (Bandung #2340 contract):
    // the future-only HC adapter would otherwise let reconcile.ts false-CANCEL aged backfilled rows.
    // 🔴 If you SKIP the backfill, REMOVE this line (live-only HC omits upcomingOnly).
    upcomingOnly: true,
  },
  kennelCodes: ["psh3-th"],
},
```

## Adapter notes / new-scraper plan
**Config-only — no adapter code.** `HARRIER_CENTRAL` already exists (`src/adapters/harrier-central/adapter.ts`,
registered in `registry.ts`) and is proven on Bandung/Barbados/Moonshine/Hua Hin Full Moon/Taiwan/Lisbon/Porto/Shanghai.
The `publicKennelId` GUID filter is the stable key. The Lisbon-era `GEOCODE_FAIL_SENTINELS` + `composeHcLocation`
placeholder handling already ship in the adapter — no new sentinels needed.

**⚠️ Claude Code: verify before writing/relying on any code.**
- Confirm the source config against the **current** `HarrierCentralConfig` type (adapter fields `publicKennelId`,
  `defaultKennelTag`, `defaultTitle`, `staleTitleAliases`) — mirror `sources.ts:7157` exactly. ⚠️ **`upcomingOnly`
  is NOT a `HarrierCentralConfig` field** — it's a `Source.config` flag read by the scrape pipeline
  (`scrape.ts` → `reconcileStaleEvents`), not enforced by the adapter's typed config. Set it on the source row.
- Live-verify `adapter.fetch(source)` — **0 upcoming is expected** (new monthly kennel between postings). If the
  next run has posted by build time, confirm it resolves `kennelTag: psh3-th`, `startTime "16:30"`, UTC-noon date.
- The adapter emits **no `description`/`cost`** (verified PR #2484) → the backfill must omit both to match live output.
- Backfill script: `safeFetch` (not raw `fetch`), `.runs` unwrap, client-side `PublicKennelId` filter, **tight
  ≤2-week windows** (global cap ~200), Thailand bbox coord scrub, `Hares`-equals-location drop, `description` omit,
  06:00–20:00 time gate, freeze to `scripts/data/psh3-th-history.json`.

## Sibling sweep
The HC source (`publicKennelId` filter) feeds **only `psh3-th`**. The Hua Hin cluster's other kennels are
**distinct kennels with their own run-number sequences**, NOT siblings on this source:
- **Cha-Am / Hua Hin H3 (`cah3`, slug `h2h3-cha-am-h3`)** — already live (WordPress/static + STATIC_SCHEDULE).
- **Hua Hin Full Moon H3 (`h2fmh3`)** — handed off 2026-07-01 (`handoffs/2026-07-01-h2fmh3.md`), own HC kennel.
Pranburi shares organizers/hares (the "Moonies" — #4's hares "Penny lame, knockout neptune" match h2fmh3 #45) but
is its own HC kennel with its own numbering. No unmapped siblings on this source.

## Deep-dive checklist (nothing deferred)
- [x] logo (AVIF platform blob → flag self-host, magic-byte ext)  [x] foundedYear (2026, inferred — flagged)  [x] socials (none verified → blank+flag)  [x] schedule (monthly Sunday 16:30 + scheduleNotes)  [x] hashCash (฿200/฿100 — members>non-members flagged)
- [x] description  [x] source live-verified (0 upcoming + recently-active #4/#5 captured verbatim; Azure feed → Claude Code reconfirm)  [x] history depth/pagination assessed (~5 via global-runs isFuture=0; tight windows)
- [x] coord sanity checked (#4 real, #5 none; bbox-scrub for backfill)  [x] end times noted (none)  [x] kennelCode collision-checked (`psh3-th`; bare `psh3`=Puget Sound)  [x] kennelCodes source guard set (`["psh3-th"]`)  [x] region.ts (0 edits — Hua Hin exists)

## Implementation gotchas (repo knowledge)
- **`config.upcomingOnly: true` REQUIRED with the backfill** (Bandung #2340 contract) — future-only HC adapter +
  a past-run backfill means reconcile false-CANCELs the archive without it. Remove it only if you drop the backfill.
- **HC `Hares` is unreliable for this kennel** — #5's `Hares` is the venue (location-bleed). Drop `Hares` when it
  equals `LocationOneLineDesc`; keep real names (#4).
- **HC `global-runs` past rows: coord default-pin + AM/PM-time typos** — bbox-scrub (Thailand) + 06:00–20:00 gate.
- **Global feed caps ~200 rows/window** — a monthly kennel needs **tight (≤2-week) windows** or it's crowded out
  (a 3-month window returned 200 rows and 0 Pranburi in this research run; a 5-day window found it).
- **Self-host the AVIF logo**, confirm ext by magic bytes (not Content-Type).
- **`friendlyKennelName`**: shortName "Pranburi H3" is >4 chars → synthesized titles are clean ("Pranburi H3 #N");
  no `fullName`-derivation risk. (Do NOT set shortName to HC's "PSH3" — 4 chars would trigger fullName derivation.)
- **PII**: sanitize any `description` carrying bank/account/phone (the shared "Moonies" crew embedded bank PII on h2fmh3 #45).

---
_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
