# Onboarding Handoff — SH2B / Seattle Hash House Bikers (Seattle, WA) — 2026-07-18

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/sh2b-20260718`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **0 `region.ts` edits** —
>    SH2B is a Seattle-metro kennel and reuses the existing **"Seattle, WA"** METRO (confirmed
>    seeded — `psh3`/`seamon-h3` already use `region: "Seattle, WA"`).
> 3. Configure the adapter exactly as in **Adapter notes** — this is **pure config-only
>    HARRIER_CENTRAL** (the adapter already exists at `src/adapters/harrier-central/adapter.ts`,
>    registered in `registry.ts`). Mirror the Hamburg H7 / Lisbon HC source rows in `sources.ts`
>    (`sources.ts:4099` / `:4119`).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md`
>    — call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Because SH2B shows **0
>    upcoming** at research time (recently-active seasonal-monthly — see below), the live
>    `getEvents` path may return 0 events; that is EXPECTED and not a failure. Confirm the adapter
>    resolves `kennelTag: "sh2b"` with no unmatched, and that the historical backfill script
>    (step 5) yields the 15 counted runs. **DO NOT run `npx prisma db seed` here.**
> 5. Apply the **Historical backfill** one-shot (`scripts/backfill-sh2b-history.ts`) — 15 counted
>    runs #46→#60 via `global-runs?isFuture=0`. Worth it (clean, monotonic, real Seattle venues).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- <path>` for the seed edits
>      AND `scripts/backfill-sh2b-history.ts` / `scripts/data/sh2b-history.json` — squash merges
>      have silently dropped follow-up commits before).
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds the new kennel/alias/source)
>    - Run the backfill one-shot, then trigger a scrape from `/admin/sources`.
>    - Spot-check `hashtracks.xyz/kennels/sh2b` for ~15 historical events (monthly, #46→#60) + the
>      next run appearing on the first scrape after SH2B posts it to HC.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists & feed verified via `hashruns.org/api/global-runs`)
- Effort estimate: **config-only** (mirror the Hamburg H7 / Lisbon HC source row) + a ~15-run frozen-JSON backfill one-shot (mirror `backfill-sembach-h3-history.ts` / `backfill-eels-h3-history.ts`)
- One-line: Seattle's **bike hash** ("a drinking club with a cycling problem") — a clean seasonal-monthly (Sunday, April–October) Puget Sound bike-bash with a 15-run HC record #46→#60; a distinct discipline (bike hash) not otherwise covered in the well-populated Seattle metro.

## Dedup result
- Kennel in seed: **no** (`grep -in "sh2b\|Seattle Hash House Bikers" prisma/seed-data/*` → 0 matches)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-18 (**485 slugs**); **no `sh2b` / `seattle-bike` / `bikers` slug**. The bike-hash slugs present (`bike-hash`, `melbourne-bike-hash`, `sin-city-bike-hash`) are Stuttgart/Melbourne/Las-Vegas, none Seattle. Per-slug check: `/kennels/sh2b` renders a hard **404** (Chrome-rendered, not just curl).
  - 🔴 **NOT to be confused with South Sound H3** — that sibling *is* already live (`ssh3-wa`, shortName `SSH3`, slug `ssh3`, Olympia WA; `kennels.ts:2742`). South Sound surfaced in the same HC recent-past sweep and was correctly excluded as already-live. SH2B is the Seattle **bike** hash (`SH2B`), a different kennel.
- Decision: **full onboard**
- kennelCode: `sh2b` (collision check: **clear** — grep-clean in `kennels.ts` + `aliases.ts`; bare `SH2B` unused globally; distinct from `sh3-wa`/`ssh3`/`ssh3-wa`)
- Pre-onboarding admin-event check: no partial/live page exists (404) → no admin-seeded `Event` rows to dedup/purge. (Claude Code: re-confirm at build if desired.)

## Live source verification  ✅
- Source: **HARRIER_CENTRAL** — `publicKennelId: 72a5b2f7-0942-4157-b301-557701eb242e` (shortName `SH2B`, HC slug `SH2B`). Enumerated via `hashruns.org/api/global-runs` from the Chrome extension (same-origin fetch on `hashruns.org`), 2026-07-18.
- Events seen: **15 HC rows** (all counted) — **#46 2024-04-21 → #60 2026-06-14**; **0 upcoming** on the future feed at research time.
- **Source-count parity:** HC future feed (`isFuture=1`) shows **0 upcoming** for SH2B → parity N/A; applied the **Step 3 recently-active rule** instead (see below). The past feed is a **complete run of the kennel's HC lifetime** (#46→#60, 15 consecutive counted runs, no gaps in-season, no dups) — not a sparse subset.
- **Recently-active rationale (0 upcoming, NOT blocked):** SH2B is a **seasonal monthly bike hash** — it runs one Sunday per month **April → October** and is dormant in winter. Evidence (verbatim HC run dates, all Sundays):
  - **2024 season:** #46 Apr-21, #47 May-12, #48 Jun-09, #49 Jul-21, #50 Aug-18, #51 Sep-15 (6 runs, monthly Apr–Sep)
  - **2025 season:** #52 Apr-27 ("2025 Reboot"), #53 May-18, #54 Jun-15, #55 Jul-20, #56 Aug-17, #57 Sep-21, #58 Oct-19 (7 runs, monthly Apr–Oct)
  - **2026 season (in progress):** #59 May-24 ("Tour de Franzia Season Opener" — desc: *"opening the season of bashing. We're a month late this year"*), #60 Jun-14 ("June Bash")
  - Last run **#60 2026-06-14 (34 days ago)** = ~1.2× the ~monthly in-season interval; ran in **both** of the last two in-season months (May + June 2026); consistent monthly cadence across **two full prior seasons**. Far exceeds the "≥3 of last 4 (in-season) months" bar. This is a robust recurring cadence, not a thin single-datapoint (contrast the Rio Full Moon / Big Lik holds).
  - Independently corroborated by the kennel's Facebook page **facebook.com/HashBikersWA** (25 followers, "Hash Bikers WA"): a recent post reads *"The next ride scheduled will be August 20th. It will be an over nighter. Have 12 rooms booked…"* — i.e. the kennel is actively organising upcoming rides that simply aren't posted to HC yet.
  - **Onboard with `upcomingOnly:true` + a 15-run recent-history backfill; the next run (#61) appears on the first scrape after SH2B posts it to HC (expect a July/August 2026 in-season run).**
- Sample events (VERBATIM from `global-runs`, most-recent first):
  1. 2026-06-14 12:00 — **"SH2B #60 (June Bash)"** — hares **Cougar Rican** — **South Kirkland Park & Ride**, Bellevue WA (47.64438, −122.19636) — cost m/nm 5/5
  2. 2026-05-24 12:00 — **"SH2B#59 (Tour de Franzia Season Opener)"** — hares **Hood Whorenament** — **sbəq̓ʷaʔ (sbaqwah) Diagonal Ave Park & Shoreline Habitat**, Seattle (47.56048, −122.34355) — cost 0/0
  3. 2025-10-19 12:00 — **" SH2B #58 (Tour De Franzia)"** — hares **Hood Whorenament** (leading space in EventName — trim)
  4. 2025-09-21 10:00 — **"SH2B #57 (Sail Away & Ride)"** — hares **Dim Sum** — **Seattle Water Taxi Pier** (47.601553, −122.3379358) — cost 0/0
  5. 2024-04-21 12:00 — **"SH2B #46 (Tour de Franzia)"** — hares **Cougar Rican & Hood Whorenament** — **Northshore Athletic Fields**, Woodinville WA (47.7334089, −122.1445727) — cost 1/1 (oldest HC row)
- History depth / pagination: **15 HC rows total, #46→#60**, reachable in full via 6-month `global-runs?isFuture=0&minEventDate/maxEventDate` windows filtered on `PublicKennelId`. Runs **#1–#45 predate the kennel's HC join (before 2024-04) and are NOT on the feed** — no deeper HC backfill possible. (FB page has older photos but is not a structured backfill source; skip.)
- Coord sanity: **clean** — all 15 pins fall inside the Puget Sound / Greater Seattle bbox (lat 47.43–47.82, lng −122.35…−121.77); **0 default/garbage pins**, 0 missing coords. No `dropCachedCoords` needed, but keep the standard fail-safe bbox scrub (WA: lat 47.0–48.3, lng −122.7…−121.5) as a precaution.
- End times: none exposed (HC carries only start). Accept absence.
- Notes: tz field on HC = **`America/Los_Angeles`** (correct — **no HC quirk**). Prices: small bike-hash fees, HC price fields range **0–5 USD** across rows (mostly 5) → the HC price field is unreliable per platform notes → **hashCash unverified, flag** (do not assert a value). Times vary **10:00–14:00** local (typically noon). Logo = `bundle://C-180` — an **app bundle asset, NOT a fetchable URL** → **no logo available** (see metadata).
- **Field-fill assertion table** (from the 5 sample rows above):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 5 / 5 | HC `EventName` verbatim ("SH2B #60 (June Bash)" etc.) — all real, keep; **trim leading/trailing spaces** (#58 has a leading space); merge synthesizes only if ever blank |
  | `startTime` | 5 / 5 | `HH:MM` from `EventStartDatetime` (10:00–14:00, typically 12:00) |
  | `endTime` | 0 / 5 | accept absence |
  | `location` (venue name) | 5 / 5 | `LocationOneLineDesc` ("South Kirkland Park & Ride", "Seattle Water Taxi Pier") |
  | `locationStreet` | ~2 / 5 | `LocationStreet` present on some rows — map when non-empty, else leave undefined |
  | `locationUrl` (Maps) | 0 / 5 | not a structured field — leave undefined |
  | `hares` | 5 / 5 | `Hares` field — CSV; leave `undefined` on blank (not `null` — blank means "not provided", not an explicit clear) |
  | `cost` | 0 / 5 | HC price 0–5 USD unreliable → leave undefined (do NOT synth a value) |
  | `description` | ~4 / 5 | `EventDescription` (ride notes, venue address) — 🔴 scan for PII (addresses/emails) before backfill (see gotchas) |
  | `trailLengthText` | 0 / 5 | not in feed |
  | `coords` (lat/lng) | 5 / 5 | `Latitude`/`Longitude` (clean Seattle-metro pins) |

## Kennel metadata (deep-dive complete)
- **fullName:** Seattle Hash House Bikers  (source: HC `KennelName` via hashruns.org/api/global-runs)
- **shortName:** SH2B  (source: HC `KennelShortName`)  — 🔴 **length 4 → NOT >4**, so `friendlyKennelName` does **not** short-circuit to the shortName; it derives from `fullName`. Because every HC `EventName` here is real, the synth title never fires in practice — but **run the ≤4-char check at build** (see gotchas) and set `defaultTitle` so any future placeholder synthesizes cleanly.
- **region:** Seattle, WA  (reuse existing METRO — **0 region.ts edits**; venues span Seattle/Bellevue/Kenmore/Woodinville/Lynnwood/North Bend, all Greater-Seattle)
- **country:** USA
- **aliases:** ["SH2B", "Seattle Hash House Bikers", "Hash Bikers WA", "Seattle Bike Hash"]  (bare `SH2B` grep-clean globally; 🟡 Claude Code: quick `grep -in '"<alias>"' prisma/seed-data/aliases.ts prisma/seed-data/kennels.ts` on each of the descriptive aliases before commit and drop any global collision)
- **website:** **none** → blank + flag. (The FB "Contact info" lists `gargoyle@hashbikers.com`, but **`hashbikers.com` DNS-resolves to a parked "for sale" HugeDomains page** — verified 2026-07-18, Status 0 / `54.243.117.197` — so it is NOT the kennel's live site; do NOT cite it. The Seattle-area hub `wh3.org` — used as `website` by the sibling running kennels — may list SH2B's schedule but was not confirmed to carry a dedicated SH2B page, so leave `website` null.)
- **facebook:** https://www.facebook.com/HashBikersWA/  (verified live 2026-07-18 — "Hash Bikers WA", 25 followers, active ride posts; facebook.com is a known platform, no DNS check needed)
- **instagram / twitter / discord:** none found → blank
- **schedule:** **monthly, Sunday, ~12:00 (noon), April–October season** (dormant Nov–Mar)  (source: all 15 HC run dates are Sundays in Apr–Oct; times 10:00–14:00). Day-of-month varies (2nd/3rd/4th Sunday — NOT a fixed ordinal), so a flat `scheduleDayOfWeek`/`scheduleTime` is the right representation (no `scheduleRules` — an RRULE would over-fit the varying ordinal). Capture the seasonal window in `scheduleNotes`.
  - `scheduleDayOfWeek: "Sunday"`
  - `scheduleTime: "12:00 PM"`  (🔴 12-hour in the Kennel profile field; the adapter's `RawEventData.startTime` stays 24-hour verbatim from HC, e.g. "12:00"/"10:00"/"14:00")
  - `scheduleFrequency: "Monthly"`
  - `scheduleNotes: "Seasonal bike hash — one Sunday per month, roughly noon, April through October; dormant in winter."`
- **foundedYear:** **unverified** → leave blank + flag. (HC #46 @ 2024-04; extrapolating ~6–7 runs/yr backward puts #1 ≈ 2017–2018, but that is inference, not a cited fact — per the manual, do not assert. If a "Run #1" / anniversary reference surfaces on the FB page or wh3.org at build, record it with the citation.)
- **hashCash:** **unverified** → blank + flag. (HC price fields = 0–5 USD across rows, mostly 5; the HC price field is unreliable — likely ~$5 pay-at-ride, but not confirmed by a citable source.)
- **dogFriendly / walkersWelcome:** n/a — this is a **bike** hash (participants ride); leave blank.
- **description:** "Seattle's bike hash — a 'drinking club with a cycling problem'. Monthly Sunday bike-bashes around the Puget Sound area through the April–October season."  (paraphrased from the FB page + HC run data; keep short)
- **logoUrl:** **none available** → leave blank + flag. HC returns `KennelLogo: "bundle://C-180"` — an in-app bundle asset, **not** a `harriercentral.blob.core.windows.net` URL and **not web-fetchable**. Do NOT invent a logo URL. If a logo is wanted, Claude Code can pull one from the FB page at build (optional) and self-host to `public/kennel-logos/sh2b.<ext>` (confirm ext via magic bytes) — otherwise ship without a logo.
- **lat/lng:** Greater Seattle — use ~47.61, −122.33 (Seattle centroid), or inherit the "Seattle, WA" METRO centroid; venues span Woodinville → North Bend → Bellevue.

## Historical backfill
- Available: **15 counted runs** (#46 2024-04-21 → #60 2026-06-14) — fields: date, title (`EventName`), hares, venue (`LocationOneLineDesc` + partial `LocationStreet`/`City`), coords, some descriptions. All 15 `IsCountedRun=1`, monotonic #46→#60, no dups, clean Seattle-metro coords, times 10:00–14:00 (all within the 06:00–21:59 gate).
- Plan: **one-shot `scripts/backfill-sh2b-history.ts`** — worth it (clean 2-season record; the kennel page should show real history immediately). Set source `config.upcomingOnly: true` so `reconcile.ts` doesn't false-cancel the aged-out backfill rows.
- **Preferred pattern (freeze a curated dataset + dumb loader):** pull the 15 rows via `global-runs?isFuture=0`, curate to a frozen `scripts/data/sh2b-history.json`, commit the JSON + a dumb loader `scripts/backfill-sh2b-history.ts` (mirror `backfill-sembach-h3-history.ts` / `backfill-eels-h3-history.ts`). Do NOT commit the parser.
- **Frozen-dataset validation (run before shipping):**
  1. **PII scrub** — grep the `description` bodies for `@` (emails) and phone-like patterns; redact. (SH2B descriptions include venue street addresses, e.g. #60 "10610 NE 38th Pl, Kirkland" — a public venue, low PII risk, but scrub emails/phones anyway.)
  2. **Run-number continuity** — #46→#60 is one-per-in-season-month; the winter gaps (Oct→Apr) are EXPECTED (seasonal) — do NOT flag them as misparses. Within a season, flag any step whose day-gap is <20d or >40d (none expected).
  3. **Gap sanity** — each event date within −1…+60d of its slot; catches misparse (none expected — HC serves ISO datetimes).
  4. **Monotonicity** — run numbers non-decreasing over time (clean; #46…#60 with no dups).
  5. **Field bleed** — `hares`/`location` strings containing `:` may indicate a missed boundary (none seen; hares are clean names like "Cougar Rican", "Hood Whorenament").
- **Title hygiene:** trim leading/trailing whitespace on `EventName` (#58 = `" SH2B #58 (Tour De Franzia)"`). Keep the verbatim theme names otherwise.

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` keyed by **kennelCode** — confirmed via `head prisma/seed-data/aliases.ts` (`export const KENNEL_ALIASES: Record<string, string[]> = { "nych3": [...], ... }`). NOT an object array.

```ts
// kennels.ts — Kennel[] (array of objects). Append one entry (near the other "Seattle, WA" kennels):
{
  kennelCode: "sh2b",
  shortName: "SH2B",
  fullName: "Seattle Hash House Bikers",
  region: "Seattle, WA",           // reuse existing METRO — 0 region.ts edits
  country: "USA",
  facebookUrl: "https://www.facebook.com/HashBikersWA/",
  scheduleDayOfWeek: "Sunday",
  scheduleTime: "12:00 PM",        // 🔴 12-hour in the profile field (HC run times are 10:00–14:00, typically noon)
  scheduleFrequency: "Monthly",
  scheduleNotes:
    "Seasonal bike hash — one Sunday per month, roughly noon, April through October; dormant in winter.",
  description:
    "Seattle's bike hash — a 'drinking club with a cycling problem'. Monthly Sunday bike-bashes around the Puget Sound area through the April–October season.",
  latitude: 47.61, longitude: -122.33,
  // website / logoUrl / foundedYear / hashCash / dogFriendly / walkersWelcome: unverified → omit (leave null/flag)
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]). Add one key:
"sh2b": ["SH2B", "Seattle Hash House Bikers", "Hash Bikers WA", "Seattle Bike Hash"],

// sources.ts — Source[] (array of objects). Append one row (mirror Hamburg H7 sources.ts:4099 / Lisbon :4119):
{
  name: "Seattle Hash House Bikers Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified via hashruns.org/api/global-runs 2026-07-18).
    publicKennelId: "72a5b2f7-0942-4157-b301-557701eb242e",
    defaultKennelTag: "sh2b",
    defaultTitle: "Seattle Hash House Bikers",  // merge fallback for any placeholder — HC EventNames are all real, kept verbatim
    upcomingOnly: true,          // REQUIRED — backfill present + HC getEvents is future-only (reconcile would false-CANCEL the 15 past rows)
    // No staleTitleAliases needed — all 15 HC EventNames are real ("SH2B #N (<theme>)"), no placeholders.
  },
  kennelCodes: ["sh2b"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only** — the `HARRIER_CENTRAL` adapter already exists (`src/adapters/harrier-central/adapter.ts`, registered in `registry.ts`) and drives every HC row above. No new adapter code. The config keys off `publicKennelId` (the GUID) exactly like Hamburg H7 / Lisbon / Bandung. The daily `getEvents` path is future-only (0 upcoming today is expected, and normal between in-season postings / over winter); the backfill script supplies history.

**⚠️ Claude Code: verify before writing real code.** The only code you author is the backfill one-shot (`scripts/backfill-sh2b-history.ts`) + its frozen `scripts/data/sh2b-history.json`. Mirror `backfill-sembach-h3-history.ts` / `backfill-eels-h3-history.ts`. Confirm against current types:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); dates **UTC noon**; `startTime` a `"HH:MM"` string.
- The backfill inserts canonical `Event` rows directly (frozen JSON → dumb loader), same as the Sembach/EELS one-shots — not through the adapter.
- Coords are clean; keep the WA bbox scrub (lat 47.0–48.3, lng −122.7…−121.5) + 06:00–21:59 time gate as fail-safes (no rows should be dropped by them for SH2B).
- Trim whitespace on `EventName` (#58 leading space); keep verbatim theme titles otherwise.
- 🔴 **`friendlyKennelName("SH2B", "Seattle Hash House Bikers")` ≤4-char check** — run it at build:
  ```bash
  npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("SH2B","Seattle Hash House Bikers"));'
  ```
  All HC EventNames are real so the synth title never fires in the current data, but confirm the output is sane (e.g. "Seattle Hash House Bikers Trail #N") in case a future placeholder appears. `config.defaultTitle: "Seattle Hash House Bikers"` already provides a clean fallback.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC `bundle://` asset → no fetchable URL → blank+flag)  [x] foundedYear (unverifiable → blank+flag)  [x] socials (FB `HashBikersWA` only; website parked-for-sale)  [x] schedule (monthly Sunday Apr–Oct, single pattern → flat fields + seasonal note)  [x] hashCash (HC 0–5 USD unreliable → blank+flag)
- [x] description  [x] source live-verified (HC `global-runs`, 15 rows #46→#60)  [x] history depth/pagination assessed (15 counted, complete HC lifetime)
- [x] coord sanity checked (all clean Seattle-metro)  [x] end times noted (none)  [x] kennelCode collision-checked (`sh2b` clear; distinct from live South Sound `ssh3-wa`)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — backfill present + HC `getEvents` is future-only, so without it `reconcile.ts` false-CANCELs the 15 backfilled past rows on the next scrape (Bandung contract).
- **Seasonal dormancy is normal, not staleness** — SH2B does not run Nov–March. Do NOT add reconcile logic that treats the winter gap as the source going dead; `upcomingOnly:true` already prevents false cancellation.
- **`friendlyKennelName` ≤4-char check** — shortName "SH2B" is exactly 4 chars, so it does NOT short-circuit; verify the derived synth title at build (see Adapter notes). `defaultTitle` config provides a clean fallback.
- **Rejecting upstream coords needs `dropCachedCoords: true`** — n/a here (coords clean), but keep in mind if a future scrape returns a bad pin.
- **No logo** — HC logo is `bundle://C-180` (app asset, not a URL). Do NOT fabricate a `harriercentral.blob…` URL. Ship without a logo, or optionally pull one from the FB page and self-host to `public/kennel-logos/sh2b.<ext>` (confirm ext via magic bytes).
- **PII scrub the backfill `description` bodies** — grep `@`/phone patterns, redact before committing the frozen JSON (descriptions carry venue street addresses — public venues, but scrub emails/phones).
- **Sonar** — the backfill is a dumb JSON loader; no regex/complexity concerns. Keep any URL literals `https://` in tests.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
