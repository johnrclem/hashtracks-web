# Onboarding Handoff — Try it Thursdays H3 (Callington/Cornwall, England) — 2026-07-06

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/titsh3-20260706`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Add the **Cornwall METRO**
>    to `src/lib/region.ts` as noted in **Region edits** (UK COUNTRY already exists; NO inference edit).
> 3. Configure the source exactly as in **Adapter notes** — this is **pure config-only
>    HARRIER_CENTRAL** (adapter already exists at `src/adapters/harrier-central/adapter.ts`; no new
>    adapter code).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` from local env (the Azure host `harriercentralpublicapi.azurewebsites.net`
>    is reachable there; it is NOT from the research sandbox). Expect **≥3 upcoming events** (weekly
>    Thursday, very active #1611+). Validate: events non-empty + upcoming, dates UTC-noon, `startTime`
>    `"HH:MM"`, `kennelTag` resolves with no unmatched. **DO NOT run `npx prisma db seed` here.**
> 5. **Historical backfill** — implement `scripts/backfill-titsh3-history.ts` (see **Historical
>    backfill**). This is why `upcomingOnly:true` is set. ~115 counted runs #1496 (2024-04-25) → #1610.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- scripts/backfill-titsh3-history.ts`,
>      the seed edits, region.ts). Squash-merge sometimes drops a follow-up commit — recover if missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod, which can revert unrelated production config drift —
>      barbados #2471/#2463 lesson).
>    - Run `scripts/backfill-titsh3-history.ts` against prod (one-shot) to insert the historical runs.
>    - Trigger a scrape from `/admin/sources` to publish the upcoming events.
>    - Spot-check `/kennels/try-it-thursdays-h3` for ~115 historical + 3 upcoming events + Cornwall region badge.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists & feed verified live 2026-07-06)
- Effort estimate: **config-only source** + **2 `region.ts` edits** (new Cornwall METRO under existing
  UK COUNTRY) + a ~80–120 LoC one-shot backfill script (model on
  `scripts/backfill-bandung-h3-history.ts` / `scripts/backfill-fishhh-history.ts`). No new adapter code.
- One-line: Adds **Try it Thursdays H3 (TITs)** — a very active weekly-Thursday SW-England (Callington,
  Cornwall / Plymouth border) kennel, config-only via Harrier Central, with a ~115-run backfill and
  3 upcoming events.

## Dedup result
- Kennel in seed: **no** (grep `titsh3|tits|try.?it.?thursday` in `prisma/seed-data/` → no matches)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP
  2026-07-06, **480 slugs**; no `try`/`thursday`/`tit`/`callington`/`cornwall`/`swingletree`/`kelly`
  slug. (Nearby SW-England siblings that ARE live: `plympton-h3`, `plymouth`-area kennels.)
- Pre-onboarding admin-event check: kennelCode `titsh3` is brand-new (no seed presence) — no
  pre-seeded admin `Event` rows expected. (Claude Code: confirm `/kennels/try-it-thursdays-h3` 404s at build.)
- Decision: **full onboard**
- kennelCode: `titsh3` (collision check: **clear** — grep-clean in `kennels.ts` + `aliases.ts`;
  bare `tits`/`tit-h3` also unused)

## Live source verification  ✅ (source live; 3 upcoming, weekly Thursday, very active)
- Source: **HARRIER_CENTRAL** — `publicKennelId: 2cc90a71-62e8-40ea-b26e-c79aed6fc417` (shortName
  `TITs`). Verification feed: `hashruns.org/api/global-runs` (public web front-end for the same HC data).
  Adapter path = Azure `getEvents` (future-only), reachable from Claude Code's env (NOT the research sandbox).
- Events seen: **3 upcoming** (`isFuture=1`, filtered client-side by `PublicKennelId`), date range
  **2026-07-09 → 2026-07-23** (#1611–#1613, weekly Thursday 19:30). Plus **~115 counted past runs** via
  `isFuture=0`, range **2024-04-25 (#1496) → 2026-07-02 (#1610)**.
- **Source-count parity:** HC future feed returned all 3 of this kennel's upcoming rows (no pagination
  gap — weekly kennel, small future window). Backfill parity: every 6-month `isFuture=0` window returned
  a full ~23–27-run weekly set (no under-pagination). ✅
- Sample events (**VERBATIM from `hashruns.org/api/global-runs`, 2026-07-06**):
  1. `#1611` 2026-07-09T19:30 — "#1611 Gymslip" — hares "Gymslip" — Kit Hill middle car park — 19:30 — coords 50.5193, -4.2934 (Callington/Cornwall)
  2. `#1612` 2026-07-16T19:30 — "#1612 Shit Name" — hares "Shit Name" — The London Inn — 19:30 — coords 50.3832, -4.0476
  3. `#1613` 2026-07-23T19:30 — "#1613" (empty theme) — hares n/a — (venue TBC) — 19:30
  4. (past) `#1610` 2026-07-02T19:30 — "#1610 Nice Tackle" — hares "Nice Tackle" — Wembury playing fields car park, Barton Close
  5. (past) `#1609` 2026-06-25T19:30 — "#1609 Brillo Pad" — hares "Brillo Pad" — Mutton Cove 🚗 park
- **Naming pattern:** HC `EventName` = **`"#N <HareName>"`** — the hare's hash-name doubles as the
  "theme" (e.g. "#1610 Nice Tackle" where "Nice Tackle" is the hare). This is the kennel's own
  naming, kept verbatim by `applyTitleFallback`; `defaultTitle` synthesizes only for empty-name rows
  (#1613, #1608). ⚠️ Some `Hares` values are the placeholder **`"Required please 🙏🏼"`** (hare TBD,
  e.g. #1606/#1608) — the backfill/live path should treat those as **empty hares**, not a real name.
- History depth / pagination: **~115 counted runs reachable** (#1496 2024-04-25 → #1610 2026-07-02;
  HC-join ≈ April 2024 — windows before 2024-04 return 0). Weekly. Pagination =
  `global-runs?isFuture=0&minEventDate&maxEventDate` in ≤6-month windows, filter `PublicKennelId`
  client-side (the endpoint IGNORES `publicKennelId` and returns ALL kennels' rows). Per-window counts
  observed: 23 (2024-04→09), 26 (2024-10→2025-03), 27 (2025-04→09), 26 (2025-10→2026-03), 14 (2026-04→07-06).
- Coord sanity: **⚠️ 1 bogus pin** — #1567 (2025-09-04) at **50.0101, 8.4548 (Frankfurt, Germany —
  geocode-fail default, must be scrubbed)**. All other sampled coords sit in the SW-England box.
  Backfill MUST apply a **Devon/Cornwall bounding-box scrub** (lat 49.8–51.2, lng −5.8…−3.5) → drops
  #1567; merge re-geocodes from venue text.
- End times: **none** (HC exposes no end timestamp).
- Notes: HC `KennelIANATimezone` = **`Europe/London`** (correct — NO timezone-quirk fix, unlike the
  Belgrade/KRASH `Europe/Warsaw` and Heraultics `Europe/Berlin` HC records). `EventCurrencyType` is
  null on every row and `EventPriceForMembers/ForNonMembers = 0/0` → fees free-or-unset (see hashCash).
  One row (#1608 2026-06-18) carries `07:30` + loc `"Tbc"` — the `"tbc"` location is dropped by the
  adapter's `GEOCODE_FAIL_SENTINELS`; keep the event (it ran) but its `07:30` is ambiguous (real early
  run vs typo) — the 06:00–20:00 time gate keeps it; flag for spot-check.
- **Field-fill assertion table** (from the ~8 most-recent sampled rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 7 / 8 | HC `EventName` "#N `<hare>`" kept verbatim; `defaultTitle` synthesizes "Try it Thursdays H3 #N" for empty rows (#1613/#1608) |
  | `startTime` | 8 / 8 | `HH:MM` from `EventStartDatetime` (all 19:30; #1608 = 07:30, in-gate → kept, flag); 06:00–20:00 gate drops any out-of-range placeholder |
  | `endTime` | 0 / 8 | HC has none — accept absence |
  | `location` (venue) | ~7 / 8 | `LocationOneLineDesc` (pub/car-park names); `"Tbc"` rows drop via sentinel — leave undefined |
  | `locationStreet` | 0 / 8 | not in the `isFuture=1` feed (PascalCase street fields exist on `isFuture=0` — join for backfill parity) |
  | `locationUrl` (Maps) | 0 / 8 | not in feed |
  | `hares` | ~6 / 8 | HC `Hares`; ⚠️ null out placeholder `"Required please 🙏🏼"`; real names double as the title theme |
  | `cost` | kennel default (see hashCash) | fee `0/0` on all rows → free-or-unset; no per-event override |
  | `description` | present but **OMIT** | HC `EventDescription` — never copy (PII / faithful-to-adapter rule) |
  | `trailLengthText` | 0 / 8 | not in feed |
  | `coords` (lat/lng) | ~7 / 8 valid | `Latitude`/`Longitude`; 1 bogus (#1567 Frankfurt) → bbox scrub + venue re-geocode |

## Kennel metadata (deep-dive complete)
- fullName: **Try it Thursdays Hash House Harriers** (source: `hashruns.org/api/global-runs` `KennelName` = "Try it Thursdays H3")
- shortName: **Try it Thursdays H3** (readable form → slug `try-it-thursdays-h3`; the HC acronym
  "TITs" is kept as an alias — mirrors the Fengyuan "FISHHH" → "Fengyuan H3" / Bandung "BHHH2" →
  "Bandung H3" precedent, and avoids a crude slug)
- region: **Cornwall** / country: **UK** (kennel's registered base is Callington, Cornwall; note it
  operationally straddles the Tamar — many venues are Plymouth/Devon, sibling of the already-live
  Plympton H3 `ph3-gb`. Cornwall chosen per queue + kennel identity; Plymouth METRO already exists if
  a reviewer prefers 0 region.ts edits — see Region edits note.)
- aliases (key `titsh3`): `["TITs", "TITS", "Try it Thursdays", "Try It Thursdays H3", "TIT H3"]`
  - `TITs`/`TITS`/`TIT H3` grep-clean in `aliases.ts` + `kennels.ts` (no global collision).
- website: **none found** — flag "unverifiable (HC-only kennel)". DNS-checked candidates
  `tryitthursdays.co.uk`/`.com`, `titsh3.co.uk`, `callingtonhash.co.uk` = **all NXDOMAIN (Status 3)**;
  `tits.org.uk` resolves (Status 0) but is **almost certainly an unrelated org** owning the acronym
  (a3h.org / Hawaii-trade-body lesson) — **do NOT cite it** without a content check. Do NOT substitute
  `hashruns.org/TITs` as the website.
- facebook / instagram / twitter / discord: **none found** — leave blank + flag. (WebSearch was down
  [HTTP 529] and Chrome auto-denied a new search domain with no user present; a UK-based FB group
  likely exists — Claude Code may re-check at build via WebSearch/gotothehash.)
- schedule: **weekly Thursday 19:30** (source: HC feed — every run Thursday `19:30`, #1496→#1613
  unbroken weekly). `scheduleDayOfWeek: "Thursday"`, `scheduleTime: "7:30 PM"` (12-hr for the profile
  field), `scheduleFrequency: "Weekly"`. Single pattern → **no `scheduleRules` needed**.
- foundedYear: **unverifiable** — leave blank + flag. (Run # is deep — #1610 in 2026 — implying a
  ~mid/late-1990s origin if it were always weekly, but numbering history is unknown; do NOT assert a
  year. Claude Code: a milestone-run post or FB "About" may confirm — check at build.)
- hashCash: **unverifiable** — leave blank + flag. (HC `EventPriceForMembers/ForNonMembers = 0/0`,
  `EventCurrencyType` null on every row → "free or simply unset". UK Thursday pub-hashes commonly
  charge a nominal £1–£2; do NOT assert. Confirm at build.)
- dogFriendly / walkersWelcome: **unknown** — leave blank
- logoUrl: `https://harriercentral.blob.core.windows.net/harrier/Try%20it%20Thursdays%20H3.png`
  → **⚠️ self-host** to `public/kennel-logos/titsh3.png` (served `/kennel-logos/titsh3.png`).
  **Magic-byte-confirmed PNG** during research (first 4 bytes `89 50 4E 47`, 417 KB) — the `.png`
  extension is correct here (still re-confirm at build per policy).
- description: "A weekly Thursday-evening Hash House Harriers kennel based around Callington, Cornwall
  and the Plymouth/Tamar-valley border in south-west England." (factual, source-grounded)
- lat/lng: kennel centroid ≈ **50.503, -4.313** (Callington; runs cluster 50.35–50.52 / −4.30…−3.99
  across the Cornwall/Devon border)

## Region edits (`src/lib/region.ts` — UK COUNTRY already exists @ region.ts:58)
Add **Cornwall METRO** only. UK is a country→metro country; the England-outside-London metros use the
**violet** palette (`bg-violet-100 text-violet-700` / `#8b5cf6`) — mirror Plymouth/Newcastle/Lancaster.
**`COUNTRY_INFERENCE_RULES` already covers England** (`region.ts:3893` → `/\b(uk|england|…)\b/`) and the
seed kennel carries an explicit `country: "UK"` → **NO inference edit** (and do NOT add bare `cornwall`
to the rule — it collides with Cornwall, NY and Cornwall, Ontario; Lune Valley/`lancaster` precedent).
`COUNTRY_CODE_TO_NAME` UK/GB already present → NO edit.

1. **`REGION_SEED_DATA`** — add METRO (mirror the Plymouth record @ region.ts:1607):
   ```ts
   {
     name: "Cornwall",
     country: "UK",
     timezone: "Europe/London",
     abbrev: "CWL", // grep-clean; "COR" also free
     colorClasses: "bg-violet-100 text-violet-700",
     pinColor: "#8b5cf6",
     centroidLat: 50.503,
     centroidLng: -4.313, // Callington / Tamar valley
     aliases: ["Cornwall, England", "Callington", "Kernow"],
   },
   ```
2. **`STATE_GROUP_MAP`** (@ region.ts:~4148, next to `"Plymouth": "United Kingdom"`):
   add `"Cornwall": "United Kingdom",`

> Note: `COUNTRY_GROUP_MAP` for UK metros — follow the existing UK pattern in the file (the England
> metros like Plymouth/Newcastle are grouped via the `country` fallback in `groupRegionsByCountry`;
> match whatever Plymouth/Lancaster do — do not invent a new entry if the siblings don't have one).

## Historical backfill
- Available: **~115 counted runs** — fields per row: run number, date, time (all 19:30), name
  ("#N `<hare>`"), hares, venue text, lat/lng (fee 0/0, currency null). Range **#1496 2024-04-25 →
  #1610 2026-07-02**; weekly, essentially unbroken.
- Plan: **one-shot insert** `scripts/backfill-titsh3-history.ts` (model on
  `scripts/backfill-bandung-h3-history.ts` / `scripts/backfill-fishhh-history.ts`). Set source
  `config.upcomingOnly: true` (REQUIRED — the HC adapter is future-only, so without it `reconcile.ts`
  false-CANCELs the entire backfilled archive inside the 365-day window; Bandung #2340 / Asunción contract).
- **Extractor rules (apply all):**
  1. **Devon/Cornwall bounding-box coord scrub** — drop `Latitude`/`Longitude` outside lat 49.8–51.2,
     lng −5.8…−3.5 (drops #1567's Frankfurt pin 50.0101/8.4548 → merge re-geocodes from venue text).
  2. **`IsCountedRun === 0` filter** — drops **#1565 (2025-08-21)** (the one uncounted row in-window);
     also apply a `/^hash cancelled$/i` name guard defensively (none seen here).
  3. **Time gate 06:00–20:00** — keep in-range `startTime`, else `undefined`. All runs are 19:30; #1608
     is `07:30` (in-gate, kept — flag as possible typo).
  4. **Null out placeholder hares** — `Hares === "Required please 🙏🏼"` (and trimmed variants) → empty.
  5. **OMIT `description`** — never copy HC `EventDescription` (PII risk).
  6. **Keep `hares`** verbatim otherwise (real hash-names; they also seed the "#N `<hare>`" title theme).
  7. Response shape: `isFuture=0` returns `{ totalMatchingEvents, runs: [...] }` — read `.runs`
     defensively (`raw.runs ?? (Array.isArray(raw)?raw:[])`); `publicKennelId` is IGNORED by the endpoint
     → filter `PublicKennelId` client-side; page ≤6-month windows; dedup by `PublicEventId`.
  > 🟢 Preferred pattern (H7): freeze a curated `scripts/data/titsh3-history.json` + dumb loader; don't
  > commit the throwaway parser. Run the frozen-dataset validation checklist (PII scrub, run-number
  > monotonicity, gap sanity, field-bleed) before shipping.

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` (kennelCode → string[]). Key by **kennelCode** (`titsh3`),
> NOT the slug — verified against `head prisma/seed-data/aliases.ts`.

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "titsh3",
  shortName: "Try it Thursdays H3",
  fullName: "Try it Thursdays Hash House Harriers",
  region: "Cornwall",
  country: "UK",
  scheduleDayOfWeek: "Thursday",
  scheduleTime: "7:30 PM", // 12-hr profile field; RawEventData.startTime stays 24-hr "19:30"
  scheduleFrequency: "Weekly",
  logoUrl: "/kennel-logos/titsh3.png", // ⚠️ self-host from HC blob (magic-byte-confirmed PNG); re-confirm ext at build
  description:
    "A weekly Thursday-evening Hash House Harriers kennel based around Callington, Cornwall and the Plymouth/Tamar-valley border in south-west England.",
  latitude: 50.503,
  longitude: -4.313,
  // foundedYear / hashCash / website / socials: none verifiable (HC-only kennel) — leave unset + flag
}

// aliases.ts — Record<string, string[]>  (key = kennelCode)
"titsh3": ["TITs", "TITS", "Try it Thursdays", "Try It Thursdays H3", "TIT H3"],

// sources.ts — Source[] (array of objects). Mirror the Bandung HC row (sources.ts:7313).
{
  name: "Try it Thursdays H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    publicKennelId: "2cc90a71-62e8-40ea-b26e-c79aed6fc417",
    defaultKennelTag: "titsh3",
    // TITs names events "#N <hare>" (kept verbatim); defaultTitle synthesizes
    // "Try it Thursdays H3 #N" only for empty-name rows (#1613/#1608).
    defaultTitle: "Try it Thursdays H3",
    staleTitleAliases: ["Placeholder event for TITs"],
    // upcomingOnly:true — the HC getEvents API is future-only and this source owns a
    // ~115-run historical backfill (scripts/backfill-titsh3-history.ts); without this guard
    // reconcile.ts false-CANCELs those past runs as they age off the 365-day window
    // (Bandung #2340 / Asunción contract).
    upcomingOnly: true,
  },
  kennelCodes: ["titsh3"],
}
```

## Adapter notes / config explanation
**Pure config-only HARRIER_CENTRAL** — the adapter already exists
(`src/adapters/harrier-central/adapter.ts`, registered in `registry.ts`). No new adapter code. The
config mirrors the Bandung HC row (`sources.ts:7313`) — the closest analog (HC source + one-shot
backfill + `upcomingOnly:true`).

- `publicKennelId` GUID is the stable filter (verified live via `hashruns.org/api/global-runs`
  2026-07-06 — 3 upcoming #1611–#1613, weekly Thursday 19:30). `defaultKennelTag`/`kennelCodes` = `titsh3`.
- HC event names are `"#N <hare>"` (non-placeholder) so `applyTitleFallback` passes them verbatim;
  `defaultTitle` + `staleTitleAliases` cover the empty-name rows (#1613, #1608).
- Placeholder-venue geocode sentinels (`GEOCODE_FAIL_SENTINELS`, `hcGeocodeFailed`,
  `stripPlaceholderLocation`) already exist in the adapter and cover `"Tbc"` (#1608) on the live path —
  no config needed. **The bogus Frankfurt pin (#1567) is a BACKFILL-path concern only** (the adapter's
  `hcGeocodeFailed` does NOT run on the `global-runs` backfill path — the bbox scrub in the backfill
  extractor is what catches it).

**⚠️ Claude Code: verify before writing the backfill code.** Any snippet here is illustrative; the
authority is the live repo. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`). Check `prisma/schema.prisma` for canonical `Kennel`/`Event` fields.
- Dates as **UTC noon**, `startTime` as `"HH:MM"` string, `cuid()` IDs.
- `friendlyKennelName("Try it Thursdays H3", "Try it Thursdays Hash House Harriers")` — shortName is
  19 chars (>4) so it short-circuits to "Try it Thursdays H3" → synth title "Try it Thursdays H3
  Trail #N" is clean. Confirm at build.
- Backfill: model on `scripts/backfill-bandung-h3-history.ts`; freeze the curated JSON, don't commit
  the parser (H7 pattern).

## Deep-dive checklist (nothing deferred)
- [x] logo (self-host + magic-byte-confirmed PNG)  [~] foundedYear (unverifiable — flagged)  [~] socials (none found — flagged)  [x] schedule (weekly Thursday 19:30)  [~] hashCash (HC fee 0/0 — unverifiable, flagged)
- [x] description  [x] source live-verified (hashruns.org global-runs; 3 upcoming + ~115 past)  [x] history depth/pagination assessed (~115 runs, ≤6mo windows, HC-join ~2024-04)
- [x] coord sanity checked (#1567 Frankfurt pin → bbox scrub)  [x] end times noted (none)  [x] kennelCode collision-checked (`titsh3` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** here (future-only adapter + backfill) — omitting it
  false-CANCELs the whole archive within 365 days (Bandung #2340; HC-batch-10 retro confirms this is
  the RULE for every HARRIER_CENTRAL source that carries a backfill).
- **Backfill bbox coord scrub + `IsCountedRun` filter + placeholder-hares null** as specified above —
  the adapter's own `hcGeocodeFailed`/`dropApiCoords` does NOT run on the `global-runs` backfill path.
- **Rejecting upstream coords needs `dropCachedCoords: true`** if a bad pin was ever stored (re-scrape
  keeps the stale pin otherwise) — relevant only if #1567 is ever ingested before the scrub.
- **Self-host the logo** into `public/kennel-logos/titsh3.png`; re-confirm the extension via Content-Type
  + magic bytes at build (research pull = PNG `89 50 4E 47`).
- **Region: mirror the Plymouth/Newcastle/Lancaster metro exactly** (violet palette) — don't invent
  colors; no trailing-zero numeric literals (Sonar S6749) — `-4.313`/`50.503` are fine.
- **Tests:** `beforeEach(vi.restoreAllMocks)` if you add fetch-count assertions in the backfill test.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the
brief — no separate prompt needed._
