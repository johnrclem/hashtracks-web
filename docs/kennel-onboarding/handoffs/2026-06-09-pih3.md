# Onboarding Handoff — Porto Invicta H3 (Porto, Portugal) — 2026-06-09

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/pih3-20260609`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). This is HashTracks' **second
>    Portugal kennel** (after Lisbon H3, shipped PR #2031). **Portugal COUNTRY + Lisbon METRO already
>    exist in `src/lib/region.ts` on `main`** — do NOT re-add them. You only need to add the **Porto
>    METRO** region + its grouping (see **Adapter notes → Region (NEW METRO under existing country)**).
> 3. Configure the **config-only `HARRIER_CENTRAL`** source exactly as in **Ready-to-paste seed**
>    (the `HarrierCentralAdapter` already exists in `src/adapters/registry.ts` — no new adapter code).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: events non-empty,
>    dates UTC-noon, `startTime` "HH:MM", `kennelTag` resolves to `pih3` with no unmatched. Resolve
>    the one `⚠️ Claude Code must confirm` item in **Live source verification** (the API is
>    allowlist-blocked from the research sandbox — it was verified from a browser page context
>    instead; confirm it fetches from your local env, where the HC API is reachable). **DO NOT run
>    `npx prisma db seed` here.**
>    - ⚠️ **Expect a SMALL upcoming count (1 event at research time).** HC `getEvents` is future-only;
>      the kennel's regular run #36 (2026-06-06) has already aged out, leaving only the special
>      **#28 "Prelude Hash in Porto" (2026-10-07)**. This is a **recently-active** onboard (regular
>      run 3 days ago — see **Live source verification**), not a low-data failure. A non-empty result
>      with #28 = success.
> 5. No historical backfill (none available — see **Historical backfill**).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, run after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge can silently drop a follow-up
>      commit): `git log -1 -- prisma/seed-data/sources.ts`, `… kennels.ts`, `… aliases.ts`,
>      `src/lib/region.ts`, `public/kennel-logos/pih3.<ext>`. Open a small recovery PR if any is missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive; seeds new kennel/alias/source + Porto METRO region)
>    - Trigger a scrape from `/admin/sources` (or the equivalent admin action) to publish events to prod
>    - Spot-check `https://www.hashtracks.xyz/kennels/porto-invicta-h3` for the upcoming event(s) +
>      sample date (2026-10-07 "Prelude Hash in Porto"). More regular biweekly-Saturday runs appear on
>      the first scrape after the kennel posts them to Harrier Central.

## Summary
- Type: **full onboard** (🇵🇹 second Portugal kennel — first **Porto / Northern Portugal** kennel on HashTracks; Lisbon H3 was the first, PR #2031)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `registry.ts` & feed verified live)
- Effort estimate: **config-only** + **Porto METRO** region (Portugal COUNTRY already seeded — 2 small `region.ts` edits + 1 optional inference-regex edit) + self-host one logo. No new adapter code, no backfill script.
- One-line: Porto Invicta H3 (PIH3, est. 2025) is Porto's young, active alternate-Saturday family hash that publishes its schedule to Harrier Central — zero new code, mirrors the Lisbon H3 / Hamburg H7 HC pattern exactly.

## Dedup result
- Kennel in seed: **no** (grep `porto|invicta|pih3|c4a3b093` in `kennels.ts`/`sources.ts`/`aliases.ts` → 0 hits)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP, **434 slugs** (up from 432 on 2026-06-07); **no `porto*`/`invicta*`/`pih3` slug**. Only Portugal-family slug present is `lisbon-h3` (Lisbon H3, now live). No kennel generates slug `porto-invicta-h3`.
- Decision: **full onboard**
- kennelCode: **`pih3`** (collision check: `grep '"pih3"' / porto-invicta` in `kennels.ts` + `aliases.ts` → **clear**, no collision. Matches HC's own `kennelUniqueShortName` "PIH3".)
- slug: **`porto-invicta-h3`** (from shortName "Porto Invicta H3"; free — confirmed sitemap-absent; no accent so no `slug:` pin needed)
- Pre-onboarding admin-event check: kennel has no prod slug/page → no admin-seeded `Event` rows expected. Low risk; Claude Code may spot-check prod for stray `pih3` events before first scrape but none anticipated.

## Live source verification  ⚠️ needs Claude Code to confirm from local env (API allowlist-blocked from research sandbox)
- Source: **HARRIER_CENTRAL** — `https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, filter `publicKennelId: c4a3b093-3e4b-4add-91c0-21ed3fa76322` (`kennelUniqueShortName: "PIH3"`). **All three filters — `publicKennelIds` (GUID), `cityNames:"Porto"`, and `kennelUniqueShortName:"PIH3"` — returned the IDENTICAL single-event set (single kennel "Porto Invicta H3"). Verified 2026-06-09.**
  - ⚠️ The Azure HC API is **not in the research-sandbox network allowlist** (DNS `EAI_AGAIN`). It was verified by executing the time-token + `fetch` **inside a browser page context** (Chrome MCP, `crypto.subtle` SHA-256, same epoch/divisor as `src/adapters/harrier-central/token.ts`) — `status 200`, real data. The HC API is reachable from a normal dev machine; **Claude Code: confirm `adapter.fetch(source)` returns the event(s) from your local env.** (Same plumbing as the live Lisbon/Tokyo/Singapore/Hamburg/Morgantown HC sources, so this is routine.)
- Events seen: **1** upcoming, **2026-10-07** (single future event at research time — see recently-active note).
- 🟡 **Recently-active onboard (NOT a block).** HC is **future-only**. At research time only **#28 (2026-10-07)** is in the forward window. The kennel's **regular run #36 was 2026-06-06** (only **3 days before this research run** — captured in the 2026-06-07 queue verification) — well within ~2× a biweekly interval → the source is **live and recently active**, not stale. The kennel runs **alternate Saturdays** (kennel site: *"We hash on alternate Saturday afternoons"*); regular summer runs appear on HC as the hares post them. Onboard now; the next regular run lands on the first scrape after it's posted.
- **Source-count parity:** HC by-kennel returns its full forward set (1). Platform UI (the kennel funnels registration *to* HC) shows 1 upcoming. Parity = **1/1 (100%)** — single API response, no `nextPage`, no pagination needed. (Per Step 3: with N=1 future event + recent-cadence evidence, parity is satisfied and the recently-active rule applies.)
- Sample event (**verbatim from the API**, theme title unaltered; trailing space is in the source):
  1. `#28` — **2026-10-07** 11:00 — "Prelude Hash in Porto" — hares: "Mark Sex " — loc: "Porto,Portugal" — coords `41.15000633, -8.620001263`
  - This is the kennel's **special "Pan-Iberian PreLube"** event (kennel site `/prelube-oct-26` + flyer) — a one-off prelude run for a Pan-Iberian hash weekend, hence its 11:00 start vs the usual afternoon. Run numbers ARE present (`eventNumber` = 28). ⚠️ **Numbering is non-monotonic on this source:** the special #28 is dated *later* (Oct 2026) than the regular #36 (Jun 2026) — store the source `eventNumber` faithfully, do NOT "correct" the apparent out-of-sequence number (the adapter passes it through; this is a source quirk, not a bug).
- History depth / pagination: **single API response, no pagination.** HC `getEvents` returns **future-only** (no past events). A run number ~#36 implies the kennel has run ~36 times since late 2024/2025, but **no source exposes that archive** (see **Historical backfill**).
- Coord sanity: the one event carries **real Porto coordinates `41.15, -8.62`** (central Porto — geographically correct, not a wrong-continent default). `locationOneLineDesc` and `resolvableLocation` are both the city-level string **`"Porto,Portugal"`** (no street venue). Adapter behaviour to expect: the HC adapter's `hcGeocodeFailed(placeName, resolvable)` drops coords **only when `placeName === resolvable`** — here `"Porto,Portugal" === "Porto,Portugal"` is **true**, so the adapter **drops** the HC pin (+ `dropCachedCoords:true`) and the merge pipeline geocodes "Porto,Portugal" → Porto centre. Net result is a correct Porto pin either way. **Not a blocker.**
- End times: **none** (HC `getEvents` payload has no end timestamp).
- Notes: Config-only HC, identical pattern to **Lisbon H3** (`sources.ts:3667`) and **Hamburg H7** (`sources.ts:3647`), both of which use `publicKennelId` + `defaultTitle` + `staleTitleAliases`. Mirror them exactly.
- **Field-fill assertion table** (from the 1-event sample):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 1 / 1 | HC sets it; real themes pass through trimmed; "Placeholder event for PIH3" → `defaultTitle`+`#N` ("Porto Invicta H3 #N") via `staleTitleAliases` |
  | `runNumber` | 1 / 1 | `eventNumber` (28). Store faithfully even when non-monotonic vs date (see sample note) |
  | `startTime` | 1 / 1 | `HH:MM` from `eventStartDatetime` (11:00 for the #28 special; regular runs are afternoon ~15:00) |
  | `endTime` | 0 / 1 | not in HC feed — accept absence |
  | `location` (venue name) | 1 / 1 *(city-level only)* | `"Porto,Portugal"` is a city string, not a real venue — adapter drops the matching default pin; merge geocodes to Porto. Don't synthesize a venue |
  | `locationStreet` | 0 / 1 | not in feed |
  | `locationUrl` (Maps) | 0 / 1 | not in feed |
  | `hares` | 1 / 1 | from `hares` ("Mark Sex"); trimmed by adapter |
  | `cost` | 0 / 1 | kennel default `€5` lives on `Kennel.hashCash`; no per-event override |
  | `description` | 0 / 1 | not in HC feed — leave undefined |
  | `coords` (lat/lng) | 1 / 1 *(real Porto pin)* | see **Coord sanity** — dropped via `hcGeocodeFailed` (city==resolvable), re-geocoded to Porto |

## Kennel metadata (deep-dive complete)
> Every field cites a source URL. The metadata source domain **`ononinvicta.club` passed the DNS check** (dns.google A-record `Status:0`, IPs `147.79.72.236`/`147.79.79.225`). 🔴 **The branded `pih3.run` domain is NXDOMAIN** (dns.google A-record `Status:3`) despite appearing in search results — it is **NOT** used as the website. Use `ononinvicta.club`.
- fullName: **Porto Invicta Hash House Harriers** / shortName: **Porto Invicta H3** / region: **Porto** / country: **Portugal**
- aliases: `["PIH3", "Porto Invicta Hash House Harriers", "Porto Invicta H3", "Invicta H3"]`
  - All four verified absent from `aliases.ts` (`grep '"PIH3"'` etc. → 0 hits). `PIH3` is **not** a global collision (unlike Lisbon's bare `LH3`), so it's safe to include as the public short alias. (`pih3` is also the kennelCode — fine; the seed alias key is the kennelCode `pih3`.)
- website: **`https://www.ononinvicta.club/`** (source: live site, DNS-confirmed, HTTP 200; `meta-og:site_name` "Porto Invicta Hash House Harriers"). ⚠️ The branded `pih3.run` is NXDOMAIN on its A record — omitted.
- facebookUrl: **`https://www.facebook.com/groups/1237115737373498`** (source: ononinvicta.club footer "Community" links; it's a FB **group**, not a Page → not usable for `FACEBOOK_HOSTED_EVENTS`)
- instagramHandle: **`portoinvictah3`** (source: instagram.com/portoinvictah3, linked from ononinvicta.club footer)
- twitterHandle / discordUrl: none found
- contactEmail (not a seed field unless schema supports — note only): **`invictahash@gmail.com`** (source: ononinvicta.club footer + About-FAQ). ⚠️ web search surfaced `invictahash3@gmail.com` but the kennel's own site consistently lists `invictahash@gmail.com` — site is canonical.
- schedule: **Saturday, ~3:00 PM, Biweekly** (source: ononinvicta.club About-FAQ — *"We hash on alternate Saturday afternoons … Runs will be about 5 to 10km in length and 60 to 90 minutes duration"*).
  - ⚠️ **`scheduleTime` is approximate.** The kennel publishes no fixed clock time ("afternoons"); the only HC time captured is the #28 **special** at 11:00 (unrepresentative). Photo timestamps on the site suggest regular starts ~14:50–15:08 (`20250315_150811`, `20250607_145008`). Seeded as **"3:00 PM"** as a best estimate; **HC per-event `startTime` is authoritative** once events flow. Flag in `scheduleNotes`.
- foundedYear: **2025** (source: ononinvicta.club footer — *"Serving Porto's running and social community since 2025"*, repeated site-wide; "© 2025"). ⚠️ **Note:** the gallery carries a few photos dated **Oct 2024 / Jan 2025** (`20241027_…`, `whatsapp-image-2025-01-04…`) and an "Inaugural Run" Facebook event — i.e. soft-launch / founders' trial runs in late 2024 before the formal "since 2025" branding. Canonical `foundedYear: 2025` (kennel's own repeated claim). (The About-FAQ line "Hash House Harriers … founded in 1938" refers to the **global** HHH movement, NOT PIH3 — do not use it.)
- hashCash: **`€5`** (source: ononinvicta.club About-FAQ — *"There's a small fee per event (€5)…"* — corroborated on the Events page: *"€5 per run for drinks and snacks after the run/walk"*)
- dogFriendly: **true** (source: ononinvicta.club Events page register form explicitly asks *"are you bringing dogs, children, decrepit relatives?"* — dogs accommodated). Lower-confidence than other fields; flag if reviewer prefers `null`.
- walkersWelcome: **true** (source: About-FAQ — *"We welcome walkers, joggers, and runners of all abilities. Our trails typically include shorter walking routes and longer running options"*)
- description: `"Porto Invicta Hash House Harriers (PIH3) is Porto's social running and walking club, founded in 2025 and part of the global Hash House Harriers network. It hashes on alternate Saturday afternoons around Porto and Northern Portugal — 5–10km trails for runners and walkers, easily reached by metro or urban rail, followed by beer, food and the usual circle. On On!"`
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/Porto%20Invicta%20H3.png`** (HC blob — non-tokenized but third-party CDN; same convention as Lisbon's HC-blob logo). ⚠️ **Self-host**: download to `public/kennel-logos/pih3.<ext>` and reference `/kennel-logos/pih3.<ext>`. **Confirm the real extension via magic bytes** (`file public/kennel-logos/pih3` — `\x89PNG`=PNG, `RIFF…WEBP`=WebP, `\xff\xd8`=JPEG). (Alternative: the site logo `assets.zyrosite.com/.../pih3logo-mxBXGV1kroIPeqxp.png` is a **tokenized Hostinger/Zyro CDN** URL that rotates — prefer the HC blob.)
- lat/lng: **41.15, -8.62** (central Porto; matches HC's own kennel pin and the city centroid. Per-run venues vary and are announced day-of.)

## Historical backfill
- Available: **none.** HC `getEvents` exposes **future-only** events. Run ~#36 implies ~36 runs since late 2024/2025, but **no source exposes them**: the kennel's own site (`ononinvicta.club`, Hostinger Website Builder) has **no structured calendar / blog feed / archive API** — the Events page is a free-text "next run" blurb + flyer, and the blog/gallery pages are unstructured marketing content. The Events page itself directs registration *to* Harrier Central. There is no Google Sheet / WordPress / Squarespace / Blogspot archive.
- Plan: **none** (future-only adapter; the kennel page populates from the live HC forward event(s) on first scrape, and fills in as the kennel posts its alternate-Saturday runs to HC). A young kennel (~36 runs) with no structured archive does not warrant a backfill script.

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` (kennelCode → string[]) — confirmed via the live Lisbon entry. **Keyed by kennelCode (`pih3`), NOT slug.**

```ts
// kennels.ts — Kennel[] (array of objects). Add next to the Lisbon "// ── Portugal: Lisbon ──" block (~kennels.ts:2844).
{
  kennelCode: "pih3", shortName: "Porto Invicta H3", fullName: "Porto Invicta Hash House Harriers",
  region: "Porto", country: "Portugal",
  foundedYear: 2025,
  scheduleDayOfWeek: "Saturday", scheduleTime: "3:00 PM", scheduleFrequency: "Biweekly",
  scheduleNotes: "Alternate (biweekly) Saturday afternoons around Porto and Northern Portugal, reachable by metro/urban rail. 5–10km trails (60–90 min) for runners and walkers, with a mid-run refreshment 'Pitstop'. Exact start time varies and is announced per-run via Harrier Central / Facebook (seeded ~3:00 PM from photo timestamps; HC per-event times are authoritative). Per-run venues announced day-of by the hares.",
  description: "Porto Invicta Hash House Harriers (PIH3) is Porto's social running and walking club, founded in 2025 and part of the global Hash House Harriers network. It hashes on alternate Saturday afternoons around Porto and Northern Portugal — 5–10km trails for runners and walkers, easily reached by metro or urban rail, followed by beer, food and the usual circle. On On!",
  hashCash: "€5",
  dogFriendly: true,
  walkersWelcome: true,
  logoUrl: "/kennel-logos/pih3.<ext>",   // ← self-host the HC blob; confirm ext via magic bytes
  facebookUrl: "https://www.facebook.com/groups/1237115737373498",
  instagramHandle: "portoinvictah3",
  website: "https://www.ononinvicta.club/",
  latitude: 41.15, longitude: -8.62,
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]).
"pih3": ["PIH3", "Porto Invicta Hash House Harriers", "Porto Invicta H3", "Invicta H3"],

// sources.ts — Source[] (array of objects). Mirror the Lisbon H3 HC record (sources.ts:3667).
{
  name: "Porto Invicta H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter; cityNames:"Porto" and kennelUniqueShortName:"PIH3"
    // both returned the identical single-event set (all three verified 2026-06-09).
    publicKennelId: "c4a3b093-3e4b-4add-91c0-21ed3fa76322",
    defaultKennelTag: "pih3",
    // Young kennel that pre-creates slots — synthesize "Porto Invicta H3 #N" if a
    // "Placeholder event for PIH3" row appears (mirrors Lisbon H3 / Hamburg H7 #1166).
    defaultTitle: "Porto Invicta H3",
    staleTitleAliases: ["Placeholder event for PIH3"],
  },
  kennelCodes: ["pih3"],
},
```

> **`upcomingOnly` intentionally OMITTED** — matches every existing HC source (Lisbon H3, Hamburg H7, Tokyo H3, Singapore Sunday H3, Morgantown H3 all omit it). The HC adapter emits future-only and survives reconciliation without it. Don't add it. (Porto has no archive to protect anyway.)

## Adapter notes / new-scraper plan

**Config-only — no new adapter code.** `HARRIER_CENTRAL` → `HarrierCentralAdapter` is registered in `src/adapters/registry.ts`. The config above is the entire implementation. The adapter already handles: time-based token auth, the `[[...events]]` response shape, `staleTitleAliases`+`defaultTitle` fallback, TBA stripping, `hcGeocodeFailed` coord-dropping (which fires for this kennel's `"Porto,Portugal"==="Porto,Portugal"` rows), run-number passthrough, and the `days`/`scrapeDays` window.

### Region (NEW METRO under an existing country — Portugal already seeded)
🔴 **Portugal COUNTRY + Lisbon METRO are ALREADY in `src/lib/region.ts` on `main`** (Lisbon H3, PR #2031 / commit `1934133b`). **Do NOT re-add the Portugal COUNTRY record, the `COUNTRY_GROUP_MAP "Portugal"` entry, or the `COUNTRY_CODE_TO_NAME PT` entry — they exist.** You only add the **Porto METRO**:

1. **`REGION_SEED_DATA`** — add the Porto metro record immediately **after the existing Lisbon metro** (currently ends `region.ts:1827`, before `// ── Switzerland ──`). Mirror Lisbon's metro shade/pin (Spain precedent: all metros under a country share the lighter shade + pin):
   ```ts
   {
     name: "Porto",
     country: "Portugal",
     level: "METRO",          // confirm the exact field/optional against a sibling record
     timezone: "Europe/Lisbon",
     abbrev: "OPO",
     colorClasses: "bg-green-100 text-green-700",
     pinColor: "#22c55e",
     centroidLat: 41.1579,
     centroidLng: -8.6291,
     aliases: ["Porto, Portugal", "Oporto"],
   },
   ```
   🔴 **Sonar S6749:** no trailing-zero numeric literals (none here — `41.1579`/`-8.6291` are fine). Copy a sibling record verbatim for the exact field names/optionals (`level` may be inferred elsewhere — match the Lisbon record's shape exactly).
2. **`STATE_GROUP_MAP`** — add `"Porto": "Portugal",` immediately after the existing `"Lisbon": "Portugal",` (`region.ts:3574`).
3. **`COUNTRY_GROUP_MAP`** — **no edit needed.** Per the Lisbon/Spain/France precedent (comment at `region.ts:3789`), the metro resolves via `STATE_GROUP_MAP` first, so only the country-group key `"Portugal"` is reachable — and it already exists. (Do NOT add a `"Porto"` key here; Lisbon didn't add `"Lisbon"` either.)
4. **`COUNTRY_CODE_TO_NAME`** — **no edit needed** (`PT: "Portugal"` already exists, `region.ts:3902`).
5. **`COUNTRY_INFERENCE_RULES`** (`region.ts:3365`, currently `[/\b(portugal|lisbon|lisboa|estoril|cascais)\b/, "Portugal"]`) — **recommended (optional) edit: add `oporto|invicta`** →
   ```ts
   [/\b(portugal|lisbon|lisboa|estoril|cascais|oporto|invicta)\b/, "Portugal"],
   ```
   🔴 **Do NOT add bare `porto`.** The Portugal rule sits at line 3365, **before** the Brazil rule at line 3379 (`/\b(brazil|brasil|bras[ií]lia)\b/`). Bare `\bporto\b` would match **"Porto Alegre, Brazil"** and misfire it to Portugal. PIH3's HC `eventCityAndCountry` is **"Porto, Portugal"** — which **already matches the existing `portugal` token**, so `inferCountry()` is *already correct for this kennel without any edit*. The `oporto|invicta` addition only future-proofs metro-name-only / kennel-name text and carries **no** Brazil-collision risk. **Strictly optional** — include it for robustness, or skip it (correctness holds either way because the seed sets `country: "Portugal"` explicitly and the event text contains "Portugal").

**⚠️ Claude Code: verify before writing real code.** Any snippet here is illustrative; the live repo is authoritative. Before finalizing:
- `RawEventData` field names — `kennelTags` is `string[]` (the HC adapter already emits `kennelTags: [kennelTag]`). The walker-friendliness `Kennel` field is `walkersWelcome` (NOT `walkerFriendly`); the dog field is `dogFriendly`. Both are plumbed into the seeder (per VH3/Lisbon retro). Confirm `website` is a valid `Kennel` field (Lisbon set `facebookUrl`/`instagramHandle`; confirm `website` column name).
- Confirm the `Region` seed object's exact field names/optionals against the current `REGION_SEED_DATA` type by copying the Lisbon metro record (`region.ts:1817`) verbatim and editing values.
- `friendlyKennelName("Porto Invicta H3", "Porto Invicta Hash House Harriers")` — shortName is **16 chars (>4)** so it short-circuits to the shortName; titles are also set by the HC adapter (`defaultTitle: "Porto Invicta H3"`), so synthesized titles render "Porto Invicta H3 #N" cleanly. No verbose-name risk.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob — flagged self-host to `public/kennel-logos/pih3.<ext>`, confirm ext via magic bytes)  [x] foundedYear (2025, kennel site; 2024 soft-launch photos noted)  [x] socials (FB group + Instagram; no Twitter/Discord)  [x] schedule (Saturday/biweekly/afternoon; scheduleTime approximate — flagged)  [x] hashCash (€5, two corroborating pages)
- [x] description  [x] source live-verified (1 upcoming event via HC API, browser-context fetch — Claude Code to reconfirm from local env; recently-active rationale recorded)  [x] history depth assessed (future-only; no backfill source — young kennel, unstructured site)
- [x] coord sanity checked (real Porto pin; dropped via `hcGeocodeFailed` city==resolvable → re-geocoded to Porto)  [x] end times noted (none)  [x] kennelCode collision-checked (`pih3` clear)  [x] kennelCodes (source guard) set (`["pih3"]`)

## Sibling-kennel sweep
- HC `getEvents cityNames:"Porto"` returned **only PIH3** (1 kennel, 1 event). No co-located siblings on Harrier Central.
- No other Porto / Northern Portugal H3 kennel surfaced in web search or on the kennel's own cross-links. The Oct 2026 "Prelude Hash in Porto" / "Pan-Iberian PreLube" is a **special event hosted by PIH3**, not a separate kennel.
- Other Portugal kennels noted for future targeting (already in the queue's leads): **Algarve HHH** (`algarvehashhouseharriers.com`) — different region; verify a live dynamic source before queueing.

## Source confirmation (queue proposed HARRIER_CENTRAL — confirmed, no pivot)
- Queue proposed config-only **HARRIER_CENTRAL** with `publicKennelId c4a3b093-…`. **Confirmed live** 2026-06-09 (browser page-context fetch, status 200, 1 future event + recent #36 evidence). No source pivot needed.
- The kennel's own website (`ononinvicta.club`) is **deliberately NOT used as a scrape source** — it's an unstructured Hostinger marketing site (no calendar/feed) that itself directs registration to Harrier Central. HC is the correct primary (and only structured) source.

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **HC API is allowlist-blocked from the research sandbox but reachable from a normal dev machine.** Live-verify with a throwaway `npx tsx -e` calling `adapter.fetch(source)` — expect the #28 future event (and any new regular runs the kennel has posted since 2026-06-09). The count + verbatim title here were captured via a browser page-context `fetch` because the sandbox can't resolve the Azure host.
- **Small upcoming count is expected, not a failure.** 1 future event (#28) at research time; HC is future-only and the regular #36 aged out. This is a **recently-active** onboard. Do not treat a low count as a broken scrape.
- **Non-monotonic run numbers** — store HC `eventNumber` faithfully; the special #28 (Oct) is numbered lower than the regular #36 (Jun). Never renumber to satisfy a linter/review bot.
- **Coord default-pin (see Coord sanity):** the adapter already drops the city-level pin via `hcGeocodeFailed` (`"Porto,Portugal"==="Porto,Portugal"`) + sets `dropCachedCoords:true`; the merge re-geocodes to Porto. No action needed; keep config-only.
- **`upcomingOnly` not set** — matches all existing HC sources; do not add it.
- **Self-host tokenized/CDN logos** → `public/kennel-logos/pih3.<ext>`. **NEVER pre-fill the extension** — confirm via `curl -sI` Content-Type AND magic bytes (`\x89PNG`=PNG, `RIFF…WEBP`=WebP, `\xff\xd8`=JPEG). The handoff uses a literal `<ext>` placeholder in `logoUrl` deliberately.
- **Portugal region already exists** — only add Porto METRO + the `STATE_GROUP_MAP "Porto"` line (+ optional inference-regex `oporto|invicta`). Do NOT re-add Portugal COUNTRY / `COUNTRY_GROUP_MAP "Portugal"` / `COUNTRY_CODE_TO_NAME PT` (all on `main` from Lisbon PR #2031) — duplicate region records will conflict at seed time.
- 🔴 **Inference regex: bare `porto` is a Brazil collision hazard** (Portugal rule precedes Brazil's `brasil|brasília`; "Porto Alegre, Brazil" would misfire). Add only `oporto|invicta`, or skip the inference edit entirely (PIH3 text already contains "Portugal").
- **Sonar S6749:** no trailing-zero numeric literals in the Porto region seed (`41.1579`/`-8.6291`/`41.15`/`-8.62` are all fine).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief — no separate prompt needed._
