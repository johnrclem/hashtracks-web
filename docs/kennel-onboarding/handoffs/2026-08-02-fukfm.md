# Onboarding Handoff — F.U.K Full Moon H3 (London / Greater North London) — 2026-08-02

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/fukfm-20260802`.
> 2. **This is a `source-add`.** The kennel + aliases ALREADY EXIST in seed
>    (`prisma/seed-data/kennels.ts:1084` `fukfm`; `prisma/seed-data/aliases.ts:153`). **Do NOT
>    duplicate the kennel or the alias block.** Apply only: (a) the new **Source** row below, and
>    (b) the small **kennel-field corrections/enrichment** below (the seeded `scheduleNotes` is
>    stale — see Kennel metadata). No region edits (region `London` already seeded — 0 `region.ts` edits).
> 3. Implement the **new bespoke `FukFullMoonH3Adapter`** exactly as in **Adapter notes** (a
>    static-SSR FrontPage forward-hareline `<table>` scraper; Cheerio, no browserRender). Prefer to
>    **share the parser helper with `HertsHashAdapter`** if that PR (handoff `2026-08-01-herts-h3.md`)
>    has already merged — the two sites are the same legacy-FrontPage platform; otherwise mirror it.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` snippet or a small script under
>    `scripts/`. 🔴 **Build the test fixture from real `curl -s https://fukfmh3.co.uk/hareline.htm`
>    output, NOT this file's rendered sample** — the sandbox `curl`/`web_fetch` differ; these
>    FrontPage pages wrap cells in MS-Office `<p>`/`<b>`/`<span>` runs with `&nbsp;` and `<br>` that
>    a clean-text sample hides (Legacy MS-Office platform note). Validate: events non-empty +
>    upcoming, dates UTC-noon, `startTime` "HH:MM", `kennelTag` resolves to `fukfm` with no unmatched.
>    **DO NOT run `npx prisma db seed` here.**
> 5. No historical backfill (forward-only hareline, no archive page — see Historical backfill).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR whose body carries the metadata, live-verification results, and the
>    deep-dive checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- <path>` for the adapter, the
>      registry edit, the sources.ts edit, the kennels.ts field edits). Squash-merge sometimes drops
>      a follow-up commit — open a small recovery PR if anything's missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive — this is what makes `fukfm` go LIVE: it's seeded-but-not-yet-
>      deployed, currently `/kennels/fukfm` → 404. Seeding publishes the kennel + its new Source.)
>    - Trigger a scrape from `/admin/sources` to publish events to prod.
>    - Spot-check `hashtracks.xyz/kennels/fukfm` for the ~17 upcoming runs (#495 → #511) + sample dates.

## Summary
- Type: **source-add** (kennel + aliases already seeded; add the Source + correct stale schedule)
- Adapter: **HTML_SCRAPER** (**NEW bespoke `FukFullMoonH3Adapter`, ~150–220 LoC + tests** — static
  SSR FrontPage `<table>`; NOT config-only — run-number cell, multi-line date cell with **year
  inference**, `TBC`/`Invite Only!` venue placeholders, what3words-not-coords all need code)
- Effort estimate: **new ~150–220 LoC adapter + tests** (share the parser with `HertsHashAdapter` if
  merged → closer to ~80–120 LoC of FUKFM-specific glue)
- One-line: adds the first source to an already-seeded-but-dark London kennel (First UK Full Moon H3,
  est. 1990) — a clean single-kennel FrontPage forward-hareline with **17 upcoming monthly-Saturday
  runs through Dec 2027**; continues yesterday's Herts H3 thread (Herts flagged FUKFM as a sibling).

## Dedup result
- Kennel in seed: **yes @ `kennels.ts:1084`** (`fukfm` / shortName `FUKFM` / "First UK Full Moon Hash
  House Harriers") — and alias block **yes @ `aliases.ts:153`**.
- Source in seed: **no** (`grep -in fukfm prisma/seed-data/sources.ts` → 0 hits). Sourceless.
- Live status: **NOT live** — `/kennels/fukfm` renders a real **404** (Chrome MCP, 2026-08-02:
  `document.title = "404: This page could not be found."`). Seeded-but-not-deployed; the post-merge
  `prisma db seed` makes it live. No admin-seeded `Event` rows exist (404 page) → **no admin-event
  dedup concern**.
- Live sitemap dedup: **confirmed NOT live — read via Chrome MCP, 491 slugs (2026-08-02).** No
  `fukfm` / `fuk` / FUKFM-style full-moon slug. The 5 `*full-moon*` slugs present
  (`berlin-full-moon`, `hua-hin-full-moon-h3`, `kl-full-moon`, `melbourne-full-moon`,
  `summit-full-moon`) are all unrelated kennels.
- Decision: **source-add** — add the Source, correct the stale schedule, enrich blanks; do NOT
  re-create the kennel/aliases.
- kennelCode: `fukfm` (already the seeded code — **no new code, no collision check needed**).

## Live source verification  ✅
- Source: **HTML_SCRAPER** — `https://fukfmh3.co.uk/hareline.htm` ("Receding Hare Line"). DNS
  `fukfmh3.co.uk` → **Status 0**, `185.194.90.9` (Chrome `dns.google/resolve`, 2026-08-02). Page is
  **plain SSR `text/html`** (legacy MS-Office/FrontPage "Save as Web Page"); `web_fetch` returned the
  full forward-hareline `<table>`. **Feed HEAD-check:** sandbox `curl` is allowlist-blocked (exit 56)
  — Claude Code confirms the real DOM at build (it's `text/html`, no gating; the HTML listing was
  fully fetchable here).
- Events seen: **17 upcoming**, date range **Sat 29 Aug 2026 (#495) → Sat 11 Dec 2027 (#511)**.
  Monthly cadence, almost all Saturday Noon (a couple of evening pub-crawls at 7pm noted in-cell).
- **Source-count parity:** the source is a single forward `<table>` with no "total events" UI and no
  pagination — the 17 rows ARE the whole reachable forward window. Parity N/A (no separate count to
  reconcile against); recently-active evidence is the 17 dated upcoming rows themselves. Not a
  0-upcoming case.
- Sample events (VERBATIM from the hareline — run numbers and hare names are real, not constructed):
  1. **#495** — Sat 29 Aug 2026, **Noon** (pub opens 10:00) — **The Waterside Inn**, Bridgefoot,
     Ware, Hertfordshire **SG12 9DW** — hare **Paxo** — w3w `adults.script.lifted` — dogs welcome
  2. **#496** — Sat 26 Sep 2026, Noon — **The Grenadier**, 18 Wilton Row, Belgravia, London
     **SW1X 7NR** — hare **Bangers** — w3w `groups.dull.wipe`
  3. **#497** — Sat 24 Oct 2026, Noon — **The Southampton Arms**, 139 Highgate Road, Kentish Town,
     London **NW5 1LE** — hares **Chikki, Tootuf & Sidney** — w3w `tree.hurry.carry`
  4. **#498** — Sat 21 Nov 2026, Noon — **The Essex Arms**, Warley Hill/Myrtle Road, Brentwood,
     Essex **CM14 5HA** — hare **Compressed Fart** — w3w `spot.best.cage`
  5. **#499** — Sat 19 Dec 2026, Noon — venue **TBC** (Cambridge) — hare **My Lil'**
  - Later rows (#500 Sat 23 Jan **'27** → #511 Sat 11 Dec **'27**) carry an explicit `'27` year
    marker and many have `TBC` venues/hares (a recurring "Bermondsey Beer Mile Part I–IV" series by
    hare Runs2Eat). **No event carries an explicit theme/title** — titles are undefined (merge
    synthesizes; see field table).
- History depth / pagination: **single page, no archive.** Nav is only Home / Future Runs (this
  hareline) / Vindaloo / Links — no run-report/archive collection exists on the site. Forward-only.
- Coord sanity: **no decimal coords** — venues are **what3words** (`///a.b.c`) + UK postcode only.
  Leave lat/lng undefined (merge geocodes postcode/venue/London centroid). **Do NOT set `locationUrl`
  to the what3words link** — it isn't a Maps URL (same rule as Herts H3).
- End times: **none** (only start "Noon"/"7pm").
- Notes: SSR static HTML → `fetchHTMLPage` + Cheerio, **no browserRender**. See
  `source-platform-notes.md` → "Legacy MS-Office static HTML" + the new **FUKFM addendum** appended
  this run (year-inference gotcha).
- **Field-fill assertion table** (from the 17 sampled forward rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 0 / 17 | leave undefined → `merge.ts` synthesizes "FUKFM Trail #N" (`shortName` "FUKFM" is 5 chars > 4 → `friendlyKennelName` short-circuits to it → clean title; verify at build) |
  | `runNumber` | 17 / 17 | integer from the "Run No." cell |
  | `startTime` | 17 / 17 | "Noon" → `"12:00"`; "7pm"/evening pub-crawl → `"19:00"` |
  | `endTime` | 0 / 17 | accept absence |
  | `location` (venue) | ~13 / 17 | venue-name link text; `"TBC"`/`"Invite Only!"` → leave undefined (don't synth) |
  | `locationStreet` | ~13 / 17 | address + postcode from the "Details" cell |
  | `locationUrl` (Maps) | 0 / 17 | what3words is NOT a Maps URL → do NOT set |
  | `hares` | ~14 / 17 | CSV from "Hare/s" cell (`"Chikki, Tootuf & Sidney"`); `"TBC"` → undefined |
  | `cost` | 0 / 17 | kennel default unpublished → leave undefined |
  | `description` | ~14 / 17 | transport/station text from "Details" (`Ware Station, Hertford East Line…`) |
  | `coords` (lat/lng) | 0 / 17 | undefined → merge geocodes postcode/venue/London centroid |

## Kennel metadata (deep-dive complete)
- fullName / shortName / region / country: **First UK Full Moon Hash House Harriers** / **FUKFM** /
  **London** (Greater North London) / **UK** — all as already seeded (`kennels.ts:1084`). Keep.
- aliases: already seeded (`aliases.ts:153`): `["FUKFMH3", "FUK Full Moon", "First UK Full Moon"]`.
  Keep as-is — no additions needed (do NOT touch; alias key is the kennelCode `fukfm` and it's correct).
- website: `https://fukfmh3.co.uk` (already seeded). Keep.
- facebook / instagram / twitter / discord: **none found** — no social links on the site (Links page
  is only links to other hashes); web search surfaced no FUKFM social. **Leave blank + flag
  "unverifiable — no working source".**
- schedule: **Monthly, Saturday, ~12:00 PM (Noon)** — 🔴 **CORRECT the seeded `scheduleNotes`.** The
  seed currently says `scheduleFrequency: "Monthly"`, `scheduleNotes: "Every full moon evening, 7:30 PM"`
  — the **live hareline shows monthly Saturday Noon** (the group re-formed — "We're back in black" —
  and now runs Saturday-noon pub-crawl trails, with occasional 7pm evening runs). Source:
  `fukfmh3.co.uk/hareline.htm` (all 17 forward rows are Saturday; times "Noon"). Recommended edit:
  ```ts
  // scheduleDayOfWeek/scheduleTime kept as fallback; schedule reflects the live hareline (monthly Saturday Noon).
  scheduleFrequency: "Monthly",
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "12:00 PM",              // 🔴 12-hr in Kennel.scheduleTime (RawEventData.startTime stays 24-hr "12:00")
  scheduleNotes: "Monthly Saturday noon pub-crawl trail (occasional 7pm evening runs); named for its full-moon origins",
  ```
  (Optional: a `scheduleRules` RRULE `FREQ=MONTHLY;BYDAY=SA` — but the exact BYSETPOS varies month to
  month per the hareline, so a flat `scheduleDayOfWeek` is safer than a wrong RRULE. Adapter events
  carry the real dates regardless.)
- foundedYear: **1990** (already seeded — keep). Verified: **March 1990**, "the First U.K. Full Moon
  Hash (first European Full Moon Hash)", founders Martin "Vertigo" Pooley, Alan "Smartarse" Drew &
  Rob "Pisshead" Long (source: `hertshash.co.uk/hash_history.htm`; corroborated by `fukfmh3.co.uk`).
- hashCash: **not published on the site** → **leave blank + flag "unverifiable — no working source".**
- dogFriendly: **true** (runs carry the "dogs welcome" icon, e.g. #495; source `fukfmh3.co.uk/hareline.htm`).
  "Horrors welcome" (children) also indicated.
- walkersWelcome: not explicitly stated → leave as seeded / blank.
- logoUrl: `https://fukfmh3.co.uk/images/wolfeyes.png` (the wolf-eyes header image on both home +
  hareline) — ⚠️ **self-host** to `public/kennel-logos/fukfm.<ext>` and **magic-byte the extension**
  (`curl -sI` Content-Type + first bytes; don't assume `.png` from the URL). `images/fmhowl.png` is a
  secondary candidate if the header one reads poorly.
- description: **"A Monthly Social Drinking club with a Running Problem"** (the site's own
  meta-description; source `fukfmh3.co.uk`). Runs are Saturday-noon **pub-crawl** trails across
  Greater North London / the Home Counties (Herts, Essex, Cambridge, London zones).
- lat/lng: not set (London metro centroid suffices; venues roam widely).

## Historical backfill
- Available: **none** — the site has no archive/run-report collection (nav = Home / Future Runs /
  Vindaloo / Links; the hareline is forward-only #495→#511). Run #495 implies ~494 past runs since
  1990, but **none are reachable on this source**. (Contrast Herts H3, which had a separate
  `run_reports.htm`.)
- Plan: **none.** `config.upcomingOnly: true` on the source suppresses stale-event reconciliation as
  forward rows age off.

## Ready-to-paste seed

> Kennel + alias blocks ALREADY EXIST — do NOT re-add. Only the Source below is new; the kennel
> edits are field corrections to the existing `kennels.ts:1084` object (schedule + dogFriendly + logo
> + description), NOT a new object.

```ts
// kennels.ts:1084 — EDIT the existing `fukfm` object in place (do NOT add a new one).
// Before: scheduleFrequency: "Monthly", scheduleNotes: "Every full moon evening, 7:30 PM",
// After (corrected + enriched):
{
  kennelCode: "fukfm", shortName: "FUKFM", fullName: "First UK Full Moon Hash House Harriers",
  region: "London", country: "UK",
  website: "https://fukfmh3.co.uk", foundedYear: 1990,
  scheduleFrequency: "Monthly",
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "12:00 PM",
  scheduleNotes: "Monthly Saturday noon pub-crawl trail (occasional 7pm evening runs); named for its full-moon origins",
  dogFriendly: true,
  description: "A Monthly Social Drinking club with a Running Problem — Saturday-noon pub-crawl trails across Greater North London and the Home Counties.",
  logoUrl: "/kennel-logos/fukfm.<ext>", // ⚠️ self-host from https://fukfmh3.co.uk/images/wolfeyes.png; magic-byte the ext
  // hashCash: unverifiable (not published) → leave unset + flag
  // facebook/instagram/twitter/discord: none found → leave unset + flag
}

// aliases.ts:153 — ALREADY PRESENT, do NOT change:
//   "fukfm": ["FUKFMH3", "FUK Full Moon", "First UK Full Moon"],

// sources.ts — ADD this Source object (array of objects; shape mirrors Desert/Creek HTML_SCRAPER rows):
{
  name: "F.U.K Full Moon H3 Hareline",
  url: "https://fukfmh3.co.uk/hareline.htm",
  type: "HTML_SCRAPER" as const,
  trustLevel: 6,                          // bespoke HTML scraper
  scrapeFreq: "daily",
  scrapeDays: 365,                        // forward hareline reaches ~16 months out (#495→#511)
  config: { upcomingOnly: true },         // 🔴 REQUIRED — rolling forward hareline; past runs age off → reconcile would false-cancel
  kennelCodes: ["fukfm"],
}
```

## Adapter notes / new-scraper plan

**Not config-only.** Build a **new bespoke `FukFullMoonH3Adapter`** (~150–220 LoC), static Cheerio,
no browserRender. **Reference adapters:** mirror `HertsHashAdapter` (handoff `2026-08-01-herts-h3.md`
— same FrontPage forward-hareline `<table>` platform; if merged, extract a shared parser helper),
`src/adapters/html-scraper/dublin-hash.ts` (`fetchHTMLPage` + table plumbing), and
`src/adapters/html-scraper/burlington-hash.ts` (label/field cleaning + Sonar-safe field terminators).

**Fetch:** `fetchHTMLPage("https://fukfmh3.co.uk/hareline.htm")` from `@/adapters/utils` (static SSR;
UK site, day-first dates).

**Table shape** (columns): `Run No.` | `When?` | `Where?` | `Hare/s` | `Details`.
- **Row filter / fail-loud:** iterate `<table> <tr>`; keep only rows whose first cell parses to an
  integer run number. SKIP the header row, the legend row (CAMRA/dogs/horrors icons), and the nav
  footer row (they have no integer run#). 🔴 **`if (rows.length === 0) errors.push("FUKFM hareline: 0 run rows parsed — markup drift")`** — single-page single-table source needs the zero-guard so a
  markup change surfaces instead of silently reconcile-cancelling live events.
- **`runNumber`:** `Number.parseInt(cellText, 10)`.
- **Date (the tricky bit — YEAR INFERENCE):** the "When?" cell is multi-line, e.g.
  `"Sat 29th\nAugust\nNoon\n(Pub Opens at 10:00Hrs)"`. Near rows are **year-less**; later rows carry
  an explicit **`'27`** marker (`"Sat 23rd January '27"`). Plan:
  1. Normalise `'YY` → `20YY`; strip weekday prefix + ordinal suffix; extract `DD`, month name, and
     year-if-present.
  2. For year-less rows, **infer** the year so dates are monotonically non-decreasing from the first
     row and ≥ scrape date (increment year when the month sequence wraps Dec→Jan). Prefer letting
     `chronoParseDate` / `chrono-node` validate the month name (per the S5843 note — don't enumerate
     months in regex).
  3. UTC noon (`Date.UTC(y, m-1, d, 12, 0, 0)`).
- **`startTime`:** "Noon" → `"12:00"`; a "7pm"/evening indication → `"19:00"`. 24-hr `"HH:MM"` string.
- **Venue / street:** "Where?" cell link text → `location`; address + postcode from "Details" →
  `locationStreet`. `"TBC"` / `"Invite Only!"` → leave `location` undefined (don't synthesize).
- **Hares:** "Hare/s" cell → CSV; normalise `" & "`/`", "`; `"TBC"`/`"Limited Numbers"` → undefined.
- **`description`:** transport/station lines from "Details" (`Ware Station, Hertford East Line…`).
- **Coords:** none — what3words only. Leave lat/lng undefined; **do NOT set `locationUrl`** to the
  what3words link.
- **`kennelTags`:** `["fukfm"]` (single-kennel source — no `kennelPatterns`, no filtering).

**Registry entry** — add to `htmlScraperEntries` in `src/adapters/registry.ts` (the single source of
truth; `htmlScrapersByUrl` derives from it) + the import:
```ts
{ pattern: /fukfmh3\.co\.uk/i, name: "FukFullMoonH3Adapter", factory: () => new FukFullMoonH3Adapter() },
```

**⚠️ Claude Code: verify before writing real code.** Any snippet above is illustrative; the live repo
is authority. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`); check `prisma/schema.prisma` for canonical names.
- Imports — `fetchHTMLPage` from `@/adapters/utils`; `chronoParseDate` from `@/adapters/utils` (has
  the `D MMM YY` fast-path); if you fetch anything else use `safeFetch` from `@/adapters/safe-fetch`
  (NOT raw `fetch`).
- `title` — leave `undefined`; `merge.ts` synthesizes "FUKFM Trail #N". Confirm
  `friendlyKennelName("FUKFM", "First UK Full Moon Hash House Harriers")` returns "FUKFM" (5 chars > 4
  → short-circuits) and not the verbose fullName:
  `npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; console.log(friendlyKennelName("FUKFM","First UK Full Moon Hash House Harriers"));'`
- `kennelPagesStopReason` — set ONLY on genuine truncation (HTTP/fetch error); a non-empty string
  suppresses stale-event reconciliation. Leave null on a clean single-page parse.
- 🔴 **Fixture from real `curl -s https://fukfmh3.co.uk/hareline.htm`** — MS-Office/FrontPage DOM
  (`<p>`/`<b>`/`<span>` + `&nbsp;`/`<br>`), not the clean text in this handoff.

## Deep-dive checklist (nothing deferred)
- [x] logo (`images/wolfeyes.png` → ⚠️ self-host + magic-byte ext)  [x] foundedYear (1990, March — verified)
  [x] socials (none found → blank+flag)  [x] schedule (corrected: monthly Saturday Noon)  [x] hashCash (unpublished → blank+flag)
- [x] description  [x] source live-verified (17 upcoming, DNS Status 0, feed is `text/html`)  [x] history depth assessed (none — forward-only, no archive)
- [x] coord sanity (what3words only → undefined, no fabrication)  [x] end times (none)  [x] kennelCode (existing `fukfm`, no collision)  [x] kennelCodes source guard set (`["fukfm"]`)

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** — the hareline is a rolling forward list; a past run
  drops off it, so without `upcomingOnly` `reconcile.ts` would `CANCEL` valid aged-off events.
- **Single-page single-table → the `rows.length === 0` fail-loud guard is mandatory** (see above) —
  otherwise a markup drift makes the scrape "succeed" with `events: []` and the zero-event health
  alert never fires (baseline is 0 for a brand-new source).
- **Do NOT set `latitude`/`locationUrl` from what3words** — no decimal coords exist; a what3words
  link is not a Maps URL. (If a future re-scrape ever set a bad coord, clearing it needs
  `dropCachedCoords: true`; not applicable now since none are ever set.)
- **`Kennel.scheduleTime` is 12-hour `"12:00 PM"`**; `RawEventData.startTime` is 24-hour `"12:00"`.
  Different fields, different formats.
- **Self-host the logo** into `public/kennel-logos/fukfm.<ext>`; confirm the real extension via
  Content-Type + magic bytes (don't pre-fill `.png`).
- **Sonar:** keep date/field regexes S5852/S5843-safe (split date+time; validate month via chrono,
  don't enumerate). `Number.parseInt(s, 10)`, `s.replaceAll(...)`, no negated ternaries.
- **`fukfm` is seeded-but-dark** — the value of this PR only appears after the post-merge
  `npx prisma db seed` publishes the kennel + source (currently `/kennels/fukfm` → 404).

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the
brief — no separate prompt needed._
