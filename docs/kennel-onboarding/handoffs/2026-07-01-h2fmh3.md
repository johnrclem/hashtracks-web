# Onboarding Handoff — Hua Hin Full Moon H3 (Hua Hin, Thailand) — 2026-07-01

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/h2fmh3-20260701`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **No `region.ts` edits** —
>    the "Hua Hin" region already exists (verified below).
> 3. Configure the **config-only `HARRIER_CENTRAL`** source exactly as shown (mirror the Bandung H3
>    source row `prisma/seed-data/sources.ts:7129`). No new adapter code.
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` — call
>    `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. The Azure HC host is reachable from your
>    local env (it is NOT from the research sandbox). Validate: events non-empty + upcoming, dates
>    UTC-noon, `startTime` "HH:MM", `kennelTag` resolves (`h2fmh3`, no unmatched). Expected upcoming =
>    **#45 "Hua Hin Full Moon 4th Anniversary run" — Sat 2026-07-11 16:30** (see sample).
> 5. **Historical backfill (recommended):** one-shot `scripts/backfill-h2fmh3-history.ts` from
>    `hashruns.org/api/global-runs?isFuture=0` (see **Historical backfill**). Because a backfill is
>    present, `config.upcomingOnly: true` is **REQUIRED** (Bandung contract). If you decide the ~44-run
>    archive isn't worth it, **remove `upcomingOnly` from the source config** (live-only HC omits it).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 8. **Post-merge runbook (separate step, after PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge sometimes drops a follow-up commit):
>      `git log -1 -- scripts/backfill-h2fmh3-history.ts` (+ the seed files). Recovery PR if missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive)
>    - Trigger a scrape from `/admin/sources` to publish events to prod
>    - Spot-check `/kennels/hua-hin-full-moon-h3` for the expected event count + #45 on 2026-07-11.

## Summary
- Type: **full onboard**
- Adapter: `HARRIER_CENTRAL` (**config-only — adapter exists (`src/adapters/harrier-central/adapter.ts`) & feed verified live**)
- Effort estimate: **config-only** (zero new adapter code) + **0 `region.ts` edits** (Hua Hin region exists) + one-shot backfill script (~44 rows)
- One-line: Adds the **full-moon sibling** of the already-live Hua Hin/Cha-Am day kennel (`cah3`) — a monthly Saturday-nearest-full-moon trail in Thailand's Prachuap Khiri Khan / Phetchaburi coast, via the same config-only Harrier Central pattern as Bandung/Barbados/Moonshine.

## Dedup result
- Kennel in seed: **no** (`grep -i h2fmh3 prisma/seed-data/*.ts` → 0 hits; no "Hua Hin Full Moon" kennel)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read via Chrome MCP 2026-07-01, **463 slugs**; no `h2fmh3` / `hua-hin-full-moon-h3` / any full-moon-Hua-Hin slug. The only Hua Hin slug is **`h2h3-cha-am-h3`** = the distinct **day** kennel (`cah3`, already live) — a different kennel with its own run-number sequence, NOT this full-moon kennel.
- Decision: **full onboard**
- kennelCode: `h2fmh3` (collision check: **clear** — grep of `kennels.ts`/`aliases.ts` shows `h2fmh3`/`H2FMH3` unused; distinct from every other Full Moon kennel: cfmh3, dcfmh3, sffmh3, fmh3-sd, bh3fm, mfmh3, ffmh3, klfmh3, bfmh3, vienna-fmh3, etc.)
- Pre-onboarding admin-event check: no prior `Event` rows expected (kennel absent from sitemap). Claude Code: confirm no admin-seeded `h2fmh3` events before first scrape; none anticipated.

## Live source verification  ✅ (upcoming event captured)
- Source: `HARRIER_CENTRAL` — `publicKennelId: 8f7cac29-e669-452d-8d19-218d9491567d` (shortName `H2FMH3`).
  Discovery/verification feed: `https://hashruns.org/api/global-runs?isFuture=1` (web_fetch, 2026-07-01),
  filtered client-side to this GUID. **Adapter path** (`harriercentralpublicapi.azurewebsites.net`) is
  Azure-allowlist-blocked from the research sandbox → ⚠️ **Claude Code must reconfirm `adapter.fetch(source)`
  from local env** (routine for HC; the host is reachable there).
- Events seen: **1 upcoming** (monthly full-moon cadence → 1 upcoming is normal, not a "stale" signal).
- **Source-count parity:** HC `getEvents` is future-only; platform UI (`hashruns.org` isFuture=1) shows exactly
  the 1 upcoming run this feed returned → parity **N/A / 1-of-1**. Recently-active evidence: upcoming run is
  **2026-07-11** (10 days out), run **#45** on a ~monthly full-moon kennel → live and active.
- Sample events (VERBATIM from `global-runs` — these are real, not constructed):
  1. **#45** — "Hua Hin Full Moon 4th Anniversary run" — **Sat 2026-07-11**, start **16:30** (Asia/Bangkok;
     GMT `2026-07-11T09:30:00Z`) — hares **"Penny lame, knockout neptune"** — location **"Mintara hotel
     phetchaburi"** — coords **12.59326659945281, 99.93353998011504** — price **200 / 200** (members/non-members;
     currency THB inferred, HC carries no currency field) — tags include "Full Moon Run".
- History depth / pagination: HC adapter is future-only. **`hashruns.org/api/global-runs?isFuture=0` serves the
  HC archive** back to the kennel's HC-join date. Run #45 ⇒ ~44 past runs reachable (monthly, ~2022→present).
  See **Historical backfill**. (Windowed pull: `isFuture=0` requires `minEventDate`+`maxEventDate`, ignores
  `publicKennelId` → filter client-side by `PublicKennelId`; response rows under `.runs`.)
- Coord sanity: **clean** — the sampled event carries a real Hua Hin/Phetchaburi pin (12.59, 99.93), NOT a
  region-default. ⚠️ For the **backfill** path, HC past rows can carry geocode-fail default pins on real
  venues — apply the country bounding-box scrub (Thailand) as with Barbados #2340/#2463.
- End times: **none** (HC exposes start only) → `endTime` undefined.
- Timezone: HC `KennelIANATimezone` = **`Asia/Bangkok`** — **correct** (no Bandung-style quirk); the existing
  "Hua Hin" region is already `Asia/Bangkok`.
- Notes: Not JS-rendered / no auth. Config-only HC. 🔴 **PII in EventDescription:** the #45 anniversary event's
  `EventDescription` contains payment PII (a bank account number + payee name: "Payments to penny lame, bangkok
  bank, 003-…"). The HC adapter maps `EventDescription → description`. Confirm the adapter/merge does not surface
  raw bank/account PII on the event card; for the backfill, **strip descriptions matching bank/account/phone
  patterns**. (This appears to be a one-off flyer-style anniversary blurb; routine monthly runs are terser.)
- **Field-fill assertion table** (from the 1 sampled upcoming event):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 1 / 1 | HC adapter keeps `EventName` verbatim ("…4th Anniversary run" — a real theme). `defaultTitle`+`staleTitleAliases` only fire on empty/placeholder names. |
  | `startTime` | 1 / 1 | "16:30" from `EventStartDatetime` (local). |
  | `endTime` | 0 / 1 | HC has no end field — accept absence. |
  | `location` (venue) | 1 / 1 | `LocationOneLineDesc` = "Mintara hotel phetchaburi". |
  | `locationStreet` | 0 / 1 | not in HC feed. |
  | `locationUrl` (Maps) | 0 / 1 | not a discrete field (a Maps link may sit in the description). |
  | `hares` | 1 / 1 | `Hares` = "Penny lame, knockout neptune" (looked reliable here; HC `Hares` is per-kennel unreliable elsewhere — verify at build it isn't location-bleed). |
  | `cost` | 1 / 1 | ฿200 (kennel default) from `EventPriceForMembers/NonMembers`. |
  | `description` | 1 / 1 | present — 🔴 **contains PII, sanitize** (see Notes). |
  | `trailLengthText` | 0 / 1 | not in feed. |
  | `coords` (lat/lng) | 1 / 1 | 12.593, 99.934 (real). Backfill: bbox-scrub Thailand. |

## Kennel metadata (deep-dive complete)
- fullName: **Hua Hin Full Moon Hash House Harriers** / shortName: **Hua Hin Full Moon H3** / region: **Hua Hin** / country: **Thailand**
  (source: HC `KennelName` = "Hua Hin Full Moon Hash", `hashruns.org/api/global-runs` 2026-07-01)
- aliases: `["Hua Hin Full Moon H3", "H2FMH3", "Hua Hin Full Moon Hash", "Hua Hin Full Moon HHH"]`
  (`H2FMH3` = HC `KennelShortName`/`KennelSlug`; all four collision-checked clear). 🔴 Do **NOT** add bare
  `"H2H3"`/`"Full Moon Hash"` — `H2H3` belongs to the sibling `cah3`, `Full Moon Hash` is a shared generic.
- website / facebook / instagram / twitter / discord: **unverifiable from a working source — leave blank + flag.**
  HC is the only confirmed live source; no own dynamic site found. (The day-kennel `cah3.net` is a *different*
  kennel — do not attribute it here.) Claude Code may check `hashruns.org/H2FMH3/about` for a FB/website link at build.
- schedule: **Monthly**, Saturday nearest the full moon, **16:30** (from HC event #45 Sat 2026-07-11 16:30, and
  the "Full Moon Run" tag). 🔴 `scheduleTime` is 12-hour in `kennels.ts` → **"4:30 PM"**; keep `scheduleFrequency:
  "Monthly"` + a `scheduleNotes` describing the full-moon cadence. (Day-of-week drifts with the moon — don't
  hard-assert a weekday beyond the note.)
- foundedYear: **2022** (source: HC event #45 titled "Hua Hin Full Moon **4th Anniversary** run", July 2026 →
  4th anniversary ⇒ founded ~2022; corroborated by run #45 on a monthly kennel ≈ 3.7 years). Confidence **medium**
  — flag "inferred from anniversary title; confirm at build if a primary source surfaces."
- hashCash: **฿200** (source: HC `EventPriceForMembers`/`NonMembers` = 200 on #45; currency THB inferred from
  country — HC has no currency field). ⚠️ #45 is an **anniversary** run; the routine full-moon fee may differ
  (sibling `cah3` is ฿100 adults). Flag "confirm the standard fee at build; ฿200 is the anniversary-event figure."
- dogFriendly / walkersWelcome: unknown → leave blank.
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/HuaHinFullMoonHash.avif`** (HC `KennelLogo`).
  🔴 **AVIF** (not PNG) and a platform-CDN blob → **self-host** to `public/kennel-logos/h2fmh3.<ext>`; confirm the
  real extension by **magic bytes** (`avif`/`RIFF…WEBP`/`\x89PNG`), do NOT hard-code `.avif`/`.png`.
- description (short about-us): *"Hua Hin Full Moon Hash House Harriers runs monthly on the Saturday nearest each
  full moon along Thailand's Prachuap Khiri Khan / Phetchaburi coast (Hua Hin / Cha-Am area). The full-moon sibling
  of the day-time Hua Hin & Cha-Am H3."* (synthesized from verified region + cadence; no unverified claims)
- lat/lng: optional — the Hua Hin region centroid already exists; a kennel-level pin from #45 (12.59, 99.93) is fine.

## Region — NO `region.ts` edits required (verified)
The **"Hua Hin"** region already exists and is fully wired (confirmed in `src/lib/region.ts`, 2026-07-01):
- `REGION_SEED_DATA` — "Hua Hin" METRO record present (`~L2944`), timezone `Asia/Bangkok`, aliases
  `["Hua Hin, Thailand", "Hua Hin, TH"]`.
- `STATE_GROUP_MAP` — `"Hua Hin": "Thailand"` (`~L4035`).
- `COUNTRY_GROUP_MAP` — `"Hua Hin": "Thailand"` (`~L4335`).
- `COUNTRY_INFERENCE_RULES` — Thailand rule already covers `hua hin` (`~L3826`).
- Thailand COUNTRY + code-map already seeded. → **0 edits.** (The kennel seed carries `region: "Hua Hin",
  country: "Thailand"` explicitly, so inference is belt-and-suspenders.)

## Historical backfill
- Available: **~44 past runs** (#1 → #44, monthly full-moon, ~2022→2026) via `hashruns.org/api/global-runs?isFuture=0`
  — fields per row: date, run #, EventName, Hares, LocationOneLineDesc, lat/lng, member/non-member price, description.
  Coverage is from the kennel's **HC-join date forward** (may not reach the true 2022 founding if HC was joined later).
- Plan: **one-shot `scripts/backfill-h2fmh3-history.ts`** (per the Bandung/Barbados pattern). Recommended — gives the
  kennel page schedule + full recent history immediately. Because it is present, **`config.upcomingOnly: true` is
  REQUIRED** (else `reconcile.ts` false-CANCELs the aged past rows the future-only adapter never re-confirms).
  - `isFuture=0` **requires** `minEventDate`+`maxEventDate` (HTTP 400 without); **ignores `publicKennelId`** → filter
    client-side by `PublicKennelId === "8f7cac29-e669-452d-8d19-218d9491567d"`. Page ≤6-month windows; dedup by `PublicEventId`.
  - Response shape: rows under **`.runs`** (`const arr = raw.runs ?? (Array.isArray(raw) ? raw : [])`).
  - 🔴 **Coord bbox scrub (Thailand ≈ lat 5.5–20.5, lng 97–106):** drop out-of-box lat/lng → merge re-geocodes from venue text.
  - 🔴 **Time gate 06:00–20:00:** drop AM/PM-typo start times to `undefined` (don't fabricate a corrected time).
  - 🔴 **Strip PII descriptions:** omit/redact any `description` matching bank/account-number/phone patterns (the
    anniversary flyer PII noted above will recur in some past rows).
  - Dedup duplicate run numbers **by date** (keep both if genuinely different dates; drop only same-event mis-dates).
  - Freeze the curated output as `scripts/data/h2fmh3-history.json` + a dumb loader (H7 pattern); don't commit the parser.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects). Insert near the other Thailand kennels (cah3, crh3).
{
  kennelCode: "h2fmh3",
  shortName: "Hua Hin Full Moon H3",
  fullName: "Hua Hin Full Moon Hash House Harriers",
  region: "Hua Hin",
  country: "Thailand",
  // website/facebook/etc. intentionally omitted — no verified dynamic own-site (HC-only). Add at build if found.
  logoUrl: "/kennel-logos/h2fmh3.<ext>", // 🔴 self-host the HC AVIF blob; confirm <ext> by magic bytes
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "4:30 PM", // 12-hr in kennels.ts; RawEventData.startTime stays 24-hr "16:30"
  scheduleFrequency: "Monthly",
  scheduleNotes: "Monthly full-moon trail — the Saturday nearest each full moon, ~16:30. Full-moon sibling of the daytime Hua Hin & Cha-Am H3 (cah3).",
  hashCash: "฿200", // ⚠️ from the HC anniversary event; confirm standard fee (sibling cah3 is ฿100 adults)
  foundedYear: 2022, // inferred from the "4th Anniversary" run (Jul 2026); confirm at build
  description: "Hua Hin Full Moon Hash House Harriers runs monthly on the Saturday nearest each full moon along Thailand's Prachuap Khiri Khan / Phetchaburi coast (Hua Hin / Cha-Am area). The full-moon sibling of the daytime Hua Hin & Cha-Am H3.",
  latitude: 12.59, longitude: 99.93,
},

// aliases.ts — Record<string, string[]> (kennelCode → string[]). Key is the KENNELCODE, not the slug.
"h2fmh3": ["Hua Hin Full Moon H3", "H2FMH3", "Hua Hin Full Moon Hash", "Hua Hin Full Moon HHH"],

// sources.ts — Source[] (array of objects). Mirror the Bandung H3 HC row (sources.ts:7129).
{
  name: "Hua Hin Full Moon H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    publicKennelId: "8f7cac29-e669-452d-8d19-218d9491567d",
    defaultKennelTag: "h2fmh3",
    // HC names events with real themes (e.g. "Hua Hin Full Moon 4th Anniversary run"), kept verbatim.
    // defaultTitle synthesizes "Hua Hin Full Moon H3 #N" only for empty/placeholder names.
    defaultTitle: "Hua Hin Full Moon H3",
    staleTitleAliases: ["Placeholder event for H2FMH3"],
    // REQUIRED because this source owns a one-shot historical backfill (Bandung/Asunción contract):
    // the future-only HC adapter would otherwise let reconcile.ts false-CANCEL aged backfilled rows.
    // 🔴 If you SKIP the backfill, REMOVE this line (live-only HC omits upcomingOnly).
    upcomingOnly: true,
  },
  kennelCodes: ["h2fmh3"],
},
```

## Adapter notes / new-scraper plan
**Config-only — no adapter code.** `HARRIER_CENTRAL` already exists (`src/adapters/harrier-central/adapter.ts`,
registered in `registry.ts`) and is proven on Bandung/Barbados/Taiwan/Lisbon/Porto/Shanghai. The `publicKennelId`
GUID filter is the stable key. The Lisbon-era `GEOCODE_FAIL_SENTINELS` + `composeHcLocation` placeholder handling
already ship in the adapter — no new sentinels needed for this kennel (its venue is real).

**⚠️ Claude Code: verify before writing/relying on any code.**
- Confirm the source config against the **current** `HarrierCentralConfig` type (field names `publicKennelId`,
  `defaultKennelTag`, `defaultTitle`, `staleTitleAliases`, `upcomingOnly`) — mirror `sources.ts:7129` exactly.
- Live-verify `adapter.fetch(source)` returns **#45 on 2026-07-11 16:30**, `kennelTag: h2fmh3`, coords ~12.59/99.93.
- Confirm the adapter's `description`/PII handling for the anniversary event (see Live-source Notes).
- Backfill script: `safeFetch` (not raw `fetch`), `.runs` unwrap, client-side `PublicKennelId` filter, Thailand
  bbox coord scrub, 06:00–20:00 time gate, PII-description strip, freeze to `scripts/data/h2fmh3-history.json`.

## Sibling sweep
The HC source (`publicKennelId` filter) feeds **only `h2fmh3`**. The Hua Hin area's other kennel — the daytime
**Hua Hin & Cha-Am H3 (`cah3`, `h2h3-cha-am-h3`)** — is **already live** with its own WordPress/static + STATIC_SCHEDULE
sources and its own run-number sequence; it is a **distinct kennel**, not a sibling on this source. No unmapped siblings.

## Deep-dive checklist (nothing deferred)
- [x] logo (AVIF platform blob → flag self-host, magic-byte ext)  [x] foundedYear (2022, inferred — flagged)  [x] socials (none verified → blank+flag)  [x] schedule (monthly full-moon Sat 16:30 + scheduleNotes)  [x] hashCash (฿200 — anniversary figure, flagged)
- [x] description  [x] source live-verified (1 upcoming #45 captured verbatim; Azure feed → Claude Code reconfirm)  [x] history depth/pagination assessed (~44 via global-runs isFuture=0)
- [x] coord sanity checked (real pin; bbox-scrub for backfill)  [x] end times noted (none)  [x] kennelCode collision-checked (`h2fmh3` clear)  [x] kennelCodes source guard set (`["h2fmh3"]`)  [x] region.ts (0 edits — Hua Hin exists)

## Implementation gotchas (repo knowledge)
- **`config.upcomingOnly: true` REQUIRED with the backfill** (Bandung #2340 contract) — the future-only HC adapter +
  a past-run backfill means reconcile false-CANCELs the archive without it. Remove it only if you drop the backfill.
- **HC `global-runs` past rows: coord default-pin + AM/PM-time typos** — bbox-scrub (Thailand) + 06:00–20:00 gate.
- **HC `Hares` is per-kennel unreliable** (location-bleed elsewhere) — here it looked clean; verify at build.
- **Self-host the AVIF logo**, confirm ext by magic bytes (not Content-Type) — Rocketspark/Gamma precedent.
- **`friendlyKennelName`**: shortName "Hua Hin Full Moon H3" is >4 chars → synthesized titles are clean ("Hua Hin
  Full Moon H3 #N"); no `fullName`-derivation risk.
- **PII**: sanitize any `description` carrying bank/account/phone (anniversary flyer + likely some past rows).

---
_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
