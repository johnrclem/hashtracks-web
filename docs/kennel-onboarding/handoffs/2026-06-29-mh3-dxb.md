# Onboarding Handoff — Moonshine H3 Dubai (Dubai, UAE) — 2026-06-29

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/mh3-dxb-20260629`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **No `region.ts` edits** —
>    UAE COUNTRY + Dubai METRO + inference already shipped via Desert H3 (PR #2294); verified present
>    in `src/lib/region.ts` (COUNTRY "United Arab Emirates", METRO "Dubai", inference `dubai`,
>    `STATE_GROUP_MAP`/`COUNTRY_GROUP_MAP`/`COUNTRY_CODE_TO_NAME AE` all present).
> 3. **Config-only HARRIER_CENTRAL** — no new adapter code. Mirror the Bandung H3 source row
>    (`prisma/seed-data/sources.ts:7100`) exactly (it's the HC-with-backfill precedent).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` — call
>    `adapter.fetch(source)` via a throwaway `npx tsx -e '…'` from local env (the Azure host
>    `harriercentralpublicapi.azurewebsites.net` is allowlist-blocked from the research sandbox but
>    reachable from your local env). Validate: ≥1 upcoming event (expect **#364, 2026-07-03**), dates
>    UTC-noon, `startTime` "HH:MM", `kennelTag` resolves to `mh3-dxb` with no unmatched. **DO NOT run
>    `npx prisma db seed` here** — seeding + prod scrape is the post-merge step (8 below).
> 5. **Historical backfill (worth it):** the one-shot `scripts/backfill-mh3-dxb-history.ts` — 17 past
>    runs (#347–#363) from `hashruns.org/api/global-runs?isFuture=0`. See **Historical backfill** for
>    the recipe + the 3 data-quality fixes that MUST be applied (online #360, 08:00 #361, hares-bleed #363).
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive checklist.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (squash-merge can drop a follow-up commit — the
>      mijash3 backfill was lost this way). Spot-check: `git log -1 -- scripts/backfill-mh3-dxb-history.ts`,
>      `git log -1 -- scripts/data/mh3-dxb-history.json`.
>    - `eval "$(fnm env)" && fnm use 20`
>    - `npx prisma db seed` (additive)
>    - `npx tsx scripts/backfill-mh3-dxb-history.ts` (one-shot; idempotent insert of the 17 past runs)
>    - Trigger a scrape from `/admin/sources` to publish the upcoming run(s) to prod
>    - Spot-check `hashtracks.xyz/kennels/moonshine-h3-dubai` for ~18 events (#347 → upcoming #364) +
>      the full-moon cadence.
> Everything you need is in the sections that follow.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (**config-only — adapter exists & feed verified live**)
- Effort estimate: **config-only** source row (zero new adapter code) + a ~80–120 LoC one-shot backfill
  script (frozen JSON + dumb loader, mirror `scripts/backfill-bandung-h3-history.ts`).
- One-line: Adds **Moonshine H3 Dubai** — Desert H3's monthly full-moon offshoot, an active Harrier
  Central kennel (3rd Dubai kennel) — via a pure config-only HC row + a 17-run backfill, reusing the
  already-shipped UAE/Dubai region.

## Dedup result
- Kennel in seed: **no** (grep `Moonshine`/`MH3D`/`mh3-dxb` in `prisma/seed-data/` → only hit is Desert
  H3's title-filter comment at `sources.ts:7376`).
- Source in seed: **no**.
- Live sitemap dedup: **confirmed NOT live** — read via Chrome MCP 2026-06-29, **461 slugs**; no
  `moonshine-h3-dubai` / `moonshine` / `dubai` slug (the `mh3`/full-moon slugs present are unrelated
  Munich/Montreal/Minneapolis + other kennels' full-moon hashes). Dubai sibling `desert-h3` **is** live
  (confirms UAE COUNTRY + Dubai METRO already seeded → 0 region.ts edits); `creek-h3` not yet live
  (still handed-off, consistent).
- Decision: **full onboard**.
- kennelCode: `mh3-dxb` (collision check: bare `mh3`/`MH3` heavily taken — Munich `mh3-de`, Montreal
  `mh3-ca`, Minneapolis `mh3-mn`, Miami `mia-h3`, Madrid, Memphis → suffixed `-dxb` for Dubai; **bare
  "MH3" alias OMITTED**). `mh3-dxb` itself is free (grep-clean).

## Live source verification  ✅
- Source: **HARRIER_CENTRAL** — `publicKennelId d5f74649-a19c-4ffb-b55c-bcbc7caeb09e` (shortName `MH3D`,
  KennelName "Moonshine Hash House Harriers Dubai", tz `Asia/Dubai`, continent Asia). Adapter URL is the
  Azure `…/api/PortalApi/` (HC convention). **Verified 2026-06-29 via `hashruns.org/api/global-runs`**
  (the public web front-end for the same HC data) — Chrome page-context fetch.
  - `⚠️ Claude Code must confirm at build`: re-run `adapter.fetch(source)` against the Azure host from
    local env (allowlist-blocked from the research sandbox — routine for HC). Expect the same upcoming set.
- Events seen: **1 upcoming (#364) + 17 past (#347–#363) = 18 total**, date range **2025-02-14 →
  2026-07-03**.
- **Source-count parity:** N/A for an upcoming-count gate (HC `getEvents` is future-only; 1 upcoming).
  Applied the **recently-active rule instead** and it passes strongly: monthly cadence, last past run
  **#363 on 2026-05-29** (31 days before research) **AND** an actively-posted upcoming **#364 on
  2026-07-03** — this kennel is currently posting, not merely between runs.
- Sample events (verbatim from `hashruns.org/api/global-runs`, run numbers/names/themes as the source
  reports them):
  1. **#364** — "Moonshine Run 364" — 2026-07-03 20:00 — hares **Spiv** — *59 Street Kitchen & Bar,
     Donatello Hotel Al Barsha* — 10 AED — desc "Monthly full moon hash" — coords 25.1156, 55.1939  (**UPCOMING**)
  2. **#363** — "MH3D Run 363" — 2026-05-29 20:00 — *Ashoka Mixolounge, Mercure Dubai Barsha Heights* —
     10 AED — coords present — 🔴 **Hares field = the venue string** (HC location-bleed; treat as no hares)
  3. **#362** — "MH3D Run 362" — 2026-04-29 20:00 — hares **Plum Sucker and Compressed Fart** —
     *Urban Bar & Kitchen (UBK), Cluster A, JLT* — 10 AED — coords present
  4. **#361** — "MH3D Run 361" — 2026-04-03 **08:00** — hares **F'ing Dipstick** — *#14 Rocks Villas,
     Dubai Media City* — 10 AED — 🟡 **08:00 local / 04:00 GMT (internally consistent — preserve, NOT a
     12h typo; an outlier morning run)**
  5. **#360** — "MH3D Run 360" — 2026-03-06 20:00 — hares **Toilet Brush** — *"Online due to current
     security climate in Dubai…"* — **fee 0** — 🟡 **online/Zoom run, no in-person venue, no coords**
  6. **#347** — "MH3D Run 347" — 2025-02-14 20:00 — hares **Compressed Fart** — *The Cheeky Camel, Voco
     Hotel, JLT Cluster J* — 10 AED — coords 25.0679, 55.1420 — desc "Moonshine H3 run (Snow moon)"
- History depth / pagination: **17 reachable past runs** (#347–#363) on `hashruns.org/api/global-runs?isFuture=0`
  (coverage from the kennel's HC-join Feb 2025 forward — NOT lifetime). `isFuture=0` REQUIRES
  `minEventDate`+`maxEventDate`, IGNORES `publicKennelId` (filter client-side by `PublicKennelId`), rows
  are **PascalCase**. A deeper pre-2021 archive exists on `deserthash.org` category `?cat=4` ("Moonshine",
  back to 2018, ~#300 era) but with a **2021→2025 gap** and a messier WP-post format → **optional, low
  priority** (the clean 17-row HC backfill is sufficient for "schedule + recent runs immediately").
- Coord sanity: **clean** — per-venue distinct coords; **no repeated default pin** (sampled lats span
  25.008–25.116). 2 of 18 lack coords (#351 + the online #360) → HC adapter renders those unlocated, map
  falls back to the Dubai centroid. No `dropCachedCoords` trap.
- End times: **none** (HC exposes start only).
- Notes: HC is reachable via the Azure `PortalApi` from local env only (sandbox uses `hashruns.org`
  mirror). See `source-platform-notes.md` → "Harrier Central". This kennel pairs the future-only HC
  adapter with a backfill ⇒ **`upcomingOnly: true` is REQUIRED** (the Bandung exception — see gotchas).
- **Field-fill assertion table** (n = 18 sampled rows: #347–#364):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 18 / 18 | HC `EventName` ("Moonshine Run N" / "MH3D Run N" — real, kept verbatim); `defaultTitle` synth only on empty/placeholder |
  | `startTime` | 18 / 18 | `HH:MM` from `EventStartDatetime` (20:00 standard; **#361 = 08:00** preserved verbatim) |
  | `endTime` | 0 / 18 | not in HC feed — accept absence |
  | `location` (venue) | 17 / 18 | HC `LocationOneLineDesc`; **#360 = "Online…"** → keep text, no coords |
  | `locationStreet` | 0 / 18 | feed is single-line `LocationOneLineDesc`, no separate street — leave undefined |
  | `locationUrl` (Maps) | 0 / 18 | not in feed — leave undefined |
  | `hares` | 16 / 18 | HC `Hares`; **#351 blank, #363 = venue-bleed → drop** (treat as no hares) |
  | `cost` | 18 / 18 | kennel default **10 AED**; **#360 = 0** (online) — adapter maps `EventPriceForMembers` |
  | `description` | 18 / 18 | HC `EventDescription` ("Monthly full moon hash", lunar names e.g. "Snow Moon") |
  | `trailLengthText` | 0 / 18 | not in feed — leave undefined |
  | `coords` (lat/lng) | 16 / 18 | HC `Latitude`/`Longitude`; #351 + #360 null → fall back to Dubai centroid |

## Kennel metadata (deep-dive complete)
- **fullName:** Moonshine Hash House Harriers Dubai *(source: `hashruns.org/api/global-runs` `KennelName`,
  2026-06-29)*
- **shortName:** Moonshine H3 Dubai *(>4 chars → `friendlyKennelName` returns it directly; clean synth
  "Moonshine H3 Dubai Trail #N" — but HC `EventName` supplies real titles so synth rarely fires)*
- **region:** Dubai · **country:** United Arab Emirates *(canonical NAME; matches Desert H3 `dh3-ae`)*
- **aliases:** `MH3D`, `Moonshine H3`, `Moonshine Hash Dubai`, `Moonshine HHH Dubai` — **bare "MH3"
  OMITTED** (global collision: Munich/Montreal/Minneapolis/Miami/Madrid/Memphis). `MH3D` + the
  Moonshine-prefixed forms are grep-clean in `aliases.ts`/`kennels.ts`.
- **website:** `https://www.deserthash.org/?page_id=50` *(the Moonshine page on the parent DH3 site —
  source-verified live 2026-06-29; DNS for `deserthash.org` already passed at the Desert H3 build)*
- **facebook / instagram / twitter / discord:** none Moonshine-specific (shares the parent **DH3**
  channels — DH3 FB `facebook.com/BarbieHasher`). Leave Moonshine socials blank rather than attach the
  parent's. *(source: deserthash.org/?page_id=50 has only generic share buttons.)*
- **schedule:** **Monthly — every full moon.** Meet ~7:00 PM at a pub, run ~8:00 PM *(source:
  deserthash.org/?page_id=50: "meets in Dubai every full moon… meet at a local pub around 7pm… run,
  usually around 8pm")*. HC start times confirm **20:00**. **Run DAY varies with the lunar cycle** (sampled
  Mon/Wed/Fri) — NOT a fixed weekday. → set `scheduleFrequency: "Monthly"`, `scheduleTime: "8:00 PM"`,
  omit `scheduleDayOfWeek` (lunar), and **omit `scheduleRules`** (the dated HC feed is authoritative for a
  full-moon kennel; a fixed-weekday RRULE would mis-project). Capture the cadence in `scheduleNotes`.
- **foundedYear:** **unverifiable — leave blank + flag.** No founding date on the Moonshine page or HC.
  Run-number extrapolation (#304 in Aug 2021, #364 in Jul 2026 ⇒ ~monthly; back-projecting #1 lands
  ~mid/late-1990s) is a *soft* signal only — do NOT commit it. Probe the `deserthash.org` Moonshine
  archive (`?cat=4`) for a milestone/anniversary post at build if you want to confirm; otherwise omit.
- **hashCash:** **"10 AED"** *(source: `hashruns.org/api/global-runs` — `EventPriceForMembers = 10`,
  `EventCurrencyType` AED, consistent across all 17 in-person runs; the online #360 was free. Matches DH3's
  pub-run pricing.)*
- **dogFriendly / walkersWelcome:** not stated for Moonshine specifically — leave unset (don't inherit the
  parent's flags without a Moonshine-specific source).
- **logoUrl:** **⚠️ self-host** to `/kennel-logos/mh3-dxb.<ext>`. Preferred source = the dedicated Moonshine
  werewolf logo on the kennel's own WP uploads:
  `https://www.deserthash.org/wp-content/uploads/2019/04/moonshinelogo-artworklarge-blk.jpg` *(source:
  deserthash.org/?page_id=50 og:image)*. Alternative = HC blob
  `https://harriercentral.blob.core.windows.net/harrier/Moonshine%20H3%20Dubail.png` (generic round logo;
  filename has a typo "Dubail" but the URL resolves). **NEVER pre-fill the extension** — confirm via
  `curl -sI` Content-Type AND magic bytes (`\xff\xd8`=JPEG, `\x89PNG`=PNG) before naming the file.
- **lat/lng:** use the Dubai centroid **25.2048, 55.2708** for the `Kennel` record (venues rotate monthly;
  per-event coords come from HC). Mirrors Desert H3.
- **Sibling sweep:** the HC `publicKennelId` filters to **Moonshine only** (single kennel). Its Dubai
  siblings are separate kennels with their own sources: **Desert H3** (`dh3-ae`, already live) and **Creek
  H3** (`ch3-ae`, handed off 2026-06-27). "Virtual DH3" (deserthash.org `?cat=7`) is a defunct
  lockdown-era online series, not a real kennel. **No unmapped sibling gap** from this source.

## Historical backfill
- Available: **17 past runs** (#347–#363, 2025-02-14 → 2026-05-29) — fields: date, run#, title, hares,
  one-line location, lat/lng, fee, description.
- Plan: **one-shot insert** `scripts/backfill-mh3-dxb-history.ts` — **worth it** (gives the kennel page
  schedule + recent runs immediately on ship). Per the H7/Bandung pattern: **freeze a curated dataset**
  `scripts/data/mh3-dxb-history.json` + a **dumb loader** (mirror `scripts/backfill-bandung-h3-history.ts`);
  do NOT commit a parser. Pull recipe (verified 2026-06-29):
  ```
  GET https://hashruns.org/api/global-runs?isFuture=0&minEventDate=2024-11-01&maxEventDate=2026-06-29
  ```
  (split into ≤6-month windows to stay under the ~2 MB cap; the feed returns ALL kennels — filter
  client-side by `PublicKennelId === d5f74649-…`; dedup by `PublicEventId`; rows are **PascalCase**;
  join `LocationOneLineDesc` for `location`). Because the live HC adapter is future-only and owns this
  backfill, **set `config.upcomingOnly: true`** on the source (else `reconcile.ts` false-CANCELs the past
  rows as they age past the 365-day window — the Bandung #2340 contract).
- 🔴 **Curated-backfill data-quality fixes (apply before freezing — verified, not speculative):**
  1. **#360 (2026-03-06)** — online/Zoom run, `LocationOneLineDesc` = "Online due to current security
     climate in Dubai…", `fee 0`, no coords. **Keep the row for run-# continuity** (it's a genuine event),
     but drop coords and keep `cost: "Free"`/0. Don't let the long sentence geocode — leave coords unset
     (merge falls back to centroid).
  2. **#361 (2026-04-03)** — `08:00` local / `04:00` GMT. The GMT field **agrees** with 08:00 (unlike
     Bandung's contradicting `02:30`), so this is internally consistent — **preserve `startTime: "08:00"`
     verbatim**, do NOT "correct" to 20:00. It doesn't collide with any other row, so it can't corrupt on
     load. (Flag for an eyeball; keep.)
  3. **#363 (2026-05-29)** — HC `Hares` = "Ashoka Mixolounge, Mercure Dubai Barsha Heights" = the **venue
     string** (HC location-bleed, a known per-kennel quirk). **Drop hares for this row** (emit none); the
     venue still populates `location`.
  - Validation checklist (run on the frozen JSON before shipping): PII scrub (`@`/phone) — none expected;
    run-number monotonic 347→363 with no decrease; gap sanity (monthly ⇒ ~28–35d between runs; #350→#351
    is ~35d, fine); no same-`(kennel,date,startTime)` collisions; field-bleed scan for `:` in hares/location
    (catches the #363 bleed).
- 🟡 Optional deeper history: `deserthash.org/?cat=4` ("Moonshine" category, posts back to 2018, ~#300
  era) — a separate WP-post scrape with a 2021→2025 gap before the HC sequence. Low priority; skip unless
  you want pre-2025 depth.

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects). Add near the UAE block (after Desert H3 dh3-ae ~line 5612).
{
  kennelCode: "mh3-dxb", // bare "mh3"/"MH3" globally taken (Munich/Montreal/Minneapolis/Miami/Madrid) → -dxb (Dubai)
  shortName: "Moonshine H3 Dubai",
  fullName: "Moonshine Hash House Harriers Dubai",
  region: "Dubai",
  country: "United Arab Emirates", // canonical NAME; code "AE" lives in region.ts (already seeded via Desert H3)
  website: "https://www.deserthash.org/?page_id=50", // Moonshine page on the parent DH3 site
  // foundedYear OMITTED — unverifiable (no founding date on-site or in HC; do not guess)
  // Lunar full-moon cadence: run DAY varies, so no scheduleDayOfWeek and no scheduleRules
  // (the dated Harrier Central feed is authoritative; a fixed-weekday RRULE would mis-project).
  scheduleTime: "8:00 PM", // 12-hr seed format; RawEventData.startTime stays 24-hr "20:00"
  scheduleFrequency: "Monthly",
  scheduleNotes:
    "Every full moon: meet at a Dubai pub around 7:00 PM for drinks, run sets off around 8:00 PM. Locations rotate monthly. Moonshine is the Desert Hash House Harriers (DH3) full-moon hash.",
  hashCash: "10 AED", // HC EventPriceForMembers across all in-person runs (the online run was free)
  logoUrl: "/kennel-logos/mh3-dxb.<ext>", // ⚠️ self-host; confirm ext by Content-Type + magic bytes
  description:
    "Dubai's full-moon hash, run by the Desert Hash House Harriers (DH3). Meets every full moon at a rotating Dubai pub — drinks around 7:00 PM, run sets off around 8:00 PM, hash cash 10 AED. A relaxed monthly evening trail under the full moon.",
  latitude: 25.2048, // Dubai centroid (venues rotate; per-event coords come from HC)
  longitude: 55.2708,
},

// aliases.ts — Record<string, string[]> keyed by kennelCode (NOT slug).
"mh3-dxb": ["MH3D", "Moonshine H3", "Moonshine Hash Dubai", "Moonshine HHH Dubai"],
// bare "MH3" intentionally OMITTED — global collision (Munich/Montreal/Minneapolis/Miami/Madrid/Memphis)

// sources.ts — Source[] (array of objects). Mirror the Bandung H3 HC row (sources.ts:7100).
{
  name: "Moonshine H3 Dubai Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live via hashruns.org/api/global-runs 2026-06-29 —
    // upcoming #364 2026-07-03 20:00; monthly full-moon cadence).
    publicKennelId: "d5f74649-a19c-4ffb-b55c-bcbc7caeb09e",
    defaultKennelTag: "mh3-dxb",
    // HC names events "Moonshine Run <N>" / "MH3D Run <N>" (real names, kept verbatim).
    // defaultTitle synthesizes "Moonshine H3 Dubai #N" only when an event name is empty or a
    // known placeholder appears (mirrors Bandung H3 / Taiwan H3 / Shanghai H3).
    defaultTitle: "Moonshine H3 Dubai",
    staleTitleAliases: ["Placeholder event for MH3D"],
    // upcomingOnly:true — REQUIRED (NOT the no-backfill HC convention): the HC getEvents API is
    // future-only and this source owns a 17-run historical backfill
    // (scripts/backfill-mh3-dxb-history.ts); without this guard reconcile.ts would false-CANCEL
    // those past runs as they age off the 365-day window (Bandung #2340 contract).
    upcomingOnly: true,
  },
  kennelCodes: ["mh3-dxb"],
},
```

## Adapter notes / new-scraper plan
**Config-only — no new adapter code.** The `HARRIER_CENTRAL` adapter already exists
(`src/adapters/harrier-central/adapter.ts`, registered in `registry.ts`) and is feed-verified for this
kennel. The source row above is the entire implementation. Reference precedent: **Bandung H3**
(`sources.ts:7100`) — the HC-with-backfill case, identical shape (GUID + `defaultKennelTag` +
`defaultTitle` + `staleTitleAliases` + `upcomingOnly: true`).

The backfill (`scripts/backfill-mh3-dxb-history.ts`) is the only new code: a dumb loader over a frozen
`scripts/data/mh3-dxb-history.json`, mirror `scripts/backfill-bandung-h3-history.ts`. UTC-noon dates,
`startTime` "HH:MM", `cuid()` IDs, dedup against the merge fingerprint.

**⚠️ Claude Code: verify before writing real code.** Snippets above are illustrative; the live repo is
authoritative. Before building the backfill loader, confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); check `prisma/schema.prisma`
  for canonical event field names used by the backfill insert.
- The exact `Source.config` shape accepted by the HC adapter (read `src/adapters/harrier-central/adapter.ts`
  — confirm `publicKennelId` / `defaultKennelTag` / `defaultTitle` / `staleTitleAliases` / `upcomingOnly`
  are the current keys).
- `friendlyKennelName("Moonshine H3 Dubai", "Moonshine Hash House Harriers Dubai")` — shortName is >4
  chars so it should pass through directly; sanity-check the synthesized fallback title once anyway.

## Deep-dive checklist (nothing deferred)
- [x] logo (⚠️ self-host — deserthash.org Moonshine werewolf JPG or HC blob; confirm ext by magic bytes)
- [~] foundedYear (unverifiable — left blank + flagged; run-# soft-signals ~1990s, not committed)
- [x] socials (none Moonshine-specific; shares parent DH3 channels — documented, not invented)
- [x] schedule (monthly full moon, 8:00 PM; lunar → no fixed weekday, no scheduleRules)
- [x] hashCash (10 AED, HC-sourced, consistent)
- [x] description  [x] source live-verified (HC via hashruns.org; Azure leg flagged for Claude Code)
- [x] history depth/pagination assessed (17 HC rows; deeper deserthash.org `?cat=4` optional)
- [x] coord sanity checked (clean, per-venue, no default pin)  [x] end times noted (none)
- [x] kennelCode collision-checked (`mh3-dxb`)  [x] kennelCodes (source guard) set  [x] 0 region.ts edits confirmed

## Implementation gotchas (for Claude Code — repo knowledge, not source knowledge)
- **`config.upcomingOnly: true` is REQUIRED here** (future-only HC adapter + a backfill) — the Bandung
  #2340 contract. Without it `reconcile.ts` false-CANCELs the 17 backfilled rows once they age past the
  365-day window. This is the exception to "omit upcomingOnly on HC sources."
- **HC `Hares` location-bleed** — for #363 the `Hares` value is the venue. The backfill must drop hares
  when they look like the location (contain a venue/`:`/match `LocationOneLineDesc`). Per-kennel quirk.
- **Online run #360** — keep for run-# continuity but drop coords + set fee 0; don't geocode the long
  "Online due to…" sentence.
- **#361 08:00** — preserve verbatim (GMT agrees; internally consistent; no collision). Do NOT normalize.
- **Dry-run the frozen JSON through the merge** and scan for same-`(kennel,date,startTime)` collisions +
  impossible times before trusting "preserve quirks" (Bandung #2340 discipline).
- **Self-host the logo** to `public/kennel-logos/mh3-dxb.<ext>`; **NEVER pre-fill the extension** — confirm
  Content-Type AND magic bytes first (the og:image URL says `.jpg`; verify).
- **0 region.ts edits** — UAE COUNTRY + Dubai METRO + inference (`dubai`) already shipped (Desert H3 PR
  #2294). Do NOT re-add them.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
