# Onboarding Handoff — Fengyuan H3 (Taichung, Taiwan) — 2026-07-03

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/fishhh-20260703`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). Add the **Taichung METRO**
>    to `src/lib/region.ts` as noted in **Region edits** (Taiwan COUNTRY already exists).
> 3. Configure the source exactly as in **Adapter notes** — this is **pure config-only
>    HARRIER_CENTRAL** (adapter already exists at `src/adapters/harrier-central/adapter.ts`; no new
>    adapter code).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` from local env (the Azure host `harriercentralpublicapi.azurewebsites.net`
>    is reachable there; it is NOT from the research sandbox). Because the kennel is **0-upcoming /
>    recently-active** (last run #22 2026-06-19, ~2 weeks before handoff; irregular cadence), the
>    adapter may legitimately return **0 events** at build — that is EXPECTED, not a failure. Confirm
>    the source resolves the kennel + no unmatched-kennel errors; the live run appears on the first
>    scrape after HC posts #23. Validate any returned events: UTC-noon dates, `startTime` "HH:MM",
>    `kennelTag` resolves. **DO NOT run `npx prisma db seed` here.**
> 5. **Historical backfill** — implement `scripts/backfill-fishhh-history.ts` (see **Historical
>    backfill**). This is the reason `upcomingOnly:true` is set. 20 counted runs back to 2022-08-07.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (separate step, after the PR merges):**
>    - `git checkout main && git pull`
>    - **Verify each expected file landed on `main`** (`git log -1 -- scripts/backfill-fishhh-history.ts`,
>      the seed edits, region.ts). Squash-merge sometimes drops a follow-up commit — recover if missing.
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod, which can revert unrelated production config drift —
>      barbados #2471/#2463 lesson).
>    - Run `scripts/backfill-fishhh-history.ts` against prod (one-shot) to insert the 20 historical runs.
>    - Trigger a scrape from `/admin/sources` to publish any upcoming event.
>    - Spot-check `/kennels/fengyuan-h3` for ~20 historical events + Taichung region badge.

## Summary
- Type: **full onboard**
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists & feed verified)
- Effort estimate: **config-only source** + ~2–3 `region.ts` edits (new Taichung METRO under existing
  Taiwan COUNTRY) + a ~80–120 LoC one-shot backfill script (model on
  `scripts/backfill-bandung-h3-history.ts`). No new adapter code.
- One-line: Adds **Fengyuan H3 (FISHHH)** — a 5th Taiwan metro (Taichung) — an irregular-Sunday
  kennel active since 2022, config-only via Harrier Central, with a 20-run backfill.

## Dedup result
- Kennel in seed: **no** (grep `fishhh|fengyuan|taichung` in `prisma/seed-data/` → no matches)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP
  2026-07-03, **476 slugs**; no `fengyuan`/`fishhh`/`taichung`/`fish` slug. Live Taiwan kennels =
  `taipei-h3`, `new-taipei-h3`, `kaohsiung-h3`, `taiwan-h3` (Taichung genuinely absent).
- Pre-onboarding admin-event check: kennelCode `fishhh` is brand-new (no seed presence) — no
  pre-seeded admin `Event` rows expected. (Claude Code: confirm `/kennels/fengyuan-h3` 404s at build.)
- Decision: **full onboard**
- kennelCode: `fishhh` (collision check: **clear** — grep-clean in `kennels.ts` + `aliases.ts`;
  distinct from `twh3-tw` Taiwan H3)

## Live source verification  ✅ (source live; 0-upcoming / recently-active)
- Source: **HARRIER_CENTRAL** — `publicKennelId: b66324a8-80a1-41b6-8c78-3a86111a4de0` (shortName
  `FISHHH`). Discovery/verification feed: `hashruns.org/api/global-runs` (the public web front-end for
  the same HC data). Adapter path = Azure `getEvents` (future-only), reachable from Claude Code's env.
- Events seen: **0 upcoming** (checked `isFuture=1`, 200-row global cap — FISHHH absent, expected for
  an irregular kennel between postings) + **20 counted past runs** via `isFuture=0`, date range
  **2022-08-07 (#1) → 2026-06-19 (#22)**.
- **Recently-active evidence (Step 3 rule — 0 upcoming is NOT a block here):** the kennel is literally
  named "**Irregular** Sunday" and runs sporadically (gaps of 1–6 months). Last run **#22 —
  2026-06-19** (~14 days before this handoff). Prior runs #21 2026-01-18, #20 2025-08-17, #19
  2025-07-12, #18 2025-06-01 → clearly a live, active-cadence kennel that simply hasn't posted #23.
  **Onboard on recent-cadence evidence; #23 appears on the first scrape after HC posts it.**
- **Source-count parity:** N/A — HC platform shows 0 upcoming (future-only API). Applied the Step 3
  recently-active evidence rule instead (recent-run sample + cadence above).
- Sample events (**VERBATIM from `hashruns.org/api/global-runs`, 2026-07-03**):
  1. `#22` 2026-06-19T19:30 — "FISHHH #22 Mystery Hare" — hares "Ghost Protocock, Hand S…" — (venue not in truncated sample) — 19:30
  2. `#21` 2026-01-18T12:00 — "FISHHH #21, The Scenic Route" — hares "Shut the Fedora Up" — Shepi Park, behind BaFang Night Market — 12:00 — coords 24.2455, 120.7048
  3. `#20` 2025-08-17T13:15 — "FISHHH #20, \"Swimmer La American has Reached Our Shores\"" — hares "Shut the Fedora Up" — Small temple SE over the river from Fengyuan Boulevard — 13:15 — coords 24.2364, 120.7266
  4. `#19` 2025-07-12T18:31 — "Mystery Hares! FISHHH #19 An Urban Night Trail FREE!" — hares "Frog Fucker, Psycho Mate" — Fengyuan train station SW — 18:31 — ⚠️ coords **52.3447, 4.8728 (AMSTERDAM — bogus geocode-fail pin, must be scrubbed)**
  5. `#18` 2025-06-01T10:15 — "Even lame hashes can hare (Tanks for coming)" — hares "STFU" — Fengzhou Park, Shengang, NW of Fengyuan — 10:15 — coords 24.2658, 120.7003
- History depth / pagination: **20 counted runs reachable** (#1–#12, #14–#15, #17–#22; #13 & #16 are
  real gaps). Full lifetime is recoverable because the kennel joined HC at inception (#1 = 2022-08-07).
  Pagination = `global-runs?isFuture=0&minEventDate&maxEventDate` in ≤6-month windows, filter
  `PublicKennelId` client-side (the endpoint ignores it and returns ALL kennels' rows).
- Coord sanity: **⚠️ 1 of 20 has a bogus default pin** — #19 at Amsterdam (52.3447, 4.8728). #8 &
  #12 venue text `undefined` but coords in-range; #15 & #17 have no coords. Backfill MUST apply a
  **Taiwan bounding-box scrub** (lat 21.9–25.3, lng 119.3–122.1) → drops #19's pin; merge re-geocodes.
- End times: **none** (HC exposes no end timestamp).
- Notes: HC `KennelIANATimezone` = `Asia/Taipei` (correct — no timezone-quirk fix needed, unlike
  Bandung). `EventCurrencyType` is `undefined`/null on every row → infer **TWD (NT$)** from Taiwan.
  Times genuinely vary (10:15–19:30 + one placeholder 23:59) — irregular kennel, not a parse error.
- **Field-fill assertion table** (from the ~8 most-recent sampled rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 8 / 8 | HC `EventName` carries real "FISHHH #N …" themes → verbatim; `defaultTitle` synthesizes only for empty/placeholder |
  | `startTime` | 8 / 8 | `HH:MM` from `EventStartDatetime`; **06:00–20:00 gate** drops the #10-type `23:59` placeholder → undefined |
  | `endTime` | 0 / 8 | HC has none — accept absence |
  | `location` (venue) | ~7 / 8 | `LocationOneLineDesc`; a couple rows (`#8`/`#12`) are `undefined` — leave undefined, don't synth |
  | `locationStreet` | 0 / 8 | not in feed |
  | `locationUrl` (Maps) | 0 / 8 | not in feed |
  | `hares` | 8 / 8 | HC `Hares` (real hash names here, not location-bleed — but spot-check per HC caveat) |
  | `cost` | kennel default **NT$300** | fee field `300/300`; per-event FREE specials (#19) → `Event.cost` override only |
  | `description` | present but **OMIT** | HC `EventDescription` — never copy (PII/faithful-to-adapter rule) |
  | `trailLengthText` | 0 / 8 | not in feed |
  | `coords` (lat/lng) | ~6 / 8 valid | `Latitude`/`Longitude`; 1 bogus (Amsterdam), 2 undefined → bbox scrub + venue re-geocode |

## Kennel metadata (deep-dive complete)
- fullName: **Fengyuan Irregular Sunday Hash House Harriers** (source: `hashruns.org/api/global-runs` `KennelName`)
- shortName: **Fengyuan H3** (chosen readable geographic form → slug `fengyuan-h3`; the HC acronym
  "FISHHH" is kept as an alias — mirrors the Bandung "BHHH2" → "Bandung H3" precedent)
- region: **Taichung** / country: **Taiwan**
- aliases (key `fishhh`): `["FISHHH", "Fengyuan Irregular Sunday H3", "Fengyuan Irregular H3"]`
  - `FISHHH` grep-clean in `aliases.ts` + `kennels.ts` (no global collision). Bare CJK city token
    `豐原` intentionally placed on the **Taichung region** aliases, not the kennel.
- website: **none found** — flag "unverifiable (HC-only kennel)". Claude Code: optionally check
  `hashruns.org/FISHHH/about` at build. Do NOT substitute `hashruns.org/FISHHH` as the website.
- facebook / instagram / twitter / discord: **none found** — leave blank + flag. (Web search surfaced
  only the *separate, older* **Taichung H3** [`taichunghash.com`, Saturdays, est. 1998] — a distinct
  kennel, NOT FISHHH; a possible future target.)
- schedule: **irregular** — nominal day **Sunday** (per the kennel name), but actual run days/times
  vary (Fri/Sun; 10:15–19:30). `scheduleFrequency: "Irregular"`, `scheduleDayOfWeek: "Sunday"` (nominal,
  flagged), `scheduleTime` left blank (genuinely varies). **No `scheduleRules`** — no reliable RRULE.
- foundedYear: **2022** (source: `hashruns.org/api/global-runs` — run **#1 = 2022-08-07**, verified)
- hashCash: **NT$300** (source: `hashruns.org` fee field `EventPriceForMembers/ForNonMembers = 300/300`;
  currency null → inferred TWD from Taiwan — flag "confirm at build". Occasional FREE specials, e.g. #19.)
- dogFriendly / walkersWelcome: **unknown** — leave blank
- logoUrl: `https://harriercentral.blob.core.windows.net/harrier/Fengyuan%20Irregular%20H3.png`
  → **⚠️ self-host** to `public/kennel-logos/fishhh.<ext>` on disk (served path `/kennel-logos/fishhh.<ext>`). URL says `.png` but **confirm the real
  extension via `curl -sI` Content-Type + magic bytes** before committing (HC blob logos have been
  WebP-in-a-.png-URL before). Use a literal `<ext>` placeholder until confirmed.
- description: "An irregular-cadence Hash House Harriers kennel in Fengyuan, Taichung, Taiwan —
  running roughly monthly on Sundays (times vary) since 2022." (factual, source-grounded)
- lat/lng: kennel centroid ≈ **24.2545, 120.7188** (Fengyuan; runs cluster 24.23–24.27 / 120.70–120.73)

## Region edits (`src/lib/region.ts` — Taiwan COUNTRY already exists @ region.ts:1723)
Taiwan is a country→metro country (like Singapore/HK/NZ), sky palette. Add **Taichung METRO** only.
**COUNTRY_INFERENCE_RULES already covers Taichung** (line ~3840 alternation includes `taichung` AND
`[台臺][灣北中南]` covers `台中`) → **NO inference edit**. `COUNTRY_CODE_TO_NAME.TW` already present →
NO edit.

1. **`REGION_SEED_DATA`** — add METRO (mirror the Kaohsiung record @ region.ts:1746):
   ```ts
   {
     name: "Taichung",
     country: "Taiwan",
     timezone: "Asia/Taipei",
     abbrev: "TXG",
     colorClasses: "bg-sky-100 text-sky-700",
     pinColor: "#0ea5e9",
     centroidLat: 24.1477,
     centroidLng: 120.6736,
     aliases: ["Taichung, Taiwan", "台中", "Fengyuan", "豐原"],
   },
   ```
2. **`STATE_GROUP_MAP`** (@ region.ts:~4041, next to `"Taipei": "Taiwan"`): add `"Taichung": "Taiwan",`
3. **`COUNTRY_GROUP_MAP`** (@ region.ts:~4342): the manual requires every metro wired to the country
   name. ⚠️ **Note the existing Taiwan pattern**: only `"Taiwan": "Taiwan"` is present — the live
   Taipei/Kaohsiung metros are NOT individually listed here (unlike China, which lists both
   `"China"` + `"Shanghai"`). **Add `"Taichung": "Taiwan",` per the manual, but first confirm
   `groupRegionsByCountry` behavior** — if it falls back to the region's `country` field for metros
   absent from this map (which is why Taipei/Kaohsiung already group correctly), the add is
   belt-and-suspenders; either way it is safe.

## Historical backfill
- Available: **20 counted runs** — fields per row: run number, date, time, theme title, hares,
  venue text, lat/lng (fee 300/300, currency null). Date range **2022-08-07 (#1) → 2026-06-19 (#22)**;
  present: #1–#12, #14, #15, #17–#22; **gaps: #13, #16** (real, not fetch misses).
- Plan: **one-shot insert** `scripts/backfill-fishhh-history.ts` (model on
  `scripts/backfill-bandung-h3-history.ts`). Set source `config.upcomingOnly: true` (REQUIRED — the HC
  adapter is future-only, so without it `reconcile.ts` false-CANCELs the entire backfilled archive
  inside the 365-day window; Bandung #2340 / Asunción contract).
- **Extractor rules (learned quirks — apply all):**
  1. **Taiwan bounding-box coord scrub** — drop `Latitude`/`Longitude` outside lat 21.9–25.3, lng
     119.3–122.1 (drops #19's Amsterdam pin 52.3447/4.8728 → merge re-geocodes from venue text).
  2. **Time gate 06:00–20:00** — drop out-of-range `startTime` to `undefined` rather than fabricate
     (drops #10's `23:59` placeholder; keeps legit night runs #19 18:31 / #22 19:30).
  3. **`IsCountedRun === 0` filter** — none present here (all 20 are counted=1), but filter defensively.
  4. **OMIT `description`** — never copy HC `EventDescription` (PII risk).
  5. **Keep `hares`** verbatim (real hash names here) but spot-check for venue-bleed per the HC caveat.
  6. Response shape: `isFuture=0` returns `{ runs: [...] }` — read `.runs` defensively
     (`raw.runs ?? (Array.isArray(raw)?raw:[])`); `publicKennelId` is IGNORED by the endpoint → filter
     `PublicKennelId` client-side; page ≤6-month windows.
  > 🟢 Preferred pattern (H7): freeze a curated `scripts/data/fishhh-history.json` + dumb loader;
  > don't commit the throwaway parser. Run the frozen-dataset validation checklist (PII scrub,
  > run-number monotonicity, gap sanity, field-bleed) before shipping.

## Ready-to-paste seed

> `aliases.ts` is `Record<string, string[]>` (slug/kennelCode → string[]). Key by **kennelCode**
> (`fishhh`), NOT the slug — verified against `head prisma/seed-data/aliases.ts`.

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "fishhh",
  shortName: "Fengyuan H3",
  fullName: "Fengyuan Irregular Sunday Hash House Harriers",
  region: "Taichung",
  country: "Taiwan",
  foundedYear: 2022,          // verified: HC run #1 = 2022-08-07
  scheduleDayOfWeek: "Sunday", // nominal ("Irregular Sunday"); actual days/times vary — flag
  scheduleFrequency: "Irregular",
  hashCash: "NT$300",         // fee 300/300; currency inferred TWD — confirm at build
  logoUrl: "/kennel-logos/fishhh.<ext>", // ⚠️ self-host from HC blob; confirm <ext> via magic bytes
  description:
    "An irregular-cadence Hash House Harriers kennel in Fengyuan, Taichung, Taiwan — running roughly monthly on Sundays (times vary) since 2022.",
  latitude: 24.2545,
  longitude: 120.7188,
  // website / socials: none found — leave unset + flag (HC-only kennel)
}

// aliases.ts — Record<string, string[]>  (key = kennelCode)
"fishhh": ["FISHHH", "Fengyuan Irregular Sunday H3", "Fengyuan Irregular H3"],

// sources.ts — Source[] (array of objects). Mirror the Bandung HC row (sources.ts:7157).
{
  name: "Fengyuan H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    publicKennelId: "b66324a8-80a1-41b6-8c78-3a86111a4de0",
    defaultKennelTag: "fishhh",
    // FISHHH names events "FISHHH #N - <theme>" (real names, kept verbatim);
    // defaultTitle synthesizes "Fengyuan H3 #N" only for empty/placeholder rows.
    defaultTitle: "Fengyuan H3",
    staleTitleAliases: ["Placeholder event for FISHHH"],
    // upcomingOnly:true — the HC getEvents API is future-only and this source owns a
    // 20-run historical backfill (scripts/backfill-fishhh-history.ts); without this guard
    // reconcile.ts false-CANCELs those past runs as they age off the 365-day window
    // (Bandung #2340 / Asunción contract).
    upcomingOnly: true,
  },
  kennelCodes: ["fishhh"],
}
```

## Adapter notes / config explanation
**Pure config-only HARRIER_CENTRAL** — the adapter already exists
(`src/adapters/harrier-central/adapter.ts`, registered in `registry.ts`). No new adapter code. The
config above mirrors the Bandung HC row (`sources.ts:7157`), which is the closest analog (HC source +
one-shot backfill + `upcomingOnly:true`).

- `publicKennelId` GUID is the stable filter (verified live via `hashruns.org/api/global-runs`
  2026-07-03). `defaultKennelTag`/`kennelCodes` = `fishhh`.
- HC event names are real ("FISHHH #22 Mystery Hare") so `applyTitleFallback` passes them verbatim;
  `defaultTitle` + `staleTitleAliases` are cheap defensive insurance for empty-name rows.
- Placeholder-venue geocode sentinels (`GEOCODE_FAIL_SENTINELS`, `hcGeocodeFailed`,
  `stripPlaceholderLocation`) already exist in the adapter — no config needed; they handle any "TBD"/
  "No location provided" rows on the live path. **The bogus Amsterdam pin (#19) is a BACKFILL-path
  concern only** (the adapter's `hcGeocodeFailed` does NOT run on the `global-runs` backfill path — the
  bbox scrub in the backfill extractor is what catches it).

**⚠️ Claude Code: verify before writing the backfill code.** Any snippet here is illustrative; the
authority is the live repo. Confirm against current types/imports:
- `RawEventData` field names — `kennelTags` is `string[]` (NOT `kennelTag`); `walkersWelcome` (NOT
  `walkerFriendly`). Check `prisma/schema.prisma` for canonical `Kennel`/`Event` fields.
- Dates as **UTC noon**, `startTime` as `"HH:MM"` string, `cuid()` IDs.
- `friendlyKennelName("Fengyuan H3", "Fengyuan Irregular Sunday Hash House Harriers")` — shortName is
  11 chars (>4) so it short-circuits to "Fengyuan H3" → synth title "Fengyuan H3 Trail #N" is clean.
  Confirm at build (`npx tsx -e 'import {friendlyKennelName} from "./src/pipeline/merge"; …'`).
- Backfill: model on `scripts/backfill-bandung-h3-history.ts`; freeze the curated JSON, don't commit
  the parser (H7 pattern).

## Deep-dive checklist (nothing deferred)
- [x] logo (flagged self-host + confirm ext)  [x] foundedYear (2022, verified via run #1)  [~] socials (none found — flagged)  [x] schedule (irregular Sunday, flagged)  [x] hashCash (NT$300, inferred currency flagged)
- [x] description  [x] source live-verified (hashruns.org global-runs; 0-upcoming recently-active)  [x] history depth/pagination assessed (20 runs, ≤6mo windows)
- [x] coord sanity checked (Amsterdam pin #19 → bbox scrub)  [x] end times noted (none)  [x] kennelCode collision-checked (`fishhh` clear)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- **`config.upcomingOnly: true` is REQUIRED** here (future-only adapter + backfill) — omitting it
  false-CANCELs the whole archive within 365 days (Bandung #2340). Per the HC-batch-10 retro
  (2026-07-02), this is the RULE for every HARRIER_CENTRAL source, not an exception — a prior
  "omit `upcomingOnly` on live-only HC" convention was corrected.
- **Backfill bbox coord scrub + time gate** as specified above (Barbados #2162 / #2137 precedent) —
  the adapter's own `hcGeocodeFailed`/`dropApiCoords` does NOT run on the `global-runs` backfill path.
- **Rejecting upstream coords needs `dropCachedCoords: true`** if a bad pin was ever stored (re-scrape
  keeps the stale pin otherwise) — relevant only if #19 is ever ingested before the scrub.
- **Self-host tokenized logo** into `public/kennel-logos/fishhh.<ext>`; confirm real extension via
  Content-Type + magic bytes (`RIFF`=WebP, `\x89PNG`=PNG, `\xff\xd8`=JPEG). Do NOT pre-fill `.png`.
- **Region: mirror the sibling metro exactly** (Kaohsiung/Taipei sky palette) — don't invent colors;
  no trailing-zero numeric literals (Sonar S6749).
- **Tests:** `beforeEach(vi.restoreAllMocks)` if you add fetch-count assertions in the backfill test.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the
brief — no separate prompt needed._
