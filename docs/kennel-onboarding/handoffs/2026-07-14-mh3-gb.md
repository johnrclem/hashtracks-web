# Onboarding Handoff — Manchester H3 (Manchester, England) — 2026-07-14

> ## ▶ FOR CLAUDE CODE — implement this entire file, end to end
> You are being given this whole file. Do the full onboarding now, autonomously:
> 1. Branch off a clean `main`: `onboard/mh3-gb-20260714`.
> 2. Apply the **Ready-to-paste seed** below (kennel + alias + source). **2 `region.ts` edits** —
>    add a **Manchester** METRO under the already-seeded UK COUNTRY (METRO record + `STATE_GROUP_MAP`
>    entry). No `COUNTRY_INFERENCE_RULES` / `COUNTRY_GROUP_MAP` edit (see **Region seeding**).
> 3. Configure the adapter — **pure config-only HARRIER_CENTRAL** (no new adapter code); mirror the
>    **live-only HC rows** Steel City H3 / Taiwan H3 / Lisbon H3 (`prisma/seed-data/sources.ts`).
>    🔴 **This is a LIVE-ONLY HC source (no backfill) → OMIT `upcomingOnly`** (HC `getEvents` is
>    future-only and established HC kennels survive reconciliation without it — Lisbon/Taiwan/Steel City contract).
> 4. **Live-verify the adapter directly** (no DB write) per `.claude/rules/live-verification.md` —
>    call `adapter.fetch(source)` via a throwaway `npx tsx -e '…'`. Validate: **3 upcoming events**
>    (verified 2026-07-14: 2026-07-16 Thu, 2026-07-25 Sat, 2026-07-30 Thu), dates UTC-noon-safe,
>    `startTime`s "18:00"/"14:00"/"18:30", `kennelTag` resolves to `mh3-gb` with no unmatched. (If the
>    2026-07-16 run has since passed, expect the later two + whatever new runs are posted.) **DO NOT run
>    `npx prisma db seed` here.**
> 5. **No historical backfill** — HC's `global-runs?isFuture=0` returns **0 past rows** for this
>    `PublicKennelId` across 2024→2026 (the kennel joined HC ~July 2026; its real history #1–~#289
>    predates HC and is not on any clean machine-readable feed — the kennel's own site lists past runs
>    only as prose). See **Historical backfill**.
> 6. `eval "$(fnm env)" && fnm use 20 && npx tsc --noEmit && npm run lint && npm test`.
> 7. Commit and open a PR carrying the metadata, live-verification results, and the deep-dive
>    checklist below. Follow `docs/source-onboarding-playbook.md` throughout.
> 8. **Post-merge runbook (after the PR merges):**
>    - `git checkout main && git pull`
>    - Verify each expected file landed on `main` (`git log -1 -- <path>` for the seed rows, the
>      `region.ts` edits, and the self-hosted logo).
>    - `eval "$(fnm env)" && fnm use 20`
>    - Seed the new kennel/source/alias to prod — prefer a **scoped `seedKennels` run on this kennel's
>      subset** over a full `npx prisma db seed` (a full seed re-writes `Source.config` for every source
>      whose seed-data differs from prod — barbados #2471/#2463).
>    - Trigger a scrape from `/admin/sources` to publish the events to prod.
>    - Spot-check `/kennels/manchester-h3` for the 3 upcoming runs.

## Summary
- Type: **full onboard** (first Manchester kennel on hashtracks)
- Adapter: **HARRIER_CENTRAL** (config-only — adapter exists in `src/adapters/registry.ts` & feed verified live)
- Effort estimate: **config-only source** + **2 `region.ts` edits** (new Manchester METRO under UK COUNTRY). No new adapter code, **no backfill script**.
- One-line: Adds Manchester H3 — Greater Manchester's well-established (est. ~2012) Hash House Harriers, a major UK city — via a live Harrier Central kennel with 3 genuine upcoming runs.

## Dedup result
- Kennel in seed: **no** (`grep -in 'manchester\|"mh3-gb"' prisma/seed-data/` → nothing; bare `mh3` code belongs to Munich `mh3-de` / Montreal `mh3-ca` / Minneapolis `mh3-mn`, all distinct)
- Source in seed: **no**
- Live sitemap dedup: **confirmed NOT live** — read `hashtracks.xyz/sitemap.xml` via Chrome MCP 2026-07-14, **485 slugs**; **zero** `manch` / `manchester` / `mh3-gb` slug fragments. (Bare `mh3` slug IS present = Munich/Montreal/Minneapolis MH3 [slug = `toSlug("MH3")`]; Manchester's slug is `toSlug("Manchester H3")` = `manchester-h3`, which is absent.)
- Decision: **full onboard**
- kennelCode: **`mh3-gb`** (collision check: **clear** — `grep -in '"mh3-gb"' prisma/seed-data/kennels.ts prisma/seed-data/aliases.ts` returns nothing. Bare `mh3`/`MH3` is globally taken by Munich/Montreal/Minneapolis/Miami/Madrid/Memphis/Morgantown/Manila/Dubai → suffix required; matches the HC `kennelUniqueShortName` "MH3-GB"). slug → `manchester-h3` (`toSlug("Manchester H3")`, all-ASCII, free — NOT "MH3" which slugifies to the taken `mh3`).
  - 🔴 **Omit the bare `"MH3"` alias** — globally claimed (aliases.ts:202 mia-h3, :301 mh3-mn, :504 mh3-ca; kennels shortName "MH3" for Munich/Montreal/Minneapolis). kennelCode-exact-match + alias-match run before pattern, so a bare `MH3` alias would misroute. Publish `MH3-GB` instead.

## Live source verification  ✅ (3 genuine upcoming events — verified 2026-07-14 via Chrome page-context)
- Source: **HARRIER_CENTRAL** — `harriercentralpublicapi.azurewebsites.net/api/PortalApi/`, `publicKennelId: 3e87bc76-a1cc-4b04-82ce-e893a3dad797` (uniqueShortName `MH3-GB`). Verified live 2026-07-14 by (a) reproducing the adapter's exact `getEvents` POST in-browser (SubtleCrypto token) → **3 events**, AND (b) sweeping `hashruns.org/api/global-runs?isFuture=0` over 2024-01 → 2026-07 (windows returned up to 242 rows / 68 kennels each but **0 for this `PublicKennelId`** → the kennel joined HC ~July 2026 [logo blob stamped `20260710`] and has **no HC past-run archive**).
- Events seen: **3 upcoming** (from HC `getEvents`) + **0 in the HC past-runs feed**. Date range 2026-07-16 → 2026-07-30.
- **Source-count parity:** HC `getEvents` (the platform's own future feed) shows **3 upcoming**; the adapter will yield **3**. Ratio 3/3 = 100%. ✅ (Deep run # [~#289] with 0 HC archive is expected — HC coverage starts at the kennel's join date, not its lifetime; see "Run-number ≠ archive depth". Runs #1–~#288 are historical, not archived on this source.)
- Sample events (VERBATIM from `getEvents` 2026-07-14, fields kept as-is incl. trailing spaces):
  1. **"Try it Thursday "** — **2026-07-16 (Thu) 18:00** — hares "Beer Sniffer" — location "The Salisbury Ale House" — coords **53.474001 / -2.240965** (Manchester city centre) — `eventNumber: 289` — `isCountedRun: 1` — `daysUntilEvent: 2`.
  2. **"R*n 289 - How do you do?"** — **2026-07-25 (Sat) 14:00** — hares "Horny Devil, Beer Sniffer" — location "The Jolly Sailor" — coords **53.3856 / -2.15339** (Manchester/Marple area) — `eventNumber: 289` — `isCountedRun: 1` — `daysUntilEvent: 11`.
  3. **"Dey do do, don't dey?"** — **2026-07-30 (Thu) 18:30** — hares "Cwoardelia Snatch, FCUK" — location "Rivington Brew Co" — coords **53.62137 / -2.57466** (Chorley — a real away venue N of the city) — `eventNumber: 289` — `isCountedRun: 1` — `daysUntilEvent: 16`.
- 🔴 **HC `eventNumber` quirk — the future feed returns the kennel's CURRENT run number (`289`) on ALL 3 rows**, not a per-event number. This is benign here because **all three events carry real theme names** ("Try it Thursday", "R*n 289 - How do you do?", "Dey do do, don't dey?"), so the HC adapter keeps `eventName` verbatim as the title and `defaultTitle`/`Trail #N` synthesis never fires. Do NOT rely on `eventNumber` for a per-event run number on this source. (No adapter change needed — just don't treat `289` as authoritative for the Sat/2nd Thu rows.)
- History depth / pagination: **0 events in the HC feed's past window** (`global-runs?isFuture=0`, `PublicKennelId`-filtered, returned nothing across 2024→2026). HC `getEvents` is future-only. The kennel's real history lives only on its **own Weebly/Create site** (`manchesterh3.co.uk/past-rns.html`, prose list) — see **Historical backfill**. Three future events; no pagination needed.
- Coord sanity: **clean.** All three events carry real pins (Manchester city centre / Marple / Chorley) — no region-default sentinel, no duplicate-pin cluster. The Chorley pin (Rivington Brew Co, 53.621/-2.575) is a genuine away venue, not a default. No `dropCachedCoords` trap.
- End times: none (HC exposes start only).
- Notes: **tz — HC records for this kennel carry `KennelIANATimezone = Europe/London` (correct, no quirk)**; the Manchester METRO is `Europe/London`. Config-only; no browserRender/auth. HC `getEvents` carries no currency field — the sample prices are `0` (unpopulated), so use the kennel-level `hashCash: "£3 (free on Thursdays)"` (from the kennel's own site). The Azure host is allowlist-blocked from the research sandbox — verified via Chrome page-context fetch; **Claude Code: reconfirm `adapter.fetch(source)` from local env** where the host resolves.
- **Field-fill assertion table** (from the 3 upcoming rows):

  | Field | n filled / n sampled | Plan if low |
  |---|---|---|
  | `title` | 3 / 3 | Real event names → HC adapter sets `title` from `eventName`; `defaultTitle:"Manchester H3"` synthesizes only for empty/placeholder names. |
  | `startTime` | 3 / 3 | From `eventStartDatetime` (18:00 / 14:00 / 18:30). |
  | `endTime` | 0 / 3 | Not in feed — accept absence. |
  | `location` (venue name) | 3 / 3 | `locationOneLineDesc` (Salisbury Ale House / The Jolly Sailor / Rivington Brew Co) — real places, survive sentinel filter. |
  | `locationStreet` | 0 / 3 | Not populated — leave undefined. |
  | `locationUrl` (Maps) | 0 / 3 | Not in feed — coords carry the pin. |
  | `hares` | 3 / 3 | `hares` = "Beer Sniffer" / "Horny Devil, Beer Sniffer" / "Cwoardelia Snatch, FCUK" (real hash names). |
  | `cost` | 0 / 3 | HC price `0` (unpopulated) → per-event cost undefined; kennel-level `hashCash` "£3 (free on Thursdays)". |
  | `description` | 0 / 3 | Live HC adapter emits no `description`. |
  | `trailLengthText` | 0 / 3 | Not in feed (kennel site says weekend trails ~10K — not per-event; don't synth). |
  | `coords` (lat/lng) | 3 / 3 | `syncLat`/`syncLong` populated & real. No bbox scrub needed on 3 real live pins. |

## Kennel metadata (deep-dive complete)
- fullName: **Manchester Hash House Harriers** / shortName: **Manchester H3** / region: **Manchester** / country: **United Kingdom**
  - (HC `KennelName` = "Manchester Hash House Harriers", `KennelShortName` = "MH3", `KennelUniqueShortName` = "MH3-GB", `eventCityAndCountry` = "Manchester, United Kingdom". Source: HC `getEvents` 2026-07-14.)
- aliases: `["Manchester H3", "Manchester Hash House Harriers", "MH3-GB"]` — all clear (grep-checked 2026-07-14). 🔴 **Omit bare `"MH3"`** — global collision (Munich/Montreal/Minneapolis/Miami/Madrid/…).
- website: **`https://www.manchesterh3.co.uk`** (DNS-verified 2026-07-14 → 199.34.228.75; Weebly/Create site; `http://` redirects to `https://` so use the `https://` literal — no S5332 http hotspot). Confirmed live: Home + About + MH3's History pages all render.
- facebook: **`https://www.facebook.com/ManchesterH3`** (linked from the kennel site header). instagram: **`manchesterh3`** (`https://www.instagram.com/manchesterh3/`). twitter/discord: none. *(Also links a Strava group, Garmin Connect group `3866957`, and a WhatsApp group — not standard `Kennel` fields; mention in `description` if desired, don't invent columns.)*
- schedule: **2nd Sunday + 4th Saturday of the month** (main ~10K countryside trails, "meeting at 14:00 for a 14:30 start") **and alternate Thursdays** (city-centre social from The Salisbury, 2 Wakefield Street, M1 5NE, 18:30 start). Source (verbatim): `manchesterh3.co.uk/about.html` — "Our main runs are held on the 2nd Sunday and the 4th Saturday meeting at 14:00 for a 14:30 start. We also meet on alternate Thursdays … at The Salisbury … for a 18:30(ish) start." This is genuinely **multi-pattern → `scheduleRules`** (see below).
  ```ts
  // scheduleDayOfWeek/scheduleTime kept as fallback; scheduleRules is authoritative.
  scheduleDayOfWeek: "Saturday",
  scheduleTime: "2:30 PM", // 12-hr per kennels.ts convention (scheduleRules startTime stays 24-hr "14:30")
  scheduleFrequency: "Biweekly",
  scheduleRules: [
    { rrule: "FREQ=MONTHLY;BYDAY=4SA", startTime: "14:30", label: "4th Saturday countryside trail" },
    { rrule: "FREQ=MONTHLY;BYDAY=2SU", startTime: "14:30", label: "2nd Sunday countryside trail" },
    // Alternate (biweekly) Thursday city social — INTERVAL=2 needs an anchorDate (a known real Thu run).
    { rrule: "FREQ=WEEKLY;INTERVAL=2;BYDAY=TH", startTime: "18:30", anchorDate: "2026-07-16", label: "Alternate Thursday city social (TITs)" },
  ],
  ```
  ⚠️ The 2nd-Sunday start time isn't stated separately on the site (only the shared "14:00 for a 14:30 start" phrasing) — `14:30` is inferred to match the Saturday trail; confirm if a Sunday run surfaces with a different time. The HC feed uses the **meet** time (14:00) for the Sat run while the site states a 14:30 **start** — both are the same event; `scheduleRules` uses the site's stated start.
- foundedYear: **2012** (source: `manchesterh3.co.uk/mh3s-history.html` — founder "Madam Sin … after ten triumphant years at the helm" [page image stamped March 2022 → founded ~2012] + WebSearch of the site's anniversary pages showing an 11th Anniversary in 2023 → 2012). Medium-high confidence (no single "founded 2012" string; corroborated by two independent anniversary signals).
- hashCash: **"£3 (free on Thursdays)"** (source, verbatim: `manchesterh3.co.uk/about.html` — "Hash trail subs are £3 (free on Thursday) with the proceeds going to fund non-establishment beer stops and snacks." First trail earns a free beer.)
- dogFriendly: **true** (source: about.html — "dogs and children normally welcome (venue dependent)").
- walkersWelcome: **true** (source: home page — "a non-competitive running and walking group").
- logoUrl: **`https://harriercentral.blob.core.windows.net/harrier/MH3-GB_20260710170404.jpg`** → ⚠️ **self-host** to `public/kennel-logos/mh3-gb.<ext>` (HC blob; confirm the extension via **magic bytes**, NOT the `.jpg` name — HC blobs are often WebP/PNG/AVIF regardless of URL suffix). Reference `/kennel-logos/mh3-gb.<ext>`. (The kennel site's own images are a placeholder `.gif` — prefer the HC blob.)
- lat/lng: **53.4808 / -2.2426** (Manchester city centre — matches the Manchester METRO centroid; each event's own pin comes from the feed).

## Region seeding — 2 `region.ts` edits (new Manchester METRO under UK COUNTRY)
UK COUNTRY is already seeded (`src/lib/region.ts:58`, `name: "United Kingdom"`). Add a **Manchester** METRO, mirroring the existing UK metros Liverpool (`:1641`) / Birmingham (`:1652`) / Newcastle (`:1587`) — all violet:

1. **`REGION_SEED_DATA`** — insert a METRO record (place with the other UK metros, e.g. after Birmingham `:1661`):
   ```ts
   {
     name: "Manchester",
     country: "UK",
     timezone: "Europe/London",
     abbrev: "MCR", // confirmed free (grep 'abbrev: "MCR"' / '"MAN"' → none)
     colorClasses: "bg-violet-100 text-violet-700",
     pinColor: "#8b5cf6",
     centroidLat: 53.4808,
     centroidLng: -2.2426,
     aliases: ["Manchester, England", "Greater Manchester"],
   },
   ```
2. **`STATE_GROUP_MAP`** — add (in the UK block near `:4202`, by Liverpool/Birmingham):
   ```ts
   "Manchester": "United Kingdom",
   ```
- 🔴 **NO `COUNTRY_INFERENCE_RULES` edit** — the UK rule already covers `england` (`region.ts:3937`: `/\b(uk|england|scotland|wales|london|surrey|sussex)\b/`), and the metro alias "Manchester, England" resolves the country via that. **Do NOT add a bare `manchester` token** — it collides with US Manchesters (NH/CT). (The seed kennel carries explicit `country: "United Kingdom"` anyway; inference only guards the free-text research path.)
- 🔴 **NO `COUNTRY_GROUP_MAP` edit** — UK metros resolve via `STATE_GROUP_MAP` first (confirmed: Liverpool/Birmingham/Lancaster/Cornwall are in `STATE_GROUP_MAP` only, absent from `COUNTRY_GROUP_MAP`).

## Historical backfill
- Available on HC: **none** — `global-runs?isFuture=0` returns 0 rows for this `PublicKennelId` (HC-join ~July 2026; the adapter path is future-only). So **no HC backfill script.**
- Real history exists but only on the kennel's **own Weebly/Create site** (`manchesterh3.co.uk/past-rns.html`) as a prose list — no clean machine-readable feed (no `?format=json`, no WP/Blogger API; it's a Weebly site). Deep run # (~#289) implies ~14 years of runs but they are **not** archived on any live JSON/REST source.
- Plan: **skip the backfill (recommended).** Rationale: no structured archive source exists; the daily HC scrape accumulates history going forward. **Live-only source → `config.upcomingOnly` OMITTED.** (If a future run wants the prose past-runs list, it would be a manual Weebly scrape — low value, error-prone; out of scope here.)

## Ready-to-paste seed

```ts
// kennels.ts — Kennel[] (array of objects)
{
  kennelCode: "mh3-gb", // bare mh3 taken by Munich/Montreal/Minneapolis/… → suffixed; matches HC uniqueShortName MH3-GB
  shortName: "Manchester H3",
  fullName: "Manchester Hash House Harriers",
  region: "Manchester", // NEW METRO — see Region seeding (2 region.ts edits)
  country: "United Kingdom",
  website: "https://www.manchesterh3.co.uk", // Weebly/Create; DNS + live verified 2026-07-14 (http→https)
  facebookUrl: "https://www.facebook.com/ManchesterH3",
  instagramHandle: "manchesterh3",
  logoUrl: "/kennel-logos/mh3-gb.<ext>", // self-hosted from HC blob; verify ext by magic bytes
  scheduleDayOfWeek: "Saturday", // fallback; scheduleRules is authoritative
  scheduleTime: "2:30 PM", // 12-hr per kennels.ts convention
  scheduleFrequency: "Biweekly",
  scheduleRules: [
    { rrule: "FREQ=MONTHLY;BYDAY=4SA", startTime: "14:30", label: "4th Saturday countryside trail" },
    { rrule: "FREQ=MONTHLY;BYDAY=2SU", startTime: "14:30", label: "2nd Sunday countryside trail" },
    { rrule: "FREQ=WEEKLY;INTERVAL=2;BYDAY=TH", startTime: "18:30", anchorDate: "2026-07-16", label: "Alternate Thursday city social (TITs)" },
  ],
  foundedYear: 2012, // manchesterh3.co.uk history/anniversary pages (founder "ten years at the helm" c.2022; 11th anniversary 2023)
  hashCash: "£3 (free on Thursdays)", // about.html: "Hash trail subs are £3 (free on Thursday)"; first trail earns a free beer
  dogFriendly: true, // about.html: dogs and children normally welcome (venue dependent)
  walkersWelcome: true, // home page: non-competitive running AND walking group
  description: "Manchester Hash House Harriers (MH3) — 'a drinking club with a running problem' — is Greater Manchester's hash, founded around 2012 by founding member Madam Sin. Main ~10K trails run on the 2nd Sunday and 4th Saturday of the month (meet 14:00 for a 14:30 start) in green areas around Greater Manchester with good public-transport links; on alternate Thursdays there's a laid-back city-centre social ('TITs' — Try it Thursday) from The Salisbury Ale House (2 Wakefield Street, M1 5NE) at 18:30. Dogs and children are usually welcome (venue dependent). Hash cash is £3 (free on Thursdays), funding beer stops and snacks — your first trail earns a free beer.",
  latitude: 53.4808, longitude: -2.2426, // Manchester city centre
},

// aliases.ts — Record<string, string[]>  (keyed by kennelCode, NOT slug)
"mh3-gb": ["Manchester H3", "Manchester Hash House Harriers", "MH3-GB"],
// 🔴 bare "MH3" OMITTED — global collision (Munich/Montreal/Minneapolis/Miami/Madrid/Memphis/Morgantown/Manila/Dubai)

// sources.ts — Source[] (array of objects). Mirror a LIVE-ONLY HC row (Steel City H3 / Taiwan H3 / Lisbon H3).
{
  name: "Manchester H3 Harrier Central",
  url: "https://harriercentralpublicapi.azurewebsites.net/api/PortalApi/",
  type: "HARRIER_CENTRAL" as const,
  trustLevel: 8,
  scrapeFreq: "daily",
  scrapeDays: 365,
  config: {
    // GUID is the most stable filter (verified live 2026-07-14 via HC getEvents [3 upcoming]
    // + hashruns.org/api/global-runs [0 past rows — kennel joined HC ~July 2026]).
    // HC KennelIANATimezone = Europe/London (correct, no quirk); Manchester METRO is Europe/London.
    publicKennelId: "3e87bc76-a1cc-4b04-82ce-e893a3dad797",
    defaultKennelTag: "mh3-gb",
    // HC names events with real themes ("Try it Thursday", "R*n 289 - How do you do?", …) → kept verbatim;
    // defaultTitle synthesizes only for empty/placeholder names.
    defaultTitle: "Manchester H3",
    staleTitleAliases: ["Placeholder event for Manchester H3"],
    // 🔴 upcomingOnly OMITTED — LIVE-ONLY HC source (no backfill). HC getEvents is future-only and
    //    established HC kennels survive reconciliation without it (Lisbon/Taiwan/Steel City contract).
  },
  kennelCodes: ["mh3-gb"],
},
```

## Adapter notes / new-scraper plan
**Pure config-only HARRIER_CENTRAL — zero new adapter code.** The `HARRIER_CENTRAL` adapter already
exists in `src/adapters/registry.ts` (`src/adapters/harrier-central/adapter.ts` + `token.ts`) and is
used by ~35 seeded kennels (Steel City H3, Taiwan H3, Lisbon H3, KRASH, Bandung, Divahhh, …). The source
row above is the whole live implementation. The adapter derives events from HC's `getEvents` API
(future-only), resolves `mh3-gb` via `defaultKennelTag`, and applies `defaultTitle`/`staleTitleAliases`
for name synthesis.

**⚠️ Claude Code: verify before writing/relying on any field.** This is config-only, so the main checks are:
- Confirm `publicKennelId` returns Manchester's runs via a throwaway `adapter.fetch(source)`. **Expect 3
  events** at build (2026-07-16 / 07-25 / 07-30); if 07-16 has passed, expect the remaining + any new ones.
- Confirm `kennelTag` resolves to `mh3-gb` with **no unmatched** and `startTime`s are "18:00"/"14:00"/"18:30".
- Confirm the display tz is **`Europe/London`** (from the Manchester METRO; HC feed agrees — no override).
- 🔴 **Do not depend on `eventNumber`** — the HC future feed returns `289` on every current row (quirk; see
  Live source verification). Titles come from `eventName` (all real here), so this is benign.
- `RawEventData` field names are `kennelTags: string[]` (not `kennelTag`); `walkersWelcome` (not
  `walkerFriendly`). Don't hand-set `title` — the HC adapter + `defaultTitle` handle it.
- **`friendlyKennelName("Manchester H3", …)`** — shortName is 13 chars (>4), so it short-circuits to
  "Manchester H3" (synth title "Manchester H3 Trail #N"). No verbose-synthesis risk. (The HC adapter uses
  the real `eventName` anyway, so synthesis is unlikely to fire.)

## Sibling sweep
- The kennel's own "TITs / Try it Thursday" city socials, Red Dress Run, Christmas run, and anniversary
  weekends are **Manchester H3's own events on the same `MH3-GB` feed** — not a separate kennel. They ship
  under `mh3-gb`; no sibling gap. (`kennelCodes: ["mh3-gb"]` is complete.)
- 🔴 **Do NOT conflate with the already-shipped Cornwall kennel "Try it Thursdays H3" (`titsh3`, GUID
  `2cc90a71-62e8-40ea-b26e-c79aed6fc417`).** Different GUID, different city (Callington, Cornwall),
  different kennel — Manchester's "Try it Thursday" is just the name of its Thursday social series.
- No other Manchester-area HC kennel surfaced in the `global-runs` sweep sharing this GUID or city.

## Deep-dive checklist (nothing deferred)
- [x] logo (HC blob → flag self-host + magic-byte ext)  [x] foundedYear (2012 — site anniversary/history signals, medium-high)  [x] socials (FB + Instagram confirmed off the kennel site)  [x] schedule (2nd Sun + 4th Sat + alt Thu → scheduleRules)  [x] hashCash (£3, free Thu — own site)
- [x] description  [x] source live-verified (3 genuine upcoming runs)  [x] history depth assessed (0 on HC; only a prose list on the Weebly site — backfill skipped)
- [x] coord sanity checked (3 real pins; no default-pin trap)  [x] end times noted (none)  [x] kennelCode collision-checked (`mh3-gb` clear; bare MH3 omitted)  [x] kennelCodes (source guard) set

## Implementation gotchas (for Claude Code — repo knowledge)
- 🔴 **LIVE-ONLY HC → OMIT `upcomingOnly`.** There is no backfill, so this is a live-only source; per the
  HC platform contract (Lisbon/Taiwan/Steel City) `upcomingOnly` is omitted and the established HC kennel
  survives reconciliation. Only add `upcomingOnly:true` if you decide to ship a manual Weebly backfill.
- 🔴 **`eventNumber` quirk** — every future row returns the kennel's current number (`289`). Titles are
  real `eventName`s so this is harmless; just don't treat `289` as the per-event run number.
- **`mh3-gb` is a kennelCode-suffix collision fix** — bare `mh3`/`MH3` = Munich/Montreal/Minneapolis/…
  Suffix the code AND omit the bare `MH3` alias (kennelCode-exact-match + alias-match run before pattern, so
  a bare `MH3` alias would misroute).
- **Self-host the HC blob logo** to `public/kennel-logos/mh3-gb.<ext>`; confirm ext by magic bytes (the
  `.jpg` URL name is not authoritative — HC blobs are often WebP/PNG/AVIF).
- **2 region.ts edits only** — Manchester METRO record + `STATE_GROUP_MAP` entry. No inference / no
  COUNTRY_GROUP_MAP edit (UK covers `england`; UK metros resolve via STATE_GROUP_MAP). Confirm `abbrev "MCR"`
  is still free at build (it was 2026-07-14).
- **`scheduleRules` INTERVAL=2 needs `anchorDate`** — the alternate-Thursday rule carries
  `anchorDate: "2026-07-16"` (a verified real Thursday run) so the biweekly phase is unambiguous.
- **No admin-seeded events to purge** — the kennel isn't live (no `manchester-h3`/`mh3-gb` slug in the
  sitemap), so there are no pre-existing prod `Event` rows to dedup against.
- **HC feed carries no currency** — event prices are `0` (unpopulated); use the kennel-level
  `hashCash: "£3 (free on Thursdays)"` from the kennel's own site (don't infer a per-event cost from `0`).
- **Website is `https://` (http→https redirect)** — use the `https://` literal in the seed so no S5332
  http hotspot fires.

---

_Implementation directive is at the top of this file (**▶ FOR CLAUDE CODE**). The whole file is the brief._
